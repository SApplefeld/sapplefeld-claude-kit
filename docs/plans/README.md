# Active Plans

This folder holds active plans only: specs that are parked, open, or in progress. A plan is the single source of truth for one effort's intent and state, and a fresh or post-compaction session resumes from it.

## Rules

- A plan lives here while it is being worked. When it reaches `Status: Complete` or is abandoned, it moves to `../archive/` in the same close-out that finished it. The `curating-docs` skill does the move with `git mv` so history is preserved.
- Naming follows the kit convention: `<project>_<content-type>_v<n>.md` (for example `claude-kit_docs-lifecycle_spec_v1.md`). Increment the version rather than overwriting a prior one.
- The `Status` header drives the lifecycle. `session-start.js` scans this folder: `Ready` plans are surfaced as authored and parked with no resume push, `In Progress` plans are surfaced for resume, and `Complete` plans still sitting here are flagged as unarchived.
- A plan doc's header and structure are a frozen v1 machine contract, not only kit convention: an external engine parses eleven shapes out of every plan here, case-sensitively and anchored to the start of a line, and five of them carry a value rule it enforces silently. The `curating-docs` skill holds the table; reword nothing in a header without reading it.
- When a plan relates to or supersedes another, cross-reference it in a `## Related` section so the library stays navigable.

## Current
A queue position stated in an entry below is that plan's place in the queue as armed, not a live position: a plan closing advances the leash without touching this file, so `kit-goal.js status` is the authority on where the run stands.

- **`claude-kit_review-loop-exit_spec_v1.md`** (Ready, three sections; authored 2026-09-07 by the Expert seat on the operator's keyboard rulings, to run second in the worker's queue behind the plan in flight) gives the review loop the terminal condition executing-work never stated: the loop ends at the first round with no Critical and no owed Major, where a behavior finding states a failure scenario and a claim finding states none and its fix changes only a sentence; the code-reviewer charters carry that definition as a pinned copy and rate a claim finding Minor under a `[claim]` tag, with two narrowly drawn exceptions (a security boundary, and a delta-internal contradiction of an acceptance clause on a published contract surface) keeping their bar; a second instance of a claims class takes a mechanical check or a deletion rather than a Standing Brief Amendment; a behavior Major reversing the prior round's fix mandates a consult at the second reversal; and a proposed scenario probe holds the moment.
- **`claude-kit_liveness-by-session-identity_spec_v1.md`** (Ready, four sections; kaizen pass 2026-09-02, queued ninth) rekeys the two liveness decisions the kit makes, the registry prune and the heavy-slot claim release, from the reusable seat name onto the session identifier the artifact already carries, with the harness transcript's own modification time as the corroborating leg no agent writes, so a seat name reused across a fleet restart stops shielding its dead predecessor's record; it also carries five owed coordinator-runbook amendments (the sync attempt marker as a durability leg, the clearance as a named third act, the incident funnel's archive fallback, the lookup-step tier clause, and the unleashed-worker funnel dependency).
- **`claude-kit_claim-protocol-writer-side_spec_v1.md`** (Ready, three sections; kaizen pass 2026-09-02, queued tenth) gives the heavy-process claim a kit verb with an exclusive create, a clock-read start that doubles as a per-actor token, an actor line, a session-and-token-scoped release, and a tombstone log, and states the waiter's rule the protocol never had: a waiter's bound is its own clock, a lapsed estimate is no release, and the deferring session re-checks when the blocker clears.
- **`claude-kit_memory-record-provenance_spec_v1.md`** (Ready, three sections; kaizen pass 2026-09-02, queued eleventh) adds an `author:` field the memq authoring verbs write and every read surface prints, admits a store-relative anchor on a machine-scoped operator-tier record so the tier holding a machine's own facts gets drift detection, and pins the backup-shadow case.
- **`claude-kit_kaizen-code-batch_spec_v1.md`** (Ready, ten independent sections; kaizen pass 2026-09-02, queued eighth) is the pass's code slate: a goal status render that hides no row, an arm that reconciles its directory against the session's own, a version-aware doctrine refresh with an ownership header, a per-item backlog scanner, a sidecar that strips the harness footer before judging, a doctor that grades a lock-file history hit as untrack-and-move-on and places its declined-prompt line, three pins (marker parity, archive chain, line-ending-normalized prose), the docs-curator charter naming its guard, and a warning at a non-registering Chapter.
- **`claude-kit_kaizen-prose-batch_spec_v1.md`** (Ready, twelve sections; kaizen pass 2026-09-02, rides the corpus audit's adjudication lane on the operator's routing) is the pass's prose slate, one clause per surface under the writing-skills RED and GREEN discipline: the two git instruments in the scope-and-safety bullet, three control clauses in the silent-check bullet, the backlog-finding check, the dash bar's scope, the clearance sentence, the absence-finding scope step, the anchor re-emit rule, the citation's artifact, the silent tool's result field, the Chapter template's machine-read line with the bracket and blind-dispatch statements, and the lookup-step tier clause.
- **`claude-kit_corpus-audit_spec_v1.md`** (Ready, seven sections; arms after the seven plans queued on 2026-09-02 close) has the kit's own behavior-shaping prose swept by cold readers who never loaded it, on the argument that a session which has loaded the corpus grades with the judgment that corpus shaped. It names four defect classes nothing currently sweeps: instructions that conflict across documents, incident-born density later machinery superseded, operator-approval gates installed before the loop could maintain itself, and assumptions about the harness that upstream releases have quietly falsified.
- **`claude-kit_session-roles_notes_v1.md`** (Notes, deliberately non-executable like the warden candidates bank) is the fact base and open-question set for the session-roles design dialogue: the operator's organizer/worker/expert proposal, the mandate-not-privilege position, nine open questions, and the pointer map to every durable record the 2026-08-25 peer-messaging experiment produced. Written ahead of a deliberate compaction so the dialogue resumes from the doc.
- **`claude-kit_sidecar-verdict-split_spec_v1.md`** (Ready, two sections; authored 2026-09-04 by the Expert seat on the operator's keyboard ruling adopting both fixes, for the kit worker's queue) gives the judgment sidecar a fourth verdict, `unproven`, for a check that could not establish what its intent claimed, leaving `diverged` for a result that visibly contradicts the intent, so a route defect and a check that merely found what it was built to find stop wearing one word; carries the word through the finding, the inbox item, the advisory sentence and the rollup; and gives the battery the partial-input and route-defect cases the current prompt carries without one, so a wording change can no longer shift verdicts with no case moving.
- **`claude-kit_function-hooks-spike_spec_v1.md`** (Ready, six sections; unarmed since 2026-09-07, removed from the queue on the operator's keyboard instruction because a persona prototype built on function hooks in a sibling repository is expected to answer what it measures; authored 2026-09-05 by the Expert seat) measures, in a flag-gated headless lane the ordinary suite never runs, what Claude Code's function-hooks prototype can carry for the kit: the module format, the import reach, the per-hook budget and the fail-open shape; an in-process port of the recognition nudge checked for parity against the shipped hook in one run; a `prompt.context` recovery block read across resume, with compaction left to operator verification; a tool-result secrets redactor proven both directions against the transcript and a fixture-registered PostToolUse hook; and whether a turn-end fork shares the session's prompt cache, read from its usage counts. It ends in a port, wait or never ruling per surface, a superseded operator-tier record and a draft upstream comment, and ships nothing into the installed plugin.
- **`claude-kit_test-audit_spec_v1.md`** (Ready, eight sections; thirteenth in the armed queue) censuses the suite, adjudicates every test against the earn and retire classes with a recorded verdict per test, cuts cluster by cluster with the whole gate after each, retires the wording pins over prose that is not a pinned copy, and re-baselines the ratchet caps at the cut sizes.
- **`claude-kit_coordinator-sync-machine-scope_spec_v1.md`** (Ready, three sections; fourteenth in the armed queue, authored 2026-09-03 by the Expert seat on a finding the machine coordinator seat routed on the operator's keyboard instruction, and Branch-and-PR like the git-channel plan beside it since both change the sync path) gives the memory store's sync channel a machine axis over the coordinator directory: the outbound add stages only this machine's own `coordinator/<machine>/` markdown, and an upstream change under that directory refuses the intake with its own reason code, fail-closed like an inbound leak, so the board's single-writer contract holds by construction rather than by no machine happening to edit another's files. One decision is flagged for the operator at arming: the Admin request inbox is inside the scope, which closes the cross-machine append the role skill currently entertains.
- **`claude-kit_capacity-gate_spec_v1.md`** (Ready, three sections; twelfth in the armed queue on the operator's ruling that it is heavier, reaffirmed 2026-09-02) stops a fable-override dispatch from paying finishing-work's never-started ladder (roughly twenty minutes per gate) to discover a tier exhaustion the machine's account rotator already measured: a read-only meter over claude-swap's two cache files, re-read at every fable dispatch because the active seat rebalances as often as every 60 seconds, downgrading with a measured record where the worst of the scoped and general windows sits at the floor, never polling the usage endpoint, and leaving the ladder live behind a passing gate. Distilled from NEO-CLAUDE's kaizen note at 6aab3dd.
- **`claude-kit_messaging-seat_handoff_v1.md`** (Handoff, non-executable, no `Status:` header and no leash) is what the `KIT: Messaging` seat handed its own successor across a deliberate restart. It carries the seat's standing duties, its first moves on wake, and four open items, and asks to be archived once those close.
- **`claude-kit_piano-substrate_notes_v1.md`** is a direction note, not an executable spec, header marked Direction 2026-09-03: the operator's long-horizon aim of loops on mixed timers outside the main thinking loop (fast loops that cancel or redirect, slow loops for goals and memory), the inventory of the eight loops the kit already runs with their cadences and authority, and the five questions a design has to settle first, authority and SDK billing ahead of mechanics. Signal: the armed queue and the subtraction cycle drain.
- **`claude-kit_warden-adoption-candidates_notes_v1.md`** is the candidate bank the adopted specs grew from, header marked Dispositioned 2026-08-22, each candidate carrying its disposition and evidence. It deliberately carries no `## Sections of Work` block and no `Commit Model:` header, so neither plan parser reads it as work in flight.

Completed plans are in `../archive/`. Each entry below states what that plan delivered when it shipped, which is history rather than current behavior: a later plan may have narrowed or removed what an earlier one built, so read `../architecture.md` and the skills for how the kit works now (most recent: `claude-kit_scenario-probes_spec_v1.md`, which made cold scenario probes a standing regression instrument for the kit's prose: a frozen probe set under `test/probes/`, one file per governed moment with its scenario, its context shape and a proposed answer awaiting the operator's ruling; a runner under `tools/probe-corpus/` that snapshots the shape's files at a ref or the worktree, dispatches a cold reader at the production tier through the headless CLI in an isolated configuration directory, and diffs the verdicts against the rulings with the ruled-mismatch count as its exit code; the size ratchet's own root over the probe files; and hook-ins at the writing-skills RED and GREEN step, the finishing pass's step 5, and the Chapter template's Gate and Decisions / Surprises slots, so a wording change that moves a ruled answer is a finding rather than a surprise. Before it, `claude-kit_checkpoint-session-validation_spec_v1.md`, which made the chapter checkpoint caller-scoped: `open` and `clear` refuse a session that is not the one the checkpoint blesses, through one guard whose leg order the plan states, the record carries the opener's id, the gate honors only a record whose opener matches the compacting session, a bare shell's `open` is refused naming `consent` as the operator's verb, and the executing-work skill's section loop names the refusals its two checkpoint steps can meet with the re-arm remedy and its bound. Before it, `claude-kit_reviewer-tier-hybrid_spec_v1.md`, which narrowed the flat Fable reviewer rule to one tier above the section's writer with Fable as the ceiling (haiku at sonnet, sonnet at opus, opus and fable at fable), kept the uncap's frontmatter efforts for a Fable reviewer on the Agent tool, ran a reviewer below Fable at `high` through the Workflow route, re-aimed a below-Fable reviewer whose tier could not run one tier up, returned the Opus `max` row to a first-aim Fable gate alone with finishing-work's compensation predicate made chain-wide, and had the finishing pass treat a section the rule aimed below Fable as uncleared, so the sonnet- and haiku-tier sections stop drawing Fable reviewers while the opus-tier majority keeps them. Before it, `claude-kit_write-time-neighbours_spec_v1.md`, which made `memq add-type` and `memq add-operator` run the store's semantic search over the incoming record before writing and print its three nearest live neighbours with scores, provenance and a likely-overlap label at a seeded floor, never refusing; gave `decay-scan` a per-tier pairs block nominating live same-tier records above that floor as replacement candidates, nominating and never moving; had the memory-system skill state both and send a project-tier author to `memq find` before a Write; made the index honour the check's cancellation so an expired check no longer holds the process open, and export the composition every indexed record is embedded through; and put the cross-store hit line under one exported composer so `find`'s two blocks and the neighbours block cannot drift apart. Before it, `claude-kit_reviewer-fable-uncap_spec_v1.md`, which ran every per-section reviewer at Fable with effort set by lens (the code and document pairs at `low`, the security reviewer at `medium`, the finishing reviews at `high` through the Workflow route), retired the one-tier-up rule and its Opus cap of 2026-08-19, narrowed the Opus `max` row back to the Fable-unavailable case it was built for, moved the per-section effort into the reviewer agents' frontmatter so the common dispatch rides the Agent tool, and recorded the post-cap review-round baseline (217 closed sections, per row and by reviewer class) that the backlog's open reviewer-tiering experiment judges the new levels against.)
