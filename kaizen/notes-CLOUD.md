2026-07-03 | CLOUD | claude-kit | Haiku implementation tier shipped (implementer-haiku: transcription band, brief must name the exact sibling and the self-surfacing gate, one Critical-finding review escalates to sonnet). Shipped on Scott's direction ahead of a heavy-usage week, not from observed demand, so the band edges are provisional. Driving signal, same accounting as the open sonnet-tier experiment: haiku-tier sections passing first-round adversarial review keep the band; escalations to sonnet are the mis-band counter; do not widen speculatively. Same-day cost re-pin: qa-verifier moved from opus to sonnet (revert is one line in agents/qa-verifier.md if finishing passes start missing acceptance-criterion nuance; docs-curator deliberately kept opus because mistake-vs-deviation classification gates the finishing run).
