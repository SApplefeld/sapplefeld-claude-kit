# Claude-Kit Docs

This directory is the working library and project history for the kit itself: the documents *about* the solution, the active plans, and the archived record of finished work. It is repo-level material, a sibling of `README.md`, `home/`, `kaizen/`, and `settings/`. It is not part of the installable plugin payload (`plugins/claude-kit/`), so nothing here loads or shows up when someone installs the plugin.

## Folder map

The library has three zones, and each zone has one job.

- **Root (`docs/`)** holds the stable documents about the solution and this index. Architecture, design rationale, and any security model live here. These describe what the kit *is*, not a single effort.
- **`plans/`** holds active plans only: specs that are open or in progress. A plan lives here while it is being worked. The moment it reaches `Status: Complete` or is abandoned, it moves to `archive/`. See `plans/README.md`.
- **`archive/`** holds finished and abandoned plans (their Chapters intact) and dated backlog snapshots. It is immutable history. Nothing here is live. See `archive/README.md`.

## Documents about the solution

- **`architecture.md`** is the system overview: what ships in the plugin payload, which parts are prose and which are code, the boundary between the always-on doctrine and the on-demand skills, the doctrine's bold leads as its index and the siting question that follows from it (the absence-check rule under its own lead, "A silent check earns its silence with a withheld control", because its moment of use is authoring a silent grep rather than authoring a test), the same question answered at the Dispatch Brief, where the whole-worktree prohibition and the hostile-boundary reuse step are standing rather than conditional fields because the party that can see the hazard is not the party that acts on it, the wedge hallmark that decides when a quiet dispatch may be stopped (owned by `finishing-work`, pointed at by every other surface that names it, filling its middle term with one reading per dispatch shape: byte growth on the class's own windows for a dispatch that has taken a turn, the transcript's non-synthetic assistant-line count on a five-minute pair every class shares for one that has taken none, and elapsed time where the transcript cannot be read, with the doctrine's positive-control rule under the zero-growth one), the outline-first reading rule that keeps its principle in the doctrine and routes its language anchors to `csharp-style` and `sql-style`, the test-discipline split that keeps each gate moment's lane in the doctrine and gives `testing-discipline` the litmus for what earns a test and the classes that retire one, the cost shapes, the lane mechanics, the red protocol and the contention rule, with the surfaces that point at that skill rather than restating it, the adversarial reviewer's charter among them, the matching prose split that leaves whether a sentence belongs to the doctrine and gives `writing-skills` the sentence-shape bars the document-length bullet defers to, the size ratchet over both corpora (a committed per-file cap on every curated and test file, read by `kit-size.js` and gated by `test/size-ratchet.test.js`, with a cap moving both ways through the reader's `sync` verb under the ledger rule `writing-skills` owns), the scenario-probe corpus beside it as the regression instrument over the prose the ratchet only measures (one file per governed moment under `test/probes/` carrying its scenario, its context shapes and the ruled answer, a repo-only runner that hands each shape to a cold headless reader and diffs the verdict against the ruling with the ruled-mismatch count as its exit code, and the hook-ins in `writing-skills`, `finishing-work` and the Chapter template's two slots), the doctrine parity gate's split between whole-body identity and the presence pins a deferring bullet earns, with structural sweeps beside them holding the cadence's install-surface condition to one wording across every surface that states it and every git-integration step to naming its lane or carrying an anchored exemption, and every tracked file's prose enumerating the classes that retire a test to naming the skill those classes live with rather than defining them again, the behavior-or-claim class a finding is read at and the class-keyed condition that ends a section's review loop, carried into the two code-reviewer charters as a pinned copy because an agent resolves no pointer into a skill, the consult as the kit's one reasoning-escalation instrument (a read-only fresh-context consultant convened at a counted trigger floor, with no in-context advisor beside it), the document review pair a section's `Audience:` line summons beside the code pair (a `blind-reader` per named persona, and once on a spec before it leaves brainstorming, given the documents and nothing about their intent, followed on that spec by a `plan-reviewer` that reads it against its own Goal under six closed questions before it is written Ready, and a `prose-reviewer` that checks every claim against the fact base before it judges style) and the per-run parallelization request `/kit-goal` arming carries, the intake gap check the doctrine states and the three workflow skills instance (what an input does not state is enumerated and routed to a source, a low-blast default, or a decision ask, and the declared assumptions reach the operator on the recap, a mid-run `BLOCKED:`, or the close-out status, split for the record between the plan doc's `## Assumptions` section, which freezes at approval inside the engine's fingerprint, and the Chapters' `Assumptions:` lines), the plain-language recap that opens the close-out status and rides the finishing Chapter's `Recap:` field, quoting the plan's Goal, stating what the tree now does and listing the run's refinements off the Chapters, the three surfaces the communication register rides (the source skill, the home doctrine mirror every subagent inherits, and the `Kit` output style that binds the main thread late in a session) with the parity test holding them byte-identical, the hook wiring and which hooks stand down under the external-engine marker, the Stop-hook goal leash and the ordered plan queue one arming can carry (the arm CLI binds the session it runs inside once it corroborates that session's id against a transcript on disk, with the stop and auto-compaction claim points as the fallback for an arm it could not corroborate, a terminal state advances the leash plan by plan under a single binding, only the last plan releases, and the session-start notice tells the leash holder from a bystander), the working-tree resolution that gives each of a repository's trees its own leash, against the opposed store resolution `memq` answers with because a memory is about the repository where a leash is about an execution tree, the queue position derived by walking the reading tree's own copies of those docs for their `Status:` headers rather than trusting a stored index nothing level-triggers, artifact-borne dispatch authority (a plan doc's `## Dispatch Authorization` section is the arming grant a receiver traces to the operator before arming, inert to the engine's parsed shapes, recorded per plan in the goal state as asserted provenance that grants nothing, with `--append` growing a queue where the bare arm replaces one and warns about what it drops, and the session-start advisory that names a possible sibling session in the same working tree), the three-state PreCompact gate that schedules native auto-compaction onto the boundaries a plan run declares, a section's close or an interim board entry during a closure drought, for the session holding a kit goal's leash (claiming that leash from its own transcript where no session holds it yet, which is what makes the gate reachable on a run that never stops) and defers it to the safety ceiling for a hands-on session, with the transcript-read detection that tells a `/goal`- or `/loop`-driven session from an interactive one, the two release markers that land a deferred compaction earlier than either hold would (a goalless seat's own role-boundary declaration, which releases the hands-on deny alone, and the operator-consent marker, which releases either deny, both single-shot, both age-bounded at the deferral episode's own four-hour idle bound rather than at restated figures, and journaled under their own reasons, with a Stop hook that opens the role-boundary marker for a registered seat off its own status push so that declaration is structural rather than a step prose asks a seat to remember), the landing sweep that retires the landing session's markers whatever clause allowed, the manual `/compact` the auto-only matcher never routes to the gate at all so that no marker is spent by one, the checkpoint and its two staleness bounds (a short one for a boundary declared below the trigger and a long one for a boundary declared while offers are already being held, selected by a flag the gate corroborates against its own record of the hold), the decision state and capped journal the gate writes under the project's `.kit/` and the deferral episode they carry, the PostToolUse nudge that reports that hold to a model the gate's stderr never reaches, the shared safety valve, and the five-verb CLI and the doctor setting that operate it, the two release verbs among them writing into the caller's own current directory while the gate reads at the compacting session's, and the consent verb's `--project` closing that silent miss for the operator by resolving against a named project and refusing loudly where the named session has no transcript there, with one resolver deciding where a project's `.kit/` sits so a store-resident project's gate state stays off the replicated tree, the canary's four probe classes and the build stamp its integrity check reads, the one path renderer in the shared hook library that every channel a model reads binds at its own write boundary, with its four ordered steps and the containment rule that decides what is under the home directory, the backlog block session start emits in any project holding a `docs/backlog.md` and the 90-day aging check the skills run on the same items, the memory store's shape (its three tiers and the routing ladder they complete, the two frontmatter placements the store holds at once, since Claude Code rewrites every Write into a project's memory directory into a `metadata:` map that memq's field reader takes alongside the top level, the run-scoped pending tier's writes, the session hook's eight blocks and what each session state gets from them, plus the single stand-down that replaces every cwd-derived one when an unpinned working directory names a network share, the same predicate's refusal at twelve of memq's sixteen verbs with the four shared-tier verbs it leaves running, the exit-status split behind those refusals and the mapped-drive residual none of them closes, the data-to-model boundary, the `anchors:` field a project memory carries (a repo-relative path at its git blob SHA, written by `memq anchor`, reported as drift by four read surfaces that each keep a could-not-check answer distinct from a clean one, and withheld from the fleet grant), the `triggers:` field beside it (six typed recognition patterns written by `memq triggers`, held to a four-character floor asked of all six types and a bare-token bar asked of the three fragment types alone, matched against the session's own work by a hook riding four boundaries, the two tool ones plus a prompt arriving and a subagent dispatching, that nudges with a pointer and never a body, plus the nudge log under the project's `.kit/` and the `nudgeStampRate` join over it that the semantic tier's gate reads) and the PreToolUse frontmatter guard that holds a hand-written record to what memq will read of it and refuses the write tools on the two shared tiers, the evidence-based retention gradient and the `supersedes:` pointer beside it, which labels the record a newer one replaces, demotes it in semantic ranking and nominates it for archive whatever its idle clock, authored by hand in the project tier and by a creation-only flag in the two shared ones, the shared tiers' whole-body repair and gated delete with the closing backup sweep that ends a completed delete, the two channels a body arrives on and the `cmd.exe` truncation the file channel routes around, its two digests, `recall` over what the store holds and `recent` over what it recorded inside a window, the `unstamped` read-versus-applied diff a Chapter boundary adjudicates from, the skip count every sidecar reader carries beside its status so a torn line reads as a partial loss rather than as clean evidence, and the one sanctioned exit that line has, the sync repo and the allowlist that bounds it and the store's own self-sync on Windows (a detached runner the session hook spawns commits pending memories, screens the inbound tree against the same mode-and-path allowlist, and pushes), a git worktree's resolution of its main checkout's store through a two-way `.git` handshake, the sync runner's own state file and the atomic replace that writes it, the doctor's push-destination check and the four states that block it, a `push.default` refusing this branch's pair of names among them, and the semantic index behind hybrid find with the archive suppression its default block carries, the write-time neighbours block the two shared-tier authoring verbs print before a creation and the decay scan's pairs block beside it, both reading one seeded floor, both bounded at 20 seconds with the index honouring the cancellation, both standing down on a bare store or embedder root, and the one composer all three producers of the cross-store hit line print through), the status line's two files and the different lifetimes that split them (a stable launcher the doctor byte-copies to `~/.claude/bin`, the versioned widget it loads in-process rather than spawning, and the project-local render cache keyed on the goal state and the armed plan doc, which refuses entry to any render whose position walk read a file outside that pair, with the pre-render budget and the stale-but-drawn fallback that keep a saturated box's segment on screen), session roles as the coordination vocabulary a machine's concurrent sessions share (starter seats in a set the design states as open, declared in layers none of which authenticates, with the machine-coordinator seat's four closed functions, the fourth being kaizen capture, dispositioning and dispatch, its board in the memory store, the routing that gives every board candidate a home at the moment of writing (the two board kinds as permissions and, as refusals winning over them, a durable lesson to a memory-store record and kit friction to a kaizen note, with tier selection and authoring left to the memory-system skill), homing as the operation that returns a grown board's content to those destinations and takes the line off with no pointer and no tombstone once it has confirmed the landing, the cold-successor readability test as the board's health rule with no size figure behind it, and its runbook's partly exercised state, the unrun branches named rather than counted: the cold start against an absent board, the seat handoff, the stub forms, the contest freeze, the homing round itself, and the routed finding's own path from boarding through the ask for a missing disposition pointer to reading one that lands), the per-machine coordinator directory those seats run on (four file forms under one contract the `role` skill owns, a writer rule stated per file rather than over the directory, the session registry that is the one place a working directory may live, the one-file heavy-process claim whose backstop is its holder rather than a process poll, and the coordinator's two acts that reach across the writer rule, each recorded before it fires, and the registry stamp CLI that reads the clock for the two time fields a session's push writes and audits the directory's stamps against the comparators beside them, reporting rather than gating), the `/role` takeover that arms a seat's wake before it reads anything and the standing-delegation model it resolves from a hostname-keyed operator-tier record, the seat-tier table the peer-sessions Roles section owns, the blocked-escalation ladder those seats define between them (the worker's expert ask before it declares, the coordinator's brief racing that worker's own operator ping, and an answer returning on a channel the brief names in advance rather than through the seat), the goal release event stream and its consumer contract, the judgment sidecar (the capture hook's two duties and their independent directory off switches, the judge daemon with its rollup, battery and harvest commands under `sidecar/`, the 15-minute freshness horizon that skips an entry the drain reaches too late and records the skip per stretch so a discarded backlog cannot read as a quiet fleet, the liveness stamp and timestamped diagnostics that tell a stopped daemon from an idle one, with the wrapper's stand-down line in its own log because a running daemon holds the daemon log locked, the machine-local state under `~/.claude/kit-sidecar/`, and the shared endpoint client both the daemon and `memq find` reach the off-machine model through), and the external surfaces the kit touches.
- **`security-model.md`** states what the kit's local state assumes and what it does not claim: the single principal and the absence of any boundary between projects, the memory store's per-tier provenance with each path's guard tabulated (the SessionStart project-index emission among them, fenced unconditionally rather than keyed on the pin), the machine coordinator directory sitting outside that set with the registry stamp CLI's audit as the one guarded path over it, the seven gated environment overrides, six of which redirect a path and the seventh of which steers a control's timing instead (the merged-PR push guard's no-deadline switch, whose only direction is toward waiting longer, so no setting of it can stand that guard down), plus the three deliberately ungated behavior-steering variables (the external-engine marker, `KIT_GOAL_STOP_RETRY_MS`, whose reader clamps both ends of what any value can do, and the harness's own `CLAUDE_CODE_SESSION_ID`, whose three readers gate it differently: the arm-time leash bind answers to an existence-checked transcript rather than to a second signal, the checkpoint CLI's session-scoped verbs, the two release ones and the chapter checkpoint's own `open` and `clear`, take the value on character set and leading character alone, a weaker gate whose worst wrong value costs a compaction's timing, and the memory store resolver's session-transcript leg reads it to decide which project's tier a session resolves, bounded to selecting among project directories that already exist), the scenario-probe runner's own pair of ungated overrides outside that set, since nothing an installed session loads reads them, with the runner section's account of the credential copy it puts under the OS temp root at modes only a POSIX platform honours, the shape containment it applies twice, and the absolute home-directory paths its refusals can print on stderr, and what the gates buy, the ungated `HOME`/`USERPROFILE` residual under all of them, the residual command-injection exposure on the cmd.exe wrapper, the size reader as the one payload script outside the hooks that reaches both read boundaries, taking a repository-supplied root from the Chapter template and writing one file through two of its verbs, the seven PreToolUse hooks (five that deny, one that grants, and the recognition nudge that does neither and is covered instead among the paths carrying store content into context, each with the invariant it enforces, the accepted risk it carries, its compensating controls, and the gap it cannot see), the memory frontmatter guard among them, which is the mechanism behind the shared tiers' CLI-only authoring and whose fourteen deny paths and lexical store-root containment are both stated, the read-only guard's coverage of the second dispatch route and the prose-only requirement that keeps a Workflow-dispatched read-only agent governed, the doctor's sync report as a trusted output channel where store-derived branch names are sanitized and the two pasteable commands take different treatment, the `git branch -m` remedy a four-clause gate of its own and the manual push recipe none, since it interpolates the doctor's own home-derived path rather than any store-derived string and prints only in the FIXED branch that has proven there is nothing there to leak, with the unredacted origin URL named as the disclosure neither treatment covers, how the kit invokes git (the executable never resolved against the repository under inspection, and the environment guard spelled three times, at the hooks' shared library, at the sync path's own funnel, which the unattended runner and a hand run of the script both pass through, and at the push guard's shell-form environment, which carries the strip and not the two config pins, with a Windows-only behavioural parity pin between the first two, the doctor's own bare calls against two roots on none of the three, and four residuals riding with the pins), the nine-agent strict roster that is hand-maintained in three places with no parity check and the three prose-reading members inside it that read untrusted prose while holding a shell, the compaction gate, which schedules rather than guards, its two deny states (the leashed run held to the boundaries it declares and the hands-on session held to the ceiling, the second reaching every session the harness offers a compaction to rather than only opted-in runs), the accepted risk in reading automation evidence out of attacker-influenceable transcript text and the structural parsing that bounds it, the two release markers as self-declared scheduling inputs that release a deferral or are inert and can never produce a deny, with the consent marker's authority asserted rather than authenticated and bounded by prose alone, and the marker scoping's own widest claim, that neither marker is scoped to its writer, so a party with access to another session's project directory (a strict read-only agent among them, the guard screening no `node <script>` invocation) can land a compaction mid-chapter on that session's run, the claim predicate the gate binds a leash with (the Stop hook's own, single-sourced, its two invocation shapes and the anti-steal exclusions and argument-block boundary that bound them, and what a forged claim would and would not buy), and its one not-fail-open direction (a ceiling assuming the roughly 1,000,000-token window these sessions carry) covered by disclosure rather than by any check, the gate's own two records under `.kit/` as user-writable files, one of which steers which staleness bound a checkpoint takes, the status line's render cache as the one `.kit/` file whose stored line reaches a terminal whole and so is sanitized at the print rather than at the parse, the goal state file's eleven readers, ten of them the leash's own project with nine hardened alike and the doctor named as the exception, and the machine-coordinator seat's foreign read through the CLI as the eleventh, the working tree that now decides where that file lives, which moves the writes as well as the reads, the one field naming a path outside the project (`boundTranscript`, stated and stat'ed rather than followed), the reported queue position as repository content becoming a trust input for a number a session is told, held by four bounds and deciding nothing, the status line's own render cache refusing to store any line whose position walk read past the one doc its key stats, the one field in it taken from a document rather than from its own writer (the recorded arming authorization, asserted and never authenticated, screened at both ends because the readers print it), and the compare-and-swap that stands in for a lock when a queued leash advances or grows, what the build's hook manifest does and does not cover (the output style is payload it never hashes, and it is system-prompt content in every session), what retention does and does not bound (archiving is demotion rather than deletion, the 425-day ceiling that bounds an extended memory's idle clock alone, since a `supersedes:` pointer nominates the record it names whatever that clock reads and is withheld from the unattended vector at both the grant hook and the CLI, the `pinned:` exemption that outranks both, and beside all of them the shared-tier delete verbs, which remove rather than demote, with the local residue a completed delete leaves enumerated and the two consent gates a shared retirement takes), the coordinator board as coordination state that is never an authority surface (a board the seat writes itself cannot satisfy a receiver's trace back to the operator), its readership set by whatever remote the store is configured with rather than by anything the kit reads or licenses, so the content bars hold whatever that readership is and the private-remote premise stays a precondition of an installation, the writes the seat makes outside its own directory as a standing part of its loop (a memory-store record, a note in the kit checkout's kaizen inbox whose confirmed landing is the commit and push that skill's capture rule defines, which runs no gate and reaches this repository's public remote, and an outcome-journal entry at each end of a homing round, which is that round's own write rather than the routing's), the operator tier's prompt-free grant re-priced against that per-pass store write, with the board's absence and contest branches and the homing round standing as prose no seat has yet run, the judgment sidecar's inventories (the capture spool as a machine-local plaintext concentration whose activation lever is the daemon's first run and which is stated as not tamper-evident, the recognition egress that sends an observed project's whole memory index off the machine, the delivery inbox as the one surface where model-derived text is placed into another model's context, framed and capped rather than trusted, the memq relevance channel's bounded candidate export with its locality warning and endpoint fingerprint as detection controls, and the evaluation batteries, whose freezing step rather than any runtime control is where public disclosure is decided), and why fail-open hooks mean silence is not evidence of health. Written as preconditions that can be re-tested rather than assurances that cannot.
- **`fleet-integration.md`** is the contract between the kit and the external engine that spawns sessions with it: the nine environment variables, two of them gate signals, that must never be omitted, the nine hooks that change behavior under the marker and the three controls that go dark with the canary, the run-scoped memory tier and the promotion the engine owns, the memq grant and the spawn-environment coupling that can turn it off, and the three surfaces the engine reads back. It assembles what `architecture.md` and `security-model.md` describe mechanism by mechanism rather than restating either.

## Living documents

- **`backlog.md`** is the single living handoff and next-steps doc. It carries only active items, each carrying the date it was parked, which is what the session-start block and the prune pass age an item from. Completed items are pruned out to a dated snapshot in `archive/` so the doc never grows without bound.

## How the library is maintained

The `curating-docs` skill owns the mechanics: it archives a plan when it completes, prunes the backlog, cross-references related plans, and refreshes this index. `finishing-work` calls it at close-out, `brainstorming` calls it when a new plan is written, and it can be invoked directly to tidy or retrofit an existing tree.

## Active plans

The plan documents sit in `plans/`. `plans/README.md` carries each one's full description and the go state of its header; the running order of the armed plans rides in their own entries, each as a place in the queue as armed rather than a live position, since a plan closing advances the leash without touching this file and `kit-goal.js status` is what tracks where the run stands.

- **`plans/claude-kit_liveness-by-session-identity_spec_v1.md`** (Ready, four sections; kaizen pass 2026-09-02, queued ninth) rekeys the registry prune and the heavy-slot claim release from the reusable seat name onto the session identifier, with the harness transcript's modification time as the corroborating leg no agent writes, and carries five owed coordinator-runbook amendments.
- **`plans/claude-kit_claim-protocol-writer-side_spec_v1.md`** (Ready, three sections; kaizen pass 2026-09-02, queued tenth) gives the heavy-process claim a kit verb with an exclusive create, a clock-read start doubling as an actor token, a scoped release and a tombstone log, and states the waiter's rule.
- **`plans/claude-kit_memory-record-provenance_spec_v1.md`** (Ready, three sections; kaizen pass 2026-09-02, queued eleventh) adds an `author:` field the memq authoring verbs write, admits store-relative anchors on machine-scoped operator-tier records, and pins the backup-shadow case.
- **`plans/claude-kit_kaizen-code-batch_spec_v1.md`** (Ready, ten independent sections; kaizen pass 2026-09-02, queued eighth) is the pass's code slate: nine small instruments that stop lying or start existing, each with a red-first test.
- **`plans/claude-kit_kaizen-prose-batch_spec_v1.md`** (Ready, twelve sections; kaizen pass 2026-09-02, rides the corpus audit's adjudication lane on the operator's routing) is the pass's prose slate, one clause per surface under the writing-skills RED and GREEN discipline.
- **`plans/claude-kit_corpus-audit_spec_v1.md`** (Ready, seven sections; arms after the seven plans queued on 2026-09-02 close) has the kit's behavior-shaping prose swept by cold readers who never loaded it, because a session that has loaded the corpus grades with the judgment that corpus shaped. Four defect classes are in scope: cross-document conflicts, incident-born density later machinery superseded, operator-approval gates installed before the loop could maintain itself, and assumptions about the harness that upstream releases have falsified.
- **`plans/claude-kit_session-roles_notes_v1.md`** (Notes, non-executable) banks the session-roles design dialogue's fact base, positions, and open questions, with the pointer map to the peer-messaging experiment's durable records.
- **`plans/claude-kit_sidecar-verdict-split_spec_v1.md`** (Ready, two sections; authored 2026-09-04 by the Expert seat on the operator's ruling adopting both fixes) gives the judgment sidecar a fourth verdict, `unproven`, for a check that could not establish its claim, keeps `diverged` for a visible contradiction, carries the word through every reading surface, and gives the battery the partial-input and route-defect cases it lacks.
- **`plans/claude-kit_function-hooks-spike_spec_v1.md`** (Ready, six sections; fifteenth and last in the armed queue, appended 2026-09-05 on the operator's keyboard instruction recorded in its authorization section; authored 2026-09-05 by the Expert seat) measures, in a flag-gated headless lane the ordinary suite never runs, what Claude Code's function-hooks prototype can carry for the kit: the module format, the import reach, the per-hook budget and the fail-open shape; an in-process port of the recognition nudge checked for parity against the shipped hook; a recovery block read across resume; a tool-result secrets redactor proven both directions against the transcript; and whether a turn-end fork shares the session's prompt cache. It ends in a port, wait or never ruling per surface and a draft upstream comment, and ships nothing into the installed plugin.
- **`plans/claude-kit_test-audit_spec_v1.md`** (Ready, eight sections; thirteenth in the armed queue) censuses the suite, adjudicates every test against the earn and retire classes with a recorded verdict per test, cuts cluster by cluster with the whole gate after each, retires the wording pins over prose that is not a pinned copy, and re-baselines the ratchet caps at the cut sizes.
- **`plans/claude-kit_coordinator-sync-machine-scope_spec_v1.md`** (Ready, three sections; fourteenth in the armed queue, authored 2026-09-03 by the Expert seat on a finding the machine coordinator seat routed on the operator's keyboard instruction, Branch-and-PR) gives the memory store's sync channel a machine axis over the coordinator directory: the outbound add stages only this machine's own `coordinator/<machine>/` markdown, and an upstream change under that directory refuses the intake with its own reason code, fail-closed like an inbound leak, so the board's single-writer contract holds by construction rather than by accident. The Admin request inbox is inside the scope, flagged for the operator at arming.
- **`plans/claude-kit_capacity-gate_spec_v1.md`** (Ready, three sections; twelfth in the armed queue on the operator's ruling, reaffirmed 2026-09-02) gives fable-override dispatch a pre-flight meter over claude-swap's usage cache, re-read at every dispatch, so tier exhaustion is a measured downgrade rather than a twenty-minute timeout discovery, with the never-started ladder kept live as the backstop.
- **`plans/claude-kit_messaging-seat_handoff_v1.md`** (Handoff, non-executable) is the seat handoff the `KIT: Messaging` session wrote for its own successor across a deliberate restart: the seat's standing duties, its first moves on wake, and its open items, which it asks be archived once they close.
- **`plans/claude-kit_piano-substrate_notes_v1.md`** is a direction note, not an executable spec, header marked Direction 2026-09-03: the operator's long-horizon aim of loops on mixed timers outside the main thinking loop (fast loops that cancel or redirect, slow loops for goals and memory), the inventory of the eight loops the kit already runs with their cadences and authority, and the five questions a design has to settle first, authority and SDK billing ahead of mechanics. Signal: the armed queue and the subtraction cycle drain.
- **`plans/claude-kit_warden-adoption-candidates_notes_v1.md`** is the candidate bank the two adopted specs grew from, header marked Dispositioned 2026-08-22: five design ideas from evaluating Warden-AI, each now carrying its disposition and the evidence (including the NEO probe that parked impact analysis), so nothing is re-proposed from the same README. It remains deliberately non-executable.

Everything else is in `archive/`.

Completed plans are in `archive/`. Each entry below describes what that plan delivered when it shipped, which is the record of how the kit got here rather than a description of how it works now: a later plan may have narrowed or removed what an earlier one built, so read `architecture.md` and the skills for current behavior. Most recent: `claude-kit_scenario-probes_spec_v1.md`, which made cold scenario probes a standing regression instrument for the kit's prose: a frozen probe set under `test/probes/`, one file per governed moment with its scenario, its context shape and a proposed answer awaiting the operator's ruling; a runner under `tools/probe-corpus/` that snapshots the shape's files at a ref or the worktree, dispatches a cold reader at the production tier through the headless CLI in an isolated configuration directory, and diffs the verdicts against the rulings with the ruled-mismatch count as its exit code; the size ratchet's own root over the probe files; and hook-ins at the writing-skills RED and GREEN step, the finishing pass's step 5, and the Chapter template's Gate and Decisions / Surprises slots, so a wording change that moves a ruled answer is a finding rather than a surprise. Before it: `claude-kit_checkpoint-session-validation_spec_v1.md`, which made the chapter checkpoint caller-scoped: `open` and `clear` refuse a session that is not the one the checkpoint blesses, through one guard whose leg order the plan states, the record carries the opener's id, the gate honors only a record whose opener matches the compacting session, a bare shell's `open` is refused naming `consent` as the operator's verb, and the executing-work skill's section loop names the refusals its two checkpoint steps can meet with the re-arm remedy and its bound. Before it: `claude-kit_reviewer-tier-hybrid_spec_v1.md`, which narrowed the flat Fable reviewer rule to one tier above the section's writer with Fable as the ceiling (haiku at sonnet, sonnet at opus, opus and fable at fable), kept the uncap's frontmatter efforts for a Fable reviewer on the Agent tool, ran a reviewer below Fable at `high` through the Workflow route, re-aimed a below-Fable reviewer whose tier could not run one tier up, returned the Opus `max` row to a first-aim Fable gate alone with finishing-work's compensation predicate made chain-wide, and had the finishing pass treat a section the rule aimed below Fable as uncleared, so the sonnet- and haiku-tier sections stop drawing Fable reviewers while the opus-tier majority keeps them. Before it, `claude-kit_write-time-neighbours_spec_v1.md`, which made `memq add-type` and `memq add-operator` run the store's semantic search over the incoming record before writing and print its three nearest live neighbours with scores, provenance and a likely-overlap label at a seeded floor, never refusing; gave `decay-scan` a per-tier pairs block nominating live same-tier records above that floor as replacement candidates, nominating and never moving; had the memory-system skill state both and send a project-tier author to `memq find` before a Write; made the index honour the check's cancellation so an expired check no longer holds the process open, and export the composition every indexed record is embedded through; and put the cross-store hit line under one exported composer so `find`'s two blocks and the neighbours block cannot drift apart. Before it, `claude-kit_reviewer-fable-uncap_spec_v1.md`, which ran every per-section reviewer at Fable with effort set by lens (the code and document pairs at `low`, the security reviewer at `medium`, the finishing reviews at `high` through the Workflow route), retired the one-tier-up rule and its Opus cap of 2026-08-19, narrowed the Opus `max` row back to the Fable-unavailable case it was built for, moved the per-section effort into the reviewer agents' frontmatter so the common dispatch rides the Agent tool, and recorded the post-cap review-round baseline (217 closed sections, per row and by reviewer class) that the backlog's open reviewer-tiering experiment judges the new levels against.

## Archive

See `archive/` for completed plans and backlog snapshots.
