# The corpus is read by cold probes at the tier that runs it, and a wording change that moves an answer fails a test

Status: In Progress
Commit Model: Commit-and-Push
Created: 2026-09-01

Session model: any executor session in the kit repo; three sections, tiers per section. Authored 2026-09-01 by the session that shipped the precedence-and-ownership plan, at the operator's question whether the probe experiment belongs in that session or in a follow-on. This document is the follow-on. Anchors are authoring-time; re-locate every hit by content.

## Dispatch Authorization

Authorized 2026-09-02 by the operator, first-hand on the allowlisted relay thread, to be appended to the kit worker's armed queue: the cold-probe regression instrument for behavior-shaping prose as designed here, its three Decisions taken at their recorded recommendations unless the operator rules otherwise. The operator's word was a standing instruction to the KIT: Expert seat to append every valuable parked plan to the worker's queue; that seat recorded it here and ran the append. Per the peer-sessions trace rule this section is a warrant only for a citing session that did not author it, and the receiving session performs its own trace: the grant is the operator's message on the Expert session's relay thread, and the plan arms only by the operator's word or the Expert seat's append under it.

## Goal

The kit tests its hooks mechanically and its prose by review, and review by a session that loaded the prose ratifies what it read. The pilot that preceded this plan showed a cheaper instrument works: a cold reader at the tier the corpus is written for, handed a concrete situation and a fixed set of the governing files with no intent story, reports what the corpus instructs and where it forks. Run before and after a wording change, that reading is a regression test for prose: the answer to a governed moment either held or moved. When this plan is done, the kit carries a frozen probe set keyed on the moments that bite in production, each probe naming its scenario, the context shape it runs under, and the answer the operator ruled; a runner that snapshots the shape's files, dispatches a cold reader per probe at the production tier, and diffs the verdicts against the rulings; and the two hook-ins that make the runner routine, one at the skill-amendment step and one at the corpus audit's conflict lane.

## Evidence

- The pilot, 2026-09-01: one Opus extraction pass over sixteen git-governing documents (170 instructions, 20 conflicts, 4 verified at the file); one cold Sonnet probe on three scenarios that reached the right commit-and-push answer with the full file set and listed six hesitation passages; and a before-and-after pair on the precedence section, in which the Branch-and-PR pull-request moment read CONTESTED before with the reader supplying its own reconciliation, and the other two scenarios resolved both times. The precedence plan's Chapter carries the readings: `docs/archive/claude-kit_precedence-and-ownership_spec_v1.md`.
- The false bars the operator reported arise under a subset of the corpus rather than under the whole of it: a checklist copy that dropped the exception, a brief that omitted the plan header, a post-compaction context holding a summarized skill. A probe run only on the full file set under-reports them, which is why each probe names its context shape.
- The writing-skills skill already requires a RED and a GREEN reading for any change to behavior-shaping content, in a fresh session rather than a subagent of the editing session, and names the headless `claude -p` as the vehicle. The runner is that discipline made repeatable.
- The corpus-audit plan's conflict lane already specifies scenario probes at Opus and Sonnet as a one-time sweep; this plan makes the probe set the standing artifact that lane consumes and adds to.

## Decisions

Ruled 2026-09-02 by the operator, first-hand on the allowlisted relay thread, from the kaizen pass's close-out recap: all three stand at their recommendations.

1. **Tier per probe.** Recommended: Sonnet by default, Opus for probes on moments only an orchestrator meets (dispatch, review roster, compaction boundary), matching the corpus-audit plan's representative-reader ruling. Alternative: Opus everywhere, which costs more and under-reports the confusion that bites Sonnet seats.
2. **What a mismatch does.** Recommended: reports, never blocks, in the first version; the runner's exit code is the count of mismatches on probes whose ruling state is `ruled`, with mismatches on `proposed` probes reported beside it and never counted, and with a shape the probe file marks as a designed mismatch reported apart and never counted, its agreement with the answer being the finding that marker exists to surface, and the finishing pass reads it as a gate reading in the Chapter. Alternative: a pre-commit block on prose files, declined for now because a probe's reading is itself model output and a flaky block on doctrine edits would train authors to skip the runner.
3. **Cadence.** Recommended: on demand at any behavior-shaping wording change and once per corpus audit; no timer. Alternative: weekly with the kaizen pass, deferred until the set's stability is known.

## Sections of Work

### 1. The probe set and its rulings. Model: opus

`test/probes/<moment>.md`, one file per probe, frontmatter carrying the moment, the context shape as an ordered file list under the plugin root and `home/`, the tier, and the ruled answer as a verdict (RESOLVED with the action, CONTESTED with the owner the ruling assigns, or SILENT with the declared default), body carrying the scenario text verbatim as the reader receives it. The first set covers the moments that bit in the month before authoring, drawn from the precedence plan's contested list and the kaizen inbox: the commit and the push at section close under each commit model; the pre-send checklist met after an authorized push; a peer message asking a leashed session for work; a compaction nudge arriving mid-section with no checkpoint open; an operator-approval gate met on a run the operator armed; a seat asked to push the memory store; a stale anchor in a plan the session resumes. Each probe runs under at least two context shapes, the full governing set and the narrowest production shape that reaches the moment (a brief-only subagent, doctrine plus the output style, doctrine plus one skill). The rulings are gathered in one batched brief to the operator in the client-briefing register, and the set freezes at the ruling.

Acceptance: every probe file parses, names a shape whose files exist, and carries a ruled verdict; the batched brief and its rulings recorded in the Chapter; suite delta against a recorded baseline.

### 2. The runner. Model: opus

`tools/probe-corpus/run.mjs`: for each probe, copies the shape's files into a scratch directory under `.kit/`, composes the reader prompt from a fixed template (the pilot's prompt is the seed, stored beside the runner), invokes the headless CLI at the probe's tier with the scratch directory as the reader's only input, parses the verdict line, and diffs it against the ruling. Output is one report per run under `.kit/` with each probe's verdict, the ruling, match or mismatch, and the reader's cited passages, so a mismatch is re-locatable by content; the exit code is the count of mismatches on `ruled` probes, a `proposed` probe's mismatch being reported and never counted, since every probe is proposed until the operator rules and a count over proposed rulings would gate on nobody's ruling, and a shape carrying a per-shape `designed-mismatch:` marker reported apart and never counted, since a red the set was built to show is not a reading that moved. The runner takes a `--before <ref>` option that checks the shape's files out of a git ref into the scratch directory instead of the worktree, so a before-and-after pair is two invocations and no hand copying. The template's no-intent-story bar is stated in the runner's own comments and in the template, per the blind-reader discipline.

Acceptance: the runner reproduces the pilot's three verdicts on the precedence plan's before-and-after snapshots (CONTESTED, RESOLVED, RESOLVED before; the after readings as the precedence Chapter records them); a deliberately broken ruling produces a nonzero exit, as the runner's own control; suite delta against a recorded baseline.

### 3. The hook-ins. Model: sonnet

`plugins/claude-kit/skills/writing-skills/SKILL.md`, in the RED and GREEN paragraph: where a change touches a moment a `ruled` probe covers, the runner's before-and-after pair is the RED and GREEN, a `proposed` probe's reading being evidence for the rulings batch instead, and a mismatch on a ruled probe the change did not intend to move is a finding. `plugins/claude-kit/skills/finishing-work/SKILL.md`: a plan whose files in scope include behavior-shaping prose runs the probe set at the finishing pass and names the reading in the Chapter, beside the lanes. `docs/plans/claude-kit_corpus-audit_spec_v1.md` is not edited (a Ready plan the operator authorized); its Related section is the place a later session points at this plan, and the audit's conflict lane is briefed to add every conflict it confirms to the probe set as a new file. Whole-file review for both skill amendments, per the recorded skill-amendment defect mode.

Acceptance: both skills carry the hook-in in one paragraph each, pointing at the runner rather than restating it and agreeing with every unchanged neighbour; the parity and shape suites green at baseline; suite delta against a recorded baseline.

### 4. The size ratchet classifies the probe set. Model: sonnet

`plugins/claude-kit/scripts/kit-size.js`: the measured corpus gains a `test/probes/` root, measured in words with the shape `test/probes/<name>.md`, listed ahead of the `test/` root because the classifier takes the first root holding a path; `test/size-budget.json` gains a cap per probe file and for `test/probes/README.md`, each at the file's current word count; `test/size-ratchet.test.js` extends whatever pins the root roster, its count, or the classifier's coverage, with a pin that a `test/probes/*.md` path classifies as words and a `test/probes/<name>.txt` path reds as unclassified. Without this root a tracked `test/probes/*.md` matches no shape under the `test/` root and reds the ratchet as unclassified.

Acceptance: `classify` over every path under `test/probes/` returns them as word-measured entries with none unclassified (proved in-process, since the check verb reads tracked files and the probe set is untracked until the section commit); `node --test test/size-ratchet.test.js` green; `node plugins/claude-kit/scripts/kit-size.js check` green; suite delta against a recorded baseline.

### 5. The Chapter template holds the probe reading. Model: sonnet
Locus: inline

`plugins/claude-kit/skills/executing-work/SKILL.md`, the Chapter template's `Gate:` line: it gains the slot the finishing pass's probe reading lands in (the summary line the runner prints to stdout: pairs, mismatches, errors, exit code, reader tiers, report path; or that no report was produced), stated as reporting and never blocking, so that finishing-work's hook-in points at the owner of the Chapter shape rather than defining a field from outside it.

Acceptance: the `Gate:` line names the reading; finishing-work's hook-in points at it and restates nothing; the parity and shape suites green; suite delta against a recorded baseline.

## Out of Scope

- Ruling on the contested moments themselves: the backlog item from the precedence plan carries those asks.
- Single-sourcing the copied charters and the output style's doctrine core through the build: a separate plan.
- A mechanical block on prose commits: declined in decision 2 for the first version.

## Related

- `docs/archive/claude-kit_precedence-and-ownership_spec_v1.md`: the plan whose pilot seeded this one and whose Chapter holds the first before-and-after readings.
- `docs/plans/claude-kit_corpus-audit_spec_v1.md`: the one-time sweep whose conflict lane consumes and extends the probe set.
- `plugins/claude-kit/skills/writing-skills/SKILL.md`: the RED and GREEN discipline the runner mechanizes.
- `sidecar/batteries/`: the frozen-battery precedent the probe set's freeze-at-ruling rule follows.

## Chapters

### Interim board 1 - 2026-09-06

Run start: the header read `Status: Ready` and now reads `In Progress`, normalized at run start per executing-work. The pilot's original cold-reader prompt is not recoverable on this machine (four scans over every kit-project transcript found no dispatch carrying it), so the runner's template is written from the precedence plan's Chapter description of the pilot; declared route (b), section 2. The reader isolation model is declared route (b), section 2: the headless CLI on this box (2.1.263) authenticates by OAuth with no API key, so `--bare` is unavailable and the cold shape is a scratch `CLAUDE_CONFIG_DIR` holding only the credentials file, confirmed or corrected by section 2's isolation control. Section 4 appended by section 3's review round (approval drift, recorded here): the size ratchet reds a tracked `test/probes/*.md` as unclassified, and no section listed the classifier.

Sections in flight: 1 (probe set, implementer-opus, dispatched 21:58Z, running; nine probe files and the parser are on disk). 2 (runner, implementer-opus, dispatched 21:58Z, running; run.mjs, template.md and probe-file.mjs import on disk). 3 (hook-ins, implementer-sonnet, DONE at 22:05Z; round 1 at opus/high via Workflow returned adversarial CHANGES_REQUIRED 0/8/4 and blind CHANGES_REQUIRED, converging: the finishing-work sentence mandated a repo-local command in a portable skill, added a field to the Gate line executing-work owns, sat unplaced in step 5's written order, dropped Decision 2's reports-never-blocks; the writing-skills sentence collided with the several-reps and home/-shape neighbours and ran past forty words. Both sentences rewritten inline by the main session at 22:15Z; lane `node --test test/doctrine-parity.test.js test/size-ratchet.test.js test/hook-canary.test.js` 199/199/0 exit 0, size check exit 0 with caps finishing-work 16860 to 16945 and writing-skills 2608 to 2698; fix-delta adversarial review at opus/high dispatched 22:21Z, running. Ruling: section 3's commit is held behind sections 1 and 2's commits so the pointers never ship ahead of their targets). 4 (size root, implementer-sonnet, dispatched 22:21Z, running).

Gate baseline: whole suite 3219 tests / 3209 pass / 1 fail / 9 skipped, exit 1, measured 2026-09-06 at plan 1's handoff gate on this main checkout at b4e249b with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the one red being test/memory-session.test.js "a pinned directory too long to name faithfully stands the session down", permanent on this box.

Rulings adopted since the last boundary: probe rulings are `proposed` until the operator's batched brief is answered, which the plan makes the operator's; the brief goes out on the relay once section 1 reports and the set freezes at the ruling. Next per section: 1 and 2 await their reports, then verification, then fable review pairs (security beside them on 2); 3 awaits its fix-delta review, then closes after 1 and 2 commit; 4 awaits its report, then opus reviewers at high.

### Interim board 2 - 2026-09-06

Sections in flight: 1 (probe set, first fable review pair returned CHANGES_REQUIRED, one Critical: the compaction probe's ruled answer contradicted executing-work's interim-boundary rule, under which the deferral nudge is itself a trigger; plus an invented gate line, a mixed action-and-owner option list on the Branch-and-PR probe, a false `main` sentence on the branch-delete probe, split no-push options on the store-push probe, and two indistinguishable stale-anchor options; fix round at implementer-opus dispatched 23:36Z from .kit/scratch/probes/brief-s1-fix.md, running). 2 (runner, fable pair plus security returned CHANGES_REQUIRED and CONCERNS: the suite copied the live credentials file, `$`-pattern substitution in the prompt composer, any nonzero git status read as absent, errors counted as mismatches, unbounded shape-file reach, credential copy lifetime, spawn cwd, the isolation control unshipped and its withheld variant unrun, and the pilot's before verdict on the pull-request moment not reproduced; fix round at implementer-opus dispatched 23:36Z from brief-s2-fix.md, running, amended in flight to print one stdout summary line: pairs, mismatches, errors, exit, tiers, report path). 3 and 5 (hook-ins and the Chapter template slot: opus-at-high adversarial plus blind round returned CHANGES_REQUIRED; the passages were rewritten by the main session at 23:52Z so the writing-skills pair sits beside the reps bar, names the repair case, scopes with `--only` and keys on a `ruled` probe; finishing-work runs the set over the tree as it then stands just ahead of the whole gate and records an unrun run as unrun; executing-work's slot names the stdout summary line and is final-Chapter-only; a further review round is owed on that delta). 4 (size root: opus pair APPROVED_WITH_CONCERNS; the two Majors, cap slack and a commit-order dependency between the classifier and the probe files, are closed by setting every cap at commit time and landing sections 1, 2 and 4 in one commit; docs/architecture.md's root enumeration folded in by the main session; a README sentence on the cap requirement handed to section 1's fix round).

Approval-region edits since the last boundary: section 5 appended (the Chapter template slot, Locus: inline); sections 4 and 5 restated in the present tense; section 3's acceptance reads one short paragraph each rather than one sentence, because two rounds showed a single sentence could not both point at the runner and agree with its neighbours.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Section lanes this stretch: `node --test test/probe-set.test.js test/probe-runner.test.js test/size-ratchet.test.js test/doctrine-parity.test.js test/hook-canary.test.js` 302/301/0/1 exit 0 at 23:20Z before the fix rounds; the parity lane over the three skills 212/212/0 exit 0 at 23:38Z and, after the 23:52Z rewrites, 212/211/1 exit 1 with the one red being the size ratchet over files the fix rounds hold mid-edit.

Rulings adopted: the batched rulings brief went to the operator on the relay at 23:30Z with all nine proposed; item 6 (the compaction nudge) is superseded by the Critical above and the brief is re-sent when the fix round lands, so the rulings stay `proposed`. Next per section: 1 and 2 await their fix reports, then verification, then a fix-delta review round each; 3 and 5 await one more opus-at-high round on the rewritten passages; 4 closes with 1 and 2 in the one commit.

### Interim board 3 - 2026-09-06

Sections in flight: 1 (probe set: fix round 1 landed at 23:05Z, lane 76/75/0/1 exit 0; fix-delta fable pair returned CHANGES_REQUIRED 0/2/7 and APPROVED_WITH_CONCERNS 0/1/5, the Majors being the Branch-and-PR probe reading red by construction, since the reader template routed a no-owner CONTESTED to OTHER and the `doctrine-plus-executing-work` shape could only read finishing-work, plus a directory entry passing both the parser and the corpus pin; fix round 2 at implementer-opus from .kit/scratch/probes/brief-s1-fix2.md landed at 23:43Z, lane 79/78/0/1 exit 0, the probe's third shape now `doctrine-plus-the-contested-surfaces`, its closing question naming the unowned value, the pin on `isFile` plus `git ls-files`, designed-mismatch shapes marked in a frontmatter comment and the README; a fix-delta review round is owed). 2 (runner: fix round 1 landed at 23:12Z, lane 49/48/0/1 exit 0; fix-delta fable pair plus security returned CHANGES_REQUIRED 0/4/4, CHANGES_REQUIRED 0/2/4 and CONCERNS 0/0/6: the `--before` leg bypassed containment through git's own `..` normalization, `readContained` judged existence before containment, the README's isolation attribution was unmeasured and a superseded bare-variant log suggested the CLI falls back to the real home when the config dir holds no credentials, plus the stale-sweep mtime premise and the spec's pilot-reproduction acceptance unmet on three stable samples; fix round 2 at implementer-opus from brief-s2-fix2.md dispatched 23:31Z, running, thirteen items including the ruled-versus-proposed mismatch split in the summary line, `probe-corpus: <n> pairs, <m> mismatches (<p> on proposed rulings), <e> errors, exit <x>, tier <tiers>, report <path>`, with `(partial)` for a run that died mid-set, and the template amendment routing a no-owner CONTESTED to the list's own value). 3 and 5 (hook-ins and the Chapter slot: round 2 at opus/high returned CHANGES_REQUIRED 0/8/5 and CHANGES_REQUIRED, round 3 CHANGES_REQUIRED 0/8/6 and APPROVED_WITH_CONCERNS, each round partly reversing the last on where the baseline belongs; the main session rewrote the three passages at 00:02Z toward the spec's own scoping: the slot stays the finishing pass's and final-Chapter-only, holding one summary line per leg with the `(partial)` mark and its moment-pin; finishing-work runs the after leg over the set and the before leg with `--only` over the mismatched moments at the effort's base ref, disposes a ruled mismatch to the close-out and a proposed one to the rulings batch, and runs ahead of the `docs/` edits because a shape names files under the plugin and the home directory alone; writing-skills triggers on any covering probe, derives the before ref as the change's parent, re-runs a mismatching leg, states that a match is single-sample evidence, keys the both-legs case on the readings agreeing, and names the designed-mismatch class; round 4 at opus/high dispatched 00:05Z, running). 4 (size root: closes with 1 and 2 in the one commit; caps for the three skills reset to exact size at each rewrite, 21612/17133/2913 words now).

Approval-region edits since the last boundary: none. docs/plans/README.md's entry for this plan corrected from "Ready, three sections; fifth in the armed queue" to "In Progress, five sections; second", a document a completed section's scope carried that this run's changes falsified, taken as the standing carve-out. Section 2's first acceptance clause (reproduce the pilot's CONTESTED, RESOLVED, RESOLVED) is unmet on three stable samples and unmeetable, the pilot prompt being unrecoverable and the pilot's file set eight where the shape carries six: the amendment to the spec is held for the operator's rulings batch, where the three readings ride as evidence, rather than rewritten by the run.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Section lanes this stretch, on this main checkout at c333aa4 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the five-file lane 326/324/0/2 exit 0 at 23:15Z after fix round 1 (delta +24 tests against 302/301/0/1); the parity lane `node --test test/doctrine-parity.test.js test/hook-canary.test.js` 121/121/0/0 exit 0 at 00:07Z after the round-4 rewrite; size check exit 0 at 00:07Z.

Rulings adopted: the rulings brief re-send to the operator is held until section 2's second fix round lands, so it carries the branch-and-pr probe's three-sample readings (RESOLVED/finishing-work under the full shape) beside the proposed unowned ruling, the compaction probe's flipped answer, the merged options, and the section 2 acceptance amendment. Next per section: 1 awaits its fix-delta round; 2 awaits its fix report, verification and a fix-delta round with security; 3 and 5 await round 4; then chapters 1, 2 and 4 in one commit, then 3 and 5.

### Interim board 4 - 2026-09-07

Sections in flight: 1 (probe set: fix-delta fable pair over fix round 2 returned APPROVED_WITH_CONCERNS 0/0/3 and APPROVED_WITH_CONCERNS 0/0/4; fix round 3 at implementer-opus from .kit/scratch/probes/brief-s1-fix3.md landed at 00:15Z, exporting the parser's two allowlist constants for the runner, holding options to slugs, refusing a `#` on a scalar, trimming the branch-and-pr closing sentence to "Name that owner." so the template alone routes a no-owner CONTESTED, naming the branch-and-pr full-shape reading in the probe README as a mismatch awaiting the operator, and setting four caps exact; lane 82/81/0/1 exit 0; a fix-delta fable pair dispatched 00:33Z, running). 2 (runner: fix round 2 landed at 00:00Z, lane 66/65/0/1 exit 0, the bare rung confirmed the credential copy authenticates and no fallback to the operator's home occurs; fix-delta fable pair plus security returned CHANGES_REQUIRED 0/3/5, APPROVED_WITH_CONCERNS 0/1/6 and CONCERNS 0/0/4; fix round 3 from brief-s2-fix3.md landed at 00:30Z with the newer-wins credential refresh, the allowlist at the read boundary, the ref-mode link refusal through `git ls-tree`, the git spawn cwd at the binary's directory, and one retire-class assert removed; lane 72/71/0/1 exit 0; fix-delta fable pair plus security dispatched 00:33Z, running). 3 and 5 (hook-ins and the Chapter slot: rounds 4 and 5 at opus/high each returned CHANGES_REQUIRED with the prior round's findings closed and new ground opened, the pattern being passages that grew to satisfy each round and then drew length and restatement findings; the main session rewrote all three at 00:20Z as short pointers, the runner's README and finishing-work's step 5 owning the mechanics, the writing-skills pair restored to the spec's own reading as the RED and GREEN for a covered moment, the caret sentence taking finishing-work's hedge after round 5 caught a false PowerShell claim, the re-run rule carving out the designed mismatch and scoping to moments, and the Gate slot asking for the summary line verbatim; parity lane 121/121/0 exit 0; round 6 at opus/high dispatched 00:27Z, running). 4 (size root: closes with 1 and 2 in the one commit; caps for the three skills at 21567/17209/2819 words).

Approval-region edits since the last boundary: Decision 2 and section 2's text now say the exit code counts mismatches on `ruled` probes with `proposed` ones reported beside it, recording the split the run directed in section 2's fix round 2, which the fix-delta adversarial review named as unamended spec drift. Section 2's first acceptance clause remains held for the operator's rulings batch, restated on the evidence: one moment (branch-and-pr) sampled three times at the before ref and not reproduced, the other two pilot moments not sampled at the before ref, the pilot's eight-file set unrecoverable from the archive.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Section lanes this stretch, on this main checkout at 2915fd6 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the five-file lane `node --test test/probe-runner.test.js test/probe-set.test.js test/size-ratchet.test.js test/doctrine-parity.test.js test/hook-canary.test.js` 355/353/0/2 exit 0 at 00:32Z after both third fix rounds (delta +29 tests against 326/324/0/2); size check exit 0 at 00:32Z; the two skips are the symlink cases this box refuses with EPERM.

Rulings adopted: none new; the rulings brief re-send still waits on section 2's fix-delta round. Next per section: 1 and 2 await their fix-delta rounds, then chapters 1, 2 and 4 in one commit; 3 and 5 await round 6, then their chapters.

### Interim board 5 - 2026-09-07

Sections in flight: 1 and 2 together (fix round 3 on each cleared its fable reviews: section 1 APPROVED 0/0/4 and APPROVED_WITH_CONCERNS 0/0/6, section 2 CHANGES_REQUIRED 0/1/5, APPROVED_WITH_CONCERNS 0/1/6 and CONCERNS 0/0/4; fix round 4 at implementer-opus from .kit/scratch/probes/brief-fix4.md landed at 00:38Z over both file sets, carrying the isolation-line Major, the credential-copy symlink refusal, and a mid-round item 10 adding the per-shape `designed-mismatch:` marker the parser validates and the runner keeps out of the prompt and out of both counts, with a `designed-agreed` status counted under the ruling state and a `<d> designed` field on the summary line; its fable trio returned APPROVED_WITH_CONCERNS 0/0/5, APPROVED_WITH_CONCERNS 0/1/5 and CONCERNS 0/0/4; fix round 5 at implementer-opus from .kit/scratch/probes/brief-fix5.md is in flight, carrying the blind Major that Ctrl+C cannot stop a paid run because the signal handler never fires inside the synchronous pair loop, plus ten Minors taken: the marker's position guard, the scratch-removal retry, the non-ENOENT rethrow in readContained, the zero-pair moment refusal, the punctuation boundary pin, a designedAgreed count in report.json, SLUG and TIERS re-checked at runProbes, the isolation line naming the scratch root, the three-control skip narrowed, and the credential write via rename). 3 and 5 together (round 7 at opus high returned CHANGES_REQUIRED 0/5/6 and CHANGES_REQUIRED 0/5/3; round 8 rewrite applied at 00:52Z: the substitution is gated on a `ruled` probe with a `proposed` pair recorded as rulings evidence beside the reps, designed-agreed and unparsed rows added to the counts-for list, the whole gate sequenced behind the probe run's marker and its claim, the Chapter slot reads one summary line per invocation with the designed-agreed warning beside it, the home/ carve-out scoped to the file the entry deploys, an after leg with no mismatched moment calling for no before leg; round 8 review at opus high is the next dispatch).

Declined with reasons this stretch: the blind's Major asking the finishing trigger to fire on probe and runner edits (the spec keys the trigger on behavior-shaping prose; a probe edit is a re-ruling by construction and the runner has its own suite); the trailing-punctuation strip in normaliseAnswer (pinned as a boundary instead); the README's single-box isolation ladder readings (they are the runner's own isolation evidence); the UNPARSED-on-a-designed-shape reading absorbed into designed (an unreadable reply is no reading). Recorded for the Chapter rather than fixed: the finishing trigger narrower than spec line 48's "behavior-shaping prose" (a change to a prose file no shape names never runs the set); this changeset itself owing a probe pair it cannot run (every probe proposed, the runner mid-round); the ls-tree dash-leading ref guard and the relative PATH entry skip; the home-entry case check in probe-set.test.js; PROBE_CLAUDE_BIN and PROBE_HOME_DIR lacking the security model's second-signal gate (a docs round item, for docs/security-model.md at the docs commit); a CLAUDE.md planted above the temp cwd being reported and counted rather than stopping the run.

Approval-region edits since the last boundary: Decision 2 and section 2's text now name the designed-mismatch class beside the ruled and proposed ones, recording item 10, which the fix-delta adversarial review named as unamended.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Lanes this stretch, on this main checkout at 8dd58a6 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the two-file lane `node --test test/probe-runner.test.js test/probe-set.test.js` 161/158/0/3 exit 0 at 00:39Z after fix round 4 (three EPERM symlink skips), `run.mjs --dry-run` 27 pairs exit 0, size check exit 0; the parity and ratchet lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js` 91/91/0/0 exit 0 at 00:53Z after round 8, size check exit 0 with the three skill caps exact at 21613, 17371 and 2948.

Rulings adopted: none new; the rulings brief re-send still waits on section 2's final fix-delta round. Next per section: 1 and 2 await fix round 5 and its fable trio, then chapters 1, 2 and 4 in one commit; 3 and 5 await round 8, then their chapters.

### Interim board 6 - 2026-09-07

Sections in flight: 1 and 2 together (fix round 5 from .kit/scratch/probes/brief-fix5.md landed at 01:02Z, all eleven items, the interruption stop pinned on the win32 control-C status end to end and on the predicate alone for the signal half this box cannot deliver, the EACCES branch driven with a NUL-carrying path rather than a stub; its fable trio returned CHANGES_REQUIRED 0/2/4, CHANGES_REQUIRED 0/1/3 and CLEAR 0/0/3, converging on one defect, an oversized reply's ENOBUFS kill read as the operator's Ctrl+C; fix round 6 from brief-fix6.md landed at 01:21Z, the predicate returning false on any spawn error, the signal handler exiting with a nonzero pending code where one is set, designedAgreed on the report's Pairs line, a library invoker with neither configDir nor homeDir refused before it copies live credentials, the README's win32 attribution reworded; its fable trio returned APPROVED_WITH_CONCERNS 0/0/2, APPROVED_WITH_CONCERNS 0/1/5 and CONCERNS 0/1/3, both Majors the same pre-existing design fact, that the synchronous pair loop defers a signal sent to the runner alone until the set finishes, justified rather than rewritten async at this point in the plan since a terminal Ctrl+C reaches the reader and stops the run as built; the main session applied fix 6b inline at 01:31Z, the signal-path cleanup calling removeReaderScratch so a surviving copy is named and retried, an empty --before or --shape refused by name with a pin, and the README stating the runner-alone signal case and the zero-count exit case; no further review round owed on 6b, judged below the bar as a guard reuse, an argument refusal and two README sentences). 3 and 5 together (rounds 8, 9 and 10 at opus high returned CHANGES_REQUIRED 0/3/8 and 0/2/7, 0/2/8 and 0/5/5, 0/2/11 and 0/2/6; rewrites 9, 10 and 11 applied by the main session at 01:07Z, 01:19Z and 01:32Z: the both-legs, designed and designed-agreed cases named in writing-skills' readings, the before leg scoped to ruled mismatches in both skills with the after leg's own list named apart, a proposed probe taking the after leg alone, an errored pair standing in for nothing so the reps run, unchanged shapes' rows named as no reading, the re-run bounded at one, the box claim held across both legs with the doctrine's summary-output fallback and the credential copy pointed at the runner README, the refused-before-any-pair state and the unrun before leg named in the step and the slot, the refusal reason's and the report path's paths held to the Delta rule, an unstable reading counted as a mismatch and named unstable, an origin update re-earning the after leg under its own claim ahead of the gate, the Gate field stated as the one other multi-line field between the contract lines under a rule on its writer with the Delta paragraph re-derived, and the home/ carve-out scoped by "only"; round 11 at opus high dispatched 01:33Z, running; the bar set for it: no surviving Major closes the sections, Minors carried to the Chapter).

Declined with reasons this stretch: the async pair loop (above); the extensionless PATH shim on win32 and the temp-root relocation (recorded for the security model docs round); the stderr full-sentence pins; the writing-skills rule's carrier in executing-work's section loop (the spec places it in writing-skills); the home-entry conditionals nothing can enter today (kept, they describe the parser's admitted shape); a cost ceiling on the paid-reader spend (spec-authorized under Decisions 2 and 3, recorded); the finishing-work step 5 paragraph's length (the acceptance reads one paragraph each). Recorded for the Chapter rather than fixed: a cross-component pin tying the skills' quoted line openings to the runner's emitters and the step-5-to-slot pointer pair; tools/probe-corpus/ under no measured root (section 4's, to be decided at its Chapter); the Decisions / Surprises field carrying a section-time reading with no named slot; home/CLAUDE.md having no staging route under the home/ carve-out; the real-terminal exit code unmeasured; the kit friction that the Bash tool collapses backslash escapes in heredocs (a kaizen note owed at the Chapter).

Approval-region edits since the last boundary: section 3's text now states the ruled gate with a proposed probe's reading as rulings evidence, and section 3's acceptance reads "one paragraph each" rather than "one short paragraph each", both taken in the round 9 and 10 rewrites after the reviews named them as unamended, recorded here as the drift they are.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Lanes this stretch, on this main checkout at 5583e73 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the two-file lane `node --test test/probe-runner.test.js test/probe-set.test.js` 176/173/0/3 exit 0 at 01:33Z after fix 6b (three EPERM symlink skips; 161/158/0/3 before fix round 5, 171/168/0/3 after it, 175/172/0/3 after fix round 6), `run.mjs --dry-run` 27 pairs exit 0; the parity, ratchet and doctrine-parity lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js test/doctrine-parity.test.js` 162/162/0/0 exit 0 at 01:33Z after round 11, size check exit 0 with caps exact at 21727, 17578, 3109 and 2581; one transient ratchet red at 01:20Z was the fix round 6 implementer growing test/probe-runner.test.js under its old cap mid-edit.

Rulings adopted: none new; the rulings brief re-send goes out at chapters 1, 2 and 4. Next per section: 1 and 2 are review-clear and await chapters 1, 2 and 4 in one commit; 3 and 5 await round 11, then their chapters.

