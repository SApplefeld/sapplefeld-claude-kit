# The corpus is read by cold probes at the tier that runs it, and a wording change that moves an answer fails a test

Status: Complete
Commit Model: Commit-and-Push
Created: 2026-09-01

Session model: any executor session in the kit repo; five sections, tiers per section, the fourth and fifth appended during the run as approval drift the Interim boards record. Authored 2026-09-01 by the session that shipped the precedence-and-ownership plan, at the operator's question whether the probe experiment belongs in that session or in a follow-on. This document is the follow-on. Anchors are authoring-time; re-locate every hit by content.

## Dispatch Authorization

Authorized 2026-09-02 by the operator, first-hand on the allowlisted relay thread, to be appended to the kit worker's armed queue: the cold-probe regression instrument for behavior-shaping prose as designed here, its three Decisions taken at their recorded recommendations unless the operator rules otherwise. The operator's word was a standing instruction to the KIT: Expert seat to append every valuable parked plan to the worker's queue; that seat recorded it here and ran the append. Per the peer-sessions trace rule this section is a warrant only for a citing session that did not author it, and the receiving session performs its own trace: the grant is the operator's message on the Expert session's relay thread, and the plan arms only by the operator's word or the Expert seat's append under it.

## Goal

The kit tests its hooks mechanically and its prose by review, and review by a session that loaded the prose ratifies what it read. The pilot that preceded this plan showed a cheaper instrument works: a cold reader at the tier the corpus is written for, handed a concrete situation and a fixed set of the governing files with no intent story, reports what the corpus instructs and where it forks. Run before and after a wording change, that reading is a regression test for prose: the answer to a governed moment either held or moved. When this plan is done, the kit carries a frozen probe set keyed on the moments that bite in production, each probe naming its scenario, the context shape it runs under, and the answer the operator ruled; a runner that snapshots the shape's files, dispatches a cold reader per probe at the production tier, and diffs the verdicts against the rulings; and the two hook-ins that make the runner routine, one at the skill-amendment step and one at the corpus audit's conflict lane.

## Evidence

- The pilot, 2026-09-01: one Opus extraction pass over sixteen git-governing documents (170 instructions, 20 conflicts, 4 verified at the file); one cold Sonnet probe on three scenarios that reached the right commit-and-push answer with the full file set and listed six hesitation passages; and a before-and-after pair on the precedence section, in which the Branch-and-PR pull-request moment read CONTESTED before with the reader supplying its own reconciliation, and the other two scenarios resolved both times. The precedence plan's Chapter carries the readings: `docs/archive/claude-kit_precedence-and-ownership_spec_v1.md`.
- The false bars the operator reported arise under a subset of the corpus rather than under the whole of it: a checklist copy that dropped the exception, a brief that omitted the plan header, a post-compaction context holding a summarized skill. A probe run only on the full file set under-reports them, which is why each probe names its context shape.
- The writing-skills skill already requires a RED and a GREEN reading for any change to behavior-shaping content, in a fresh session rather than a subagent of the editing session, and names the headless `claude -p` as the vehicle. The runner is that discipline made repeatable.
- The corpus-audit plan's conflict lane already specifies scenario probes at Opus and Sonnet as a one-time sweep; this plan makes the probe set the standing artifact that lane consumes and adds to.

## Decisions

Ruled 2026-09-02 by the operator, first-hand on the allowlisted relay thread, from the kaizen pass's close-out recap: all three stand at their recommendations.

1. **Tier per probe.** Recommended: Sonnet by default, Opus for probes on moments only an orchestrator meets (dispatch, review roster, compaction boundary), matching the corpus-audit plan's representative-reader ruling. Alternative: Opus everywhere, which costs more and under-reports the confusion that bites Sonnet seats.
2. **What a mismatch does.** Recommended: reports, never blocks, in the first version; the runner's exit code is the count of mismatches on probes whose ruling state is `ruled`, with mismatches on `proposed` probes reported beside it and never counted, and with a shape the probe file marks as a designed mismatch reported apart and never counted, its agreement with the answer being the finding that marker exists to surface, and the finishing pass reads it as a gate reading in the Chapter. Alternative: a pre-commit block on prose files, declined for now because a probe's reading is itself model output and a flaky block on doctrine edits would train authors to skip the runner.
3. **Cadence.** Recommended: on demand at any behavior-shaping wording change and once per corpus audit; no timer. Alternative: weekly with the kaizen pass, deferred until the set's stability is known.

## Sections of Work

### 1. The probe set and its rulings. Model: opus

`test/probes/<moment>.md`, one file per probe, frontmatter carrying the moment, the context shape as an ordered file list under the plugin root and `home/`, the tier, and the ruled answer as a verdict (RESOLVED with the action, CONTESTED with the owner the ruling assigns, or SILENT with the declared default), body carrying the scenario text verbatim as the reader receives it. The first set covers the moments that bit in the month before authoring, drawn from the precedence plan's contested list and the kaizen inbox: the commit and the push at section close under each commit model; the pre-send checklist met after an authorized push; a peer message asking a leashed session for work; a compaction nudge arriving mid-section with no checkpoint open; an operator-approval gate met on a run the operator armed; a seat asked to push the memory store; a stale anchor in a plan the session resumes. Each probe runs under at least two context shapes, the full governing set and the narrowest production shape that reaches the moment (a brief-only subagent, doctrine plus the output style, doctrine plus one skill). The rulings are gathered in one batched brief to the operator in the client-briefing register, and the set freezes at the ruling.

Acceptance: every probe file parses, names a shape whose files exist, and carries a ruled verdict; the batched brief and its rulings recorded in the Chapter; suite delta against a recorded baseline.

### 2. The runner. Model: opus

`tools/probe-corpus/run.mjs`: for each probe, copies the shape's files into a scratch directory under `.kit/`, composes the reader prompt from a fixed template (the pilot's prompt is the seed, stored beside the runner), invokes the headless CLI at the probe's tier with the scratch directory as the reader's only input, parses the verdict line, and diffs it against the ruling. Output is one report per run under `.kit/` with each probe's verdict, the ruling, match or mismatch, and the reader's cited passages, so a mismatch is re-locatable by content; the exit code is the count of mismatches on `ruled` probes, a `proposed` probe's mismatch being reported and never counted, since every probe is proposed until the operator rules and a count over proposed rulings would gate on nobody's ruling, and a shape carrying a per-shape `designed-mismatch:` marker reported apart and never counted, since a red the set was built to show is not a reading that moved. The runner takes a `--before <ref>` option that checks the shape's files out of a git ref into the scratch directory instead of the worktree, so a before-and-after pair is two invocations and no hand copying. The template's no-intent-story bar is stated in the runner's own comments and in the template, per the blind-reader discipline.

Acceptance: the runner reproduces the pilot's three verdicts on the precedence plan's before-and-after snapshots (CONTESTED, RESOLVED, RESOLVED before; the after readings as the precedence Chapter records them); a deliberately broken ruling produces a nonzero exit, as the runner's own control; suite delta against a recorded baseline.

### 3. The hook-ins. Model: sonnet

`plugins/claude-kit/skills/writing-skills/SKILL.md`, in the RED and GREEN paragraph: where a change touches a moment a `ruled` probe covers, the runner's before-and-after pair is the RED and GREEN, a `proposed` probe's reading being evidence for the rulings batch instead, and a mismatch on a ruled probe the change did not intend to move is a finding. `plugins/claude-kit/skills/finishing-work/SKILL.md`: a plan whose changeset touched a file a probe's shape names runs the probe set at the finishing pass and names the reading in the Chapter, beside the lanes. `docs/plans/claude-kit_corpus-audit_spec_v1.md` is not edited (a Ready plan the operator authorized); its Related section is the place a later session points at this plan, and the audit's conflict lane is briefed to add every conflict it confirms to the probe set as a new file. Whole-file review for both skill amendments, per the recorded skill-amendment defect mode.

Acceptance: both skills carry the hook-in in one paragraph each, pointing at the runner rather than restating it and agreeing with every unchanged neighbour; the parity and shape suites green at baseline; suite delta against a recorded baseline.

### 4. The size ratchet classifies the probe set. Model: sonnet

`plugins/claude-kit/scripts/kit-size.js`: the measured corpus gains a `test/probes/` root, measured in words with the shape `test/probes/<name>.md`, listed ahead of the `test/` root because the classifier takes the first root holding a path; `test/size-budget.json` gains a cap per probe file and for `test/probes/README.md`, each at the file's current word count; `test/size-ratchet.test.js` extends whatever pins the root roster, its count, or the classifier's coverage, with a pin that a `test/probes/*.md` path classifies as words and a `test/probes/<name>.txt` path reds as unclassified. Without this root a tracked `test/probes/*.md` matches no shape under the `test/` root and reds the ratchet as unclassified.

Acceptance: `classify` over every path under `test/probes/` returns them as word-measured entries with none unclassified (proved in-process, since the check verb reads tracked files and the probe set is untracked until the section commit); `node --test test/size-ratchet.test.js` green; `node plugins/claude-kit/scripts/kit-size.js check` green; suite delta against a recorded baseline.

### 5. The Chapter template holds the probe reading. Model: sonnet
Locus: inline

`plugins/claude-kit/skills/executing-work/SKILL.md`, the Chapter template's `Gate:` line, which gains the slot the finishing pass's probe reading lands in, and its `Decisions / Surprises` line, which gains the slot writing-skills' section-time pair reading lands in (per leg invocation, the summary line the runner prints to stdout quoted verbatim with its report path repo-relative, the exit code beside it where the two differ, the report's warning lines, the mismatched moments by name with each row's status, and its moment-pin; or the state finishing-work's step 5 names instead, or that no shape file was touched so no run was called for), stated as reporting and never blocking, so that finishing-work's hook-in points at the owner of the Chapter shape rather than defining a field from outside it.

Acceptance: the `Gate:` and `Decisions / Surprises` lines name their readings; finishing-work's hook-in points at the first and restates nothing; the parity and shape suites green; suite delta against a recorded baseline.

## Out of Scope

- Ruling on the contested moments themselves: the backlog item from the precedence plan carries those asks.
- Single-sourcing the copied charters and the output style's doctrine core through the build: a separate plan.
- A mechanical block on prose commits: declined in decision 2 for the first version.

## Related

- `docs/archive/claude-kit_precedence-and-ownership_spec_v1.md`: the plan whose pilot seeded this one and whose Chapter holds the first before-and-after readings.
- `docs/plans/claude-kit_corpus-audit_spec_v1.md`: the one-time sweep whose conflict lane consumes and extends the probe set.
- `plugins/claude-kit/skills/writing-skills/SKILL.md`: the RED and GREEN discipline the runner mechanizes.
- `sidecar/batteries/`: the frozen-battery precedent the probe set's freeze-at-ruling rule follows.
- `docs/plans/claude-kit_output-channel-eliding_spec_v1.md`: the plan whose producer sweep the runner's stderr messages join, since a refusal reason or a cleanup warning there can spell an absolute home-directory path.

## Chapters

### Interim board 1 - 2026-09-06

Run start: the header read `Status: Ready` and now reads `In Progress`, normalized at run start per executing-work. The pilot's original cold-reader prompt is not recoverable on this machine (four scans over every kit-project transcript found no dispatch carrying it), so the runner's template is written from the precedence plan's Chapter description of the pilot; declared route (b), section 2. The reader isolation model is declared route (b), section 2: the headless CLI on this box (2.1.263) authenticates by OAuth with no API key, so `--bare` is unavailable and the cold shape is a scratch `CLAUDE_CONFIG_DIR` holding only the credentials file, confirmed or corrected by section 2's isolation control. Section 4 appended by section 3's review round (approval drift, recorded here): the size ratchet reds a tracked `test/probes/*.md` as unclassified, and no section listed the classifier.

Sections in flight: 1 (probe set, implementer-opus, dispatched 21:58Z, running; nine probe files and the parser are on disk). 2 (runner, implementer-opus, dispatched 21:58Z, running; run.mjs, template.md and probe-file.mjs import on disk). 3 (hook-ins, implementer-sonnet, DONE at 22:05Z; round 1 at opus/high via Workflow returned adversarial CHANGES_REQUIRED 0/8/4 and blind CHANGES_REQUIRED, converging: the finishing-work sentence mandated a repo-local command in a portable skill, added a field to the Gate line executing-work owns, sat unplaced in step 5's written order, dropped Decision 2's reports-never-blocks; the writing-skills sentence collided with the several-reps and home/-shape neighbours and ran past forty words. Both sentences rewritten inline by the main session at 22:15Z; lane `node --test test/doctrine-parity.test.js test/size-ratchet.test.js test/hook-canary.test.js` 199/199/0 exit 0, size check exit 0 with caps finishing-work 16860 to 16945 and writing-skills 2608 to 2698; fix-delta adversarial review at opus/high dispatched 22:21Z, running. Ruling: section 3's commit is held behind sections 1 and 2's commits so the pointers never ship ahead of their targets). 4 (size root, implementer-sonnet, dispatched 22:21Z, running).

Gate baseline: whole suite 3219 tests / 3209 pass / 1 fail / 9 skipped, exit 1, measured 2026-09-06 at plan 1's handoff gate on this main checkout at b4e249b with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the one red being test/memory-session.test.js "a pinned directory too long to name faithfully stands the session down", permanent on this box.

Rulings adopted since the last boundary: probe rulings are `proposed` until the operator's batched brief is answered, which the plan makes the operator's; the brief goes out on the relay once section 1 reports and the set freezes at the ruling. Next per section: 1 and 2 await their reports, then verification, then fable review pairs (security beside them on 2); 3 awaits its fix-delta review, then closes after 1 and 2 commit; 4 awaits its report, then opus reviewers at high.

### Interim board 2 - 2026-09-06

Sections in flight: 1 (probe set, first fable review pair returned CHANGES_REQUIRED, one Critical: the compaction probe's ruled answer contradicted executing-work's interim-boundary rule, under which the deferral nudge is itself a trigger; plus an invented gate line, a mixed action-and-owner option list on the Branch-and-PR probe, a false `main` sentence on the branch-delete probe, split no-push options on the store-push probe, and two indistinguishable stale-anchor options; fix round at implementer-opus dispatched 23:36Z from .kit/scratch/probes/brief-s1-fix.md, running). 2 (runner, fable pair plus security returned CHANGES_REQUIRED and CONCERNS: the suite copied the live credentials file, `$`-pattern substitution in the prompt composer, any nonzero git status read as absent, errors counted as mismatches, unbounded shape-file reach, credential copy lifetime, spawn cwd, the isolation control unshipped and its withheld variant unrun, and the pilot's before verdict on the pull-request moment not reproduced; fix round at implementer-opus dispatched 23:36Z from brief-s2-fix.md, running, amended in flight to print one stdout summary line: pairs, mismatches, errors, exit, tiers, report path). 3 and 5 (hook-ins and the Chapter template slot: opus-at-high adversarial plus blind round returned CHANGES_REQUIRED; the passages were rewritten by the main session at 23:52Z so the writing-skills pair sits beside the reps bar, names the repair case, scopes with `--only` and keys on a `ruled` probe; finishing-work runs the set over the tree as it then stands just ahead of the whole gate and records an unrun run as unrun; executing-work's slot names the stdout summary line and is final-Chapter-only; a further review round is owed on that delta). 4 (size root: opus pair APPROVED_WITH_CONCERNS; the two Majors, cap slack and a commit-order dependency between the classifier and the probe files, are closed by setting every cap at commit time and landing sections 1, 2 and 4 in one commit; docs/architecture.md's root enumeration folded in by the main session; a README sentence on the cap requirement handed to section 1's fix round).

Approval-region edits since the last boundary: section 5 appended (the Chapter template slot, Locus: inline); sections 4 and 5 restated in the present tense; section 3's acceptance reads one short paragraph each rather than one sentence, because two rounds showed a single sentence could not both point at the runner and agree with its neighbours.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Section lanes this stretch: `node --test test/probe-set.test.js test/probe-runner.test.js test/size-ratchet.test.js test/doctrine-parity.test.js test/hook-canary.test.js` 302/301/0/1 exit 0 at 23:20Z before the fix rounds; the parity lane over the three skills 212/212/0 exit 0 at 23:38Z and, after the 23:52Z rewrites, 212/211/1 exit 1 with the one red being the size ratchet over files the fix rounds hold mid-edit.

Rulings adopted: the batched rulings brief went to the operator on the relay at 23:30Z with all nine proposed; item 6 (the compaction nudge) is superseded by the Critical above and the brief is re-sent when the fix round lands, so the rulings stay `proposed`. Next per section: 1 and 2 await their fix reports, then verification, then a fix-delta review round each; 3 and 5 await one more opus-at-high round on the rewritten passages; 4 closes with 1 and 2 in the one commit.

### Interim board 3 - 2026-09-06

Sections in flight: 1 (probe set: fix round 1 landed at 23:05Z, lane 76/75/0/1 exit 0; fix-delta fable pair returned CHANGES_REQUIRED 0/2/7 and APPROVED_WITH_CONCERNS 0/1/5, the Majors being the Branch-and-PR probe reading red by construction, since the reader template routed a no-owner CONTESTED to OTHER and the `doctrine-plus-executing-work` shape could only read finishing-work, plus a directory entry passing both the parser and the corpus pin; fix round 2 at implementer-opus from .kit/scratch/probes/brief-s1-fix2.md landed at 23:43Z, lane 79/78/0/1 exit 0, the probe's third shape now `doctrine-plus-the-contested-surfaces`, its closing question naming the unowned value, the pin on `isFile` plus `git ls-files`, designed-mismatch shapes marked in a frontmatter comment and the README; a fix-delta review round is owed). 2 (runner: fix round 1 landed at 23:12Z, lane 49/48/0/1 exit 0; fix-delta fable pair plus security returned CHANGES_REQUIRED 0/4/4, CHANGES_REQUIRED 0/2/4 and CONCERNS 0/0/6: the `--before` leg bypassed containment through git's own `..` normalization, `readContained` judged existence before containment, the README's isolation attribution was unmeasured and a superseded bare-variant log suggested the CLI falls back to the real home when the config dir holds no credentials, plus the stale-sweep mtime premise and the spec's pilot-reproduction acceptance unmet on three stable samples; fix round 2 at implementer-opus from brief-s2-fix2.md dispatched 23:31Z, running, thirteen items including the ruled-versus-proposed mismatch split in the summary line, `probe-corpus: <n> pairs, <m> mismatches (<p> on proposed rulings), <e> errors, exit <x>, tier <tiers>, report <path>`, with `(partial)` for a run that died mid-set, and the template amendment routing a no-owner CONTESTED to the list's own value). 3 and 5 (hook-ins and the Chapter slot: round 2 at opus/high returned CHANGES_REQUIRED 0/8/5 and CHANGES_REQUIRED, round 3 CHANGES_REQUIRED 0/8/6 and APPROVED_WITH_CONCERNS, each round partly reversing the last on where the baseline belongs; the main session rewrote the three passages at 00:02Z toward the spec's own scoping: the slot stays the finishing pass's and final-Chapter-only, holding one summary line per leg with the `(partial)` mark and its moment-pin; finishing-work runs the after leg over the set and the before leg with `--only` over the mismatched moments at the effort's base ref, disposes a ruled mismatch to the close-out and a proposed one to the rulings batch, and runs ahead of the `docs/` edits because a shape names files under the plugin and the home directory alone; writing-skills triggers on any covering probe, derives the before ref as the change's parent, re-runs a mismatching leg, states that a match is single-sample evidence, keys the both-legs case on the readings agreeing, and names the designed-mismatch class; round 4 at opus/high dispatched 00:05Z, running). 4 (size root: closes with 1 and 2 in the one commit; caps for the three skills reset to exact size at each rewrite, 21612/17133/2913 words now).

Approval-region edits since the last boundary: none. docs/plans/README.md's entry for this plan corrected from "Ready, three sections; fifth in the armed queue" to "In Progress, five sections; second", a document a completed section's scope carried that this run's changes falsified, taken as the standing carve-out. Section 2's first acceptance clause (reproduce the pilot's CONTESTED, RESOLVED, RESOLVED) is unmet on three stable samples and unmeetable, the pilot prompt being unrecoverable and the pilot's file set eight where the shape carries six: the amendment to the spec is held for the operator's rulings batch, where the three readings ride as evidence, rather than rewritten by the run.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Section lanes this stretch, on this main checkout at c333aa4 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the five-file lane 326/324/0/2 exit 0 at 23:15Z after fix round 1 (delta +24 tests against 302/301/0/1); the parity lane `node --test test/doctrine-parity.test.js test/hook-canary.test.js` 121/121/0/0 exit 0 at 00:07Z after the round-4 rewrite; size check exit 0 at 00:07Z.

Rulings adopted: the rulings brief re-send to the operator is held until section 2's second fix round lands, so it carries the branch-and-pr probe's three-sample readings (RESOLVED/finishing-work under the full shape) beside the proposed unowned ruling, the compaction probe's flipped answer, the merged options, and the section 2 acceptance amendment. Next per section: 1 awaits its fix-delta round; 2 awaits its fix report, verification and a fix-delta round with security; 3 and 5 await round 4; then chapters 1, 2 and 4 in one commit, then 3 and 5.

### Interim board 4 - 2026-09-07

Sections in flight: 1 (probe set: fix-delta fable pair over fix round 2 returned APPROVED_WITH_CONCERNS 0/0/3 and APPROVED_WITH_CONCERNS 0/0/4; fix round 3 at implementer-opus from .kit/scratch/probes/brief-s1-fix3.md landed at 00:15Z, exporting the parser's two allowlist constants for the runner, holding options to slugs, refusing a `#` on a scalar, trimming the branch-and-pr closing sentence to "Name that owner." so the template alone routes a no-owner CONTESTED, naming the branch-and-pr full-shape reading in the probe README as a mismatch awaiting the operator, and setting four caps exact; lane 82/81/0/1 exit 0; a fix-delta fable pair dispatched 00:33Z, running). 2 (runner: fix round 2 landed at 00:00Z, lane 66/65/0/1 exit 0, the bare rung confirmed the credential copy authenticates and no fallback to the operator's home occurs; fix-delta fable pair plus security returned CHANGES_REQUIRED 0/3/5, APPROVED_WITH_CONCERNS 0/1/6 and CONCERNS 0/0/4; fix round 3 from brief-s2-fix3.md landed at 00:30Z with the newer-wins credential refresh, the allowlist at the read boundary, the ref-mode link refusal through `git ls-tree`, the git spawn cwd at the binary's directory, and one retire-class assert removed; lane 72/71/0/1 exit 0; fix-delta fable pair plus security dispatched 00:33Z, running). 3 and 5 (hook-ins and the Chapter slot: rounds 4 and 5 at opus/high each returned CHANGES_REQUIRED with the prior round's findings closed and new ground opened, the pattern being passages that grew to satisfy each round and then drew length and restatement findings; the main session rewrote all three at 00:20Z as short pointers, the runner's README and finishing-work's step 5 owning the mechanics, the writing-skills pair restored to the spec's own reading as the RED and GREEN for a covered moment, the caret sentence taking finishing-work's hedge after round 5 caught a false PowerShell claim, the re-run rule carving out the designed mismatch and scoping to moments, and the Gate slot asking for the summary line verbatim; parity lane 121/121/0 exit 0; round 6 at opus/high dispatched 00:27Z, running). 4 (size root: closes with 1 and 2 in the one commit; caps for the three skills at 21567/17209/2819 words).

Approval-region edits since the last boundary: Decision 2 and section 2's text now say the exit code counts mismatches on `ruled` probes with `proposed` ones reported beside it, recording the split the run directed in section 2's fix round 2, which the fix-delta adversarial review named as unamended spec drift. Section 2's first acceptance clause remains held for the operator's rulings batch, restated on the evidence: one moment (branch-and-pr) sampled three times at the before ref and not reproduced, the other two pilot moments not sampled at the before ref, the pilot's eight-file set unrecoverable from the archive.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Section lanes this stretch, on this main checkout at 2915fd6 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the five-file lane `node --test test/probe-runner.test.js test/probe-set.test.js test/size-ratchet.test.js test/doctrine-parity.test.js test/hook-canary.test.js` 355/353/0/2 exit 0 at 00:32Z after both third fix rounds (delta +29 tests against 326/324/0/2); size check exit 0 at 00:32Z; the two skips are the symlink cases this box refuses with EPERM.

Rulings adopted: none new; the rulings brief re-send still waits on section 2's fix-delta round. Next per section: 1 and 2 await their fix-delta rounds, then chapters 1, 2 and 4 in one commit; 3 and 5 await round 6, then their chapters.

### Interim board 5 - 2026-09-07

Sections in flight: 1 and 2 together (fix round 3 on each cleared its fable reviews: section 1 APPROVED 0/0/4 and APPROVED_WITH_CONCERNS 0/0/6, section 2 CHANGES_REQUIRED 0/1/5, APPROVED_WITH_CONCERNS 0/1/6 and CONCERNS 0/0/4; fix round 4 at implementer-opus from .kit/scratch/probes/brief-fix4.md landed at 00:38Z over both file sets, carrying the isolation-line Major, the credential-copy symlink refusal, and a mid-round item 10 adding the per-shape `designed-mismatch:` marker the parser validates and the runner keeps out of the prompt and out of both counts, with a `designed-agreed` status counted under the ruling state and a `<d> designed` field on the summary line; its fable trio returned APPROVED_WITH_CONCERNS 0/0/5, APPROVED_WITH_CONCERNS 0/1/5 and CONCERNS 0/0/4; fix round 5 at implementer-opus from .kit/scratch/probes/brief-fix5.md is in flight, carrying the blind Major that Ctrl+C cannot stop a paid run because the signal handler never fires inside the synchronous pair loop, plus ten Minors taken: the marker's position guard, the scratch-removal retry, the non-ENOENT rethrow in readContained, the zero-pair moment refusal, the punctuation boundary pin, a designedAgreed count in report.json, SLUG and TIERS re-checked at runProbes, the isolation line naming the scratch root, the three-control skip narrowed, and the credential write via rename). 3 and 5 together (round 7 at opus high returned CHANGES_REQUIRED 0/5/6 and CHANGES_REQUIRED 0/5/3; round 8 rewrite applied at 00:52Z: the substitution is gated on a `ruled` probe with a `proposed` pair recorded as rulings evidence beside the reps, designed-agreed and unparsed rows added to the counts-for list, the whole gate sequenced behind the probe run's marker and its claim, the Chapter slot reads one summary line per invocation with the designed-agreed warning beside it, the home/ carve-out scoped to the file the entry deploys, an after leg with no mismatched moment calling for no before leg; round 8 review at opus high is the next dispatch).

Declined with reasons this stretch: the blind's Major asking the finishing trigger to fire on probe and runner edits (the spec keys the trigger on behavior-shaping prose; a probe edit is a re-ruling by construction and the runner has its own suite); the trailing-punctuation strip in normaliseAnswer (pinned as a boundary instead); the README's single-box isolation ladder readings (they are the runner's own isolation evidence); the UNPARSED-on-a-designed-shape reading absorbed into designed (an unreadable reply is no reading). Recorded for the Chapter rather than fixed: the finishing trigger narrower than spec line 48's "behavior-shaping prose" (a change to a prose file no shape names never runs the set); this changeset itself owing a probe pair it cannot run (every probe proposed, the runner mid-round); the ls-tree dash-leading ref guard and the relative PATH entry skip; the home-entry case check in probe-set.test.js; PROBE_CLAUDE_BIN and PROBE_HOME_DIR lacking the security model's second-signal gate (a docs round item, for docs/security-model.md at the docs commit); a CLAUDE.md planted above the temp cwd being reported and counted rather than stopping the run.

Approval-region edits since the last boundary: Decision 2 and section 2's text now name the designed-mismatch class beside the ruled and proposed ones, recording item 10, which the fix-delta adversarial review named as unamended.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Lanes this stretch, on this main checkout at 8dd58a6 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the two-file lane `node --test test/probe-runner.test.js test/probe-set.test.js` 161/158/0/3 exit 0 at 00:39Z after fix round 4 (three EPERM symlink skips), `run.mjs --dry-run` 27 pairs exit 0, size check exit 0; the parity and ratchet lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js` 91/91/0/0 exit 0 at 00:53Z after round 8, size check exit 0 with the three skill caps exact at 21613, 17371 and 2948.

Rulings adopted: none new; the rulings brief re-send still waits on section 2's final fix-delta round. Next per section: 1 and 2 await fix round 5 and its fable trio, then chapters 1, 2 and 4 in one commit; 3 and 5 await round 8, then their chapters.

### Interim board 6 - 2026-09-07

Sections in flight: 1 and 2 together (fix round 5 from .kit/scratch/probes/brief-fix5.md landed at 01:02Z, all eleven items, the interruption stop pinned on the win32 control-C status end to end and on the predicate alone for the signal half this box cannot deliver, the EACCES branch driven with a NUL-carrying path rather than a stub; its fable trio returned CHANGES_REQUIRED 0/2/4, CHANGES_REQUIRED 0/1/3 and CLEAR 0/0/3, converging on one defect, an oversized reply's ENOBUFS kill read as the operator's Ctrl+C; fix round 6 from brief-fix6.md landed at 01:21Z, the predicate returning false on any spawn error, the signal handler exiting with a nonzero pending code where one is set, designedAgreed on the report's Pairs line, a library invoker with neither configDir nor homeDir refused before it copies live credentials, the README's win32 attribution reworded; its fable trio returned APPROVED_WITH_CONCERNS 0/0/2, APPROVED_WITH_CONCERNS 0/1/5 and CONCERNS 0/1/3, both Majors the same pre-existing design fact, that the synchronous pair loop defers a signal sent to the runner alone until the set finishes, justified rather than rewritten async at this point in the plan since a terminal Ctrl+C reaches the reader and stops the run as built; the main session applied fix 6b inline at 01:31Z, the signal-path cleanup calling removeReaderScratch so a surviving copy is named and retried, an empty --before or --shape refused by name with a pin, and the README stating the runner-alone signal case and the zero-count exit case; no further review round owed on 6b, judged below the bar as a guard reuse, an argument refusal and two README sentences). 3 and 5 together (rounds 8, 9 and 10 at opus high returned CHANGES_REQUIRED 0/3/8 and 0/2/7, 0/2/8 and 0/5/5, 0/2/11 and 0/2/6; rewrites 9, 10 and 11 applied by the main session at 01:07Z, 01:19Z and 01:32Z: the both-legs, designed and designed-agreed cases named in writing-skills' readings, the before leg scoped to ruled mismatches in both skills with the after leg's own list named apart, a proposed probe taking the after leg alone, an errored pair standing in for nothing so the reps run, unchanged shapes' rows named as no reading, the re-run bounded at one, the box claim held across both legs with the doctrine's summary-output fallback and the credential copy pointed at the runner README, the refused-before-any-pair state and the unrun before leg named in the step and the slot, the refusal reason's and the report path's paths held to the Delta rule, an unstable reading counted as a mismatch and named unstable, an origin update re-earning the after leg under its own claim ahead of the gate, the Gate field stated as the one other multi-line field between the contract lines under a rule on its writer with the Delta paragraph re-derived, and the home/ carve-out scoped by "only"; round 11 at opus high dispatched 01:33Z, running; the bar set for it: no surviving Major closes the sections, Minors carried to the Chapter).

Declined with reasons this stretch: the async pair loop (above); the extensionless PATH shim on win32 and the temp-root relocation (recorded for the security model docs round); the stderr full-sentence pins; the writing-skills rule's carrier in executing-work's section loop (the spec places it in writing-skills); the home-entry conditionals nothing can enter today (kept, they describe the parser's admitted shape); a cost ceiling on the paid-reader spend (spec-authorized under Decisions 2 and 3, recorded); the finishing-work step 5 paragraph's length (the acceptance reads one paragraph each). Recorded for the Chapter rather than fixed: a cross-component pin tying the skills' quoted line openings to the runner's emitters and the step-5-to-slot pointer pair; tools/probe-corpus/ under no measured root (section 4's, to be decided at its Chapter); the Decisions / Surprises field carrying a section-time reading with no named slot; home/CLAUDE.md having no staging route under the home/ carve-out; the real-terminal exit code unmeasured; the kit friction that the Bash tool collapses backslash escapes in heredocs (a kaizen note owed at the Chapter).

Approval-region edits since the last boundary: section 3's text now states the ruled gate with a proposed probe's reading as rulings evidence, and section 3's acceptance reads "one paragraph each" rather than "one short paragraph each", both taken in the round 9 and 10 rewrites after the reviews named them as unamended, recorded here as the drift they are.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Lanes this stretch, on this main checkout at 5583e73 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the two-file lane `node --test test/probe-runner.test.js test/probe-set.test.js` 176/173/0/3 exit 0 at 01:33Z after fix 6b (three EPERM symlink skips; 161/158/0/3 before fix round 5, 171/168/0/3 after it, 175/172/0/3 after fix round 6), `run.mjs --dry-run` 27 pairs exit 0; the parity, ratchet and doctrine-parity lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js test/doctrine-parity.test.js` 162/162/0/0 exit 0 at 01:33Z after round 11, size check exit 0 with caps exact at 21727, 17578, 3109 and 2581; one transient ratchet red at 01:20Z was the fix round 6 implementer growing test/probe-runner.test.js under its old cap mid-edit.

Rulings adopted: none new; the rulings brief re-send goes out at chapters 1, 2 and 4. Next per section: 1 and 2 are review-clear and await chapters 1, 2 and 4 in one commit; 3 and 5 await round 11, then their chapters.

### Interim board 7 - 2026-09-07

Sections in flight: 1, 2 and 4 (review-clear since board 6; their three Chapters are drafted in .kit/scratch/probes/chapters-124.js and wait on the whole gate that the push to main owes, which runs once over the tree after sections 3 and 5 clear so both commits ride one gate and one push; the two commit messages sit at .kit/commit-msg-124.txt and .kit/commit-msg-35.txt). 3 and 5 together (rounds 11, 12 and 13 at opus high returned CHANGES_REQUIRED 1/1/8 and 1/3/3, 0/6/7 and 0/2/9, 0/3/7 and 0/2/10; the round 11 Critical was rewrite 11's own defect, the before leg scoped to ruled mismatches in writing-skills so the RED was unobtainable exactly where a fix worked, corrected in rewrite 12 by giving both legs the after leg's moment list with the before leg narrowed to ruled probes; rewrites 12, 13 and 14 applied by the main session at 01:45Z, 01:58Z and 02:12Z: the unstable row's leg named, the stale re-run's reading placed and its before leg re-derived, the double trigger inside a finishing pass closed with the pair running once at section close, the trigger keyed on a passage the scenario turns on with the untracked half, the two moment lists stated apart, the Decisions / Surprises slot named in executing-work's template, the Gate slot's copied state list returned to a pointer with the mismatched moments named, the warning lines sourced to report.md and the no-shape-file state added, the multi-line-field claim widened to the assumptions block, the intent test owned once and mirrored onto an intended move that did not happen, the errored re-run spelled as the full runner command, the row enumeration closed, and a cross-surface pin in test/doctrine-parity.test.js reading every flag the skills spell in a run.mjs command against KNOWN_FLAGS, the two emitted prefixes, and both pointer pairs; round 14 at opus high dispatched 02:13Z, running; the bar: no surviving Major closes the sections).

Declined with reasons this stretch: asserting the pin against a rendered line rather than the emitter's literal (the literal is the emitter); the probe files' frontmatter outside the word measure and tools/probe-corpus/ under no measured root (section 4's Chapter records the call); the home/ carve-out (anticipatory, the parser admits the entry); the sentence-shape bar on the longest sentences (one idea each). Recorded for the Chapters: every probe proposed, so the ruled branches wait on the rulings batch; the finishing procedure never yet run end to end; finishing-work's assumptions paragraph not yet pointing at the no-leading-marker rule. Two kaizen notes appended to kaizen/notes-SCOTT-CLAUDE.md at 02:05Z, uncommitted because the file carries lines this session did not author. The two operator-tier records the stamps sweep surfaced were stamped applied at 01:47Z.

Approval-region edits since the last boundary: section 3's finishing trigger now reads the built one, a changeset touching a shape-named file, narrower than the original behavior-shaping-prose wording (rewrite 13); section 5's parenthetical now describes the built slot, per leg invocation with the mismatched moments named and the no-run states (rewrite 14); both taken after reviews named them as unamended and recorded here as the drift they are.

Gate baseline: unchanged from Interim board 1 (whole suite 3219/3209/1/9 exit 1 at b4e249b). Lanes this stretch, on this main checkout at 807447d with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md: the parity, ratchet and doctrine-parity lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js test/doctrine-parity.test.js` 163/163/0/0 exit 0 at 02:12Z after rewrite 14 (162/162/0/0 at 01:45Z after rewrite 12, 163/163/0/0 at 01:58Z after rewrite 13 with the pin added), size check exit 0 with caps exact at 21787, 17666, 3215 and 6245 for the three skills and the parity test.

Rulings adopted: none new; the rulings brief re-send goes out at chapters 1, 2 and 4. Next per section: 1, 2 and 4 await the whole gate and their chapters; 3 and 5 await round 14, then their chapters, then one gate and one push for both commits.

### Chapter 1 - 2026-09-07
Completed: 1. The probe set and its rulings
Implemented By: implementer-opus (initial build plus fix rounds 1, 2 and 3 from .kit/scratch/probes/brief-s1.md, brief-s1-fix.md, brief-s1-fix2.md and brief-s1-fix3.md; fix rounds 4, 5 and 6 shared with section 2 from brief-fix4.md, brief-fix5.md and brief-fix6.md; fix 6b by the main session)
Metrics: review rounds 7 (four fable pairs on the section's own deltas, three fable trios with security on the shared rounds); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: nine probes under test/probes/ plus a README and the parser tools/probe-corpus/probe-file.mjs, every ruling `proposed` until the operator's batched brief is answered, so the set is not yet frozen. The compaction probe's first ruled answer contradicted executing-work's interim-boundary rule and now reads write-an-interim-board-entry-and-open-a-checkpoint. The branch-and-pr probe's full-shape reading came back RESOLVED to finishing-work on three stable samples against a proposed unowned ruling, recorded in test/probes/README.md as a mismatch awaiting the operator. A per-shape `designed-mismatch:` marker joined the frontmatter in fix round 4, validated by the parser and kept out of the reader's prompt. Recorded rather than fixed: the home-entry case check in test/probe-set.test.js; home/CLAUDE.md has no staging route under writing-skills' home/ carve-out. The batched rulings brief went out at 23:30Z on 2026-09-06 with all nine proposed and is re-sent from this Chapter's commit with the compaction flip, the merged options, the three-sample branch-and-pr evidence and the section 2 acceptance amendment.
Assumptions: (2026-09-06, section 1, route b) every probe ships `proposed` and the set freezes at the operator's ruling rather than at this Chapter, since the plan makes the rulings the operator's and none has arrived.
Review Findings: review: code pair at fable, Agent tool, four rounds on this section's deltas (CHANGES_REQUIRED then CHANGES_REQUIRED 0/2/7 with APPROVED_WITH_CONCERNS 0/1/5, then APPROVED_WITH_CONCERNS 0/0/3 and 0/0/4, then APPROVED 0/0/4 and APPROVED_WITH_CONCERNS 0/0/6); review: code pair plus security at fable, Agent tool, three shared rounds recorded on Chapter 2's line. Critical addressed: the compaction probe's ruled answer. Majors addressed: the invented gate line, the mixed action-and-owner option list, the false main sentence on the branch-delete probe, the split no-push options, the two indistinguishable stale-anchor options, the branch-and-pr probe red by construction under a shape that could only read finishing-work, a directory entry passing the parser and the corpus pin. Minors noted: options held to slugs, a `#` on a scalar refused, the closing sentence trimmed so the template alone routes a no-owner CONTESTED.
Stamps: adjudicated 2, stamped 2 (operator tier: forward-resource-arrangements-into-dispatch-briefs, release-an-exclusive-claim-at-the-operations-end-not-the-turns-end), swept over the 4h window covering the section since the plan's start.
Gate: targeted lane `node --test test/probe-runner.test.js test/probe-set.test.js` 176/173/0/3 exit 0 at 01:33Z on 2026-09-07 after fix 6b, the three skips the symlink cases this box refuses with EPERM (161/158/0/3 before fix round 5); whole gate `node --test test/*.test.js` 3398/3385/1/12 exit 1 (delta +179 tests and +3 skipped against the baseline, the skips being the three EPERM symlink cases, the one red unchanged), delta against the Interim board 1 baseline 3219/3209/1/9 exit 1 at b4e249b; contention lane none defined in this repo; both measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the box claimed for the span under this session's id and released at the run's marker; the one red is the permanent test/memory-session.test.js case the baseline carries
Next: 2. The runner, whose Chapter rides this commit
Commit Model: Commit-and-Push
Delta: measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
plugins/claude-kit/skills/executing-work/SKILL.md: 21857 words, cap 21857, +383
plugins/claude-kit/skills/finishing-work/SKILL.md: 17696 words, cap 17696, +836
plugins/claude-kit/skills/writing-skills/SKILL.md: 3278 words, cap 3278, +670
test/doctrine-parity.test.js: 6252 lines, cap 6252, +38; tests 72, +1
test/size-ratchet.test.js: 2845 lines, cap 2845, +27; tests 80, +2
test/probe-runner.test.js: 2581 lines, cap 2581, untracked so HEAD holds no blob and the totals below exclude it; tests 91
test/probe-set.test.js: 726 lines, cap 726, untracked so HEAD holds no blob and the totals below exclude it; tests 61
test/probes/README.md: 394 words, cap 394, untracked so HEAD holds no blob and the totals below exclude it
test/probes/branch-and-pr-pull-request-at-section-close.md: 167 words, cap 167, untracked so HEAD holds no blob and the totals below exclude it
test/probes/commit-and-push-at-section-close.md: 155 words, cap 155, untracked so HEAD holds no blob and the totals below exclude it
test/probes/compaction-nudge-mid-section-with-no-checkpoint-open.md: 283 words, cap 283, untracked so HEAD holds no blob and the totals below exclude it
test/probes/merged-plan-branch-delete-on-an-armed-run.md: 160 words, cap 160, untracked so HEAD holds no blob and the totals below exclude it
test/probes/peer-message-asking-a-leashed-session-for-work.md: 166 words, cap 166, untracked so HEAD holds no blob and the totals below exclude it
test/probes/pre-send-checklist-after-an-authorized-push.md: 151 words, cap 151, untracked so HEAD holds no blob and the totals below exclude it
test/probes/review-only-at-section-close.md: 129 words, cap 129, untracked so HEAD holds no blob and the totals below exclude it
test/probes/seat-asked-to-push-the-memory-store.md: 182 words, cap 182, untracked so HEAD holds no blob and the totals below exclude it
test/probes/stale-anchor-in-a-resumed-plan.md: 161 words, cap 161, untracked so HEAD holds no blob and the totals below exclude it
words: 216834 of cap 216835 across 48 curated files
test lines: 106132 of cap 106132 across 53 test files
tests: 3173
changed paths under no measured root: 10 (5 differing from HEAD, 5 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
excluded from those totals, untracked under a measured root: test/probe-runner.test.js, test/probe-set.test.js, test/probes/README.md, test/probes/branch-and-pr-pull-request-at-section-close.md, test/probes/commit-and-push-at-section-close.md, test/probes/compaction-nudge-mid-section-with-no-checkpoint-open.md, test/probes/merged-plan-branch-delete-on-an-armed-run.md, test/probes/peer-message-asking-a-leashed-session-for-work.md, test/probes/pre-send-checklist-after-an-authorized-push.md, test/probes/review-only-at-section-close.md, test/probes/seat-asked-to-push-the-memory-store.md, test/probes/stale-anchor-in-a-resumed-plan.md
```

### Chapter 2 - 2026-09-07
Completed: 2. The runner
Implemented By: implementer-opus (initial build plus fix rounds 1, 2 and 3 from .kit/scratch/probes/brief-s2.md, brief-s2-fix.md, brief-s2-fix2.md and brief-s2-fix3.md; fix rounds 4, 5 and 6 shared with section 1; fix 6b by the main session)
Metrics: review rounds 7 (four fable trios with security on the section's own deltas, three on the shared rounds); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: tools/probe-corpus/run.mjs, template.md, isolation-control.mjs and README.md, with test/probe-runner.test.js. The pilot's cold-reader prompt was unrecoverable on this machine, so template.md is written from the precedence plan's Chapter description (route b, Interim board 1). The reader's isolation is a scratch CLAUDE_CONFIG_DIR holding a copy of the credentials file alone, the bare rung of the isolation ladder confirming the copy authenticates and no fallback to the operator's home occurs. Section 2's first acceptance clause, reproducing the pilot's three verdicts, is unmet on three stable samples and unmeetable with the pilot's eight-file set unrecoverable; its amendment rides the rulings brief rather than this run. The summary line reads `probe-corpus: <n> pairs, <m> mismatches (<p> on proposed rulings, <d> designed), <e> errors, exit <x>, tier <tiers>, report <path>`, with `(partial)` on a run that died mid-set, exit 101 on a crash or interrupt, and the exit code the ruled mismatch count otherwise. The synchronous pair loop defers a signal sent to the runner alone until the set finishes, justified rather than rewritten async since a terminal Ctrl+C reaches the reader; the README states it. Declined with reasons: the extensionless PATH shim on win32 and the temp-root relocation, both recorded for the security model docs round with PROBE_CLAUDE_BIN and PROBE_HOME_DIR lacking the second-signal gate; a cost ceiling on the paid-reader spend; the stderr full-sentence pins. Recorded rather than fixed: the ls-tree dash-leading ref guard and the relative PATH entry skip; a CLAUDE.md planted above the temp cwd counted rather than stopping the run; the real-terminal exit code unmeasured; removeLiveScratch unpinned as internal. Kit friction for the kaizen inbox: the Bash tool collapses backslash escapes inside heredocs, which put a real NUL into a test file mid-round; and a sonnet-tier prose section drew twelve opus review rounds each partly reversing the last.
Assumptions: (2026-09-06, section 2, route b) the runner's prompt template is authored from the precedence Chapter's description of the pilot rather than the pilot's own text; (2026-09-06, section 2, route b) reader isolation is the scratch config directory rather than `--bare`, which the OAuth-authenticated CLI on this box does not offer.
Review Findings: review: code pair plus security at fable, Agent tool, seven rounds (CHANGES_REQUIRED and CONCERNS on the build; CHANGES_REQUIRED 0/4/4, 0/2/4 and CONCERNS 0/0/6; CHANGES_REQUIRED 0/3/5, APPROVED_WITH_CONCERNS 0/1/6 and CONCERNS 0/0/4; CHANGES_REQUIRED 0/1/5, APPROVED_WITH_CONCERNS 0/1/6 and CONCERNS 0/0/4; APPROVED_WITH_CONCERNS 0/0/5, 0/1/5 and CONCERNS 0/0/4; CHANGES_REQUIRED 0/2/4, 0/1/3 and CLEAR 0/0/3; APPROVED_WITH_CONCERNS 0/0/2, 0/1/5 and CONCERNS 0/1/3). Criticals addressed: the suite copying the live credentials file. Majors addressed: `$`-pattern substitution in the prompt composer, any nonzero git status read as absent, errors counted as mismatches, unbounded shape-file reach, the `--before` leg bypassing containment through git's own normalization, readContained judging existence before containment, the newer-wins credential refresh, the allowlist at the read boundary, the ref-mode link refusal through git ls-tree, the isolation line's attribution, the credential-copy symlink refusal, Ctrl+C unable to stop a paid run inside the synchronous loop, an ENOBUFS kill read as the operator's interrupt. Majors justified: the deferred runner-alone signal (above). Minors noted: the items fix rounds 5, 6 and 6b carried, recorded on Interim boards 5 and 6.
Stamps: none surfaced beyond Chapter 1's two, the same window
Gate: targeted lane `node --test test/probe-runner.test.js test/probe-set.test.js` 176/173/0/3 exit 0 at 01:33Z on 2026-09-07 after fix 6b, and `node tools/probe-corpus/run.mjs --dry-run` 27 pairs exit 0 at the same moment; whole gate `node --test test/*.test.js` 3398/3385/1/12 exit 1 (delta +179 tests and +3 skipped against the baseline, the skips being the three EPERM symlink cases, the one red unchanged), delta against the Interim board 1 baseline 3219/3209/1/9 exit 1 at b4e249b; contention lane none defined in this repo; both measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the box claimed for the span under this session's id and released at the run's marker; the one red is the permanent test/memory-session.test.js case the baseline carries
Next: 4. The size ratchet classifies the probe set, whose Chapter rides this commit
Commit Model: Commit-and-Push
Delta: measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
plugins/claude-kit/skills/executing-work/SKILL.md: 21857 words, cap 21857, +383
plugins/claude-kit/skills/finishing-work/SKILL.md: 17696 words, cap 17696, +836
plugins/claude-kit/skills/writing-skills/SKILL.md: 3278 words, cap 3278, +670
test/doctrine-parity.test.js: 6252 lines, cap 6252, +38; tests 72, +1
test/size-ratchet.test.js: 2845 lines, cap 2845, +27; tests 80, +2
test/probe-runner.test.js: 2581 lines, cap 2581, untracked so HEAD holds no blob and the totals below exclude it; tests 91
test/probe-set.test.js: 726 lines, cap 726, untracked so HEAD holds no blob and the totals below exclude it; tests 61
test/probes/README.md: 394 words, cap 394, untracked so HEAD holds no blob and the totals below exclude it
test/probes/branch-and-pr-pull-request-at-section-close.md: 167 words, cap 167, untracked so HEAD holds no blob and the totals below exclude it
test/probes/commit-and-push-at-section-close.md: 155 words, cap 155, untracked so HEAD holds no blob and the totals below exclude it
test/probes/compaction-nudge-mid-section-with-no-checkpoint-open.md: 283 words, cap 283, untracked so HEAD holds no blob and the totals below exclude it
test/probes/merged-plan-branch-delete-on-an-armed-run.md: 160 words, cap 160, untracked so HEAD holds no blob and the totals below exclude it
test/probes/peer-message-asking-a-leashed-session-for-work.md: 166 words, cap 166, untracked so HEAD holds no blob and the totals below exclude it
test/probes/pre-send-checklist-after-an-authorized-push.md: 151 words, cap 151, untracked so HEAD holds no blob and the totals below exclude it
test/probes/review-only-at-section-close.md: 129 words, cap 129, untracked so HEAD holds no blob and the totals below exclude it
test/probes/seat-asked-to-push-the-memory-store.md: 182 words, cap 182, untracked so HEAD holds no blob and the totals below exclude it
test/probes/stale-anchor-in-a-resumed-plan.md: 161 words, cap 161, untracked so HEAD holds no blob and the totals below exclude it
words: 216834 of cap 216835 across 48 curated files
test lines: 106132 of cap 106132 across 53 test files
tests: 3173
changed paths under no measured root: 10 (5 differing from HEAD, 5 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
excluded from those totals, untracked under a measured root: test/probe-runner.test.js, test/probe-set.test.js, test/probes/README.md, test/probes/branch-and-pr-pull-request-at-section-close.md, test/probes/commit-and-push-at-section-close.md, test/probes/compaction-nudge-mid-section-with-no-checkpoint-open.md, test/probes/merged-plan-branch-delete-on-an-armed-run.md, test/probes/peer-message-asking-a-leashed-session-for-work.md, test/probes/pre-send-checklist-after-an-authorized-push.md, test/probes/review-only-at-section-close.md, test/probes/seat-asked-to-push-the-memory-store.md, test/probes/stale-anchor-in-a-resumed-plan.md
```

### Chapter 3 - 2026-09-07
Completed: 4. The size ratchet classifies the probe set
Implemented By: implementer-sonnet (from .kit/scratch/probes/brief-s4.md), with docs/architecture.md's root enumeration and the cap settings by the main session
Metrics: review rounds 1; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: plugins/claude-kit/scripts/kit-size.js gains the `test/probes/` root ahead of `test/`, measured in words; test/size-budget.json carries a cap per probe file, the probe README, both new test files and the three amended skills, every cap set exact at this commit; test/size-ratchet.test.js pins the root roster and the classifier's coverage. The two review Majors, cap slack and the commit-order dependency between the classifier and the probe files, close by this commit landing sections 1, 2 and 4 together with every cap exact. tools/probe-corpus/ sits under no measured root, like plugins/claude-kit/scripts/ itself, and stays there: the runner's behavior is measured by its test file's line cap, and a script root is a separate decision. The size report's whole-corpus words line shows one word of slack across the 48 curated files, in a file outside this plan.
Assumptions: none
Review Findings: review: code pair at opus, Workflow at high (APPROVED_WITH_CONCERNS both). Majors addressed: cap slack, the commit-order dependency. Minors noted: a README sentence on the cap requirement, handed to section 1's fix round and landed there.
Stamps: none surfaced beyond Chapter 1's two, the same window
Gate: targeted lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js test/doctrine-parity.test.js` 163/163/0/0 exit 0 at 02:38Z on 2026-09-07 after rewrite 16, `node plugins/claude-kit/scripts/kit-size.js check` exit 0 at the same moment; whole gate `node --test test/*.test.js` 3398/3385/1/12 exit 1 (delta +179 tests and +3 skipped against the baseline, the skips being the three EPERM symlink cases, the one red unchanged), delta against the Interim board 1 baseline 3219/3209/1/9 exit 1 at b4e249b; contention lane none defined in this repo; both measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the box claimed for the span under this session's id and released at the run's marker; the one red is the permanent test/memory-session.test.js case the baseline carries
Next: 3. The hook-ins and 5. The Chapter template holds the probe reading, in the next commit
Commit Model: Commit-and-Push
Delta: measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
plugins/claude-kit/skills/executing-work/SKILL.md: 21857 words, cap 21857, +383
plugins/claude-kit/skills/finishing-work/SKILL.md: 17696 words, cap 17696, +836
plugins/claude-kit/skills/writing-skills/SKILL.md: 3278 words, cap 3278, +670
test/doctrine-parity.test.js: 6252 lines, cap 6252, +38; tests 72, +1
test/size-ratchet.test.js: 2845 lines, cap 2845, +27; tests 80, +2
test/probe-runner.test.js: 2581 lines, cap 2581, untracked so HEAD holds no blob and the totals below exclude it; tests 91
test/probe-set.test.js: 726 lines, cap 726, untracked so HEAD holds no blob and the totals below exclude it; tests 61
test/probes/README.md: 394 words, cap 394, untracked so HEAD holds no blob and the totals below exclude it
test/probes/branch-and-pr-pull-request-at-section-close.md: 167 words, cap 167, untracked so HEAD holds no blob and the totals below exclude it
test/probes/commit-and-push-at-section-close.md: 155 words, cap 155, untracked so HEAD holds no blob and the totals below exclude it
test/probes/compaction-nudge-mid-section-with-no-checkpoint-open.md: 283 words, cap 283, untracked so HEAD holds no blob and the totals below exclude it
test/probes/merged-plan-branch-delete-on-an-armed-run.md: 160 words, cap 160, untracked so HEAD holds no blob and the totals below exclude it
test/probes/peer-message-asking-a-leashed-session-for-work.md: 166 words, cap 166, untracked so HEAD holds no blob and the totals below exclude it
test/probes/pre-send-checklist-after-an-authorized-push.md: 151 words, cap 151, untracked so HEAD holds no blob and the totals below exclude it
test/probes/review-only-at-section-close.md: 129 words, cap 129, untracked so HEAD holds no blob and the totals below exclude it
test/probes/seat-asked-to-push-the-memory-store.md: 182 words, cap 182, untracked so HEAD holds no blob and the totals below exclude it
test/probes/stale-anchor-in-a-resumed-plan.md: 161 words, cap 161, untracked so HEAD holds no blob and the totals below exclude it
words: 216834 of cap 216835 across 48 curated files
test lines: 106132 of cap 106132 across 53 test files
tests: 3173
changed paths under no measured root: 10 (5 differing from HEAD, 5 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
excluded from those totals, untracked under a measured root: test/probe-runner.test.js, test/probe-set.test.js, test/probes/README.md, test/probes/branch-and-pr-pull-request-at-section-close.md, test/probes/commit-and-push-at-section-close.md, test/probes/compaction-nudge-mid-section-with-no-checkpoint-open.md, test/probes/merged-plan-branch-delete-on-an-armed-run.md, test/probes/peer-message-asking-a-leashed-session-for-work.md, test/probes/pre-send-checklist-after-an-authorized-push.md, test/probes/review-only-at-section-close.md, test/probes/seat-asked-to-push-the-memory-store.md, test/probes/stale-anchor-in-a-resumed-plan.md
```

### Chapter 4 - 2026-09-07
Completed: 3. The hook-ins
Implemented By: implementer-sonnet (from .kit/scratch/probes/brief-s3.md, round 1), then the main session for every rewrite from round 2 on, the passages being design-entangled with the runner's contract as it settled
Metrics: review rounds 15 (opus at high through Workflow, adversarial and blind, shared with section 5); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: writing-skills' RED and GREEN paragraph runs the runner's before-and-after pair for a change touching, in a shape-named file, a passage a probe's scenario turns on; a ruled probe's pair stands in for the reps, a proposed probe's after leg alone is rulings evidence, the readings close at four with an intent test on a ruled after-leg mismatch, and the before leg takes the after leg's moment list narrowed to ruled probes, since a pair is a reading only where both legs ran the moment (rewrite 12 corrected rewrite 11's copy of finishing-work's economy, which had made the RED unobtainable exactly where a fix worked). finishing-work's step 5 runs the whole set ahead of the whole gate under the box claim and the background-marker rule, the before leg over the moments a ruled probe mismatched, states what each row counts for with the class closed, re-runs an errored pair once by the failing leg's own command narrowed with --only and --shape, names an unstable reading, and re-derives the before leg on a stale re-run. Amendments in the approval region, recorded as drift: section 3's text names the ruled gate with a proposed reading as rulings evidence (rewrite 9), its acceptance reads one paragraph each (rewrite 10), and its finishing trigger now reads the built one, a changeset touching a shape-named file, narrower than the original behavior-shaping-prose wording (rewrite 13). Deviation from the acceptance, recorded rather than reconciled: the finishing-work hook-in's substance sits across its step 5 paragraph and the ordering paragraph beside it rather than in one, the placement rationale, the stale re-run and the box-sharing bar having grown there under review. Declined with reasons across the rounds: the async pair loop; the finishing trigger firing on probe and runner edits (a probe edit is a re-ruling by construction and the runner has its own suite); the sentence-shape bar on the two longest sentences (each one idea, the acceptance reads one paragraph each); the home/ carve-out (it describes the parser's admitted shape though no shape names a home/ entry today); a cost ceiling on the paid-reader spend. Recorded rather than fixed: every probe is proposed, so every ruled branch in both skills waits on the operator's rulings batch and until then a run spends paid readers for rulings evidence alone; the prescribed finishing procedure has never run end to end, its first real execution being this plan's own finishing pass; finishing-work's assumptions paragraph does not yet point at the no-leading-marker rule executing-work states over it; the parity pin hard-fails rather than skips where tools/probe-corpus/run.mjs is absent, which no tracked tree meets since the runner's commit lands ahead of this one; a cleanup the runner could not take reaches stderr alone, carried to the security-model docs round with the credential-copy items. Two kaizen notes appended to kaizen/notes-SCOTT-CLAUDE.md, the backslash collapse in heredocs and the unbounded review seesaw, left uncommitted because the file also carries lines this session did not author. The reviewers' round-by-round verdicts are on Interim boards 1 through 7 and the ROUNDS line below.
Assumptions: (2026-09-07, section 3, route b) the writing-skills passage keys its trigger on a passage the scenario turns on, a judgment the author makes and records at the Decisions / Surprises slot, rather than on a mechanical hunk-to-scenario map the kit does not have; the mechanical half, a changed path in a shape's files list, runs first and the judgment only narrows it.
Review Findings: review: code pair at opus, Workflow at high, 15 rounds (rounds 1 to 10 on Interim boards 1 to 6; adversarial then blind: 11 CHANGES_REQUIRED 1/1/8 and 1/3/3, 12 0/6/7 and 0/2/9, 13 0/3/7 and 0/2/10, 14 0/2/7 and 0/1/6, 15 0/3/7 and 0/2/9, every round CHANGES_REQUIRED, rewrite 16 unreviewed as the close-out fix delta). Criticals addressed: the before leg scoped so the RED was unobtainable (round 11). Majors addressed: the finishing sentence's repo-local command and Gate field written from outside its owner (round 1); the baseline's home and the reps-bar collision (rounds 2 to 5); the length-and-restatement growth (rounds 4 to 6); the proposed-versus-ruled gate, the designed cases, the box claim across both legs, the credential copy's owner, the refusal and unrun states (rounds 7 to 10); the unstable row's leg, the stale re-run's home, the double trigger inside a finishing pass (round 11); the copied state list, the cross-surface pin, the file-granular trigger, the section-time slot, the stale re-run's before leg, the mismatched moments named (round 12); the exclusivity claim on the multi-line fields, the no-shape-file state, the pin's flag half, the spec's section 5 drift, the mirrored intent test (round 13); the Decisions / Surprises slot licensing silence in the states writing-skills names, the pointer pins on curated wording re-pinned on stable tokens (round 14); a refusal reason's absolute path reaching a tracked doc, closed by respelling or leaving out any full path in a quoted line or reason, the finishing-work pointer leg satisfied by a pre-existing sentence, re-pinned inside the paragraph that spells the runner, and the Decisions / Surprises slot as unrecorded drift, closed by naming the second slot in section 5's text (round 15). Majors justified: the pin's hard failure without the runner (rounds 14 and 15, blind), the recorded commit ordering; the section-close trigger's scenario judgment being unobservable (round 15, blind), which is round 12's passage-granular Major resolved the other way, the narrowing being recorded at the slot as the author's judgment and named on this Chapter's Assumptions line. Rewrite 16, carrying round 15's three Majors and nine of its Minors, closed the sections without a sixteenth round on finding composition rather than count: rounds 13, 14 and 15 each closed every prior finding and opened only new ground, none repeating a class, each Major a one-clause repair, so a further round was judged to buy new Minors rather than convergence; its lane is on the Gate line and its delta is prose plus one pin leg the lane exercises. Round 15 Minors carried unfixed: the every-project cost of the two hook-ins (the spec places them in the skills); the designed-agreed row off the before leg's list (its finding is the marker's, whichever ref it agreed at); tools/probe-corpus/ under no measured root (section 4's call). Minors noted: the items on the boards and above.
Stamps: none surfaced beyond Chapter 1's two, the same window
Gate: targeted lane `node --test test/output-style-parity.test.js test/size-ratchet.test.js test/doctrine-parity.test.js` 163/163/0/0 exit 0 at 02:38Z on 2026-09-07 after rewrite 16, `node plugins/claude-kit/scripts/kit-size.js check` exit 0 at the same moment; whole gate `node --test test/*.test.js` 3398/3385/1/12 exit 1 (delta +179 tests and +3 skipped against the baseline, the skips being the three EPERM symlink cases, the one red unchanged), delta against the Interim board 1 baseline 3219/3209/1/9 exit 1 at b4e249b; contention lane none defined in this repo; both measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the box claimed for the span under this session's id and released at the run's marker; the one red is the permanent test/memory-session.test.js case the baseline carries
Next: 5. The Chapter template holds the probe reading, whose Chapter rides this commit
Commit Model: Commit-and-Push
Delta: measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
plugins/claude-kit/skills/executing-work/SKILL.md: 21857 words, cap 21857, +383
plugins/claude-kit/skills/finishing-work/SKILL.md: 17696 words, cap 17696, +836
plugins/claude-kit/skills/writing-skills/SKILL.md: 3278 words, cap 3278, +670
test/doctrine-parity.test.js: 6252 lines, cap 6252, +38; tests 72, +1
words: 218782 of cap 218783 across 58 curated files
test lines: 109439 of cap 109439 across 55 test files
tests: 3325
changed paths under no measured root: 1 (1 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: none
```

### Chapter 5 - 2026-09-07
Completed: 5. The Chapter template holds the probe reading
Implemented By: main session (Locus: inline), section appended at Interim board 2 as approval drift
Metrics: review rounds 15 (shared with section 3); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: executing-work's Chapter template Gate line holds, in the kit's own repository and on a final Chapter alone, the probe set's reading where finishing-work's step 5 calls for one: per invocation of each leg, the runner's summary line quoted verbatim except that a report path printed in full is respelled repo-relative, the exit code beside it where the two differ, the report's warning lines, the mismatched moments by name with each row's status, and its moment-pin; or the state step 5 names instead, or that the changeset named no shape file, none recorded as clean, a partial line marked as the leg its re-run replaced; reporting and never blocking. The Decisions / Surprises placeholder names the line writing-skills' section-time pair lands on, carrying the states that step names so a section close cannot record silence as clean, so neither hook-in defines a Chapter field from outside its owner. The Delta paragraph names the Gate line and the finishing Chapter's assumptions block as two of the author-written multi-line fields between the contract lines and holds the no-leading-marker rule over every line an author adds to any Chapter field. A cross-surface pin in test/doctrine-parity.test.js ties the flags the skills spell in a run.mjs command and the two prefixes they quote to run.mjs, and pins both pointer pairs on stable tokens rather than sentences, so a rename there reds the parity lane while the wording stays free to move. Amendments in the approval region, recorded as drift: section 5's parenthetical now describes the built slot (rewrite 14), and its text and acceptance name the Decisions / Surprises slot beside the Gate one (rewrite 16), the second slot having been built at rewrite 12 on round 11's finding and left unrecorded until round 15 named it.
Assumptions: none
Review Findings: on Chapter 4's line
Stamps: none surfaced beyond Chapter 1's two, the same window
Gate: on Chapter 4's line, the same lanes at the same moments; whole gate `node --test test/*.test.js` 3398/3385/1/12 exit 1 (delta +179 tests and +3 skipped against the baseline, the skips being the three EPERM symlink cases, the one red unchanged), delta against the Interim board 1 baseline 3219/3209/1/9 exit 1 at b4e249b; contention lane none defined in this repo; both measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the box claimed for the span under this session's id and released at the run's marker; the one red is the permanent test/memory-session.test.js case the baseline carries
Next: finishing-work
Commit Model: Commit-and-Push
Delta: measured 2026-09-07 02:46Z on this main checkout at 85ac6c0 with the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
plugins/claude-kit/skills/executing-work/SKILL.md: 21857 words, cap 21857, +383
plugins/claude-kit/skills/finishing-work/SKILL.md: 17696 words, cap 17696, +836
plugins/claude-kit/skills/writing-skills/SKILL.md: 3278 words, cap 3278, +670
test/doctrine-parity.test.js: 6252 lines, cap 6252, +38; tests 72, +1
words: 218782 of cap 218783 across 58 curated files
test lines: 109439 of cap 109439 across 55 test files
tests: 3325
changed paths under no measured root: 1 (1 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: none
```

### Interim board 8 - 2026-09-07

Finishing pass in flight, compaction boundary taken on the gate's deferral nudge. Base ref 85ac6c0 by the Chapter walk (parent of 9b5b2f5, the first commit appending a Chapter); both section commits pushed at 02:48Z with main in sync and the build stamp rebuilt. Step 1 QA (qa-verifier): PASS, whole gate 3398/3385/1/12 exit 1 at 02:58Z on e0ef09c, identical to the Chapter baseline, contention lane none defined, every acceptance line verified with the section 2 pilot-reproduction clause operator-pending. Steps 2 and 3 at fable/high through Workflow, both resolved wholly at claude-fable-5-1 (45 and 44 assistant turns): security CONCERNS 0/0/3 (home-prefixed absolute paths in stderr refusal reasons, the runner-alone signal deferral being the operative case under the backgrounded finishing shape, the inherit rung's unstated side effects); adversarial APPROVED_WITH_CONCERNS 0/1/9, the Major being that the five Chapters carry none of the probe-pair states the changeset's own Decisions / Surprises slot now requires, closed in the final Chapter per section rather than by rewriting appended Chapters. Fix delta applied at 03:11Z: the runner's empty-value refusal speaks to the flag it names with --only '' pinned, the probe-set test name no longer claims the unmet reproduction, test/probes/README.md drops the measured reading and keeps the open question, docs/architecture.md's tools/ and test/ layout lines name probe-corpus/ and test/probes/, isolation-control.mjs names the inherit rung as a live hooked session, the runner README states the tree-kill route for a backgrounded run, docs/plans/README.md says ruled-mismatch count; lane `node --test test/probe-runner.test.js test/probe-set.test.js test/size-ratchet.test.js test/doctrine-parity.test.js test/output-style-parity.test.js` 339/336/0/3 exit 0, size check exit 0, caps exact at 2582 and 387; no review round on the delta, judged below the bar as message wording, one pin, comments and prose. Step 4 docs-curator: docs/architecture.md, docs/security-model.md (new runner section, the two PROBE_* overrides placed outside the payload's ungated three) and docs/README.md updated, nine drift items, eight deviations recorded for the final Chapter and one mistake, the plan header's "three sections", confirmed by reading line 7 and fixed here as an approval-region edit recorded as drift; hygiene: the corpus-audit plan's Related is left unedited per section 3, and the eliding plan is now pointed at from this plan's Related. Step 5's probe set: the after leg `node tools/probe-corpus/run.mjs` launched 03:12:31Z backgrounded with its log and exit marker under the box claim, eight of 27 pairs done at 03:21Z; no before leg is owed since every probe is proposed. Memory: five applied stamps taken by hand walk at 03:11Z (test-suite-invocation, suite-baseline-is-not-zero-fail, skill-amendments-collide-with-neighbours, and operator-tier build-stamp-refresh and pwsh7), the sweep having found no read stamp in the window; decay stamp eleven days old, no pass due.

Gate baseline: unchanged from Interim board 1 (3219/3209/1/9 exit 1 at b4e249b); the handoff gate is owed after the archive and index edits.

Next: read the after leg, re-run any errored or mismatched pair once, append the final Chapter with its Gate line open, archive the plan through curating-docs, refresh both indexes, run the whole gate, fill the Gate line, commit the docs and the fix delta, push, close out with the board and end BLOCKED on the rulings batch unless it is answered by then.

### Chapter 6 - 2026-09-07
Completed: finishing-work over sections 1 through 5
Implemented By: main session (the finishing pass; qa-verifier, security-reviewer, adversarial-reviewer and docs-curator dispatched; the fix delta applied inline)
Metrics: review rounds 1 (the finishing pair at fable/high through Workflow, no round on the fix delta); NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: Base ref for the whole changeset 85ac6c0, the parent of 9b5b2f5, the first commit appending a Chapter, so the changeset is every commit from that one through the finishing commit. Step 1 QA (qa-verifier) PASS on e0ef09c: whole gate 3398/3385/1/12 exit 1 at 02:58Z, identical to the Chapter 4 baseline, every acceptance line verified except section 2's pilot-reproduction clause, which is operator-pending on the rulings batch (Chapter 2). Steps 2 and 3 at fable/high through Workflow, each resolved wholly at claude-fable-5-1 (security 45 assistant turns, adversarial 44, no substitution): security CONCERNS 0 Critical / 0 Major / 3 Minor (home-prefixed absolute paths in the runner's stderr refusal and cleanup messages; the runner-alone signal deferral under a backgrounded run; the inherit rung's unstated side effects); adversarial APPROVED_WITH_CONCERNS 0 / 1 / 9, the Major being that Chapters 1 through 5 carry none of the probe-pair states section 5's Decisions / Surprises slot requires, closed here per section rather than by rewriting appended Chapters: sections 1 (the probe files), 2 (the runner) and 4 (the ratchet) touched no file a shape names, so no pair was owed; sections 3 and 5 amended writing-skills, finishing-work and executing-work, files the shapes name, and the scenario judgment was that the after leg was owed at finishing over the whole set rather than per section, since every probe is proposed and the before leg reads ruled probes alone, which is the leg this Chapter's Gate line reports. The fix delta (03:11Z): the runner's empty-value refusal speaks to the flag it names with --only '' pinned; the probe-set test name no longer claims the unmet reproduction; test/probes/README.md drops the measured reading and keeps the open question; docs/architecture.md's tools/ and test/ layout lines name probe-corpus/ and test/probes/; isolation-control.mjs names the inherit rung as a live hooked session; the runner README states the tree-kill route for a backgrounded run; docs/plans/README.md says ruled-mismatch count. Recorded and left: the real-config rung's cleanup message can report a cleanup it did not take; the signal path can print its stderr line twice; the two stderr home paths go to the output-channel-eliding plan's producer sweep through the backlog. Step 4 docs-curator updated docs/architecture.md (the runner in the runtime-behavior list, both far-end pins, a probe-corpus paragraph), docs/security-model.md (a new runner section with PROBE_CLAUDE_BIN and PROBE_HOME_DIR outside the payload's ungated three) and docs/README.md (the plan entry and both summaries); its diff was read and judged true. Drift adjudicated: D1 the runtime-behavior enumeration omitted the runner, D2 the far-end pin count read one where two exist, D3 the runner's two ungated variables were undocumented, D4 the docs index carried the pre-drift status and count, D5 the backlog's suite figures were stale (fixed at board 8 as 109,440 lines / 3,325 tests / 55 files), D6 the frozen-at-ruling premise is inert while every probe is proposed, D7 the reader's scratch and credential copy sit under the OS temp root rather than .kit/ (deliberate: the run directory is the evidence artifact), D8 section 2's pilot-reproduction clause is unmet and held for the rulings batch: all eight deviations, recorded with the curated docs stating the as-built; D9 the header's "three sections" was a mistake, fixed at board 8 as an approval-region edit. Hygiene: H1 the corpus-audit plan's Related is left unedited per section 3's own rule that the later session points; H2 the eliding plan is pointed at from Related; H3 clean. Operator-pending, unchanged: the nine rulings and two amendments on the relay (re-sent 02:49Z), recorded in the backlog's first Active item. Every hook-in the plan built ran for the first time in this pass: the runner's after leg under the box claim, the Gate-line reading below, and the Decisions / Surprises states above.
Assumptions: (2026-09-06, section 1, route b) every probe ships `proposed` and the set freezes at the operator's ruling rather than at this Chapter, since the plan makes the rulings the operator's and none has arrived; (2026-09-06, section 2, route b) the runner's prompt template is authored from the precedence Chapter's description of the pilot rather than the pilot's own text; (2026-09-06, section 2, route b) reader isolation is the scratch config directory rather than `--bare`, which the OAuth-authenticated CLI on this box does not offer; (2026-09-07, section 3, route b) the writing-skills passage keys its trigger on a passage the scenario turns on, a judgment the author makes and records at the Decisions / Surprises slot, rather than on a mechanical hunk-to-scenario map the kit does not have; the mechanical half, a changed path in a shape's files list, runs first and the judgment only narrows it.
Review Findings: review: security lens at fable, Workflow at high (wholly claude-fable-5-1): CONCERNS 0/0/3, Minors noted above, two fixed in the delta (the inherit rung's header, the backgrounded stop route) and one routed to the eliding plan. review: adversarial lens at fable, Workflow at high (wholly claude-fable-5-1): APPROVED_WITH_CONCERNS 0/1/9; Major addressed in this Chapter's per-section states; Minors addressed in the delta (the flag-specific refusal, the test name, the README reading, the layout lines, the plans-index wording) or recorded above. The delta took the targeted lane and no review round, judged below the bar as message wording, one pin, comments and prose.
Stamps: five stamped by hand walk at 03:11Z (test-suite-invocation, suite-baseline-is-not-zero-fail, skill-amendments-collide-with-neighbours, and operator-tier build-stamp-refresh and pwsh7), the sweep having found no read stamp in the window; decay stamp eleven days old, no pass due
Gate: targeted lane on the fix delta `node --test test/probe-runner.test.js test/probe-set.test.js test/size-ratchet.test.js test/doctrine-parity.test.js test/output-style-parity.test.js` 339/336/0/3 exit 0 at 03:11Z on 2026-09-07, size check exit 0 with caps exact at 2582 and 387. Probe set: after leg only, since every probe is proposed and the before leg reads ruled probes alone; first invocation `node tools/probe-corpus/run.mjs` over the whole set, summary line `probe-corpus: 27 pairs, 0 mismatches (6 on proposed rulings, 0 designed), 1 errors, exit 0, tier sonnet,opus, report .kit/probe-runs/2026-09-07T03-12-31-205Z/report.md`, its warnings: 1 pair produced no reading at all (the exit code counts mismatches only); 6 pairs disagreed with a ruling the operator has not settled; 1 pair on a shape built to read against the answer agreed with it instead. Second invocation, the one re-run the finishing step allows, `node tools/probe-corpus/run.mjs --only merged-plan-branch-delete-on-an-armed-run --shape doctrine-plus-output-style`, summary line `probe-corpus: 1 pairs, 0 mismatches (1 on proposed rulings, 0 designed), 0 errors, exit 0, tier sonnet, report .kit/probe-runs/2026-09-07T03-48-59-881Z/report.md`, its warning: 1 pair disagreed with a ruling the operator has not settled; that row replaces the first run's errored row (a reader timeout at 300 seconds; the re-run's reader took 285 seconds), so the whole set reads 27 pairs, 0 errors, 7 rows on proposed rulings, 1 designed-agreed, and every row counts as rulings evidence rather than a gate, the ruled branch being dormant. The mismatched moments by name, each with its status: branch-and-pr-pull-request-at-section-close on doctrine-plus-the-contested-surfaces, mismatch on a proposed ruling, read RESOLVED to finishing-work against the proposed CONTESTED and unowned answer (the two other shapes matched); compaction-nudge-mid-section-with-no-checkpoint-open on all three shapes, mismatch on a proposed ruling, the full and doctrine-plus-executing-work shapes reading CONTESTED with an OTHER answer that executing-work's own two passages fork without a tiebreak and the doctrine-plus-output-style shape reading RESOLVED to keep-working-to-the-boundary, against the proposed RESOLVED write-an-interim-board-entry-and-open-a-checkpoint; merged-plan-branch-delete-on-an-armed-run on doctrine-plus-output-style, mismatch on a proposed ruling from the re-run, read CONTESTED with an OTHER answer naming the absent owning skill against the proposed RESOLVED remove-the-worktree-then-delete-the-branch (the two other shapes matched); peer-message-asking-a-leashed-session-for-work on doctrine-plus-output-style, mismatch on a proposed ruling, read CONTESTED against the proposed RESOLVED with the same answer decline-and-route-to-the-operator; pre-send-checklist-after-an-authorized-push on output-style-plus-executing-work, designed-agreed, the shape marked output-style-copy-lacks-the-exception read RESOLVED send-without-asking, so either the copy now carries the exception or the marker is stale, a question for the rulings batch. Moment-pin: both invocations measured 2026-09-07 03:12:31Z to 03:48:42Z and 03:48:59Z to 03:53:44Z on this main checkout at 07cca83 with the finishing delta dirty, the reader binary resolved from the operator's own install with the scratch configuration directory isolation, the box claimed under this session's id for the span, no foreign heavy process observed. Whole gate: `node --test test/*.test.js` 3398/3385/1/12 exit 1, delta against the Interim board 1 baseline 3219/3209/1/9 exit 1 at b4e249b and identical to the Chapter 4 baseline 3398/3385/1/12 exit 1 at e0ef09c, the one red the permanent test/memory-session.test.js case the baseline carries, 3 EPERM symlink skips among the 12; contention lane none defined in this repo; `node plugins/claude-kit/scripts/kit-size.js check` exit 0; measured 2026-09-07 03:54:52Z to 04:02:27Z on this main checkout at 07cca83 with the finishing delta dirty and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, the box claimed under this session's id for the span and released at the run's marker, no foreign heavy process observed
Next: none; the plan is Complete and archived in this changeset, and the operator's rulings batch reopens the corpus as a new round when it lands
Commit Model: Commit-and-Push
Delta: measured at 2026-09-07T03:54:28Z on this main checkout at 07cca83 with the finishing delta dirty and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
test/probe-runner.test.js: 2582 lines, cap 2582, +1; tests 91, +0
test/probes/README.md: 387 words, cap 387, -7
words: 218775 of cap 218776 across 58 curated files
test lines: 109440 of cap 109440 across 55 test files
tests: 3325
changed paths under no measured root: 8 (8 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
```
