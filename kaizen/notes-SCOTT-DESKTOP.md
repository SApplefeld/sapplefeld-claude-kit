2026-07-15 SCOTT-DESKTOP claude-kit: kit-doctor's relay check only warns and hands the user arm-resume-relay to run manually, which then just writes the default Window.txt; it should assume that default without the extra run, or arm the relay itself under -Fix consent like the other repairs.
2026-07-17 SCOTT-DESKTOP claude-kit: a plan section whose deliverable is docs/ content cannot be executed by an implementer role - docs-write-guard denies (exit 2) any non-curator subagent writing to docs/, yet executing-work tier routing has no rule keeping such a section off an implementer; route docs-writing sections to the main thread, or dispatch them to docs-curator (the one subagent allowed to write docs/).
