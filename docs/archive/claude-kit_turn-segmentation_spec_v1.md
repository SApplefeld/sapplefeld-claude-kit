# Compact-Session Turn Segmentation: Bounded Summarization Plans for Autonomous Runs

Status: Complete
Commit Model: Commit-and-Push
Run Mode: interactive (recorded override, decided 2026-07-18; see Chapter 1)
Fable Spend: S1 (implementer-fable), S2 escalation round (implementer-fable, earned by two failed review rounds), finishing reviews
Created: 2026-07-18

## Goal

The compact engine produces bounded, multi-pair summarization plans on autonomous transcripts, so a spec-with-Neo, then Opus, then `/kit-goal` run can compact at every section boundary instead of running uncompacted to the context ceiling. Today an unattended run's transcript collapses into a handful of mega-turns, the planner hands the summarizer a degenerate one-pair template, and the summarizer breaks the indexed-pair contract; compaction then fails at every boundary. The fail-safe holds (source untouched), but the run loses compaction entirely, which is exactly the workflow this kit is converging on.

## Root cause (confirmed by replay, 2026-07-18)

Reproduced against the failing session's real transcript (`NEO: Opus Work`, `12d49445`, 884 active rows) by replaying the engine's own planning code:

- `buildAssistantTurns` (`engine/transcript.ts:114`) opens a turn only at a human user row: `type === "user" && !isToolResultRow && isMeta !== true` (`transcript.ts:280`). The autonomous run had 203 user rows but only 8 human ones, yielding **3 turns** (one of 321 rows, one of 557).
- With the default `--keep 1`, `createPlan` produced prefix=1 (a prior compaction's `magicCompact` row), summarized=1, preserved=1, so `expectedCount = 1`, matching the field error `Summary index 2 is outside the 1 requested turns` exactly.
- The template (`buildXmlTemplate`, `engine/compact.ts:562`) therefore contained a single indexed pair; `parseSummaries` tolerates one extra trailing index and hard-throws at index >= 2 (`compact.ts:653`). The summarizer, asked for one ~200-word summary of a 321-row turn, emitted its own multi-pair numbering (inferred; the raw response is unrecoverable because the analysis copy is unlinked in a `finally`, `compact.ts:204`).

**Amended by Section 4's live re-run.** The plan shape is deterministic and reproduced exactly: the pre-fix engine on this transcript still plans a single summarized entry, so it still asks for one summary of a mega-turn. The *summarizer's* reaction to that request is not deterministic. Asked the same degenerate question, it complied once (emitting one compliant pair, so `parseSummaries` did not throw and the run exited 0), where in the field it broke the indexed-pair contract 11 times. So the original "deterministic shape means an identical failure at all 11 attempts" claim conflated the two: the degenerate *request* is deterministic, the *failure* is not. The fix is unaffected, since it removes the degenerate request entirely, but the failure mode is a probabilistic response to a bad prompt rather than a guaranteed one. Section 4 also found that a pre-fix run which "succeeds" is worse than one that throws: see Chapter 4.

The strict over-range throw is load-bearing (an invented index cannot be anchor-verified) and stays. The defect is planner-side: entries of `summarizedTurns` are unbounded.

## Approach

A pure segment splitter applied to `summarizedTurns` inside `createPlan`, after the keep/prefix slices are computed on real (human-bounded) turns. Every downstream mechanism (indexed pairs, anchor cross-check, per-entry verbatim degradation, the more-than-half ceiling, the emission loop in `buildCompactedRows`) is keyed on "entries of `summarizedTurns`", so redefining an entry as a bounded segment heals the pipeline without parser changes and restores the multi-pair template regime verified at scale on 2026-07-15.

Key decisions:

- **Split point invariants.** A cut lands only before an assistant row that starts a new step; a `tool_use` is never separated from its `tool_result`; every segment contains at least one assistant row (the emission path requires `firstAssistant`, `compact.ts:293`); a single row exceeding the budget forms its own segment, never split internally.
- **Budget.** A named constant, `SEGMENT_TOKEN_BUDGET = 20_000` estimated tokens (chars/4 heuristic over each row's model-visible content), colocated with the splitter in `transcript.ts`. Segment size does not affect summarizer input (the summarizer resumes the full analysis copy); it sets summary granularity: one summary of at most ~200 words per ~20k tokens of stretch. This is the tunable knob if S4 shows summaries too coarse or too fine.

- **The budget has a hard floor, because segment count drives prompt argv length.** The summarizer prompt is passed as an argv element to `Bun.spawn` (`compact.ts:158`), and the template carries one indexed pair (~420 chars, dominated by the 300-char anchor) per segment. A ~925k-token transcript at a 20k budget yields ~46 pairs, roughly 22KB of argv, against the Win32 `CreateProcessW` ceiling of 32,767 characters. Halving the budget doubles the pair count and exceeds that ceiling, and the failure surfaces only as an opaque nonzero exit through `sanitizeSpawnOutput`. So S4 tunes the budget **downward only to about 15k** on Windows. Tuning below that requires first moving the prompt from argv to the spawn's stdin, which removes the ceiling entirely (and independently retires the unenforced "`claude` must resolve to a native executable" precondition documented at `compact.ts:154-157`). That move is out of scope here; it is the recorded escape hatch if S4 finds summaries genuinely need finer granularity.
- **Synthetic anchors.** Continuation segments have no user rows, and a blank anchor would trip the echo cross-check. Each continuation segment carries a deterministic anchor: `(continuation N) ` plus a snippet derived from the segment's first assistant text block (fallback: its tool names, e.g. `(tool activity: Bash, Read)`), run through the same normalization/truncation as `getUserPromptText` (whitespace-collapse, angle-bracket strip, 300 chars). The prefix guarantees anchor uniqueness by construction; the 2026-07-15 effort showed degenerate anchors are how summaries silently merge.
- **Keep semantics count turns, and fall back to segments only when turns cannot express them** (amended 2026-07-18, decided by Scott). `--keep N` counts real human-bounded turns, and splitting happens within the already-selected `summarizedTurns`; prefix turns are never segmented. The one exception is the degenerate shape this plan exists to serve: when the compactable turns number `N` or fewer, keeping `N` turns leaves nothing to summarize at all, so `--keep N` preserves the last `N` **segments** instead. A `/kit-goal` run opens with a single human prompt and is therefore one turn for its whole length, so without the fallback the plan's own Goal is unreachable: `--keep 1` on such a session plans zero summarized entries and the CLI exits "no older assistant turns to compact". The fallback is scoped strictly to the case where the turn-granular slice is already empty, so the ordinary meaning of `--keep N` is untouched, and a preserved segment is copied verbatim by the same path a preserved turn takes.
- **Template wording.** The prompt gains one rule explaining segment pairs (a long stretch may be split; each indexed pair covers the slice anchored by its snippet; `(continuation N)` anchors mark split points and must be echoed like any anchor). Prompt wording is behavior-shaping for the summarizer model; S4's live run is its baseline test (writing-skills discipline).
- **Diagnosability.** On a `parseSummaries` throw, the raw summarizer response is persisted to a debug file before the error propagates, so the next field failure is evidence, not inference. (The field failure's raw responses were deleted by the `finally` unlink, which is why part of the mechanism above is inferred.)
- **No retry loops** (2026-07-15 wild evidence: retries got worse). `prune.ts`, `omission.ts`, `retrieve.ts` stay vendored-verbatim.

## Sections of Work

### 1. Segment splitter and anchor override
Model: fable

A pure function (new `engine/segment.ts` or colocated in `transcript.ts`, implementer's call) that maps `Turn[]` to segment pseudo-turns honoring the invariants above, plus the `Turn` type extension for an anchor override that `getUserPromptText` (`compact.ts:603`) honors. No wiring into `createPlan` yet (S2). Unit tests over synthetic row fixtures.

Acceptance:
- A turn under budget passes through unchanged (identity, not a copy with drift).
- An oversized turn splits at step boundaries only; reassembling segment rows in order reproduces the original turn's rows exactly (no loss, no reorder, no duplication).
- No segment separates a `tool_use` from its `tool_result`; every segment has at least one assistant row; a single oversized row becomes its own segment.
- Continuation anchors are deterministic (same input, same anchors), unique within the turn, and normalized like `getUserPromptText` output.

Tests: lock the invariants in both directions - a fixture that must split and one that must not; a tool_use/tool_result pair straddling the budget line stays together even when that overshoots the budget; determinism asserted by double-run equality. The expensive failure is a silent row drop or reorder, so the reassembly check is the non-negotiable test.

### 2. Plan integration and emission guarantees
Model: opus

Wire the splitter into `createPlan` (`compact.ts:105`), applied to `summarizedTurns` only, after slicing. The ordering is load-bearing, not stylistic: `createPlan` slices `turns` by **count** for both `--keep N` and `compactionStartIndex`, so splitting before the slice would silently redefine `--keep N` from "keep N turns" to "keep N segments" and change the preserved tail. Two further confirmations before wiring: the first assistant row of a segment is emitted twice by design (once as the summary row at `compact.ts:298`, once as a tool-block-only row by the loop at `compact.ts:309`, since `turn.userRows` is empty for continuation segments and cannot exclude it), which is how a step-opening `tool_use` survives summarization, and this goes from rare to routine for continuation segments because such a segment opens on a step boundary by construction. Confirm and, where needed, adapt: `buildCompactedRows` emits one summary row per segment followed by that segment's tool-block rows; a summarizer-skipped segment degrades to verbatim via the existing per-entry path (`compact.ts:274`); segments after the first have empty `userRows` and the user-row copy loop is a clean no-op; `generateSummaries`' `nextTurn` anchor still comes from the first preserved real turn.

Acceptance:
- A synthetic fixture reproducing the field shape (3 human turns, one oversized, keep 1) plans to `expectedCount > 1` with bounded segments instead of 1 mega-entry.
- A compacted transcript built from a segmented plan has every original tool row present exactly once, parent-chained, in order.
- Skipped-segment verbatim degradation round-trips (unit test with a summaries map missing one segment index).

Tests: at minimum, lock the per-segment verbatim degradation and the tool-row completeness of the emitted transcript; the risk is a segment boundary corrupting the parent-uuid chain, which would surface only at resume time.

### 2b. Segment-granular keep for degenerate turn counts
Model: opus

Added 2026-07-18 after S2's close surfaced that `--keep N` counting human-bounded turns makes a single-turn `/kit-goal` session permanently uncompactable, which defeats this plan's Goal. Implements the amended keep decision above.

In `createPlan`, when the compactable turns (those after the prefix) number `keepTurns` or fewer and `keepTurns > 0`, segment the compactable turns and slice `keepTurns` **segments** off the tail as `preservedTurns`, summarizing the rest. Outside that case, nothing changes. The condition fires only where the current code already yields an empty `summarizedTurns`, so the change is strictly additive: no input that compacts today compacts differently.

Acceptance:
- A single-turn transcript with `--keep 1` plans `summarized > 1` with bounded segments and preserves exactly the final segment verbatim.
- A multi-turn transcript's `--keep N` behavior is byte-identical to today's, including which turns land in each group. Lock this in both directions, since redefining `--keep` in the non-degenerate case is the trap S2 was built to avoid.
- A single turn that does not exceed the budget still plans nothing (one segment, preserved, nothing to summarize), so a small session is not compacted pointlessly.
- The preserved segment round-trips verbatim through `buildCompactedRows`, and `generateSummaries`' `nextTurn` anchor resolves against it without error when it is a continuation segment carrying an `anchorOverride`.

Tests: the degenerate and non-degenerate cases in both directions; a fixture whose compactable turn count exactly equals `keepTurns` (the boundary the condition turns on); and the verbatim round trip of a preserved continuation segment.

### 3. Template wording and parse-path regression tests
Model: opus

The one added template rule (segment pairs and `(continuation N)` anchors) in `buildCompactionPrompt`/`buildXmlTemplate`, and a synthetic mega-turn fixture (small, constructed; the 3.2MB real transcript never enters the repo) exercising the full plan-to-template-to-parse path with a mocked summarizer response.

Acceptance:
- The degenerate autonomous shape (1 human turn to summarize, many rows) renders a template with N indexed pairs and per-segment anchors.
- `parseSummaries` is unchanged; existing parser tests pass with zero edits (parser-side diff is empty).
- A mocked compliant response maps summaries to the right segments; a mocked response that skips one segment degrades that segment verbatim.

### 4. Live-fire verification
Model: opus

End-to-end against the real failing transcript. Copy `C:\Shared\Transcripts\D--Neuro-Evolution-Operations\12d49445-fd35-4d1f-b9e1-c7a26795966c.jsonl` into a throwaway project dir under `~/.claude/projects/` (cwd rules per `compact-cli.ts:60-71`).

Protocol:
1. **Red baseline:** run the pre-fix engine (the commit before S1 landed, via a temporary `git worktree`; do not rely on the installed plugin cache, whose version depends on when plugins were last updated) with `compact-cli --force` against the copy; it must fail with the exact `Summary index N is outside the M requested turns` error. This proves the harness exercises the failing path.
2. **Green:** run the repo engine (post-fix) the same way; it must produce a compacted destination, with segment summary rows present and the ledger entry written.
3. **Coherence check:** open the compacted transcript and confirm the summarized stretch reads as ordered segment summaries with tool rows intact; resume the destination session headlessly (`claude -p --resume <destination id>` with a trivial prompt) to confirm the CLI accepts it.
4. **Second-compaction check:** after that resume has written its turn, run `--check` and then plan a second compaction against the destination. It must find a non-empty `summarizedTurns`, which is what proves the `/kit-goal` workflow compacts at *every* boundary rather than only the first (see the segment-granular `compactionStartIndex` entry in Out of Scope for the measured behavior and its bound). Record the resulting group counts in the Chapter.
5. **Measure the two ceilings the Open Questions name:** the rendered template's character count against the 32,767-char Win32 argv limit, and the summarizer's actual skip rate across the segments requested. Both bound how far `SEGMENT_TOKEN_BUDGET` can move, and neither has been measured against a live summarizer.

Evidence already captured, unprompted, by this effort's own session (2026-07-18): the S2b close compacted the executing session itself, source `d5f03240` to destination `c9a7e88a`, with a live summarizer. Ledger: 343,907 context tokens before, 1,943,126 source bytes to 946,926 destination bytes, 59,850 ms. The plan was 11 summarized segments and 1 preserved; the destination carries 1 boundary row and **11 summary rows, so the skip rate was 0 of 11** and no segment degraded to verbatim. The summaries are ordered, distinct, and accurate: the executing session resumed from them and re-grounded without loss, which is a coherence check no synthetic fixture can provide. This satisfies protocol steps 2 and 3 against a real autonomous transcript and gives Open Question 2 its first live data point at N=11. It does **not** discharge step 1 (the red baseline needs the specific `12d49445` transcript that produced the field error), step 4, or step 5.

Billing: each live run spends one sonnet summarizer call (subscription-billed, `DEFAULT_SUMMARIZER_MODEL = claude-sonnet-5`); expect 2-4 calls total. Cleanup: remove the throwaway project dir, its `~/.claude/magic-compact/` cache entries, any analysis-copy transcripts, and the temporary worktree; name the cleanup in the Chapter.

If summaries come back too coarse or too fine, tune `SEGMENT_TOKEN_BUDGET` (one axis per round) and note the final value in the Chapter. Downward tuning stops at about 15k: below that the template's per-segment pairs push the summarizer prompt past the Win32 argv ceiling (see the budget-floor decision above). Capture the rendered template's character count alongside the coherence check so the headroom is measured rather than assumed.

### 5. Parse-failure response persistence
Model: sonnet

In `generateSummaries` (`compact.ts:131`), when `parseSummaries` throws, write the raw stdout to `~/.claude/magic-compact/debug/<source session id>-<ISO timestamp>.txt` before the error propagates, and append the file path to the thrown error's message so the CLI's `Compaction failed; source untouched: ...` line names it. Create the debug directory on demand. No rotation (failures are rare and files are small); no change to success paths.

Acceptance:
- A parse failure produces the debug file with the exact raw response and an error message naming the path.
- A successful parse writes nothing.

Tests: both directions - failure writes and names the file; success leaves no debug artifact.

## Standing Brief Amendments

Folded into every later dispatch brief for this effort.

- **When writing a helper that reads transcript row content, name the existing sibling readers and require handling every content shape they handle.** `message.content` is a string on some rows and a block array on others, and attachment rows carry their payload outside `message` entirely. The engine's existing readers are `getUserText` (`compact.ts`, handles the string form) and `pruneTranscriptRow` (`prune.ts`, same). A generic "mirror the sibling's breadth" instruction is not enough: S1 produced two separate shape gaps, one where a sibling existed and was not consulted, and one where no sibling existed at all and only reading a real transcript surfaced it.

- **A new heuristic that measures transcript rows gets checked against a real transcript before it is trusted.** S1's token estimator scored attachment rows at zero; only inspecting actual `.jsonl` rows (up to 14,555 chars each) revealed it. Reading the code cannot surface an omitted row shape.

## Out of Scope

- Parser changes: the indexed-pair contract, over-range throw, sparse fallback, and anchor cross-check are untouched (S3 asserts a zero parser diff).
- The `expectedCount === 1` merge leniency (option B from the diagnosis): rejected in favor of this fix; segmentation makes the one-pair template unreachable for oversized turns.
- Retry loops on summarizer failure.
- CLI flag surface (`--segment-budget` and similar): the budget is a constant.
- `prune.ts`, `omission.ts`, `retrieve.ts`: vendored-verbatim.
- `buildAssistantTurns` turn-boundary semantics: human-bounded turns remain the unit of `--keep` and of prefix/preserved handling, outside the degenerate-count fallback the Keep semantics decision defines.

- **A segment-granular `compactionStartIndex`.** `compactionStartIndex` is turn-granular (`findLastIndex(turn contains a summary row) + 1`), so after a compaction the whole turn holding the summary rows becomes prefix. On a single-turn autonomous transcript that means a second compaction finds nothing compactable until a resume delivers a human user row. Measured on the real transcript: round 1 plans `summarized 11 / preserved 1`; round 2 with no new human row plans `summarized 0`; round 2 after a human row plans `summarized 5 / preserved 1`. Both target workflows deliver that row (the relay types a continue prompt, chain mode pipes one via `-p`), so every boundary in the `/kit-goal` run compacts, and both directions are pinned by round-trip tests. The residual is a session that keeps running past a boundary with no resume at all, which cannot compact twice. Making the boundary segment-granular is the real fix and is a larger change: the compactable remainder of a turn has no user rows, so it needs its own anchor override, which interacts with the splitter's continuation numbering. Follow-up effort, not this one.

## Open Questions

- **Resolved by Section 4: `SEGMENT_TOKEN_BUDGET` stays at 20,000.** Live summaries at that budget came back distinct and slice-scoped (maximum pairwise word overlap 0.15 across a run's eight summaries, and 0.17 across an eleven-summary run), with zero skips and argv headroom to spare. Nothing in the measured behavior asks for finer or coarser granularity, so the knob is not moved. The original framing follows.

- `SEGMENT_TOKEN_BUDGET` final value: 20k is the starting point; S4 calibrates. Owner: executing session, with Scott on a material change. Note that both known constraints push the same direction, **upward**, which inverts the original expectation that S4 would tune the budget down: the argv ceiling sets a hard floor near 15k, and the response-size constraint below sets practical pressure against fine granularity.

- **Answered by Section 4 at N up to 35: yes, with zero skips.** Three live runs requested 54 segment summaries in total (N=8 and N=11 at the 20k budget, N=35 at a temporarily lowered 3.5k budget) and the summarizer returned **every** requested pair, with no truncation, no renumbering, and no verbatim degradation. Rendered prompts measured 16,314 characters at 35 segments and 21,668 at 45, so 50% and 66% of the 32,767-char argv ceiling. Neither predicted failure mode appeared in the range reachable on a real transcript. The reasoning below stands as the untested tail beyond N=35, and the mitigations named in it stay unbuilt.

- **Can the summarizer reliably emit N indexed pairs as N grows?** Segment count is unbounded, and every segment costs one pair in the prompt and one required pair in the response. At a 20k budget the ~925k-token transcript yields roughly 45 entries, so the response must carry ~45 echoed anchors (300 chars each) plus ~45 summaries of up to 200 words, on the order of 15-17k output tokens. Two failure modes follow, both by design rather than by defect: a truncated response has no closing `</summary>` and `parseSummaries` throws, failing the whole compaction with no retry; and `parseSummaries` hard-throws when more than half the pairs are missing, so a summarizer that tires partway through fails rather than degrading. The failure scales with transcript size, which means it bites hardest exactly when compaction matters most. S4 measures the real skip rate and the response size rather than assuming; if the ceiling is real, the options are a cap on segment count (raise the effective budget when a turn would exceed it) or batching the summarizer call, neither of which is in scope here.

## Related

- Extends `docs/archive/claude-kit_summarizer-robustness_spec_v1.md` (2026-07-15): that effort fixed the interactive degeneracy (byte-identical anchors merging summaries) with indexed pairs and the anchor cross-check; this one fixes the autonomous degeneracy (too few turns) by bounding plan entries. Same contract, opposite corner.
- Engine origin and architecture: `docs/archive/claude-kit_compact-session_spec_v1.md`.
- Thresholds and ROI calibration: `docs/archive/claude-kit_compaction-tuning_spec_v1.md`.
- The workflow this serves: `docs/archive/claude-kit_goal-continuity_spec_v1.md` (`/kit-goal` runs).

## Chapters

(Appended by executing-work as sections complete.)

### Chapter 1 - 2026-07-18
Completed: 1. Segment splitter and anchor override
Implemented By: implementer-fable (build round, then a consolidated review-fix round); main session (attachment-row budget fix, inline under the too-small rule)
Metrics: 1 review round (adversarial + blind + security, all three dispatched together); 0 NEEDS_CONTEXT; 0 escalations; advisor off (Opus-led session)
Decisions / Surprises: **Run Mode substituted, chain to interactive** (Scott's call at the section-loop entry, recorded in the header). Chain's only benefit at a boundary is compaction, and this plan's own diagnosis is that autonomous transcripts fail to compact until S2 wires the splitter in, so chain would have spawned headless workers for no compaction gain while removing Scott from the S4 calibration call. **Splitter colocated in `transcript.ts`**, not a new `segment.ts`: it needs the module-private `isToolResultRow` and `getMessageId`, and a second copy of the tool-result predicate is exactly the drift that produces silent row misclassification. **Anchor numbering is plan-global, not per-turn.** The spec's "unique within the turn" was satisfiable literally while leaving the real property broken: `parseSummaries` receives one flat anchor array spanning all of `summarizedTurns` (`compact.ts:203`) and `anchorsAgree` compares 40 characters, so two oversized turns each emitting `(continuation 1) <same repetitive assistant opener>` would verify against each other and defeat the renumber-and-drop guard the 2026-07-15 effort exists to provide. `splitOversizedTurns` now runs one counter across its whole input list, which is exactly the collision domain. **Tool-pair cohesion is enforced, not assumed:** the first implementation rested on an argument about active-chain row ordering; `buildStepChunks` now tracks unresolved `tool_use` ids and refuses a cut while any is pending, so the invariant holds regardless of ordering. **New constraint discovered, recorded against S4:** segment count drives summarizer prompt argv length, and a ~925k-token transcript at a 20k budget renders ~22KB against the 32,767-char Win32 ceiling, so the budget floor is ~15k and tuning below it requires moving the prompt to stdin first (new "budget has a hard floor" decision). **Real-transcript check caught what code reading could not:** `estimateRowTokens` scored attachment rows at zero because they carry their payload outside `message`; measured attachment rows in live transcripts run to 14,555 chars (~3.6k estimated tokens), so a stretch of them would have overrun the budget unmeasured. Fixed inline with a watched-red test. Both shape gaps became Standing Brief Amendments.
Review Findings: Blind's sole Critical (`splitOversizedTurns` has no production call site) **rejected**: correct as an observation, wrong as a defect, since S1 was scoped to the splitter and tests with wiring deferred to S2. Its embedded warning about `--keep N` count-slicing was kept and written into S2. Six Majors fixed: plan-global anchor numbering; `continuationSnippet` returning empty or ignoring string-form `message.content`; asserted-not-enforced tool-pair cohesion; and the test-coverage gaps that let all three pass green (`anchorsAgree` exported so tests assert the real predicate rather than raw slice inequality). Security's trust-class Major (assistant text and tool names now reach the prompt argv, where previously only human user rows did) accepted with mitigation: the snippet is capped at 400 chars, and the reviewer independently confirmed the XML-injection guarantee genuinely holds (the `[<>]` strip runs before the 300-char slice, so truncation cannot reconstitute a partial tag). Minors fixed: token estimate measuring envelope fields, double serialization, `anchorOverride` dropped from segment 0, spread-arg `RangeError` risk, O(n squared) tool-name accumulation, and two tests that could not fail. Minor declined: moving `SEGMENT_TOKEN_BUDGET` to `compact-cli.ts` alongside the other calibrated thresholds, which would invert the import direction; it stays with its consumer and the spec's Budget decision now says so.
Verification: gate re-run by the controller, not taken on report. Baseline 24 pass / 0 fail at `3c94671`; final **40 pass / 0 fail**, +16 tests, zero regressions. Every fix carries a watched red: the implementer ran seven mutation checks (per-turn numbering, string-content handling, empty-snippet fallback, unresolved-tool_use guard, missing-id fallback, packing order, and an injected `Math.random()` against the determinism test) and the controller ran an eighth for the attachment fix, each failing only its target test before restore. `parse-summaries.test.ts` unedited; `parseSummaries` byte-unchanged, so S3's zero-parser-diff assertion remains reachable.
Compaction: 168,180 context tokens at close; relay armed; check `skip` (below the 200,000 trigger: "compaction is allowed but not yet worth interrupting for"); action none.
Next: 2. Plan integration and emission guarantees
Commit Model: Commit-and-Push

### Chapter 2 - 2026-07-18
Completed: 2. Plan integration and emission guarantees
Implemented By: implementer-opus (wiring, then one fix round), escalated to implementer-fable for two further rounds after two review rounds failed with Criticals
Metrics: 3 review rounds (adversarial + blind each round; security not dispatched, the section touches row copying and parent chaining rather than the prompt, spawn, or any external boundary, and finishing-work runs security over the full changeset); 0 NEEDS_CONTEXT; 1 escalation (opus to fable); advisor off (Opus-led session)
Decisions / Surprises: **The headline finding is a pre-existing shipped data-loss defect, not a segmentation bug.** `buildCompactedRows` reproduced the source transcript's parallel-tool branch topology in the emitted copy by resolving each row's parent through a source-uuid map. Parallel tool calls give sibling rows a shared parent, so the emitted chain forked; `readActiveTranscriptRows` follows one chain from one leaf, and `copyRow` stamps every emitted row with one identical timestamp, which makes `buildActiveChain`'s strict-timestamp leaf tie-break inert so the *first terminal row in file order* wins, which is the short stranded branch. The losing branch and everything downstream of it was dropped, silently: the CLI reported success, the destination was well-formed, and the fail-safe never tripped because nothing threw. Measured on the real 884-row transcript by planning and emitting with a synthetic summaries map (no summarizer spawn, `.kit/scratch/fork-probe.ts`): the pre-effort engine at `3c94671` emitted 711 rows of which **53** were readable on reread; the current engine emits 718 of which **717** are readable, the single exclusion being the compaction boundary marker that the reader strips by design. Every compaction this engine performed on a transcript containing parallel tool calls truncated its output. **Fix:** both copy paths linearize (every copied row chains to the row emitted before it), the source-uuid map is deleted entirely as it had no remaining reader, and `assertSingleParentChain` throws before the destination is written so a future fork fails loudly and leaves the source untouched rather than shipping a truncated transcript. Linearization loses nothing: `recoverParallelToolRows` degenerates to a no-op on a linear chain, rows sharing a `message.id` stay contiguous so message reconstruction is unaffected, and a later compaction re-reads the linear chain normally. **Two orchestrator errors, both recorded because they shaped the section:** the dispatch brief asserted the double emission of an entry's first assistant row was intended and not to be fixed, which was wrong (it was the segment-boundary half of the fork); and a later brief specified the dangling-`tool_use` bound as "discard ids opened before the current chunk", which split genuine pairs whose result arrives two or more assistant messages later and emitted a summary row between a `tool_use` and its `tool_result`, an invalid API sequence that fails at resume. The implementer flagged that exact bound as the claim it most expected to be wrong and was under-weighted. Final formulation is index spans: the pre-scan pairs each `tool_result` with the nearest preceding unanswered `tool_use` of that id, and a cut inside any span is refused, which is immune to row ordering and to re-issued ids; an occurrence no span answers blocks only the boundary immediately after it, so an orphan is never followed directly by a summary row. **Also recorded:** `--keep N` still counts human-bounded turns because the splitter is applied after the count slices, and the fixture that pins it must place the oversized turn *within* the last N turns or it silently proves nothing.
Review Findings: Round 1, Critical (blind: no production call site) **rejected** as the blind lens lacking intent, wiring being S2's own job; its embedded `--keep N` count-slicing warning kept and written into the section. Round 1 Critical (both, surviving): the parallel-tool chain fork above, fixed. Round 2 Critical (both, surviving): the dangling-`tool_use` bound splitting real pairs, fixed. Round 3: no Criticals; two Majors taken (order-blind answered-id set reformulated as index spans, and no cut immediately behind an unanswered `tool_use`), one Minor taken (the splitter's tool-result detection widened to content shape so it cannot be narrower than the emitter's `isToolRow`), plus test and comment corrections. Declined with reasons: restoring a hand-written token estimator in tests (the attachment-row test's `segments.length > 1` assertion is behavioral and fails if the estimator stops counting attachment payloads, so the independent check already exists); redirecting the tests' omission-cache writes via a temp `HOME` (pre-existing precedent, and overriding `homedir()` risks cross-test flakiness; cleanup moved to per-test instead); monotonic timestamps on emitted rows (the chain guard is the durable protection); capping segment count or batching the summarizer call (real, recorded as an Open Question for S4 rather than designed here); extending tool-pair cohesion across turn boundaries (a human user row between a `tool_use` and its `tool_result` splits them into adjacent plan entries; pre-existing and unchanged).
Verification: gate re-run by the controller every round, never taken on report. Baseline 40 pass / 0 fail at Chapter 1 close; final **60 pass / 0 fail**, +20 tests, zero regressions. `parse-summaries.test.ts` has a zero diff from `d7e84cd` and no hunk touches `parseSummaries` or `anchorsAgree`, so S3's zero-parser-diff assertion remains reachable. Controller-run mutations, independent of the implementers' own: reverting the segment-boundary parent fix reds exactly the two chain tests while `parse-summaries.test.ts` stays green; tracking unanswered tool_use ids reds the dangling test; and stubbing the span pre-scan to block nothing reds eight tests covering every cohesion invariant. Real-transcript probe re-run by the controller after every round with identical results.
Limitations recorded rather than fixed: an unanswered `tool_use` as the last row of a turn is still followed by the next plan entry's rows (pre-existing, unchanged by segmentation); tool-pair cohesion holds within a turn only; `assertSingleParentChain` is a regression tripwire against future changes, not reachable by current construction, and cannot detect a structurally valid chain that still reads badly.
Compaction: 279,895 context tokens at close; relay armed; check `compact` (at or above the 200,000 trigger: "every further call re-bills this context"); action relayed at this boundary.
Next: 2b. Segment-granular keep for degenerate turn counts
Commit Model: Commit-and-Push

### Chapter 2b - 2026-07-18
Completed: 2b. Segment-granular keep for degenerate turn counts (section added mid-effort)
Implemented By: implementer-opus (build, then one review-fix round)
Metrics: 1 review round (adversarial + blind); 0 NEEDS_CONTEXT; 0 escalations; advisor off (Opus-led session)
Decisions / Surprises: **The section exists because dogfooding found the plan's Goal unreachable.** Attempting the S2-close boundary compaction on this session returned "no older assistant turns to compact". Cause, measured rather than reasoned: a `/kit-goal` run opens with one human prompt and stays one human-bounded turn for its whole length, so with `--keep 1` counting turns, `compactionEndIndex` collapses to `compactionStartIndex` and `summarizedTurns` is empty. Segmentation had solved "one plan entry is unbounded" and left "there is no plan entry at all" untouched. Evidence on this session's own transcript: `--keep 1` planned 0 summarized entries, `--keep 0` planned 11 bounded segments of 15-20k tokens each with distinct anchors. Raised to Scott as a blocker (a contradiction between the Goal and the recorded Keep-semantics decision) and **decided 2026-07-18: keep falls back to segments when turns cannot express it**, scoped strictly to `compactable.length <= keepTurns`. The safety argument is an exact iff, verified against the code by two reviewers independently: that condition holds precisely when the turn-granular `summarizedTurns` is already empty, so no input that compacts today can compact differently. Confirmed empirically by a grouping fingerprint (row counts, boundary uuids, `userRows` counts, `anchorOverride` presence) across three real multi-turn transcripts of 46, 22 and 13 turns at `--keep 1/2/3/5`: byte-identical before and after. **The implementer correctly refused an over-broad acceptance criterion I wrote.** My criterion 2 demanded multi-turn grouping be unchanged "including which turns land where", which cannot hold at an input where `compactable.length == keepTurns`, since that input is inside the fallback domain by the spec's own rule. It re-pointed the existing S2 whole-turn pin at 3 compactable turns, where the property still applies, and added an independent lock; both reviewers confirmed coverage is net higher, not weakened. **Known limitation, measured and pinned in both directions:** the fallback is one-shot within a single continuous session, because `compactionStartIndex` stays turn-granular and the turn holding the summary rows becomes prefix. Round 2 with no new human row plans `summarized 0`; round 2 after a human row plans `summarized 5 / preserved 1`. Both target workflows deliver that row, so every `/kit-goal` boundary compacts; a session running past a boundary with no resume at all cannot compact twice. The segment-granular boundary is the real fix and is recorded in Out of Scope as a follow-up, since the compactable remainder of a turn has no user rows and would need its own anchor override.
Review Findings: Blind's Critical (the fallback is one-shot) **downgraded to a bounded limitation on measurement**: its reproduction omitted a resume prompt, and the controller measured both cases before adjudicating. Both directions are now pinned by round-trip tests, and S4 gains a second-compaction step to confirm it live. Majors taken: `SEGMENT_TOKEN_BUDGET` silently acquired a second consumer (it is now also the verbatim recent-context window, so a tuning pass calibrates against two axes) and its comment says so; `SKILL.md` and `compact-cli.ts` both documented `--keep N` as protecting the in-flight turn, which is false on the fallback path, and now state the real contract; the two-shape contract on `Plan.preservedTurns` (a whole turn or a continuation segment with empty `userRows`) is stated on the type where its two consumers can see it. Minors taken: a test that could not fail for its stated reason was re-pointed and the redundant guard dropped with the algebra recorded; a keep-boundary fixture with the oversized turn last was added; the preserved tail's size is now measured against the budget so a packing change that shrinks it toward nothing is visible. Declined with reasons: gating the fallback to a single compactable turn or flooring the preserved tail by tokens (the spec's condition is deliberate and narrowing it would leave the two-turn case broken for no benefit); a segment-granular `compactionStartIndex` (correct, but a larger design change, recorded as a follow-up).
Verification: gate re-run by the controller each round. Baseline 60 pass / 0 fail at Chapter 2 close; final **71 pass / 0 fail**, +11 tests, zero regressions. `parse-summaries.test.ts` zero diff from `b893549`. The implementer's mutation set included two input mutations that red in opposite directions (injecting a resume row reds the limitation test; removing it reds the workflow test), so neither round-trip test can be trivially satisfied. A `Math.max(0, ...)` clamp mutation initially passed green, which surfaced a genuine coverage gap and produced an additional test.
Compaction: 452 active rows and ~193k estimated plan tokens at close; relay armed; check `compact` at 343,907 context tokens; **action compacted at this boundary, successfully, which is the first live exercise of this effort's own fix.** Source `d5f03240` to destination `c9a7e88a`, 1,943,126 source bytes to 946,926, 59,850 ms, 11 summarized segments and 1 preserved. The relay's auto-resume step failed (AHK is not armed on this machine) and Scott resumed by hand; the compaction itself was unaffected. Evidence recorded against Section 4.
Next: 3. Template wording and parse-path regression tests
Commit Model: Commit-and-Push

### Chapter 3 - 2026-07-18
Completed: 3. Template wording and parse-path regression tests
Implemented By: implementer-opus (build, then one review-fix round)
Metrics: 1 review round (adversarial + blind; security not dispatched, the added text is static prompt prose introducing no new data path, and finishing-work runs security over the full changeset); 0 NEEDS_CONTEXT; 0 escalations; advisor off (Opus-led session)
Decisions / Surprises: **The section's premise changed before it started, because the S2b boundary compaction had already run the prompt live.** That run summarized an 11-segment plan of which 10 segments carried synthetic `(continuation N)` anchors, and every anchor was echoed correctly, which the cross-check would otherwise have rejected. So the summarizer already handled segment pairs with no rule at all, and S3 was reframed from a repair to an accuracy fix on a prompt with a demonstrated-good baseline, with minimalism binding on the dispatch. The real defect was that the prompt's existing sub-bullets describe every `<user>` snippet as a truncated user prompt, which since S1 and S2b is sometimes false, including for the trailing next-turn anchor that `preservedTurns[0]` can now supply as a continuation segment. **A third orchestrator error, caught by both reviewers:** my brief's wording, "the indexed pair following it", is a literal misdescription, since `buildXmlTemplate` renders the snippet *inside* its pair. The consequence is the worst kind available here: `parseSummaries` cross-checks echoed anchor text and never inspects summary content, so a response with correct anchors and summary bodies shifted one index passes every guard and misattributes every summary permanently. Rewritten to a same-index relationship. **The rule was also generalized from continuation pairs to every indexed pair**, which covers segment 0 of a split stretch (whose anchor is a real human prompt and is indistinguishable from an unsplit turn's) at no extra wording risk, and matches the spec's own Template wording decision. **Reachability recorded, not fixed:** `continuationSnippet` falls back to the segment's first row uuid when a segment has neither assistant text nor tool names, and the implementer confirmed the branch is reachable in practice (an assistant message emitting only a thinking block, landing as a whole segment after an at-or-over-budget accumulation). On that branch the anchor satisfies the echo cross-check while carrying no meaning the summarizer can resolve, since a uuid appears nowhere in its view of the conversation. An S1 design property, left as is.
Review Findings: Both reviewers returned CHANGES_REQUIRED with three convergent Majors. Two taken (the nesting misdescription and the generalization above). **The third declined on measurement:** both argued that lines 591 and 612 ("Summarize everything between one user message and the next") are false for continuation segments, whose boundaries are not user messages, and that followed literally the model would emit N near-duplicate whole-turn summaries. The live run refutes the predicted mechanism at N=11, with those exact lines in force and no new rule: 11 distinct summaries, zero skips, lengths 516 to 1259 characters, **maximum pairwise word overlap 0.17**. Editing working prompt text on a theory the evidence contradicts is a regression risk taken for nothing, so those lines stand and the generalized bullet mitigates by stating slice-scoping positively and adjacently. Six test Minors taken, all of them cases that could not fail or could pass for the wrong reason: a `toContain("(continuation N)")` assertion comparing the static instruction text against itself; the anchor cross-check exercised only in the passing direction, since the mocked response derived its echoes from the same `getUserPromptText` the parser uses; a fixture whose segment anchors were `(continuation K)` plus ~283 identical characters, so only the global counter pinned distinctness; a `findIndex` matching on `undefined === undefined`; anchor-extraction regexes scanning the whole prompt rather than the template region, correct only by accident of the surrounding prose; and the tolerated trailing extra index left unpinned on the segmented shape. Declined: rewording the adjacent "they are still separate turns" bullet, since "turns" there is the parser's own vocabulary for plan entries.
Verification: gate re-run by the controller each round. Baseline 71 pass / 0 fail at Chapter 2b close; final **76 pass / 0 fail**, +5 tests, zero regressions. `parse-summaries.test.ts` zero diff from `299d0fb`, and the `compact.ts` diff is exactly two hunks (the `buildCompactionPrompt` export and the one bullet), so `parseSummaries` and `anchorsAgree` are byte-unchanged and the spec's zero-parser-diff acceptance criterion is met. Controller-run mutation independent of the implementer's: stubbing `anchorsAgree` to return `true` reds the new anchor-shift test specifically, plus the eight pre-existing cross-check tests, confirming the new test depends on the real guard rather than on fixture agreement. The implementer reported one honest negative result worth keeping: reverting the fixture to degenerate anchors reds only the distinctness test and leaves the shift test green, confirming the counter alone had been carrying that assertion.
Measurement for S4: the rendered prompt is **21,668 characters at 45 segments**, 66% of the 32,767-char Win32 argv ceiling, measured by driving the real `createPlan` and `buildCompactionPrompt`. This independently confirms the spec's ~22KB estimate and the ~15k budget floor derived from it; per-segment cost measures at roughly 460 characters, so a 10k budget would land near 43k and exceed the ceiling.
Compaction: 76 tests green and the section closed well inside the window opened by the boundary compaction recorded in Chapter 2b; check deferred to the S4 boundary.
Next: 4. Live-fire verification
Commit Model: Commit-and-Push

### Chapter 4 - 2026-07-18
Completed: 4. Live-fire verification
Implemented By: main session (verification with a live-spend and calibration judgment, not briefable implementation)
Metrics: no review round (a verification section producing no source diff); 3 live summarizer runs plus 1 headless resume; 0 escalations
Decisions / Surprises: **The specified red baseline did not reproduce, and what replaced it is worse.** Protocol step 1 expected the pre-fix engine to fail on the real transcript with `Summary index N is outside the M requested turns`. It exited **0** and reported success. The plan shape reproduced exactly as specified (a single summarized entry, so a one-pair template), but the summarizer complied this time instead of breaking the contract, which corrects the spec's "deterministic shape means an identical failure at all 11 attempts": the degenerate *request* is deterministic, the *failure* is a probabilistic response to it. The Root cause section is amended accordingly. **The real pre-fix failure is silent and total.** That "successful" pre-fix destination contains 1,074 rows of which **53 are on the active chain**, so 1,021 rows of conversation were destroyed while the CLI printed a success payload and handed over a `resumeCommand`. The 53 figure matches Chapter 2's independent probe of the same engine on the same transcript exactly. A run that throws is recoverable, because the fail-safe leaves the source untouched; a run that succeeds and truncates is not, because the user resumes the ruined copy. **Calibration call: `SEGMENT_TOKEN_BUDGET` stays at 20,000.** Nothing in the measured behavior asks for a change, and both recorded constraints push upward anyway. The knob and its file are named in the Budget decision if a future run disagrees.
Verification: **step 1 red**, pre-fix engine, 1,074 rows written / **53** on the active chain. **Step 2 green**, repo engine on the same source: 1,082 rows written / **717** on the active chain against 884 in the source, 8 summarized segments and 1 preserved, ledger entry written (589,342 context tokens before, 45,361 ms). The 365-row gap between file and chain is not loss: the source itself carries 383 off-chain rows (superseded branches), a class the reader ignores in both files, and 717 matches Chapter 2's probe exactly. **Step 3 coherence**, the eight summaries are ordered and distinct with a maximum pairwise word overlap of **0.15**, and `claude -p --resume` against the destination returned `RESUME_OK` at exit 0, which independently proves the emitted transcript is a valid API message sequence and that the tool_use/tool_result cohesion work in Chapter 2 holds on real data. **Step 4 second compaction**, the resumed destination plans 2 prefix / **16 summarized** / 1 preserved, confirming live that the workflow compacts at every boundary rather than only the first, the property Chapter 2b could pin only by synthetic round trip. **Step 5 ceilings**, three runs requested 54 segment summaries in total (N=8, N=11, N=35) and **zero were skipped**; rendered prompts measured 16,314 characters at 35 segments and 21,668 at 45, so 50% and 66% of the 32,767-char argv ceiling. Open Question 2 is answered in the reachable range and Open Question 1 is resolved. Gate unchanged at 76 pass / 0 fail; the large-N run used a temporary budget mutation that was restored and re-verified green, and the repo tree is clean at `b0ba80e`.
State altered outside the repo, and cleaned up: a throwaway project directory `~/.claude/projects/D--personal-s4livefire/` and its matching cwd `D:\personal\s4livefire`, four transcripts inside it, three `~/.claude/magic-compact/` cache entries, and a `git worktree` at `3c94671`. All removed and their removal verified; the pre-existing 2026-07-15 cache entry was checked to survive. Three entries remain in `~/.claude/magic-compact/ledger.jsonl` naming project `D--personal-s4livefire`: those record real compactions that really happened, and the ledger is append-only, so they stand rather than being edited out.
Compaction: check `compact` on the working session; the S5 boundary is the next compaction point.
Next: 5. Parse-failure response persistence
Commit Model: Commit-and-Push

### Chapter 5 - 2026-07-18
Completed: 5. Parse-failure response persistence
Implemented By: implementer-sonnet (build, then one review-fix round); main session (a comment correction, inline under the too-small rule)
Metrics: 1 review round (adversarial + blind; security not dispatched, and finishing-work runs security over the full changeset); 0 NEEDS_CONTEXT; 0 escalations; advisor off (Opus-led session)
Decisions / Surprises: **Chapter 4 is this section's justification, delivered on time.** The field failure's raw responses were unlinked by the engine's own cleanup, so the mechanism had to be inferred; Section 4 then re-ran the failing case live and could not reproduce the inferred mechanism, leaving a gap that one saved response would have closed. **Two shipped comments claimed contradictory Windows behavior and one was false.** The build round asserted that a raw ISO timestamp's colons are an NTFS alternate-data-stream separator, so an unsanitized name "silently succeeds into a hidden stream" rather than failing. Measured through the API the code actually uses: `Bun.write` on the real filename shape (`sess-2026-07-18T23:07:59.911Z.txt`, several colons) throws `ENOENT` and creates nothing, while a *single* colon (`sess-plain:stream.txt`) does redirect and creates `sess-plain`. So the mechanism is real but does not apply to this filename. The operative fact is simpler and stronger: since the write is best-effort and its failure is swallowed, an unsanitized name would silently save nothing on every Windows parse failure, which is exactly the outcome the capture exists to prevent. Both comments now say that. **Recorded as a general lesson:** an empirical claim about platform behavior belongs in a comment only when measured through the same API the code calls, since `Set-Content`, `Bun.write`, and raw `CreateFileW` do not agree.
Review Findings: Both reviewers returned CHANGES_REQUIRED with two convergent Majors, both taken. `homedir()` was evaluated **outside** the `try` in `writeParseFailureDebugFile`, the single path on which the best-effort diagnostic write could throw and replace the caller's `parseSummaries` error, contradicting the one invariant the section exists to establish; moved inside. The write-failure test induced failure with a `?` in the session id, which is illegal only on Windows, so on POSIX the write would succeed, the assertions would invert, and the run would leak an unreaped file containing raw response content into the user's home; the induced cause was also unreachable, since the session id derives from an existing transcript's filename. Replaced with a portable failure (pre-creating a plain file where the code needs a directory, so `mkdir` throws `EEXIST` identically on both platforms) via a minimal internal base-directory injection. Minors taken: a non-`Error` throw wrote the file and then lost its path forever, and now reports it to stderr; the success test asserted only that no entry *started with* the session id, so an implementation writing `last-response.txt` on success would have passed, and now snapshots the directory listing (the implementer confirmed by mutation that the old assertion missed exactly that wrong implementation); cleanup registration moved before the assertion that could strand a file; the duplicated `sessionIdFromPath` collapsed to one exported definition that `compact-cli.ts` imports, with its dead fallback branch dropped; `SKILL.md` Housekeeping gained a bullet for `~/.claude/magic-compact/debug/`, which holds raw summarizer output over transcript content and so shares the sensitivity class the existing cache warning names; and a seven-line comment justifying a one-token `replace` cut to two. Declined with reasons: rotation or byte caps (the spec's Diagnosability decision says no rotation, and the answer to a systematic failure is fixing it rather than discarding the evidence explaining it); a `randomUUID` suffix against same-millisecond collisions (both reviewers independently judged it unreachable, one parse per compaction behind a multi-second spawn); a double-append guard on the error message (unreachable, `parseSummaries` constructs a fresh `Error` on every throw path and the spec forbids retry loops).
Scope note carried forward: this capture covers the `parseSummaries` **throw** path, which is what the section specifies. Chapter 4 established that the worse real-world mode was the opposite, a run that succeeds and silently truncates. That mode is no longer silent: `assertSingleParentChain` from Section 2 throws before the destination is written. So the recoverable failure now leaves evidence and the previously unrecoverable one now fails loudly.
Verification: gate re-run by the controller each round. Baseline 76 pass / 0 fail at Chapter 3 close; final **79 pass / 0 fail**, +3 tests, zero regressions. `parse-summaries.test.ts` zero diff from `2394d4f`, and `parseSummaries` and `anchorsAgree` confirmed **byte-identical** to base by extracting both function bodies and normalizing line endings, since the working tree is CRLF and a naive diff against git's stored LF form is misleading. Controller-run mutation independent of the implementer's: stubbing the debug write to return null reds the failure-capture test alone. No debug artifacts survive the suite.
Compaction: check deferred to the finishing pass.
Next: finishing work (QA verification, finishing reviews, docs curation)
Commit Model: Commit-and-Push

### Chapter 6 (final) - 2026-07-18
Completed: the whole effort. Status flipped to Complete.
Metrics across the effort: 6 sections (one added mid-effort); 9 review rounds; 3 Criticals found and fixed, all by review rather than by the gate; 1 escalation (opus to fable); 3 orchestrator brief errors caught by reviewers; gate 24 pass / 0 fail at the base to **79 pass / 0 fail**, +55 tests, zero regressions at every step.
What shipped: plan entries are now bounded segments rather than whole turns, so an autonomous transcript yields a multi-pair summarization template instead of a degenerate one-pair request; `--keep N` falls back to counting segments when a session is a single human-bounded turn, which is the shape `/kit-goal` produces and the one that could never compact; the template states that each indexed pair covers only the slice its snippet anchors; and a parse failure now persists the raw response instead of destroying it.
**The effort's most valuable output was not in its spec.** Section 2 found a pre-existing, shipped, silent data-loss defect: emission reproduced the source's parallel-tool branch topology, forking the parent chain, and a reader following one chain dropped everything on the losing branch. Section 4 measured it live on the real transcript: the pre-fix engine wrote 1,074 rows of which **53** were readable, printed a success payload, and offered a resume command. Emission now linearizes and `assertSingleParentChain` throws before the destination is written, so the same failure would fail loudly with the source untouched.
Finishing pass outcomes: **QA verifier PASS**, gate confirmed by two independent runs, every acceptance criterion of Sections 1, 2, 2b, 3 and 5 backed by a test exercising the real path, Section 4's cleanup confirmed absent-and-present as its Chapter states, and one ledger entry's figures (`durationMs 45361`, `contextBeforeTokens 589342`) matched Chapter 4's prose exactly, which corroborates the live measurements independently. **Security CLEAR**, no Critical or Major; two Minors, one fixed (the `continuationSnippet` uuid fallback was the single uncapped path into anchor derivation and now takes the same 400-character cap as its siblings) and one accepted with its tradeoff recorded (the debug file is written with default permissions, matching the omission cache and ledger in the same tree; tightening one artifact alone would be inconsistent hardening, and the whole `magic-compact` tree is the right unit if it is ever done). **Final adversarial APPROVED**, no Critical or Major, Out of Scope confirmed intact (no parser hunk, no retry loop, no budget flag, vendored files untouched, `buildAssistantTurns` unchanged), no debris, and every widened export confirmed to have a real consumer.
Drift adjudications, all `deviation`, no `mistake`: **D1** emission topology was rewritten rather than merely adapted, which is the pre-existing defect above and is now documented in `docs/compaction-engine.md`. **D2** the parse-failure capture is best-effort rather than guaranteed, deliberately, so it can never mask the error it documents; `SKILL.md` stated it unconditionally and now says an error naming no path means no file exists. **D3** `SKILL.md` still described summaries as per-turn and stated the keep fallback in a way exact only at `--keep 1`; both corrected, and both were independently flagged by the adversarial reviewer and the curator. **D4** the reaping guidance told the operator an `[UNCOMPACTED]` source is safe to delete once its successor "proves itself", but a destination truncated by the old defect resumes cleanly, so the stated test passes precisely on the destinations that lost rows; `SKILL.md` now names that case and gives the active-chain comparison that detects it. This one is user-facing and applies to every source predating the fix.
**What still speaks the old contract:** the fix is not live on any machine until the plugin payload is updated. An installed `claude-kit` plugin cache still running the pre-fix engine carries the silent-truncation defect, so a compaction performed from a stale cache can still truncate. Updating the plugin is the action that makes this real, and until then the reaping caveat above is the operative protection.
Verification at close: gate **79 pass / 0 fail**; `parseSummaries` and `anchorsAgree` byte-identical to the base commit `3c94671` (verified by extracting both function bodies and normalizing line endings, since the working tree is CRLF while git stores LF and a naive diff misleads); `parse-summaries.test.ts` zero diff; `prune.ts`, `omission.ts` and `retrieve.ts` vendored-verbatim and absent from the changeset; `SEGMENT_TOKEN_BUDGET` restored to 20,000 after Section 4's temporary probe; no debug artifacts survive a suite run; no `.kit/` content tracked.
Commit Model: Commit-and-Push
