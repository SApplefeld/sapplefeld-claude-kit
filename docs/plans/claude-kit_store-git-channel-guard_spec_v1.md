# The store's git channel gets its own guard, so every caller inherits it

Status: In Progress
Commit Model: Branch-and-PR
Created: 2026-08-31

Session model: any executor session in the kit repo. Authored by the KIT: Worker (Skills) seat as a routed handoff out of `docs/plans/claude-kit_standing-grants_spec_v1.md`, whose section 2 surfaced the defect and could not fix it without shipping code that plan's Approach forbids.

## Dispatch Authorization

Authorized 2026-09-02 by the operator, first-hand on the allowlisted relay thread, to be appended to the kit worker's armed queue: the git-channel guard on the store sync helper as designed here, under the Branch-and-PR model the paragraph below keeps, so the merge still waits on the operator's review of the pull request. The operator's word was a standing instruction to the KIT: Expert seat to append every valuable parked plan to the worker's queue; that seat recorded it here and ran the append. Per the peer-sessions trace rule this section is a warrant only for a citing session that did not author it, and the receiving session performs its own trace: the grant is the operator's message on the Expert session's relay thread, and the plan arms only by the operator's word or the Expert seat's append under it.

The commit model is Branch-and-PR deliberately rather than the kit's usual Commit-and-Push. The change edits a PowerShell helper that every machine's unattended memory-store sync dot-sources, so its failure mode is every machine silently losing memory sync. That earns a human review gate before the merge, which Commit-and-Push does not provide.

## Goal

Move the git-invocation guard from the caller that first needed it onto the channel every caller runs through, so that a hand run of the memory store's sync script inherits the same environment hardening the unattended background run already gets.

The operating doctrine states the rule this plan applies: a sanitizing guard is a property of the output channel rather than of the producer that first needed it, so the moment a channel gains a second producer the guard moves to the shared boundary as an exported helper. The channel here is git-invoked-against-the-memory-store. It has just gained its second producer.

## Evidence

Every claim below was confirmed at the code, at the file and line named.

The guard exists and lives in the Node caller. `plugins/claude-kit/hooks/kit-git-lib.js:59-72` defines `gitChildEnv()`, which copies the process environment, deletes every key matching `/^GIT_/i`, then sets `GIT_TERMINAL_PROMPT=0` and `NoDefaultCurrentDirectoryInExePath=1`. `plugins/claude-kit/hooks/memory-session.js:852-862` passes that environment to the detached spawn of the sync script, additionally dropping `NODE_OPTIONS` and setting `GCM_INTERACTIVE=never`.

The guard does not exist in the script. `plugins/claude-kit/doctor/sync-store.ps1` contains zero `$env:` references. `plugins/claude-kit/doctor/install-memory-sync.ps1` contains zero `GIT_` references, and its `Invoke-MemorySyncGit` at `:355-364` is a bare `& $GitExe @all` that touches no environment. So every protection above is the Node caller's, and any other caller of the same script gets none of it.

Two callers now exist besides the hook. Any session on the box, the coordinator seat included, can run the script by hand, the coordinator skill's former store-push carve-out having been retired whole rather than narrowed. And two skills shipped on main today document a weaker path into the same store for any session: `plugins/claude-kit/skills/memory-system/SKILL.md:66` and `plugins/claude-kit/skills/finishing-work/SKILL.md:102` both spell `git -C ~/.claude pull --rebase` followed by `git -C ~/.claude push`, which is bare git under the session's own environment with no allowlist probe, no inbound tree screen, and no lock.

The exposure is not limited to the push destination. The operator-tier memory record `git-env-config-pins-beat-repo-local-config` documents, with a measured two-direction verification on git 2.55.0.windows.3, that `core.fsmonitor` and `core.hooksPath` are ordinary repo-local config keys git honours on an ordinary read, so a bare `git status` against a planted repository executes that repository's code. The sync script runs status-shaped reads. That makes the missing scrub a code-execution surface on a wrong or planted store root, not only a redirection surface on the push.

The ownership gate diverges in the same direction. `plugins/claude-kit/hooks/memory-session.js:776` gates on the `--local claudekit.memorysync` config key, and the comment at `:757-774` states that choice is deliberate because the marker-bearing `.gitignore` that `Test-MemorySyncRepoIsOwn` also accepts "rides into a clone (and could be planted by a hostile repo)". The script's own test at `plugins/claude-kit/doctor/install-memory-sync.ps1:277-278` returns true on that weaker `.gitignore` evidence before it ever reads the key at `:280`. So the script's self-check is the forgeable one, and `docs/security-model.md:65` already states that ownership rather than the path is the security gate.

## Approach

The guard moves into `Invoke-MemorySyncGit`, which is the single funnel every git call in the sync path already passes through, so one edit covers the whole channel and no caller has to remember anything. The hook's own `gitChildEnv()` stays where it is: it guards the Node-side git calls, which do not route through the PowerShell helper, and the two implementations are pinned against each other rather than merged, since neither language can call the other's.

The suppression is not a bare strip. Per the memory record's measured residual, pointing `GIT_CONFIG_GLOBAL` at an empty file also drops `safe.directory`, whose absence surfaces as a dubious-ownership refusal that reads like a permissions bug. Any entry the store genuinely needs is re-added through the same `GIT_CONFIG_KEY_<i>` channel rather than the suppression being abandoned.

Verification is two-direction by construction, on the record's own instruction: a one-direction test proves nothing, because a marker that never appears may mean the hook never fired for an unrelated reason.

## Decisions

The scope question was ruled during the standing-grants plan's section 2 and is recorded here so it is not re-litigated. Hardening the script was considered for that plan and declined on three grounds: its Approach line reads "Prose contract end to end; no code ships"; the helper is dot-sourced by every machine's unattended runner, making it the highest-blast change available under that plan's push-straight-to-main model; and doing it there would spend the risk budget on the better of the two hand paths while leaving the documented bare-git one untouched. The work is real and belongs in its own plan with a review gate, which is this one.

The expert seat's read, recorded as a claim weighed rather than a ruling adopted: it held that the channel-guard rule makes the hardening in-scope for the standing-grants plan, since it changes what a hand run inherits rather than what the run may do. That argument does not reach the Approach line, which is the fact the decision turned on.

## Sections of Work

### 1. The channel takes the guard
Model: opus

`Invoke-MemorySyncGit` builds and passes a hardened child environment for every git call it makes: every `GIT_*` key stripped, `GIT_TERMINAL_PROMPT` refused, `core.fsmonitor` and `core.hooksPath` pinned inert through the `GIT_CONFIG_COUNT` / `GIT_CONFIG_KEY_<i>` / `GIT_CONFIG_VALUE_<i>` channel, and any `safe.directory` the store needs re-added through that same channel rather than lost to the suppression.

Acceptance: a planted repository whose `core.fsmonitor` and `core.hooksPath` both append to a marker file is run both ways, and the marker appears without the guard and does not appear with it, both directions captured in the run's own output. The unattended path's behaviour is unchanged, shown by the memory-sync suite passing against a recorded baseline.

Tests: the two-direction planted-repo probe above, left as a durable test rather than a scratch script, since the whole claim is a behaviour no static reading can establish.

### 2. The two implementations are pinned against each other
Model: sonnet

Nothing today would notice if the Node guard gained a key the PowerShell guard did not. Add a pin asserting the two carry the same protections, so a later edit to one surfaces as a red rather than as a silent divergence.

Acceptance: the pin goes red when a key is added to one side and not the other, shown by a control that makes exactly that edit against a copy and leaves both files present and grammatical.

### 3. The documents catch up
Model: sonnet, Locus: inline

`docs/security-model.md` currently describes the environment hardening as a property of the background spawn, at the sections its Principals and hook-invocation passages own, and `:498` states "The child environment carries no `GIT_*` variable" as though it held for every caller. Both take the correction once the guard is on the channel. The exposure was named in the coordinator skill's store-push carve-out, which has since been retired whole, and `docs/security-model.md` is where it now belongs and is what this plan's completion re-reads against the new behaviour rather than leaving standing.

## Out of Scope

The bare-git manual push the two shipped skills document. It is the weaker path and it is real, but replacing it is a contract change to two skills with their own readers, and this plan is about the channel rather than about which path a skill recommends. The standing-grants plan's section 4 already owns those two sites.

## Related

- `docs/plans/claude-kit_standing-grants_spec_v1.md`, whose section 2 surfaced this and routed it here.
- Operator-tier memory `git-env-config-pins-beat-repo-local-config`, which carries the technique, its measured verification, and the `safe.directory` residual.

## Chapters

### Chapter 1 - 2026-09-07
Completed: 1. The channel takes the guard
Implemented By: implementer-opus (the section and fix round 1), main session (the round-2 edits, comments and test fixtures only)
Metrics: review rounds 2; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: Header normalized from `Status: Ready` to `In Progress` at run start; feature branch `store-git-channel-guard` cut from main at 13c3a1d, since the checkout sat on the trunk. The section's file set widened in fix round 1 and the round-2 edits, recorded here as approval drift: the Node guard `gitChildEnv()` in `plugins/claude-kit/hooks/kit-git-lib.js` gained the same two config pins (round 1's security Major: the session-start hook reads the store through it before the ownership gate, so after the PowerShell change it was the weaker guard on the very key this section pins), with a two-direction test in `test/kit-git-lib.test.js`; `test/memory-sync.test.js`'s `isolatedGitConfig` helper moved from `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` to a HOME, USERPROFILE and XDG_CONFIG_HOME redirect (round 1's blind Major: the funnel's strip removed the two variables, so seventeen cases read the machine's real global config while claiming to pin it out; the box's global config carries no push.default, which is why no red showed); `test/size-ratchet.test.js`'s key-list pin and `plugins/claude-kit/hooks/memory-session.js`'s comments followed. Two measured surprises changed the implementation: a Windows process environment cannot hold an empty value (the setter deletes the variable), and a GIT_CONFIG_VALUE absent while the count names it is a fatal parse error on every call, so `core.fsmonitor` is pinned to `false`, git's own disable value, rather than the empty string the section text implied; and Git for Windows reads HOME ahead of USERPROFILE, so the doctor test helper's USERPROFILE-only redirect never isolated global config, measured four ways by the implementer. The implementer's first strip test was vacuous (it planted the very GIT_CONFIG_COUNT keys the guard overwrites, so it passed with the strip removed); the red probe caught it and the durable test plants GIT_CONFIG_GLOBAL instead. Round 2's blind Major, that the hooksPath pin also disables an operator's global hooks on the store's own commit and push, is a Major justified rather than fixed: the pin stays as the section wrote it, because a hook allowed on the write verbs reopens the planted route behind the PowerShell ownership check the spec's Evidence calls forgeable; the cost is named in both guards' comments and rides to the operator. The wider class of command-running repo-local keys (credential.helper, core.sshCommand, core.askPass, core.gitProxy, gpg.program, filter and merge drivers, upload-pack and receive-pack, remote helpers and URL schemes), raised by three reviewers in round 1 and widened by round 2 to the hook channel's fetch against any opened repository, is not pinned: pinning the pinnable members overrides an operator's legitimate global config on every syncing machine, which is a risk-appetite call, so both guards' comments state the coverage as the two named members rather than the class, `docs/backlog.md` carries the decision with a recommendation, and section 3's security-model rewrite carries the residual. Also carried there: the environment-config channel needs git 2.31 or later and nothing in the doctor checks it. Operator-visible behaviour change, for the PR description: a hand run of the sync script whose shell relied on a GIT_* variable for its push (GIT_SSH_COMMAND, GIT_ASKPASS) now authenticates as the unattended run always has. Probe set: the changeset names no shape file under test/probes/, so no pair ran. Contention: a foreign heavy-process claim (AI-OS: Worker, a dotnet build) held the box before the baseline and again before the close gate; every lane here waited it out under this session's own claim, and the implementer named idle dotnet processes beside its runs. Partial substitution: none.
Assumptions: (2026-09-07, section 1, route b) the guard suppresses neither GIT_CONFIG_GLOBAL nor GIT_CONFIG_SYSTEM, since neither the section text nor the Node sibling does and suppression is what drops safe.directory; the pins are additive and the test asserts the home config is still read, so no safe.directory re-add is needed (no such entry exists on this box, per `git config --global --get-all safe.directory` exit 1); (2026-09-07, section 1, route b) the PowerShell guard also sets NoDefaultCurrentDirectoryInExePath=1, mirroring the sibling's breadth ahead of section 2's parity pin; (2026-09-07, section 1, route b) the two-direction probe lands in a new test file with a size-budget entry rather than inside the 3,000-line memory-sync file; (2026-09-07, section 1, route b) core.fsmonitor is pinned to `false` rather than the empty string, for the measured reason above; (2026-09-07, section 1, route b) the credential-helper isolation the blind lens asked for in the doctor test helper is not added, since those cases push to a local bare repository and reach no helper.
Review Findings: review: code pair at fable, Agent tool (rounds 1 and 2); review: security lens at fable, Agent tool (rounds 1 and 2). Round 1 over 13c3a1d..9385f14: adversarial APPROVED_WITH_CONCERNS 0/1/3, blind CHANGES_REQUIRED 0/1/3, security CONCERNS 0/2/2; Majors: suite isolation defeated (fixed), Node guard without the pins before the ownership gate (fixed), the class coverage overstated in the funnel comment (comment fixed, the class itself routed, above); Minors: the suppression deviation unrecorded (recorded above), the parity claim in the present tense (fixed), case-insensitivity unpinned (fixed), transport variables stripped on a hand run (recorded above), git 2.31 floor (recorded in both comments and the backlog), command precedence of a bare `git` name (pre-existing, noted), Code nullable on the throw path (pre-existing shape, noted). Round 2 over 9385f14..940764c: adversarial APPROVED_WITH_CONCERNS 0/1/3, blind APPROVED_WITH_CONCERNS 0/1/3, security CONCERNS 0/1/2; Majors: the Node comments claimed the class closed (fixed, both guards now state the residual by shape and name the hook channel's fetch), the global-hooks cost of the hooksPath pin (justified above); Minors: XDG_CONFIG_HOME in the isolation helper (fixed), an overbroad sentence about the doctor's git calls (fixed), no executable bit on the planted scripts off Windows (fixed), two unqualified strip comments in memory-session.js (fixed), the residual list incomplete (restated by shape), the 2.31 floor (as above), credential-helper isolation (declined, above). The round-2 edits are comments and test fixtures, below the owed-round bar; the author re-read them against the round-2 findings before the close gate, which is a re-read and not a review.
Stamps: adjudicated 4 over `memq unstamped --since 2h`, stamped 4, all operator tier: forward-resource-arrangements-into-dispatch-briefs, git-env-config-pins-beat-repo-local-config, a-system-checking-review-cannot-catch-a-record-only-defect, a-premise-marked-confirmed-in-a-brief-is-never-re-derived-downstream; the project tier listed none, and the stretch is accounted for by those four and the recall digest.
Gate: targeted lane `node --test test/kit-git-lib.test.js test/memory-sync-git-guard.test.js test/memory-sync.test.js test/size-ratchet.test.js` 175/175/0/0 exit 0 read from the run's own marker (.kit/scratch/gitguard/close-gate.exit) at 19:28:42Z on 2026-09-07 on this main checkout, branch store-git-channel-guard at 940764c with the round-2 edits unstaged and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, under this session's heavy-process claim; baselines on the same lanes, read from the clean detached worktree at 13c3a1d (.kit/scratch/gitguard/wt-base): memory-sync 79/79/0 exit 0 at 18:13:29Z, size-ratchet 84/84/0 exit 0, kit-git-lib 9/9/0 exit 0 (the implementer's reading), the guard file new with no baseline; delta +1 test (the Node planted-repository case) and +1 pass, no red. Earlier lanes on the way here: the first-green lane `node --test test/memory-sync-git-guard.test.js test/size-ratchet.test.js` 86/86/0 exit 0 at 18:34:11Z at 9385f14; fix round 1's lanes per the implementer at 940764c, memory-sync 79/79 exit 0, size-ratchet 84/84 exit 0, kit-git-lib 10/10 exit 0, and memory-session 85/84/1 exit 1 with the one permanent box-caused red `a pinned directory too long to name faithfully stands the session down` matching its worktree baseline. Contention lane: none defined for this repo. Probe set: no shape file in the changeset, no pair ran.
Next: 2. The two implementations are pinned against each other
Commit Model: Branch-and-PR
Delta: measured at 2026-09-07T19:29:17Z on the feature branch store-git-channel-guard at 940764c with the round-2 edits unstaged and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
test/kit-git-lib.test.js: 353 lines, cap 353, +6; tests 10, +0
test/memory-sync.test.js: 3108 lines, cap 3108, +3; tests 79, +0
words: 218775 of cap 218776 across 58 curated files
test lines: 112917 of cap 112917 across 57 test files
tests: 3393
changed paths under no measured root: 6 (6 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
```

### Interim board 1 - 2026-09-07 (section 2 in flight)
Section 2 is implemented and under review; no section closed by this entry. The pin at test/git-guard-parity.test.js went through three designs on the branch: a text-scraping extractor (6a917f3, fix 909ba4c; review rounds 1 and 2 both CHANGES_REQUIRED, adversarial 0/2/4 then 0/3/5, blind 0/2/5 then 0/2/6, the extractor pattern itself ruled the generator), a behavioural pin that runs both guards for real and compares a GIT_-filtered child environment (281076a; round 3 CHANGES_REQUIRED, adversarial 0/3/5, blind 0/2/8, every finding accepted: the filtered namespace hid non-GIT_ keys, NoDefaultCurrentDirectoryInExePath was proved by inheritance, raw environment dumps rode in failure messages, CRLF anchors were hardcoded against LF index blobs, bare rmSync in finally), and the current delta design (3022850): each side captures its child environment before and after its guard and the two added/changed/removed classifications are compared, with nine controls on temp copies including a non-GIT_ key on the Node side alone and ordering controls on both sides. Lanes at 3022850 under this session's own claim: `node --test test/git-guard-parity.test.js test/size-ratchet.test.js` 93/93/0 exit 0; `node --test test/kit-git-lib.test.js test/memory-sync-git-guard.test.js` 12/12/0 exit 0. Reviewer resolved-model tallies all opus with no substitution (round 3: 22 and 26 assistant lines). Assumptions declared so far: route b behavioural pin over textual (two reviewers independently recommended it; no consult convened); spawning powershell.exe in this test is allowed since test/memory-sync-git-guard.test.js:36 already does; the pin is a new test file with its own size-budget entry; controls run on mkdtemp copies. Foreign heavy-process claims from AI-OS: Worker came and went twice during this stretch and were waited out per protocol.
Next: review round 4 on 281076a..3022850 (adversarial with spec and round history, blind with base ref only, opus at high via Workflow), then steps 4 to 8 for section 2 and its Chapter 2.
Commit Model: Branch-and-PR

### Interim board 2 - 2026-09-07 (section 2 in flight)
Review round 4 on 281076a..3022850 came back CHANGES_REQUIRED, adversarial 0/2/8 and blind 0/2/5, with every round-3 item verified closed. Accepted: plant all seven guard names so each reads as changed regardless of ambient state; restore the count-equals-key-names direction beside the index walk; assert the strip plants as removed rather than absent and derive their names from one table; delete dead re-assertions; comment counts and one change-narrative comment; cap name lists; ordering controls assert name and kind separately; probe failure reports first and last stderr lines; both PowerShell dumps cross the same encoder; PowerShell resolved by full path. Declined with reason: splitting JS-only controls out of the Windows skip (every control runs through the two-sided comparison) and the PowerShell line-wrap concern (unconfirmed, moot once both dumps go through Write-Output). Fix landed at c503021 (655 lines); lanes under this session's claim: parity plus size-ratchet 93/93/0 exit 0, kit-git-lib plus memory-sync-git-guard 12/12/0 exit 0. Reviewer resolved-model tallies round 4: opus 22 and 16 assistant lines, no substitution.
Next: review round 5 on 3022850..c503021, then steps 4 to 8 for section 2 and its Chapter 2.
Commit Model: Branch-and-PR

### Interim board 3 - 2026-09-07 (section 2 in flight)
Review round 5 on 3022850..c503021 came back CHANGES_REQUIRED, adversarial 0/2/5 and blind 0/3/7; every item was accepted and the fix landed at 3fe0df6 (717 lines): strip plants asserted as removed entries in both deltas, extraction floor deleted, stale plant counts removed, ordering controls bound to one composed line, literal target values dropped in favour of changed-away-from-plant plus cross-side agreement, a survivor plant added, both PowerShell dumps through Console.Out.WriteLine, spawn failure worded apart from non-zero exit, config index walk over key and value sets, spawn env casing normalised, three comments corrected. Lanes under this session's claim at 3fe0df6: parity plus size-ratchet 93/93/0 exit 0, kit-git-lib plus memory-sync-git-guard 12/12/0 exit 0 (main checkout, one foreign dirty file kaizen/notes-SCOTT-CLAUDE.md).
Review round 6 on c503021..3fe0df6 came back CHANGES_REQUIRED, adversarial 0/2/5 and blind 0/2/4, all round 5 items verified landed except one stale comment. Both Majors are consequences of round 5 adjudications and are reversed or hardened: the literal target values return (dropping them left NoDefaultCurrentDirectoryInExePath and the PowerShell-side values with no literal pin anywhere in test/, and the justifying comment was false), and the survivor plant must be proved present in both post environments with a control that widens the JS strip predicate on a copy. Also accepted: a PowerShell-side strip control, the stale floor comment, the dead return, every marker line through Console.Out.WriteLine, the inert to-equals-plant clause replaced by a from-equals-plant check, and the casing comment's reason. Reviewer resolved-model tallies rounds 5 and 6: opus 22, 26, 31, 32 assistant lines, no substitution.
Next: fix round 6 to the section 2 implementer, lanes, commit, review round 7 on 3fe0df6..HEAD, then steps 4 to 8 for section 2 and its Chapter 2.
Commit Model: Branch-and-PR

### Chapter 2 - 2026-09-07
Completed: 2. The two implementations are pinned against each other
Implemented By: implementer-sonnet (the section and fix rounds 1 to 6), main session (the round 7 fold: one key-set assertion and three comment corrections)
Metrics: review rounds 7; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The pin is behavioural, not textual. The first design scraped both guard sources for their key names, and two review rounds found the extractor pattern itself generating the findings (a regex over PowerShell hashtable syntax and over Node assignment syntax cannot tell a key from a comment or a string), so the section was rewritten at 281076a to run both guards for real: the Node side requires a copy of kit-git-lib.js and calls gitChildEnv() under planted variables, and the PowerShell side spawns Windows PowerShell by its full SystemRoot path, dot-sources a copy of the installer, and runs Invoke-MemorySyncGit against a fake git that prints the environment it was handed. Round 3 then moved the comparison from a GIT_-prefixed namespace to the delta each guard makes against a captured baseline (3022850), so a key set under any prefix on one side alone reds. Round 4 planted every guard name with a conflicting value so each side is proved by the guard rather than by ambient state (c503021). Two round 5 calls were reversed in round 6, recorded as the author's mistakes: dropping the literal target values left NoDefaultCurrentDirectoryInExePath and the PowerShell-side values with no literal pin anywhere under test/, so the six literals are pinned in this file as the parity contract and bound by a load-time assertion to the plant table; and the survivor plant as first written greened when its plant never landed, so it is now proved present with its value in both post environments and covered by a control that widens the Node strip on a copy. Two controls added in round 6 make the file's silences speak for the PowerShell side as well (a case-sensitive scan on a copy leaves the lower-cased noise plant behind). Declined with reasons: splitting the Node-only controls out of the Windows skip (every control runs the two-sided comparison and the PowerShell side needs Windows); a product-side duplicate-casing hazard in gitChildEnv raised by round 5's blind lens (a differently-cased ambient spelling of NoDefaultCurrentDirectoryInExePath beside the guard's own), noted rather than fixed since the documented setter spells it canonically and the section's files in scope hold no product code, with the test's own spawn env normalised instead. Both PowerShell dumps and every marker line go through Console.Out.WriteLine, bypassing the output formatter, after round 5's blind lens computed the hooks-path dump line at 119 columns against the formatter's 120-column redirected width on this box. Contention: foreign heavy-process claims from the AI-OS worker held the box four times across the section's lanes (twice at Expected 3600 seconds, twice shorter); every lane here and every implementer lane waited them out under this session's own claim, released at each run's end. Probe set: the changeset names no shape file under test/probes/, so no pair ran. Partial substitution: none; every reviewer tally read opus on every assistant line (22, 26, 22, 16, 22, 26, 31, 32, 33, 37 lines across rounds 3 to 7). Compaction landed once mid-section, at a checkpoint opened after interim board 2, and the run resumed from the plan doc.
Assumptions: (2026-09-07, section 2, route b) spawning powershell.exe from the test is allowed, since the sibling test/memory-sync-git-guard.test.js already does; (2026-09-07, section 2, route b) the pin lives in a new test file test/git-guard-parity.test.js with its own size-budget entry rather than inside either guard's suite; (2026-09-07, section 2, route b) every control edits a mkdtemp copy of the guard file and never the tree, with anchors built on the copy's own line ending; (2026-09-07, section 2, route b) GIT_CONFIG_VALUE_1 is pinned by shape (runtime temp directory prefix, the no-hooks marker, a GUID suffix, differing between the two sides) rather than by literal, since it carries a fresh GUID; (2026-09-07, section 2, route b) the whole file skips off Windows, as its sibling does.
Review Findings: review: code pair at opus, Workflow at high (rounds 1 to 7; sonnet writer, so one tier up). Round 1 over a7ada5e..6a917f3: adversarial 0/2/4, blind 0/2/5. Round 2 over 6a917f3..909ba4c: adversarial 0/3/5, blind 0/2/6; both rulings named the text design as the generator, so the behavioural rewrite followed. Round 3 over 909ba4c..281076a: adversarial 0/3/5, blind 0/2/8; Majors: the GIT_ prefix filter hid non-GIT keys (fixed, delta design), NoDefaultCurrentDirectoryInExePath proved by inheritance (fixed, planted), raw environment dumps in failure text (fixed); Minors: CRLF anchors against LF blobs (fixed with detectEol), bare rmSync in finally (fixed), spawn without timeout or buffer cap (fixed), output encoding unpinned (fixed), and comment accuracy items (fixed). Round 4 over 281076a..3022850: adversarial 0/2/8, blind 0/2/5, all twelve round 3 items verified closed; Majors: zero-slack floor blind to ambient values equal to guard values (fixed, every name planted), lost count-versus-key-count direction (fixed); Minors: vacuous absence leg (fixed), dead re-assertions and stale counts (fixed), change-narrative comment (fixed), uncapped name lists (fixed), full-sentence ordering assertions (fixed), spawn-failure labelling (fixed), dumps on different encoders (fixed), bare powershell.exe (fixed); declined: splitting Node-only controls out of the skip, and a line-wrap concern then unconfirmed. Round 5 over 3022850..c503021: adversarial 0/2/5, blind 0/3/7; Majors: strip plants asserted absent rather than removed (fixed), the floor a sibling-covered count pin (deleted), ordering controls matching two substrings (fixed), stale plant counts (fixed), the line-wrap concern now computed at 119 of 120 columns (fixed via Console.Out); Minors all fixed except the product-side casing hazard (noted above). Round 6 over c503021..3fe0df6: adversarial 0/2/5, blind 0/2/4, all round 5 items verified landed; Majors: the survivor check greened on a missing plant (fixed with presence check and control), the literal-value drop left values unpinned tree-wide (reversed); Minors: stale floor comment, dead return, temp-path marker on the formatter path, inert to-equals-plant clause, casing comment reason (all fixed), PowerShell-side strip control absent (added). Round 7 over 3fe0df6..419d031: adversarial CHANGES_REQUIRED 0/1/2 naming the fold as foldable without a round, blind APPROVED_WITH_CONCERNS 0/1/3, all nine round 6 items verified closed; Major: the reinstated literal table unbound to the plant table (fixed, load-time key-set assertion); Minors: the sibling-pin citation overstated (fixed, five of six), the survivor header half-current (fixed), past-tense narration in a comment (fixed), the casing reason (fixed). The round 7 fold is one assertion and three comments, below the owed-round bar; the author re-read the diff against the round 7 findings before the close gate, which is a re-read and not a review. Security lens: not triggered (test file only; the process spawn is the sibling's existing shape).
Stamps: adjudicated 1 over `memq unstamped --since 4h`, stamped 1 (operator tier: workflow-parallel-caps-at-two); two further operator-tier records surfaced by the recognition nudge and stamped because they steered the fold (verify-a-splice-by-diffing-not-by-grepping, release-an-exclusive-claim-at-the-operations-end-not-the-turns-end); the project tier listed none, and the stretch is accounted for by those and the interim boards.
Gate: targeted lane `node --test test/git-guard-parity.test.js test/size-ratchet.test.js` 95/95/0 exit 0 read from the run's own marker (.kit/scratch/gitguard/s2-close-lane.exit), and `node --test test/kit-git-lib.test.js test/memory-sync-git-guard.test.js` 12/12/0 exit 0 (.kit/scratch/gitguard/s2-close-guard.exit), both at 22:56Z on 2026-09-07 on this main checkout, branch store-git-channel-guard at 419d031 with the round 7 fold unstaged and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, under this session's heavy-process claim after waiting out a foreign one; baselines on the same lanes: the parity file is new (no baseline), size-ratchet 84/84/0 at 13c3a1d (Chapter 1), so the first lane's delta is +11 tests (the parity file) and +0 red; the second lane read 12/12/0 at Chapter 1's close and reads the same. Earlier lanes on the way here, each under this session's claim: 93/93/0 and 12/12/0 at c503021 and at 3fe0df6, 95/95/0 and 12/12/0 at 419d031. Contention lane: none defined for this repo. Probe set: no shape file in the changeset, no pair ran.
Next: 3. The documents catch up
Commit Model: Branch-and-PR
Delta: measured at 2026-09-07T22:57Z on the feature branch store-git-channel-guard at 419d031 with the round 7 fold and the size-budget line unstaged and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
test/git-guard-parity.test.js: 796 lines, cap 796, +13; tests 11, +0
words: 218775 of cap 218776 across 58 curated files
test lines: 113713 of cap 113713 across 58 test files
tests: 3404
changed paths under no measured root: 1 (1 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
```

### Chapter 3 - 2026-09-07
Completed: 3. The documents catch up
Implemented By: main session (Locus: inline)
Metrics: review rounds 3; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The spec's `:498` anchor was stale; the sentence "The child environment carries no `GIT_*` variable" sat at line 595 and the spawn-property description at line 78. The section's file set widened well past `docs/security-model.md`, recorded here as approval drift: a paragraph in the security model's secrets section that the section never named still stated the old hand-run behaviour beside the new one (round 2's Critical), and three shipped skills carried the same falsified sentence, `memory-system` and `finishing-work` sharing one and `coordinator` its own, so all four were corrected in place under step 5's re-opened-document carve-out; `docs/backlog.md`'s push-guard `HOME`/`XDG_CONFIG_HOME` entry was widened to the two guarded channels under the same fold. The doctrine's per-caller enumeration rule bit three times: the doctor's bare git calls reach two roots (the kit checkout and the launch directory) rather than one; the push guard's shell-form environment is a third spelling of the guard, carrying the strip without the pins, with three git calls rather than one; and the hooks section's heading and intro, written for hooks alone, now cover a funnel that is not a hook's, so the section is retitled "How the kit invokes git". Read-verb and write-verb attribution follows the library test's measured control: `core.fsmonitor` runs on a status, `core.hooksPath` on a commit. The git 2.31 floor, carried on documents that copy one another, was confirmed against git's own 2.31.0 release note ("Two new ways to feed configuration variable-value pairs via environment variables have been introduced"). Two residuals surfaced by review and stated rather than closed: the unstripped `HOME` and `XDG_CONFIG_HOME` still select the global config the additive pins keep reading, on the store's write verbs as much as the hooks' reads; and the funnel mutates and restores its own process environment, so an interrupted in-process run can leave a shell with the guard's names set. The round 3 fold (one residual sentence, five prose corrections, the backlog widening) is below the owed-round bar; the author re-read the diff against the round 3 findings before the close lane, which is a re-read and not a review. Probe set: the changeset names no shape file under test/probes/, so no pair ran. Contention: a foreign heavy-process claim from the AI-OS worker held the box once before this section's first lane, waited out under this session's own claim. Partial substitution: none; the three reviewer tallies read opus on every assistant line (51, 49, 70). Compaction gate: offers held through the section, the checkpoint opening at this close.
Assumptions: (2026-09-07, section 3, route a) a document a completed section's scope carries that this section's behaviour change falsified is corrected here rather than routed, per executing-work step 5's standing carve-out, which is why three skills and the backlog ride this section; (2026-09-07, section 3, route b) the security model's secrets paragraph counts as such a document even though no section named it, since it describes the sync script's environment, which section 1 changed; (2026-09-07, section 3, route b) the security model is not a size-measured root, so its growth carries no budget line, while each skill's word cap follows its count exactly.
Review Findings: review: adversarial lens at opus, Workflow at high (rounds 1 to 3; sonnet tier, so one tier up); blind: no code diff (prose-only section, no Audience line). Round 1 over 3924b11..f190849: 0/4/6; Majors: the doctor's git calls understated to one root (fixed), the push guard's environment omitted as a third spelling (fixed), hook execution attributed to a read verb (fixed), two skills still stating the old hand-run behaviour (fixed); Minors: parity claim unqualified as Windows-only (fixed), hand-run parity overstated against the spawn's extras (fixed), the hook's own guarded store reads dropped from the store passage (restored), heading and cross-reference mismatch (retitled), the 2.31 floor unverified (verified at git's release note), the funnel's mutate-and-restore mechanism unstated (stated). Round 2 over f190849..6f026b1: C1/M4/m2, all round 1 items verified closed; Critical: the secrets paragraph still stating every hand path as unscrubbed (fixed); Majors: the coordinator skill's two hook-only protections miscounted (fixed), the push guard's call count wrong (fixed, three calls named), the bare-git pair narrowed to off-Windows (fixed), the open-class residual scoped to the hooks' channel alone (fixed, sync verbs and the planted store root named); Minors: a welded sentence boundary (fixed), the section intro not following the retitle (fixed). Round 3 over 6f026b1..6a3fdfd: 0/1/5, all round 2 items verified closed and the structural sweep over 118 tracked markdown files plus hook, doctor and script comments found zero stale passages (predicate: a hand run or PowerShell git call described as inheriting or carrying the session's GIT_ environment; control: the security model's own five known positives); Major: the unstripped `HOME`/`XDG_CONFIG_HOME` selector omitted from the residuals (fixed, backlog widened); Minors: `NODE_OPTIONS` mislabelled as a credential setting (fixed in both the security model and the coordinator skill), the interrupted-run trigger mis-stated as a kill (fixed), the funnel's executable-resolution answer unstated (stated), the sync verb list omitting `add` (fixed), one change-narrative phrase (fixed). Security lens: not triggered (prose only).
Stamps: adjudicated 1 over `memq unstamped --since 1h`, stamped 1 (project tier: a-universal-over-several-callers-fails-review, which shaped every enumeration in the section); the operator tier listed none, and the stretch is accounted for by that record and the three review rounds.
Gate: targeted lane `node --test test/size-ratchet.test.js test/doctrine-parity.test.js test/memory-session.test.js` 241/240/1 exit 1 read from the run's own marker (.kit/scratch/gitguard/s3-close-lane2.exit) at 23:37Z on 2026-09-07 on this main checkout, branch store-git-channel-guard at 6a3fdfd with the round 3 fold unstaged and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md, under this session's heavy-process claim; the one red is the permanent box-caused case `a pinned directory too long to name faithfully stands the session down` recorded in Chapter 1 and in project memory, so the delta against that baseline is +0 red; the same lane read 241/240/1 exit 1 at 6f026b1 and again before 6a3fdfd, each under this session's claim. Contention lane: none defined for this repo. Probe set: no shape file in the changeset, no pair ran.
Next: finishing-work
Commit Model: Branch-and-PR
Delta: measured at 2026-09-07T23:37Z on the feature branch store-git-channel-guard at 6a3fdfd with the round 3 fold and the size-budget line unstaged and the foreign dirty kaizen/notes-SCOTT-CLAUDE.md

```
repository: claude-kit
plugins/claude-kit/skills/coordinator/SKILL.md: 24070 words, cap 24070, -2
words: 218815 of cap 218816 across 58 curated files
test lines: 113713 of cap 113713 across 58 test files
tests: 3404
changed paths under no measured root: 3 (3 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
```
