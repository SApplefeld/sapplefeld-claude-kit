# Kaizen prose batch, 2026-09-02: twelve clauses the corpus owes its readers

Status: Ready
Commit Model: Commit-and-Push
Created: 2026-09-02

Session model: the corpus audit's adjudication lane, which takes this plan as an input set on the operator's routing recorded under Dispatch Authorization; twelve sections, tiers per section, eleven of them a wording change to behavior-shaping prose and so under the writing-skills skill's RED and GREEN discipline in a fresh session, and section 12 a verification of a change already at HEAD, recording the same two readings from git history. Authored by the KIT: Expert seat as the prose slate of the 2026-09-02 kaizen pass. Anchors are authoring-time; re-locate every hit by content.

## Dispatch Authorization

Routed 2026-09-02 by the operator, first-hand on the allowlisted relay thread, to the corpus audit (`claude-kit_corpus-audit_spec_v1.md`) as an input set to its adjudication lane rather than to the worker's queue, so the same warm judge that rules on the audit's findings rules on these and the corpus is edited once rather than twice. This plan is therefore never armed on its own: the audit's section 4 (provenance adjudication) reads it under the audit's own authorization and rules each section here beside the audit's own findings, the audit's section 7 carries the upheld sections into the follow-on plan with their RED and GREEN discipline as the input, and this plan flips Complete in that follow-on plan's close-out, which names the sections upheld and the sections declined. The operator's routing answered a decision batch the KIT: Expert seat put on the relay; that seat recorded it here.

## Goal

The inbox notes each section cites, several of them filed independently by three seats on two machines, describe a rule that names the moment to act but not the instrument that answers, a check that its own control cannot see past, a loop that grades its inputs and never questions their premise, or a contract stated one document away from where it is authored. Each fix is a clause or a sentence on a surface the kit already ships. When this plan is done, each surface a section names states the rule its readers kept re-deriving under pressure, in one place, with the RED reading recorded beside the GREEN.

## Evidence

Each section cites its notes in `kaizen/notes-*.md` at the pass's capture commit. The RED reading is the evidence a section produces; the notes are the evidence it starts from.

## Decisions

Decided 2026-09-02 by the Expert seat under standing adjudication; reversible at the audit's adjudication pass.

1. **Every section runs RED then GREEN in a fresh session**, per the writing-skills skill, and the Chapter records both readings. A clause whose RED reading already reaches the right answer is not added; the section records that and closes.
2. **The doctrine's two hand-edited copies land byte-identical**: `plugins/claude-kit/skills/operating-instructions/SKILL.md` and `home/claude-kit-doctrine.md`, pinned by `test/doctrine-parity.test.js`, and every doctrine section names both paths. The output style `plugins/claude-kit/output-styles/kit.md` carries only the register core and `## Before you send`, none of which any section here edits, so it is not a copy this plan touches.
3. **The em-dash rescope is applied on the operator's 2026-08-30 keyboard ruling as the note reports it**, confirmed by the operator on the relay on 2026-09-02 from the pass's close-out recap; it is the one section whose subject is his own preference.

## Sections of Work

### 1. The scope-and-safety bullet names its two git instruments. Model: opus

Doctrine, both copies (`plugins/claude-kit/skills/operating-instructions/SKILL.md` and `home/claude-kit-doctrine.md`, pinned byte-identical by `test/doctrine-parity.test.js`). The pre-add read is spelled: `git diff HEAD -- <file>` before `git add`, which is the read that shows a hunk you did not write where reading the file's content cannot. The pre-push read is spelled: `git log @{u}..HEAD` before `git push`, confirming every commit it names is yours to publish, because a push moves a branch ref and every index check proves something true about files and irrelevant to the ref. Two notes, 2026-08-29, coordinator and worker seats.

Acceptance: RED reading shows a cold reader reaching for a content read and no pre-push read; GREEN shows both instruments named; `test/doctrine-parity.test.js` green; no em dashes.

### 2. The silent-check bullet learns what a control cannot see. Model: opus

Doctrine, both copies (`plugins/claude-kit/skills/operating-instructions/SKILL.md` and `home/claude-kit-doctrine.md`), the bullet under the bold lead about a silent check earning its silence. Three clauses: a control that shares a derivation path with the check (wording, scope, escaping, a schema model) is blind to every failure in that path, and the escape is a complement formulation or an instrument the check's author did not build; a control validates the axis it varies, so where the scope is a mutable selection (a ref, an environment, a window, a tenant) the scope is the axis that earns the second control, and the scope is named in the command rather than inherited from ambient state; and where a sweep's completeness rests on a hand-authored pattern, the checked set is derived mechanically from the governing artifact and that derivation is pinned. Three notes: 2026-08-30 (four seats), 2026-08-31, and NEO-CLAUDE 2026-08-30.

Acceptance: RED and GREEN readings recorded; parity pin green.

### 3. A finding routed to the backlog earns the same open-the-code check. Model: sonnet

Doctrine, both copies (`plugins/claude-kit/skills/operating-instructions/SKILL.md` and `home/claude-kit-doctrine.md`), the finding-is-a-hypothesis bullet: a finding routed to the backlog earns the open-the-cited-code check a finding routed to a fix gets, with the recorded remedy checked against what the code is before the entry is written, since a deferral's write-up is a work order for a session with no context to catch it. NEO-CLAUDE 2026-08-28.

Acceptance: RED and GREEN; parity pin green.

### 4. The dash bar is scoped to the surfaces the operator cares about. Model: sonnet

Doctrine, both copies (`plugins/claude-kit/skills/operating-instructions/SKILL.md` and `home/claude-kit-doctrine.md`), the style bullet: the bar covers outward-facing surfaces (code comments, SQL scripts, copy, documents written for an audience, shipped skills and charters), and an internal plan document or a journal-layer artifact is tolerated. 2026-08-30, expert seat, reporting the operator's keyboard ruling; decision 3 above.

Acceptance: RED and GREEN; parity pin green; the close-out names the change for the operator.

### 5. The one-heavy-process bullet names the third act. Model: opus

The rule lands in `plugins/claude-kit/skills/role/SKILL.md`'s claim protocol (the `## The claim file` section), which the ownership map names as the owner of starting a heavy process on a shared machine: a machine-state sentence leaving the session names the poll it rests on and its age, or says "not polled this turn"; and an arbitration answer (no competing claimant) and a machine-state answer (nothing else running) are separate sentences, since the conflation survives any poll freshness. The doctrine's one-heavy-process bullet, both copies (`plugins/claude-kit/skills/operating-instructions/SKILL.md` and `home/claude-kit-doctrine.md`), and the coordinator runbook (`plugins/claude-kit/skills/coordinator/SKILL.md`) point at that rule rather than restating it. NEO-CLAUDE 2026-09-01. The coordinator half is written by `claude-kit_liveness-by-session-identity_spec_v1.md` section 2, and the ordering between the two plans is read at dispatch: where the liveness plan has closed, the runbook's wording is what this section turns into the pointer; where it has not, this section reconciles against that spec's section 2 text and leaves the runbook edit to that plan; the Chapter says which case held.

Acceptance: RED and GREEN; `test/doctrine-parity.test.js` green; the coordinator wording matches the runbook where the liveness plan has closed, its spec's section 2 text where not, and the Chapter says which; the runbook and the doctrine restate no rule the role skill owns, per the ownership map.

### 6. A finding that reports an absence is a scope claim first. Model: opus

`plugins/claude-kit/skills/responding-to-review/SKILL.md` gains a step: for a finding whose substance is that something is absent, re-read the spec section before designing the fix and route an out-of-scope absence to the existing out-of-scope route. `plugins/claude-kit/skills/executing-work/SKILL.md`'s fix loop gains the pattern rule: when a fix round's findings land on text the previous round introduced, stop patching and re-derive from the spec, the intra-section form of the recurrence rule (bold lead **The recurrence rule** in that skill's step 4, which amends the generator, the standing brief, rather than the latest instance). 2026-08-31, three consecutive fix rounds each spawning a defect.

Acceptance: RED and GREEN on both files; whole-file review for seam collisions.

### 7. An anchored edit re-emits its neighbour's lead. Model: sonnet

`plugins/claude-kit/skills/writing-skills/SKILL.md`, under the heading `## The paragraph is the edit unit for curated prose`: an insertion anchored on the tail of a block, or on the next entry's first line, restores every byte of the anchor in the replacement, then re-greps the neighbour's lead-in with a control at HEAD proving the pattern matches the intact form. Three instances across two seats (2026-08-30, 2026-09-02, seven decapitated Decisions entries in one session).

Acceptance: RED and GREEN.

### 8. A citation names the artifact it was read from. Model: sonnet

`plugins/claude-kit/skills/peer-sessions/SKILL.md`, the cite-the-artifact-class paragraph: a claim about what code does names which artifact it was read from (HEAD, the installed plugin, or a named working copy), and a claim crossing a seat boundary is re-derived from `git show HEAD:<path>` or the installed artifact, never from the worktree of a shared checkout another session is editing. 2026-08-30, fourth instance in one day across three seats.

Acceptance: RED and GREEN.

### 9. A silent tool's verdict is read from its own field. Model: sonnet

`plugins/claude-kit/skills/memory-system/SKILL.md`, where the sync script's hand run is sanctioned: the script exits 0 on every path and prints nothing, recording its verdict as the state file's `lastResult` and `reason`, so a caller reads that field and never the exit code; a push is verified by a clean worktree and a local HEAD level with the remote ref. 2026-08-31.

Acceptance: RED and GREEN; the `store-git-channel-guard` plan's edits to the same helper are not touched.

### 10. The Chapter template spells the machine-read line, the bracket says what it sees, and the blind dispatch says what it cannot hide. Model: opus

`plugins/claude-kit/skills/executing-work/SKILL.md`: the Chapter template's `Completed:` placeholder becomes the bare-number form (`Completed: 1. <section title>`), with the one-section-per-Chapter limit beside it. The machine contract in `plugins/claude-kit/skills/curating-docs/SKILL.md` registers the exact title form as well as the bare number, so the current `<section name>` placeholder is read by the machine; the bare-number form is chosen because a hand-typed title rarely matches byte-for-byte (NEO-CLAUDE 2026-08-29, three notes). The review-round tree-state bracket states that `git status --porcelain` sees tracked-file changes alone, and pairs it with an empty-directory listing at bounded depth. The blind-dispatch rule states that the `.kit/` scratch path is inside the tree the reviewer greps, so blindness is requested rather than enforced; scratch stays under `.kit/`, where the doctrine's curated-library bullet places it, and the blind reviewer's brief gains one line telling the reviewer not to read under `.kit/`. 2026-09-01, two notes.

Acceptance: RED and GREEN for each of the three; the targeted lane includes `test/doctrine-parity.test.js`, which pins the Chapter template's fields, green.

### 11. The lookup step names the tier that can override it. Model: opus

Wherever a shipped skill's step says "read the file at this path" for a location an operator-tier record may relocate (the coordinator's board read, the role skill's launch-invocation resolution, the kaizen signpost), the step names the operator tier before its not-found branch, so an absence is reported as absence only after the override layer was queried. 2026-08-29, coordinator seat, and the ASR-CLAUDE sibling note. The coordinator's instance lands in `claude-kit_liveness-by-session-identity_spec_v1.md` section 2: where that plan has closed, this section reads the runbook and lists the instance as found; where it has not, this section lists it as owed to that plan and leaves the runbook untouched; the Chapter says which. This section covers the rest and records which steps it found.

Acceptance: an enumeration of the steps found, each with its clause; RED and GREEN on one representative.

### 12. A pass clears the lines it dispositioned and reconciles the count before it commits. Model: opus

This section verifies a change already at HEAD and adds nothing. `plugins/claude-kit/skills/kaizen/SKILL.md` carries both sentences, landed by commit `7701ec5` on 2026-09-02: step 3 says the clear removes each dispositioned line by its own text and never truncates the file, so a line the pass did not read survives by construction, and that before the clearing commit the pass reconciles the staged diff's removed note lines against the triage record, every removed line named there and the counts equal, with any line the record does not name restored to the inbox rather than committed away; step 1 says each note file is read on its own with its note count taken before triage, because a loop that prints every file through one tool call can overrun the harness's output cap and lose a file's tail without a mark. The incident the rule answers is the section's evidence: the 2026-09-02 pass cleared three whole files, dropping two NEO-CLAUDE notes that stood in the file at the pass's own base commit and appear nowhere in its triage record (`kaizen/notes-NEO-CLAUDE.md` at `d09d099` held sixteen, `ade15e9` removed sixteen, the record covers fourteen; NEO-CLAUDE re-captured both at `8229041`). The RED reading is taken from git history rather than from a fresh edit: the pre-landing wording at `git show 7701ec5^:plugins/claude-kit/skills/kaizen/SKILL.md`, whose step 3 said only "clear the note lines you handled", is the RED text, and HEAD is the GREEN. A reading that finds either sentence missing at HEAD is a regression, and the section then reopens as an addition of the missing sentence under the same discipline.

Acceptance: both sentences present at HEAD (step 1's per-file read with its count, step 3's clear-by-own-text and staged-diff reconciliation), confirmed by reading the file; the RED reading recorded from the pre-landing wording in git history and the GREEN from HEAD; no edit to the skill; the kaizen README's structure bullet unchanged.

## Out of Scope

- Any code; the code slate is `claude-kit_kaizen-code-batch_spec_v1.md`.
- The board-content routing rules, which `../archive/claude-kit_board-routing-and-homing_spec_v1.md` owns.

## Assumptions

- assumed 2026-09-08 (source: Dispatch Authorization above, and the audit spec's Execution surface paragraph naming this plan): the header reads `Status: Ready` and the plan is never armed on its own by its Dispatch Authorization; a queue reader treats it as parked input to the audit's adjudication lane, never as an armable plan; reversal: a session that finds itself arming this plan directly stops before its first section and routes back to the audit lane, or the operator re-rules the routing on the relay.
- assumed 2026-09-08 (source: the audit spec's section 4 and section 7 text, which state that section 4 rules each section of this plan as a finding and that section 7's follow-on carries the upheld ones and closes this plan Complete in its close-out): the binding between the two plans lives in the audit spec's own sections and not only in its Execution surface paragraph; reversal: if the audit's arming session finds either sentence missing from the audit spec, it restores both before its section 4 opens.
- plan review 2026-09-08 (plan-reviewer at fable, effort high): 13 findings, 13 fixed, 0 assumed, 0 asked, 0 discarded.

## Related

- `claude-kit_corpus-audit_spec_v1.md`: the adjudication lane this plan rides, on the operator's routing of 2026-09-02.
- `claude-kit_kaizen-code-batch_spec_v1.md`, `claude-kit_liveness-by-session-identity_spec_v1.md`.
- `../archive/claude-kit_subtraction-bars_spec_v1.md`: the plan that wrote the size ratchet and the prose bar this plan's own edits are held to. Its section 4 added the `Delta:` field to the Chapter template in `plugins/claude-kit/skills/executing-work/SKILL.md`, which is the same template section 10 here rewrites the `Completed:` placeholder of, so the two touch one passage from opposite ends.
- Kaizen triage record `kaizen/archive/2026-09-02-pass-triage.md`.
