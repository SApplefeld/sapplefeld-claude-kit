# Run each per-section reviewer one tier above the writer with Fable as the ceiling, and a reviewer below Fable at high

Status: Complete
Commit Model: Commit-and-Push
Disjoint: yes
Created: 2026-09-06

## Goal

Each per-section reviewer dispatch runs one tier above the section's writer tier, with Fable as the ceiling rather than Opus: a haiku section reviews at sonnet, sonnet at opus, opus at fable, fable at fable, the code pair, the document pair and the security reviewer alike. A Fable reviewer keeps the effort the reviewer-fable-uncap plan put in its frontmatter (`low` for the code and document pairs, `medium` for the security reviewer) and rides the Agent tool. A reviewer below Fable, Sonnet over a haiku section or Opus over a sonnet section, runs at `high` through the Workflow route, because a model below Fable earns its findings on effort. The finishing reviews stay at Fable and `high`, and the Opus `max` row stays what it is, compensation for a Fable gate this environment could not run. When this is done the rule reads in one place with one question deciding the route, every restatement in the kit agrees, and the open reviewer-tiering experiment in `docs/backlog.md` carries the change as its next dated amendment.

## Dispatch Authorization

Authorized 2026-09-06 by the operator at the keyboard of the KIT: Expert session, who chose the hybrid (one tier up, Fable the ceiling) over the flat Fable rule and over restoring the Opus cap, and chose `high` over `medium` for a reviewer below Fable, both from the Expert seat's argued recommendation, and asked that the Expert seat make the change directly in the shared checkout as it did the uncap. The Expert seat authored this spec after that instruction and executes it unleashed, since the checkout's goal state is another session's live leash and a bare arm here would replace that queue.

## Approach

**The rule today.** `plugins/claude-kit/skills/executing-work/SKILL.md` step 3 runs every per-section reviewer at Fable whatever tier wrote the section, with effort by lens from the reviewer agents' frontmatter (`low` for adversarial-reviewer, blind-reviewer, blind-reader and prose-reviewer, `medium` for security-reviewer), the finishing reviews at Fable and `high` through `Workflow`'s `agent()`, and Opus at `max` through the same route as compensation where a Fable gate could not run. That rule landed in commit e00d1e3 on 2026-09-05 and was finished at f1b77fc on 2026-09-06 (`claude-kit_reviewer-fable-uncap_spec_v1.md`). It has not yet reached the worker session: the installed kit trails the checkout and a running session's plugin view is frozen at its start, so every review round the operator has watched the usage bars under ran the rule before it, one tier up capped at Opus.

**Why now.** The operator's reading of the weekly Fable usage on 2026-09-06, taken under the capped rule, is a few percent ahead of the normal pace. The flat rule would add a Fable reviewer to every section the cap kept at Opus or below, and the operator judged that too far in the same direction. The operator keeps two of the flat rule's decisions: Opus-tier sections earn a Fable reviewer (the cap's "opus at opus" had a reviewer reading work built by its equal), and Fable per-section runs at the low efforts. What the hybrid takes back is the sonnet-tier and haiku-tier sections, which return to Opus and Sonnet reviewers.

**What it changes, in numbers.** The 217-row baseline in Chapter 2 of the uncap plan splits the closed sections by writer tier as opus 109, sonnet 48, fable 24, blank 36, haiku 0 (an awk count over the table's tier column). Under the hybrid the opus and fable rows keep the Fable reviewer at the frontmatter efforts, so the hybrid removes Fable from about a quarter of the tiered sections relative to the flat rule. Relative to the capped rule the operator measured under, it still adds Fable at `low` on the 109 opus-tier sections while dropping the 24 fable-tier sections' pairs from `high` to `low`; whether that nets above or below the measured pace is not measured here, which is why the Opus cap is named below as the next lever.

**The rule wanted.** Every per-section reviewer runs one tier up from the section's writer tier, Fable the ceiling: haiku at sonnet, sonnet at opus, opus at fable, fable at fable, the code pair, the document pair and the security reviewer alike, with no security exception (the security reviewer over an opus- or fable-tier section runs at Fable and `medium`). After a tier escalation the escalated tier is the writer tier. An inline section's writer tier is the session's own model, since that is what built it; an untiered section takes whatever tier built it. The tier rides as the model override on every dispatch in the round, and a Fable reviewer on a Fable-led session is the inherited default with no override.

**Effort and route, argued.** One question decides the route: is the reviewer Fable? A Fable reviewer runs at its frontmatter effort, `low` for the code and document pairs and `medium` for the security reviewer, through the Agent tool, unchanged from the uncap plan and for the reasons that plan argued (the blind and adversarial lenses are bounded by the section; the security lens chases the boundaries the diff only touches). A reviewer below Fable, whichever lens, runs at `high` through `Workflow`'s `agent()` on the Reviewer Dispatch template, because a frontmatter effort is one value per agent whatever model it runs as, so the only way to give Sonnet and Opus more effort than Fable without lifting Fable too is the route that sets effort per call. The operator chose `high` over `medium` on the Expert seat's recommendation: Opus and Sonnet usage is not the scarce budget, and the operator's reading is that the lower tiers are not as adept at low effort as Fable is. The finishing reviews stay at Fable and `high` through the same route, and where that route is unavailable the fallback keeps each row's own model at the agent's frontmatter effort through the Agent tool (sonnet or opus for a below-Fable reviewer, fable for the finishing reviews, opus for a compensating dispatch per finishing-work's bare fallback), recorded as reduced effort against the row's `high`: the lost effort is not bought back with tier, because a tier climb there would put Fable on the sonnet-tier sections this plan takes it off, and the sibling fallbacks stay at their model too. The `max` row stays compensation for a Fable gate that could not run, confirmed per finishing-work's unavailability rule; a below-Fable reviewer whose tier could not run re-aims one tier up at `high`, Fable the ceiling (sonnet to opus, opus to fable by the finishing route), one dispatch at a time, since a tier answers which model reads the work and never whether to spend, and the ladder paragraph in finishing-work carves that re-aim out by name. A chain whose re-aim at fable is itself ruled out ends that dispatch's gate ungated: the compensation route is closed to a chain that already ruled Opus out, which is the chain-wide predicate this plan puts in finishing-work's ladder. The `high` a below-Fable reviewer carries per-section is the rule's own level for that model and never climbs to `max`.

**What it costs.** A review round over a haiku- or sonnet-tier section takes the Workflow route, which is the same template and the same in-turn await finishing-work already uses, on roughly a quarter of sections by the baseline split. The reviewer charters do not change, so the agent-guard test's effort pins hold as they are.

**What stays fixed.** The five reviewer charters and their frontmatter; `consult/SKILL.md` (Fable at `high`, Opus at `max` when Fable could not run); finishing-work's unavailability rule (one example sentence widened), its ladder (one carve-out clause for the below-Fable re-aim, echoed in the windows paragraph's class sentence, and its compensation predicate made chain-wide so a chain that ruled Opus out cannot reach the Opus row; every other review-class dispatch takes the ladder as before, an Opus-aimed pair such as the design council's ending ungated since the route is closed at its own tier), its finishing route and its compensating route (its `whichever reviewer` widened from the finishing reviewers to any reviewer the row compensates); the doctrine's standing-dispatch bullet, whose Workflow purpose (a read-only agent at an effort the Agent tool cannot set) is exactly what the below-Fable rows use; the implementer tiers and the tier-escalation ladder; the ownership-map row, which names "the reviewer-model rule and the effort table" without stating either.

**Close condition.** `docs/backlog.md` carries the open experiment "effort dials and reviewer tiering in flight". This plan appends a dated amendment naming the hybrid levels and the reason, and amends the operator-only install item of 2026-09-06 so one plugin update and one worker restart install both rules at once. No second experiment opens.

**Revert.** One commit restores executing-work's step 3 sentences and table, the brainstorming enumeration, the architecture clause and the root README's paragraph. The next lever if the bars keep climbing is the Opus cap (opus-tier sections back to an Opus reviewer at `high`), which is the same set of files. No runtime state, no migration. A running session reads skills from its frozen plugin view, so neither this change nor the uncap reaches the worker until `claude plugin update` and a restart.

## Sections of Work

### 1. Reword the rule, retable the efforts, and bring every restatement into agreement
Model: fable
Locus: inline

Load the `writing-skills` skill before editing: this is a rule-parameter change to behavior-shaping skills, so let that skill decide whether a behavior test is needed (none is expected, no test pins the rule sentence or the table) and record its call in the Chapter. Reviewers for this section run under whatever rule this session's frozen plugin view carries, which is the capped rule with the cached agents at `high`; say so in the Chapter.

In `plugins/claude-kit/skills/executing-work/SKILL.md` step 3: replace the bold flat-rule sentence and the sentences that depend on it (the "keys on nothing the writer did" clause, the Fable-led inherited-default remark, the "so the round rides the Agent tool" remark) with the rule wanted, the escalated-tier sentence, the inline and untiered rules, and one sentence sending effort and route to the table; keep the unavailability sentence and its pointer at finishing-work, and add after it the sentence that re-aims a below-Fable reviewer whose tier could not run one tier up at `high` (sonnet to opus, opus to fable by the finishing route), one dispatch at a time, with the compensation row left to the Fable gate alone. Replace the reviewer-effort table and its generator paragraph with six rows, each naming its route: the Fable code and document pairs at `low` (frontmatter, Agent tool), the Fable security reviewer at `medium` (frontmatter, Agent tool), a reviewer below Fable in any lens at `high` (Workflow), a re-aim row (one tier above the tier ruled out, Fable the ceiling, `high`, Workflow), the finishing reviews at Fable and `high` (Workflow, finishing-work's to specify), and the compensation row unchanged; the generator paragraph opens on the route question and then the lens question. Update the "Dispatching a reviewer above its frontmatter default" lead so it names the four dispatches that take the route: a per-section reviewer below Fable at `high`, its re-aim one tier up, the finishing reviews at `high`, and the compensating Opus at `max`. In the compensation-notch paragraph add the sentence that a below-Fable reviewer's per-section `high` is the rule's own level and not the notch. In the Chapter template, the Review Findings line opens with the round's reviewer model and route, so the finishing pass can tell a below-Fable round from a Fable one without deriving it. In the section-loop re-read paragraph at the top of the file, the closing example "the dispatch and never the fable override on its reviewers" becomes "the dispatch and never the reviewer tier bump". The word budget for this file is 21223 after the frontmatter strip and it holds 20828, so the rewrite has about 390 words of headroom.

In `plugins/claude-kit/skills/brainstorming/SKILL.md`: the expected-Fable-surface enumeration names the per-section reviewer dispatches over opus- and fable-tier sections (one tier up from the writer, Fable the ceiling) in place of "every per-section reviewer dispatch whatever tier wrote the section". This file holds 4171 words against a cap of 4185, so the replacement is at most fourteen words longer than what it replaces.

In `README.md`, the model-tiering paragraph: the judgment moment "every per-section review round" becomes the per-section review round over an opus- or fable-tier section, with the below-Fable reviewer at `high` named in the same sentence.

In `docs/architecture.md`: the clause naming the flat rule and the by-lens effort table becomes the hybrid rule and the two-question table; finishing-work's "two states" sentence stays true as written.

In `docs/backlog.md`: append a dated amendment to the open experiment item naming the hybrid levels, the reason and this plan; amend the 2026-09-06 install item so it names both rules; amend the 2026-08-11 effort-pins item's 2026-09-06 tail and the security-model item so each names the per-section rounds the Workflow route now carries.

`plugins/claude-kit/skills/finishing-work/SKILL.md` is read: its finishing route, bare fallback, ladder and compensating paragraph hold under the hybrid as written, and three sentences that carried the flat rule are folded in on review findings at net-zero words, with the ladder paragraph's carve-out for the below-Fable re-aim and its chain-wide compensation predicate, the windows paragraph's class sentence echoing the carve-out, and the compensating paragraph's `whichever reviewer` widening: the dedup instruction in its opening paragraph (a below-Fable round clears nothing for the finishing reviewers), step 3's scope sentence (the pass also owns the local read over sections whose round ran below Fable), and the inheriting-case example in its unavailability paragraph (every inheriting dispatch, `implementer-fable` included). `test/readonly-agent-guard.test.js` is read and confirmed unchanged: its pins are the frontmatter efforts, which do not move.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md`, `plugins/claude-kit/skills/brainstorming/SKILL.md`, `README.md`, `docs/architecture.md`, `docs/backlog.md`; folded during execution on review findings: `plugins/claude-kit/skills/finishing-work/SKILL.md` (three sentences that carried the flat rule, the ladder's two clauses, the windows sentence and the compensating widening); registered by the create path, not section work: `docs/README.md`, `docs/plans/README.md`.

Acceptance: a case-sensitive grep of `plugins/claude-kit/` and `docs/*.md` and `README.md` for `whatever tier wrote` and `every per-section reviewer at fable` returns nothing outside the backlog item's dated amendments, the two docs indexes' archive-chain sentences describing the uncap plan (history), and this plan; the executing-work table reads the six rows above; the five reviewer charters are byte-identical to HEAD; the targeted lane (`test/doctrine-parity.test.js`, `test/output-style-parity.test.js`, `test/size-ratchet.test.js`, `test/readonly-agent-guard.test.js`) is green at exit 0.

## Out of Scope

- The consultant's tier or effort, and the unavailability rule's triggers.
- The implementer tiers, the tier-escalation ladder, and the implementer effort pins.
- The reviewer charters: no frontmatter or body line changes.
- Measuring the outcome: the backlog experiment owns the judgement, on later plans' Chapter Metrics.
- Installing the change on this machine (`claude plugin update`) and restarting the worker: operator actions, listed under Operator Verification.

## Assumptions

- The Agent tool on this version carries a model override and no effort parameter, read from the tool schema this session holds, so the below-Fable rows need the Workflow route.
- `Workflow`'s `agent()` honors `effort` and `model` per call for an `agentType` the read-only guard governs: the mechanism finishing-work already relies on, not re-tested here.
- A Fable reviewer at `low` on an opus-tier section costs less of the allotment than Fable at `high` on a fable-tier section, and the net against the capped rule is unmeasured: the operator's reading, with the Opus cap named as the next lever.
- This session's own reviews of this section run under its frozen plugin view, so they do not exercise the new rule; the first exercise is the worker's next haiku- or sonnet-tier section after a plugin update and restart.
- The spec is executed unleashed in the shared checkout under the commit-freeze protocol with the KIT: Worker session, since the checkout's goal state is that worker's live queue.

## Operator Verification

- Run `claude plugin update`, then restart the KIT: Worker session so its plugin view carries this rule and the uncap's frontmatter efforts together; until then its reviews run under the cap.
- Watch the Fable usage bars over the next few worker sections; the next lever is the Opus cap, one commit over the same files.

## Open Questions

None at authoring.

## Related

- `claude-kit_reviewer-fable-uncap_spec_v1.md`: the 2026-09-05 plan this narrows, whose Chapter 2 holds the 217-row baseline this plan's numbers come from and whose frontmatter efforts this plan keeps.
- `claude-kit_reviewer-tier-cap_spec_v1.md`: the 2026-08-19 plan whose tier-up rule this plan restores with Fable in place of Opus as the ceiling.
- `docs/backlog.md` "Open experiment: effort dials and reviewer tiering in flight": the measurement home.
- `docs/plans/claude-kit_capacity-gate_spec_v1.md`: meters every fable-override dispatch; this plan makes the per-section rounds over opus- and fable-tier sections such dispatches on a below-fable session. The pointer runs one way because that plan is parked under another session's queue.

## Chapters

### Chapter 1 - 2026-09-06
Completed: 1. Reword the rule, retable the efforts, and bring every restatement into agreement
Implemented By: main session
Metrics: review rounds 8; NEEDS_CONTEXT 0; escalations 0; consults 0
Decisions / Surprises: The writing-skills call: no behavior test, since this is a rule-parameter change with no observed failure to reproduce and no test pins the rule sentence or the table; the agent-guard test's effort pins hold unchanged because the frontmatter did not move. The reviewers for this section ran under this session's frozen plugin view (ab815dceddca, the capped rule, cached charters at `high`, inherited Fable, Agent tool, no override), so they did not exercise the rule they reviewed. Two design points settled during the rounds rather than at authoring, both recorded here as decisions: (1) a per-section reviewer below Fable whose tier could not run re-aims one tier up at `high` through Workflow, Fable the ceiling, rather than ending ungated or taking the Opus `max` row, because a tier answers which model reads the work and never whether to spend, with finishing-work's ladder carving the re-aim out by name and its compensation predicate made chain-wide so a chain that ruled Opus out cannot reach the Opus row (the blind reviewer argued this in rounds five and six against the deferral I had written in round two); (2) the Workflow-unavailable fallback keeps each row's own model at frontmatter effort, recorded as reduced effort against the row's `high`, after one round where it climbed a tier and the adversarial reviewer showed that put Fable back on the sonnet-tier sections this plan takes it off. The plan's section text was amended in place as those decisions landed (six table rows, four Workflow dispatches, the finishing-work fold list), so the spec above describes the tree; each amendment is approval drift inside the fingerprint and is named here. Three deviations from the authored section: `plugins/claude-kit/skills/finishing-work/SKILL.md` was folded in (its dedup instruction, its step 3 scope sentence, its unavailability example, the ladder's carve-out and chain-wide predicate, the windows paragraph's class sentence, the compensating paragraph's `whichever reviewer`), at net plus one word against HEAD, exactly at its cap, after trims that lost no claim; the Chapter template's Review Findings line gained a canonical `review:` form so the finishing pass can read a round's model and route; and `docs/README.md` and `docs/plans/README.md` carry this plan's active line by the create path, not section work. The section committed in two parts under the shared-checkout freeze: the docs side at 3e9d98d (plan doc, both indexes, architecture clause, backlog amendments) so the worker's archival could proceed, then the skills, README and this Chapter in the section commit; the two one-clause docs fixes from round five (backlog's effort-pins amendment tail, architecture line 11) were held until the worker's a7d9868 landed and ride with the section commit. Deliberate asymmetry retained and recorded: a Sonnet reviewer ruled out re-aims to Opus at `high` and an Opus reviewer ruled out re-aims to Fable at `high`, both above what the writer's tier normally earns, which the ladder accepts because the re-aim is the rule's own level for the next tier rather than a compensation notch. Minors noted and not changed: the brainstorming enumeration names the standing Fable surface and not the exception paths (a re-aim to fable, the Fable fallback for the finishing reviews), which the parked capacity-gate plan should know when it meters overrides; `docs/security-model.md`'s accepted risk on the unobserved Workflow dispatch now covers per-section rounds over cheap-tier writers too (the backlog's security-model item names it; the file is the worker's); the haiku one-round rule now fires on a Sonnet-at-`high` Critical rather than a Fable one, adjudication being the filter; a split pair with the blind lens also skipped has no worked example on the review line, and no test or hook parses that line.
Assumptions: none beyond the plan's `## Assumptions`; the Agent tool's schema this session holds carries `model` and no effort parameter, read from the tool definition rather than assumed.
Review Findings: `review: code pair at fable, Agent tool` (inherited Fable, charters at `high` under the frozen view), 8 rounds. Round one: three Majors (table keyed on section tier while the rule keyed on writer tier; no unavailability route for below-Fable reviewers; quality claims resting on strong-model review with finishing-work's dedup treating below-Fable rounds as cleared), fixed. Round two: the Sonnet step-up row contradicted finishing-work's ladder, and the fallback discarded effort; fixed by deferring to the ladder. Round three: the fallback sent cheap-tier reviewers to Fable; "pair" overloaded; fixed. Round four: the override sentence contradicted the template's never-inherit rule; the finishing pass briefed cohesion-only while the rule made it the sole Fable read over cheap-tier sections; fixed. Round five: the opus-ruled-out ungated exit stood on a spend ground the same diff contradicts; the design moved to the re-aim. Round six: terminal rung of a re-aim chain decided two ways; fixed with the chain-wide predicate and the re-aim row. Round seven: finishing-work's windows paragraph and the narrowed ladder lead orphaned the council dispatches; the fallback's tier climb was unargued; fixed. Round eight returned APPROVED_WITH_CONCERNS from both lenses; its Minors (the bare fallback excluded from the re-aim carve-out, the finishing dedup keyed on the rule's aim so a compensated round still clears, the first-aim qualifier on the compensation predicate and README's stand-in sentence, an upward substitution no longer read as a ruled-out tier, the re-aim named in brainstorming's Fable surface, the fallback's record against its row's effort) were fixed as wording with the lane green and no ninth round, since neither lens returned a Major that survived its own verdict. The blind lens's round-eight Major, that the finishing pass runs no blind reviewer over cheap-tier sections, was accepted as a decision and stated in finishing-work's step 3: those sections keep their section-time blind read at the one-up tier, as they did under the capped rule. Noted and not changed: a Chapter written under a prior rule carries no model token on its review line, and the finishing pass reads it under the rule in force at the Chapter's date. Every Major across the rounds was fixed; no Major was justified rather than fixed.
Stamps: adjudicated 6, stamped 0: `memq unstamped --since 6h` lists six operator-tier reads, all made by other live sessions on this machine (this session read no memory file in this stretch), so none is this section's to stamp.
Gate: the targeted lane at section close, `node --test test/doctrine-parity.test.js test/output-style-parity.test.js test/size-ratchet.test.js test/readonly-agent-guard.test.js` over the four-file set after the last edit: 276 tests, 276 passing, 0 failing, exit 0 read from `$?` into `.kit/hybrid-lane14.exit`; the same lane read 276/276 exit 0 at the section's first edit, so the delta against that baseline is zero, with the ratchet reddening and greening between as the trims landed (each red read from the run and fixed before the next round). The contention lane did not run because this repository defines none. No whole gate at this close: this section's push lands on main, but the plan's finishing pass runs the whole gate before its own push, and this commit lands under it.
Next: finishing-work
Commit Model: Commit-and-Push
Delta: measured on SCOTT-CLAUDE on 2026-09-06 by this session, on the worktree against HEAD 2d5f2cc (another session's archival commit) immediately before this section's own commit, so the reading covers the skill and README side of section 1 and this Chapter, the docs side having landed at 3e9d98d; the box carried the KIT: Worker session's implementer live on its own files and no suite, and the reading below is what the verb printed:

```
repository: claude-kit
plugins/claude-kit/skills/brainstorming/SKILL.md: 4184 words, cap 4185, +13
plugins/claude-kit/skills/executing-work/SKILL.md: 21221 words, cap 21223, +393
plugins/claude-kit/skills/finishing-work/SKILL.md: 16860 words, cap 16860, +1
words: 214692 of cap 214695 across 48 curated files
test lines: 104172 of cap 104172 across 53 test files
tests: 3130
changed paths under no measured root: 7 (7 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: none
```

### Chapter 2 - 2026-09-06 (finishing pass)
Completed: finishing-work over the whole effort
Base Ref: 2d5f2cc, the parent of the Chapter-adding commit 0faeb51; the plan's docs-side registration landed earlier at 3e9d98d on the parent 1545d58, so the docs files' whole change reads from 1545d58.
Metrics: four finishing dispatches (qa-verifier, security-reviewer, adversarial-reviewer, docs-curator), none re-dispatched; QA three of four criteria PASS with the fourth read red on another session's over-cap file and green on the committed tree in a clean worktree; security CLEAR with two Minors; adversarial APPROVED_WITH_CONCERNS with two Majors and three Minors, all record defects in this plan doc; curator drift report adjudicated below.
Decisions / Surprises: The security waiver did not apply: a skill's `SKILL.md` is configuration under the waiver's own words, so the review ran scoped to the three skills. The finishing reviewers ran under this session's frozen plugin view (inherited Fable, charters at `high`, Agent tool), the same terms as the section's rounds. The QA lane red was `test/kit-compact-gate.test.js` over its cap in the worktree, the KIT: Worker session's in-flight file under its checkpoint-session-validation plan; the committed tree at 0faeb51 ran the same four-file lane at 276/276 exit 0 in a throwaway worktree at D:/Temp/hybrid-gate, since removed, and the worker was told. Two record corrections the finishing adversarial review earned, stated here rather than by editing the fingerprinted Approach or the append-only Chapter 1: (1) the 217-row split in the Approach counted the rows tagged `inline (opus)` and `inline (sonnet)` as blank; under this plan's own writer-tier rule they fold into their session tiers, so the split is opus 134, sonnet 59, fable 24, haiku 0, and the hybrid takes Fable off 59 of 217 sections, 27 percent, which keeps the Approach's "about a quarter" and corrects its "109 opus-tier sections" to 134; the same wrong split rode in the section commit's message and in this seat's report to the operator, both corrected by this Chapter and the close-out report. (2) Chapter 1's "Minors noted and not changed" wrongly lists the brainstorming enumeration as unchanged; the re-aim reaching fable was added to that enumeration in round eight, and the open item is closed. Two smaller record gaps, noted rather than amended inside the fingerprint: the section text's "compensation row unchanged" predates the round-eight first-aim qualifier that row gained, and the Revert paragraph omits finishing-work's folded sentences, the accurate revert shape being the backlog install item's "one commit over the hybrid plan's section 1 files", which now include finishing-work. Security Minors noted: `docs/security-model.md`'s accepted-risk paragraph on the unobserved Workflow dispatch now covers per-section rounds over cheap-tier writers, tracked at the backlog's security-model item since the file is the worker's; finishing-work's bare-fallback sentence conditions on the pair rather than the chain, finite either way as the reviewer read it, left at the file's word cap.
Review Findings: `review: security-reviewer at fable, Agent tool`; `review: adversarial-reviewer at fable, Agent tool`. Security: CLEAR, two Minors (above). Adversarial: two Majors (the 217-row split; Chapter 1's self-contradiction), both corrected in this Chapter; three Minors (section text's "compensation row unchanged", the Revert paragraph, "What stays fixed" framing), the first two recorded above and the third wording only. No Critical anywhere in the effort.
Drift Adjudications: D1 deviation, accepted: both architecture carriers had flattened the re-aim away (a Fable reviewer reads as frontmatter effort on the Agent tool, unqualified), and the curator fixed both in place at `docs/architecture.md` lines 11 and 29, naming the re-aim as the one case where the reviewer's model does not determine its route; kept as the curator wrote it. D2 deviation, accepted: the backlog's "Confirmed" defect bullet stating that the Chapter template carries no `review:` field is closed by this effort's template edit, so the close-out retires it to the Q3 snapshot with `plugins/claude-kit/skills/executing-work/SKILL.md`'s Review Findings line as the receipt. D3 deviation, accepted and deferred: `docs/security-model.md`'s accepted-risk paragraph on the unobserved Workflow dispatch is route-general and true, and its per-section scope statement stays with the backlog's security-model item, since the file is the worker's. Library hygiene: H1 is D2's retirement; H2 is the known one-way pointer to the parked capacity-gate plan, closed whenever that plan is next opened; H3 clean. No mistake class in the report.
Assumptions: none beyond the plan's `## Assumptions` and Chapter 1's.
Stamps: adjudicated 6, stamped 0: `memq unstamped --since 8h` at the close lists six operator-tier reads, all made by other live sessions on this machine (this session read no memory file in this pass), so none is this pass's to stamp; the pass wrote one project-tier amendment (`skill-amendments-collide-with-neighbours`, the owner-paragraph-family lesson, re-anchored to the two skills) and two journal entries under `reviewer-tier-hybrid-s1`.
Gate: the handoff whole gate, `node --test test/*.test.js` under this session's heavy-process claim (written 13:45:58Z, released 13:52:38Z), run after the last step that changed the tree and over the archived layout, because this pass's push lands on main, this kit's install surface with no CI between a commit and a plugin update: 3190 tests, 3180 passing, 1 failing, 9 skipped, exit 1 read from the run's own marker file, 400 s wall clock. The one red is `test/memory-session.test.js` "a pinned directory too long to name faithfully stands the session down", the standing red the project memory `suite-baseline-is-not-zero-fail` records for this box (its `D:\Temp` prefix keeps the fixture path under the 260-character guard); the last recorded whole-gate reading on this lane was 3165/3155/1/9 with the same one red, the count having grown by the worker's write-time-neighbours tests since, so the delta against that baseline is zero and no red is this effort's. The size-ratchet test was green on the worker's uncommitted cap raise in test/size-budget.json, as the worker reported before the run. The contention lane did not run because this repository defines none. The four-file targeted lane's last reading before this run, over the committed tree in a clean worktree, was 276/276 at exit 0.
Operator Handoff: run `claude plugin update`, then restart the KIT: Worker session so its plugin view carries this rule together with the uncap's frontmatter efforts; until then its reviews run under the retired Opus cap. Watch the Fable usage bars over the next worker sections; the next lever if they keep climbing is the Opus cap over opus-tier sections, one commit over this plan's section 1 files, finishing-work included.
Next: none, plan Complete
Commit Model: Commit-and-Push
Delta: measured on SCOTT-CLAUDE on 2026-09-06 by this session, on the worktree against HEAD 0faeb51 immediately before this pass's own close-out commit, so the reading covers the curator's edits, this Chapter and the archival move; the box carried the KIT: Worker session's implementer live on its own files, and the reading below is what the verb printed:

```
repository: claude-kit
test/compact-deferral-nudge.test.js: 2112 lines, cap 2112, +2; tests 77, +0
test/kit-compact-gate.test.js: 9434 lines, cap 9434, +282; tests 307, +11
test/kit-goal-worktree.test.js: 713 lines, cap 713, +2; tests 11, +0
words: 214692 of cap 214695 across 48 curated files
test lines: 104458 of cap 104458 across 53 test files
tests: 3141
changed paths under no measured root: 5 (5 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
```
