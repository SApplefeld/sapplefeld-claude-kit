# The heavy-process claim is written by a verb that carries its own exclusion, and a waiter has a rule

Status: Ready
Commit Model: Commit-and-Push
Created: 2026-09-02

Session model: any executor session in the kit repo; three sections, tiers per section. Authored by the KIT: Expert seat from the 2026-09-02 kaizen pass, which folded eleven inbox notes on the claim protocol's write side into this one design. Anchors are authoring-time; re-locate every hit by content.

## Dispatch Authorization

Authorized 2026-09-02 by the operator, first-hand on the allowlisted relay thread, to be appended to the kit worker's armed queue: the claim and release verbs and the waiter rule as designed here, tenth in the queue. The operator's word was the answer to a decision batch the KIT: Expert seat put on the relay, choosing the recommended option of appending the pass's four code-and-design specs to the worker's queue in the order code batch, liveness, claim writer, provenance; that seat recorded it here and ran the append. Per the peer-sessions trace rule this section is a warrant only for a citing session that did not author it, and the receiving session performs its own trace: the grant is the operator's message on the Expert session's relay thread, and the plan arms only by the operator's word or the Expert seat's append under it.

## Goal

The machine's one-heavy-process slot is a file, `claims/heavy-process.md` in the coordinator directory, and the protocol around it is specified almost entirely from the arbiter's side: how the coordinator reads the fields, when it probes, what a release rests on. The writer's side is a read followed by a plain write, and a waiter's side is not specified at all. Eleven inbox notes across three seats in three days describe what that asymmetry produces: a read that goes stale between the read and the write, so a claim silently replaces a live foreign one; a waiter that computes an expiry from claimant-written fields and waits on a future-dated or over-long value forever; a session that infers the file's contents from its own successful write while a foreign write has already landed; two sibling agents of one session carrying one substituted id, so one sibling's completion delete erases the other's live claim; a claim written by a foreground wrapper that dies before its release step; a check that prints the claim state and runs anyway; and a release that leaves no record at all, so a correct early release and a wrong foreign delete are byte-identical afterwards.

When this plan is done: the claim is written and released by a kit verb whose create is exclusive, so the existence check and the write are one system call and a collision is a loud refusal rather than a silent overwrite; the verb reads the clock itself, carries a per-invocation actor token beside the accountable session id, and scopes its release by both, so siblings cannot delete each other and a dispatched agent's claim says which actor wrote it; every release, the holder's own and the coordinator's arbiter release alike, runs through the verb and appends one tombstone line to a machine-local log naming who released and on what record, so the slot's history is readable and a correct release and a wrong foreign delete are no longer the same absence; the role skill states the waiter's rule in as many words as it states the arbiter's; and the dispatch brief's deliberate second copy of the protocol names the verb rather than describing the fields, so every dispatched agent on every machine gets the same behaviour from one command.

## Evidence

- The write side is a plain write today: the role skill's claim section says "read the live claim file, then write `claims/heavy-process.md`" and the executing-work brief clause repeats it ("where no live claim stands, write the claim with its full field set"), `plugins/claude-kit/skills/executing-work/SKILL.md:286-320` at HEAD `d09d099`. Neither names a primitive between the read and the write.
- The notes, all in `kaizen/notes-SCOTT-CLAUDE.md` at the pass's capture commit unless stated: the waiter gap and its second half (2026-08-30, coordinator seat, "specified entirely from the arbiter's side"); the read-then-write race and the exclusive-create primitive (2026-08-30, two notes); the same-id sibling delete (2026-08-31); the actor-versus-accountable collision seen from outside (2026-09-01); the placement rule and the gating form (KIT: Expert, 2026-09-01 and 2026-09-02); the delete-on-completion audit gap (2026-08-30); the entry-check-only re-test gap and the `Expected-seconds` misreading (2026-08-31, two notes); and the NEO note on the release being foreclosed by a successor's answer, whose fix rides the liveness plan.
- `kit-goal-lib.js` already uses the exclusive-create primitive for its own state file (`plugins/claude-kit/hooks/kit-goal-lib.js:857`, the EEXIST comment), so the verb has a sibling in the tree to mirror.
- The stamp CLI already owns clock reads for the coordinator directory (`plugins/claude-kit/hooks/kit-registry-stamp.js`, `push`, `now`), which is why the verb lands there rather than in a new file.

## Decisions

Decided 2026-09-02 by the Expert seat under standing adjudication; reversible at arming.

1. **The verb lives in the stamp CLI as `claim` and `release`.** One CLI already owns the directory's machine-written fields and resolves the calling session's id; a second CLI would duplicate its scope resolution and its sanitizer. Alternative: a new `kit-claim.js`, declined for that duplication.
2. **The actor token is the clock read itself.** `Started:` is read at the write by the verb, at millisecond precision, and doubles as the per-invocation token the release matches on, so no new field is invented for the discriminator and the file stays readable by every existing consumer. The token travels in the caller's hands and nowhere else: `claim` prints the `Started:` it wrote, `release` takes it back as a required `--started` argument, and the CLI keeps no per-session record of it, because any record keyed by the session id is one both siblings of a session would read, which is the sibling delete the token exists to stop. A separate `Actor:` line names the writer's kind (`session` or `agent`) for readers outside the protocol, which is the fix the 2026-09-01 note asks for, and carries no authority.
3. **Release appends a tombstone, and the coordinator's release is a form of the same verb.** `claims/released.log`, one line per release with the released claim's fields and the releasing actor, machine-local under the same allowlist exclusion the claims directory already has (`/coordinator/**/claims/` in the sync installer's allowlist). The holder's form is scoped by `Session:` and `Started:`; the arbiter's form, `release --arbiter --session <claimant id> --board-line <ref>`, is scoped by the claim's `Session:` alone and appends a tombstone naming the decider (the calling session) and the board line the release was recorded on, so the coordinator's delete is no longer a bare unlink and the log is the one place both kinds of release leave the same shape of record. The arbiter form changes the mechanic of the coordinator's delete and nothing about what licenses it: the two legs the role skill states stay as they are. A renewal is a release followed by a claim, and the log records both.
4. **The waiter's bound is its own clock.** A waiter never derives a bound from `Started:` or `Expected-seconds:`; past its own bound its remedy is a probe to the holder over the peer surface and an escalation to the coordinator, never a longer wait. The claim's fields are what the holder believes it is doing.
5. **The deferring session re-checks at its next heavy spawn.** The rule "where no live claim stands, write the claim" gains its re-test moment: a session that proceeded under a named contention re-runs the claim verb before its next heavy spawn rather than carrying the earlier reading forward, and that is the whole of the re-test; no session polls the claim file or the tombstone log on a cadence, and the log is the after-the-fact record of when the slot cleared, never a signal a waiter watches for. A release notifying every deferred waiter was weighed and declined as a message fan-out the protocol cannot bound.

## Sections of Work

### 1. The `claim` and `release` verbs. Model: opus

Add `claim --expected <seconds> [--repo <name>] [--name <roster name>] [--agent --session <id>]` and `release` in two forms, `release --started <value>` and `release --arbiter --session <claimant id> --board-line <ref>`, to `plugins/claude-kit/hooks/kit-registry-stamp.js`. `claim` reads the live file first for the report, then creates the claim with the exclusive flag (`wx`, the primitive `kit-goal-lib.js` already uses for its own state file); on `EEXIST` it prints the live claim's fields with the file's own modification-time age, exits nonzero, and writes nothing, which is the gating form a script uses in its own control flow. On success it prints the `Started:` value it wrote, and that printed value is the only place the token lives: the CLI keeps no per-session record of it, so a caller that loses the value has no release and reports the claim to the coordinator as one it cannot release. It reads `Session:` from the shell environment as `push` does, or from `--session` under `--agent` (a dispatched agent's shell is not relied on to carry the dispatching session's id, so the brief substitutes it exactly as it substitutes the name); `Name:` from the caller's registry entry where one exists and from `--name` otherwise (the brief substitutes the dispatching session's name); `Repo:` from the caller's registry entry where one exists and from `--repo` otherwise, refusing where neither resolves one, so no claim ever carries an empty `Repo:`; `Started:` from the clock at the write; `Actor:` as `session` or `agent` per the flag; and `Expected-seconds:` from the argument, refusing a missing or non-numeric value. `release --started <value>` deletes only a claim whose `Session:` matches the caller's own id and whose `Started:` matches the argument, appends the tombstone line, and on any mismatch leaves the file in place, names the collision, and exits nonzero; `--started` absent is a refusal, never a fallback to any remembered value. `release --arbiter` is the coordinator's release: it deletes only a claim whose `Session:` matches `--session`, appends a tombstone naming the released claim's fields, the decider (the caller's own id from the environment), and the board line, and on a mismatch leaves the file, names it, and exits nonzero; it decides nothing, the two legs that license it staying the role skill's. Either form run with no claim file present exits nonzero naming "no claim" and appends no tombstone, so a release that finds nothing to release leaves the log honest about who actually released. The tombstone is one line in `claims/released.log`, appended, never rewritten, and every path sanitizes what it prints, per the CLI's existing rule. Tests: two concurrent claims produce exactly one file and one refusal; a foreign claim refuses release and stays; a sibling claim with the same session and a different `Started:` refuses release and stays; a release appends one tombstone with the released fields; a release with no claim file exits nonzero naming no claim and appends nothing; `--expected` absent refuses; `--started` absent refuses; `--repo` absent with no registry entry refuses; an arbiter release with a mismatched `--session` refuses and stays; an arbiter release deletes and appends a tombstone naming the decider and the board line.

Files in scope: `plugins/claude-kit/hooks/kit-registry-stamp.js` (the two verbs beside `push`, `now`, and `audit`); `test/registry-stamp.test.js` (the tests above, beside the audit instruments already there); `docs/architecture.md`, the CLI paragraph under **The coordinator directory** that reads "It carries three verbs" and then names `push`, `now`, and `audit`, which gains the two verbs and the count five; `docs/security-model.md`, the read-side paragraph beginning "The paths in the table below carry the memory tiers' content into a model's context", whose sentence "The exception is `kit-registry-stamp.js audit`" names the one CLI reader that prints directory values into a model-read channel, and which `claim`'s refusal print joins, so the sentence names both verbs and states that `claim`'s print goes through the same output guard.

Acceptance: the ten tests green, watched red first; `node --test test/registry-stamp.test.js` green with delta named against a baseline recorded on that same command; the architecture paragraph names five verbs; the security model's read-side exception sentence names `claim` beside `audit`.

### 2. The role skill states the writer's and the waiter's rules. Model: opus

Amend the claim-file section (`## The claim file`) of `plugins/claude-kit/skills/role/SKILL.md`: the write is the `claim` verb and the release is `release --started` with the value `claim` printed, the exclusive create stated as the reason a collision is loud; the field set the verb writes, `Name:`, `Repo:`, `Session:`, `Started:`, `Actor:`, and `Expected-seconds:`, stated in one sentence with each field backticked, because the parity pin below derives the contract's set from that sentence; the actor line and its meaning (accountable session versus acting writer); the tombstone log as the audit leg the delete rule lacked; the waiter rule of decision 4 with its two halves (never a bound from claimant fields, and never a fact about the file from one's own past write, a fresh read being the only source of current state on a multi-writer file); the re-test moment of decision 5 (the deferring session re-runs `claim` before its next heavy spawn, and nothing polls); `Expected-seconds:` stated as an estimate that bounds nothing for a waiter, the arbiter's bounded reading of it standing as it is, and a lapsed window as no release; a release that finds no claim as a nonzero exit and no tombstone; the placement rule (a claim and its release live in the same execution unit as the run they bracket, so a wrapper that backgrounds the run never holds the claim); and the gating form (the verb's nonzero exit is what a script branches on, never a printed state before an unconditional run). The section's two statements the `Started:` parity pin reads stay in it in substance: that `Started:` is read from the clock at the moment of the write, now by the verb, and that a live claim's age is the file's modification time, which is what `claim`'s refusal prints. In the same file, amend the arbiter's mechanic sentence in the probe-and-release paragraph (the sentence beginning "The mechanic is the coordinator deleting the claim file as the slot's arbiter") so the delete is `release --arbiter --session <claimant id> --board-line <ref>`, run after the board line is written and the claimant notified exactly as now, the board line reference being the `--board-line` argument and the tombstone the verb appends being what the deletion's citation lands in; the two legs and every other sentence of the arbiter's paragraphs stay as they are. And amend the directory contract at `## The coordinator directory`: the "Four file forms live there:" list gains `claims/released.log`, the release tombstone log, appended by the `release` verb alone and machine-local under the same sync exclusion as the claim, and the sentence "The list is the contract" and its count read five. Whole-file review, because the section's neighbours restate parts of what changes.

Rewrite the two existing parity pins in `test/doctrine-parity.test.js` so they hold against the amended skill: the field-set pin (the test at `test/doctrine-parity.test.js:3211-3363`, "the box-budget brief clause agrees with the role skill's claim contract") derives the contract's set from the role skill's new field sentence and compares it against the fields `claim` writes, read from the CLI's own source (the writer's field list in `kit-registry-stamp.js`), with the count six, re-derives its role-side literal on the delete scope (`:3320`, "a writer deletes only a claim whose") against the amended release sentence, and reads the brief clause only for the literals it already asserts on the clause (the delete-scope sentence at `:3315`, the contention sentence at `:3337`, and the poll sentences at `:3350-3358`), since section 3 removes the clause's field composition and the pin cannot depend on it; the `Started:` pin (the test at `:3375-3406`) keeps its role-side assertions as they are and keeps its clause-side assertions until section 3 rewrites the clause. Watch the field-set pin red by dropping a field from the role skill's sentence, then restore it.

Files in scope: `plugins/claude-kit/skills/role/SKILL.md` (the claim-file section, the arbiter's mechanic sentence, and the directory contract's list); `test/doctrine-parity.test.js` (the two pins named above); `docs/architecture.md`, under **The coordinator directory**, the first paragraph's "four file forms live there" and its list, the writer-rule paragraph's "once over the four" and "All four forms are `.md`" (the log is not `.md` and does not sync, which that sentence has to say), and the `claims/heavy-process.md` paragraph's "the spawner reads the live claim and then writes `Name:`, `Repo:`, `Session:`, `Started:`, and `Expected-seconds:`, deleting the file at completion", which becomes the verb and its release; `docs/security-model.md`, the paragraph under **What no control here reaches**, whose writer-rule sentence "the claim scoping a delete to the writer's own session so a completing writer cannot erase a live foreign claim" gains the `Started:` token, whose "releases a foreign heavy-process claim on the claim protocol's own clock" names the arbiter form, and whose writer classes gain the tombstone log with `release` as its only writer, plus the read-side paragraph's "all four of its file forms are read directly", which reads five; `plugins/claude-kit/skills/coordinator/SKILL.md`, the paragraph under **A release the contract licenses is performed on the record**, read for the mechanic and edited only where it names the delete as a bare unlink (its "the deletion citing the line" is what the arbiter form's `--board-line` performs).

Acceptance: each rule present in the section that owns it; the arbiter's paragraphs unchanged except the mechanic sentence and where they name the new fields; the directory contract lists five forms; no em dashes; the two rewritten pins green, the field-set pin watched red first; targeted lane green with delta named (`node --test test/doctrine-parity.test.js`).

### 3. The dispatch brief clause becomes the verb, and a pin holds the copy. Model: opus

Rewrite the box-budget clause in `plugins/claude-kit/skills/executing-work/SKILL.md` (the passage beginning "The standing box-budget clause", which the parity pins slice by that lead and by the next lead, "The two-question grant audit") so the dispatched agent runs `node <root>/hooks/kit-registry-stamp.js claim --expected <n> --repo <repo under test> --name <substituted name> --agent --session <substituted id>` before its heavy work, keeps the `Started:` value that command prints, and runs `node <root>/hooks/kit-registry-stamp.js release --started <that value>` after, with `<root>`, the name, the id, and the repo substituted at brief-writing time as today, and so the clause's prose states only what an agent must know to act on the verb's outcomes: on a refusal, wait or name the contention and never write around it, re-running `claim` before the next heavy spawn rather than carrying the reading forward; on a release mismatch or a release that finds no claim, leave the file as it is and name the exit; and on a lost `Started:` value, report the claim as one it cannot release. The clause names no field and composes no value; the read-as-data sentence, the contention sentence ("the contention and proceeding never includes writing the claim"), and the legibility-not-guarantee sentence with its two poll sentences stay verbatim, since an agent acts on all of them and the field-set pin asserts the last three on the clause by literal (`test/doctrine-parity.test.js:3337-3358`); the pin's clause-side delete-scope literal (`:3315`, "delete only a claim whose `Session:` line") is re-derived against the release-mismatch sentence the clause now carries. Rewrite the clause side of the `Started:` pin (`test/doctrine-parity.test.js:3375-3406`) so it asserts the clause names both commands, `claim` and `release --started`, and carries neither a `now` command nor a `Started:` composition, the clock-at-the-write rule now living in the verb; its role side stays. Add to the field-set pin a clause-side assertion that the clause's slice carries no backticked claim-field token at all (the set the pin derives from the clause is empty by design, which is the opposite of the contract side's non-empty guard and is asserted on its own line), so the deliberate second copy cannot regrow a field list that drifts from the contract. Watch the new assertion red by adding one backticked field name to the clause, then remove it.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md` (the box-budget clause only); `test/doctrine-parity.test.js` (the clause side of the two pins named in section 2, at the ranges section 2 states).

Acceptance: the clause-side assertions green and watched red as stated; the clause names the two commands and no field-by-field composition; the field-set pin's contract side still compares the role skill against the CLI's source, untouched by this section; targeted lane green with delta named (`node --test test/doctrine-parity.test.js`).

## Out of Scope

- The probe's addressing and the disclaim disposition, which `claude-kit_liveness-by-session-identity_spec_v1.md` owns.
- Any process poll. The claim buys legibility, never a guarantee, and this plan does not change that.
- Cross-machine claims. The file stays machine-local by the sync allowlist's existing exclusion.

## Assumptions

- assumed 2026-09-08 (source: the brief clause in `plugins/claude-kit/skills/executing-work/SKILL.md`, which already substitutes the dispatching session's id and name into the brief because a dispatched agent holds no session identity of its own): a dispatched agent's shell is not relied on to carry `CLAUDE_CODE_SESSION_ID`, so `claim --agent` takes `--session <id>` from the brief rather than the environment; reversal: if the harness is shown to pass the id into every subagent's shell, section 1 makes `--session` optional under `--agent` and section 3 drops it from the brief command, with no change to the file the verb writes.
- assumed 2026-09-08 (source: the Decisions section, which states the design's reasoning per decision and is what the sections cite): this spec carries no separate `## Approach`, the Decisions section being the approach; reversal: none, a reading rule for the executor.
- plan review 2026-09-08 (plan-reviewer at fable, effort high): 15 findings, 14 fixed, 0 assumed, 0 asked, 1 discarded.

## Related

- `claude-kit_liveness-by-session-identity_spec_v1.md`: the arbiter-side half.
- `claude-kit_kaizen-prose-batch_spec_v1.md`: carries the doctrine bullet's clearance clause.
- Kaizen triage record `kaizen/archive/2026-09-02-pass-triage.md`.
