# Liveness is decided by the session, never by the seat name it wears

Status: Ready
Commit Model: Commit-and-Push
Created: 2026-09-02

Session model: any executor session in the kit repo; four sections, tiers per section. Authored by the KIT: Expert seat from the 2026-09-02 kaizen pass, which dispositioned four inbox notes from two machines and one operator-tier memory record into this one design. Anchors are authoring-time; re-locate every hit by content. Section 2 edits the coordinator skill, which the board-routing-and-homing plan is rewriting ahead of this one in the same queue, so its anchors are re-derived at dispatch rather than trusted from here.

## Dispatch Authorization

Authorized 2026-09-02 by the operator, first-hand on the allowlisted relay thread, to be appended to the kit worker's armed queue: the four sections rekeying the registry prune and the heavy-slot release on session identity as designed here, ninth in the queue. The operator's word was the answer to a decision batch the KIT: Expert seat put on the relay, choosing the recommended option of appending the pass's four code-and-design specs to the worker's queue in the order code batch, liveness, claim writer, provenance; that seat recorded it here and ran the append. Per the peer-sessions trace rule this section is a warrant only for a citing session that did not author it, and the receiving session performs its own trace: the grant is the operator's message on the Expert session's relay thread, and the plan arms only by the operator's word or the Expert seat's append under it.

## Goal

Two kit mechanisms decide whether the writer of a coordination artifact is still alive, and both decide it by joining the artifact's self-chosen `Name:` against the live roster. The kit's own naming convention makes that name stable across re-seats: a successor taking the Expert seat is `KIT: Expert` exactly as its dead predecessor was, and the operator's habit after every fleet restart is to bring seats back under the names they held, because the names say what a seat is responsible for. So on every machine that follows the convention, a dead predecessor's registry entry joins to its live successor and reads present forever, and a dead predecessor's heavy-process claim is probed at an address its live successor answers, which forecloses the release exactly when the slot is most needed. Neither failure produces a wrong answer anywhere a reader would notice: the registry accretes and the slot reads held, both in the fail-closed direction, both silently.

When this plan is done: the operator keeps the naming habit unchanged; the registry prune and the claim release decide liveness on the session identifier the artifact already carries, corroborated by an instrument no agent writes, the harness transcript's own modification time; a name match on the roster establishes presence of the name and nothing about the entry's session, so it never vetoes a verdict the other readings establish; a probe answer that disclaims a claim has its own disposition instead of counting as an answer that forecloses release; and the coordinator runbook carries five amendments the inbox has owed it since 2026-08-30, all on the same reconciliation surfaces.

## Evidence

- The role skill's directory contract states the prune's exited verdict on two readings of the entry itself, the roster resolution keying on the entry's `Name:` and the staleness test on its `Heartbeat:`, and says in its own words that no artifact on this machine independently attests that a session id is dead (`plugins/claude-kit/skills/role/SKILL.md`, the paragraph opening "Exactly one act deletes a foreign registry file"). The claim protocol addresses the probe by the claim's `Name:` while scoping the delete by `Session:`, and names the collision as a residual rather than a defect (same file, the claim-file section and the probe paragraph).
- The coordinator runbook runs the roster read as a diff against the registry keyed on the name join and states the heartbeat leg as what tells an off-roster entry apart from a dead one (`plugins/claude-kit/skills/coordinator/SKILL.md:43-47` at HEAD `d09d099`; re-locate by content, the board-routing plan is rewriting this file).
- Four kaizen notes, two per machine, describe the same join failing in both mechanisms: `kaizen/notes-NEO-CLAUDE.md` 2026-08-31 (two notes, the registry instance and its general form, with the live NEO-CLAUDE case of three dead seats shielded by three successors), and `kaizen/notes-SCOTT-CLAUDE.md` 2026-08-31 (the reusable-label note, measured as six predecessor entries against four returning seats with three immediately unprunable, and the silence-gated release note). All four sit in the store's history at the kaizen capture commit this pass made.
- The operator-tier memory `a-self-stamped-liveness-field-cannot-establish-exit` records the coordinator seat's 2026-09-02 measurement: four entries satisfied both contract legs while two of their sessions had committed within the hour, heartbeat lag of 11.2 and 13.8 hours, and the transcript modification time discriminated the two live ones from the two dead ones. Reported by that seat; this plan's section 3 makes the reading an instrument rather than a hand check.
- The transcript's location is already a kit resolution: `kit-goal.js` corroborates a binding against `<sessionId>.jsonl` under the harness's projects directory through memq's `sessionTranscriptDir` (`plugins/claude-kit/hooks/kit-goal.js:109-131`). That helper answers null for an absent transcript, an unreadable projects root, an id two project directories hold, and a listing the cap cut short alike, by its own comment (`plugins/claude-kit/scripts/memq.js:1196-1197`), so it can corroborate a binding and cannot tell an absence from an unknown; section 3 adds a resolver with distinct states rather than reusing it.

## Decisions

Decided 2026-09-02 by the Expert seat under standing adjudication; each is reversible at arming and the operator may overrule any of them there.

1. **The naming convention stands.** Seats keep their function-named addresses across re-seats. The peer-sessions skill already states that names collide by construction, so the fix lands in the mechanisms that assumed otherwise, never in the operator's habit.
2. **The roster leg becomes refusal-only.** The harness roster prints a name and a `[ref]` and never a session id, so a name match can establish only that some session wears the name. A matching row therefore blocks nothing and licenses nothing; a name matching no row remains a candidate reading as today. The verdict's evidence moves to the two readings that are about the entry's own session: its `Heartbeat:` staleness, and the transcript reading below.
3. **The transcript modification time is the corroborating leg, and it is required.** An entry is exited only where its heartbeat reads stale past the per-entry bound the coordinator runbook derives from the entry's `Role:` (twice the seat's stated cadence, the runbook's own 4-hour cadence where the seat states none) AND the transcript at `<projects>/<project>/<Session>.jsonl` is either absent or stale past the same per-entry bound. A fresh transcript reads live whatever the heartbeat says, because a session can stop stamping while it keeps working, and the error of the self-stamped field runs only toward looking dead. Alternative weighed: keying the roster join on the `[ref]` the runbook points at for collisions, declined because nothing documented maps a `[ref]` to a session id and a join on an undocumented mapping is a join on a guess.
4. **A disclaiming probe answer is a third reading, and it licenses release only with the transcript leg beside it.** Today an answer of any content forecloses the release. Under this plan the probe names the claim's `Session:` and asks the answerer whether that id is its own; an answer stating it is not is a disclaim. A disclaim alone stays what it is today, an untracked hold reported to the operator, because it is an unauthenticated assertion any peer can make. A disclaim plus a transcript reading that the claim's session is absent or stale past the claim bound licenses the release, on the record and with the notification the protocol already requires, since two independent readings then agree and one of them is machine-written. Alternative weighed: honouring a disclaim alone, declined for the reason the NEO note itself weighs, that a confused or hostile peer could free a slot in use.
5. **The clearance is a third act and it is named.** The doctrine's one-heavy-process bullet governs a session's own spawn, start, and re-run on a clean poll and says nothing about telling another session the box is clear. This plan states the coordinator's half (section 2); the doctrine's half rides the kaizen prose batch plan, cross-referenced under Related, so the two land in the same wording.

Cost of decisions 2 and 3, for the operator to accept at arming: with the roster leg refusal-only, a live seat that sits idle past its bound with both stamps stale (an Expert waiting overnight on the operator, heartbeat and transcript both eight hours old against a 4-hour cadence) reads exited, loses its entry to the prune, and re-creates it at its next push, which is the direction the role skill's contract names as the wrong one, deleting a live peer's record. What bounds the cost is that the entry is the only thing lost, its claim is untouched by the prune and comes free only through the claim protocol's own probe-and-release, and the seat's next push rewrites the entry whole; what the trade buys is a registry that stops accreting a dead entry per re-seat on every machine that follows the naming convention. The alternative, keeping a roster match as a veto, is the failure this plan exists to remove, so the choice is between this cost and the status quo, and it is the operator's.

## Sections of Work

### 1. The prune reads the session, and a name match vetoes nothing. Model: opus

Amend the role skill's directory contract (the paragraph opening "The list is the contract") so the exited verdict is stated on the three readings decision 2 and decision 3 name: the roster leg as refusal-only, the heartbeat leg unchanged, and the transcript leg with its location rule and its dispositions. The location rule is the resolver section 3 adds, which lists the harness projects directory for `<Session>.jsonl` by the entry's own `Session:` and answers a distinct state per case, never memq's `sessionTranscriptDir`, whose null folds every case but a fresh transcript into one answer. The dispositions are stated per state: present and fresh reads live whatever the heartbeat says; present and stale past the per-entry bound the runbook derives from the entry's `Role:` corroborates exit; absent (the root listed whole and no project directory holding the id) corroborates exit; unknown (the root or a project directory unreadable, or the listing cut by the cap) reads unknown, never exited; and ambiguous (two or more project directories holding the id) is read as unknown, so no state the resolver cannot settle lands in the delete direction. Amend the claim protocol on both of its liveness surfaces. The probe paragraph carries the claim's `Session:` and asks the answerer whether that id is its own, with the disclaim disposition of decision 4 beside the existing answered and unanswered ones. The release's second leg (the paragraph opening "The release is the delete rule's carve-out") takes the same refusal-only roster reading plus the transcript reading on the claim's `Session:`: a name matching two or more rows no longer reads as live, the claim's `Session:` is what the transcript is resolved by with the same per-state dispositions as the prune, and the leg's closing sentence on a manufactured no-row reading is restated against the new key, since a `Session:` any writer can set still narrows an honest writer without authenticating one. The skill states that no artifact on this machine independently attests that a session id is dead twice, as the participle clause of the prune basis in the paragraph opening "The list is the contract" and as a full sentence in the release mechanic, the paragraph opening "The mechanic is the coordinator deleting the claim file"; remove both copies, each replaced with the transcript leg's own limit: the transcript attests activity, and an idle live session and a dead one read alike until the bound passes. The takeover ritual's wake step (step 3, "Arm the seat's wake") drops its claim that the prune reaches only an entry no roster row resolves to and so never reaches an on-roster seat, since a roster match vetoes nothing after this section; a no-loop seat is exactly one whose heartbeat and transcript both go stale, and the step's remaining reason for not waking one, that a recurring timer with no pass and no runbook step to run at it buys the seat nothing, stands on its own. Whole-file review by the reviewers, because the skill's amendments collide with unchanged neighbours (project memory `skill-amendments-collide-with-neighbours`).

Acceptance: the three readings stated with their per-state dispositions on both the prune and the release; the disclaim disposition present; no sentence left in the contract, the release, or the ritual that lets a matched name veto an exit or a release; neither copy of the no-artifact sentence left, checked by grep for "independently attest"; a parity check that every copy of the prune rule in the coordinator skill points at this contract rather than restating it; targeted lane green with its delta named against a recorded baseline; no em dashes.

### 2. The coordinator runbook takes the same readings and five owed amendments. Model: opus

Re-derive anchors at dispatch: this file is rewritten by the board-routing-and-homing plan earlier in the queue. Amend the roster-diff and heartbeat paragraphs to the three-reading verdict of section 1, pointing at the role skill for the rule and stating only the coordinator's own procedure and the per-entry bound it derives from `Role:`. The BLOCKED funnel's first leg (the sentences opening "Live: the join resolves the identifier to a roster row" and "What this leg's readings buy is refusal rather than corroboration") takes the same refusal-only roster reading: two or more matching rows no longer read as live by inheriting the diff's ambiguity default, since that default is gone, and the strength the leg borrows from the role skill's contract is named as the contract's three-reading verdict rather than its registry-mediated pair. Add the probe's `Session:` ask and the disclaim disposition to the claim adjudication paragraph. Three paragraphs keep their opening sentences because `test/doctrine-parity.test.js` slices them by those leads and fails loudly on a lead that moved: "Each of the three outcomes has its own disposition.", "An off-roster entry is not by itself a dead session", and "**The BLOCKED funnel.**" with its closing landmark "**A blocker's answer never returns". Then the five amendments the inbox owed this runbook: the store-durability test gains the `kit-sync-attempt` marker as a third leg with both readings spelled (marker advanced while the state file is stale means the spawn chain is broken; both stale means no spawn was attempted); the machine-state sentence a seat sends outward names the poll it rests on and its age, or says "not polled this turn", and an arbitration answer and a machine-state answer are separate sentences, this amendment being the clearance's coordinator half under decision 5, naming the clearance as a third act beside a session's own spawn and start, with the rule itself landing in the role skill's claim protocol under `claude-kit_kaizen-prose-batch_spec_v1.md` and the runbook pointing at it, or, where that plan has not landed it yet, stating it in the words that plan's section carries and leaving that plan to turn the statement into the pointer, the Chapter saying which; the incident funnel, on a `goal-blocked` event whose `docs/plans/` path no longer resolves, tries the same filename under the project's archive home before ruling the event unresolvable; the board-read step and every other path-naming lookup in the runbook name the operator memory tier as the layer that can override the path before the not-found branch is taken, narrowed to the control the runbook's channel-extension sentence already uses (the tier records and never establishes, so a path the tier supplies is acted on only after the seat confirms it with the operator over a warranted channel, and a lookup the seat would act on unconfirmed takes the not-found branch with the record reported beside it rather than the record's path), leaving "No memory tier is a source the board is re-derived from" unamended and true; and the BLOCKED funnel paragraph states that the leading-`BLOCKED:` re-prompt is leash machinery too: the leash's Stop hook, `plugins/claude-kit/hooks/kit-goal-stop.js`, blocks a stop whose last message does not lead with `BLOCKED:` or `WAITING:`, or leads with a capacity-shaped reason, and re-prompts the run to restate a well-formed leading line, and it does so only while a goal is armed, so an unleashed worker's declaration reaches the seat only if it was well-formed on the first try.

Acceptance: each amendment present and pointing at its owner where an owner exists; the funnel's first leg carries no two-or-more-rows-read-as-live default; the three pinned leads unchanged; the runbook restates no rule the role skill owns, per the ownership map; whole-file review; targeted lane green with delta named, `test/doctrine-parity.test.js` among the files run; no em dashes.

### 3. The stamp audit gains the transcript reading. Model: opus

Extend `plugins/claude-kit/hooks/kit-registry-stamp.js audit` with one reading per registry entry: locate the entry's transcript by its `Session:` through a resolver this section adds to `kit-registry-stamp.js` itself, where the audit lives, exported beside the audit predicates `test/registry-stamp.test.js` already reads. The resolver takes the projects root from memq's `harnessProjectsRoot`, lists that root and each project directory itself through `kit-read-lib`'s bounded listing, and answers one of five distinct states, carrying the file's modification time where there is one: present-fresh and present-stale (one file found, split against the bound the caller passes), absent (every directory listed whole and none holding `<Session>.jsonl`), unknown (the root or a project directory unreadable, or a listing cut by the cap), and ambiguous (two or more project directories holding the id). It does not call memq's `sessionTranscriptDir`, whose null folds absent, unreadable, ambiguous, and cap-cut into one answer, every case but the first being one this instrument must keep apart; `kit-goal.js` keeps that helper for the binding corroboration it does today. Report the transcript's age beside the entry's heartbeat age, and flag the two disagreements that matter, a heartbeat lagging the transcript by more than the seat-stop throttle window (the session is alive and not stamping) and an absent transcript (no session by that id on this machine). The reading is a report and never a verdict, exactly as the existing findings are. State on the report what it scanned and what it could not read, so a projects root it could not open reads as unscanned rather than as every transcript absent, and an ambiguous id reads as ambiguous rather than as absent. The role skill's audit-coverage paragraph (the one opening "A read of any of these files checks the stamps", at the sentence "What it covers is worth stating exactly") names the transcript reading beside the three it lists, since that sentence exists to keep a run that scanned nothing from reading like one that found nothing. Tests over fixture directories: a fresh transcript against a stale heartbeat flags, an absent transcript flags, an unreadable projects root reports unscanned, an id two project directories hold reports ambiguous and flags nothing, and a clean pair prints no finding.

Acceptance: the five fixture cases green, watched red first against the pre-section code; the audit's coverage line and the role skill's coverage paragraph both name the transcript scan; `node --test test/registry-stamp.test.js` green with delta named against a recorded baseline.

### 4. The dispatch brief and the reviewers' charters see nothing new here, and the security model names the transcript read. Model: sonnet

`docs/security-model.md` carries the coordinator seat's posture under `## The coordinator board`, which today prices its writes, its channels, and one input-side control and enumerates no reads; add a paragraph there, beside the input-side control paragraph (the one opening "One control here is input-side"), carrying the transcript modification-time read: what it discloses (that a session by that id was active at a time), that the read stats the file and never opens its content, and that its posture is the `boundTranscript` one the goal-state section already states, a path stat'ed and never followed. The SessionStart paragraph opening "The SessionStart hook reads the harness transcript store, listing only" is about a different reader and is not the landing place. In the same file, the paragraph opening "Two seat rituals read the operator tier for something privileged" counts section 2's path override as a third such read, under the same control the first two carry: the record is confirmed with the operator over a warranted channel before the seat acts on it, at each resolution, and a lookup the seat would act on unconfirmed never takes the record's path. Confirm by reading them that the executing-work brief clause and the reviewer charters need no change, and record that confirmation in the Chapter rather than editing them.

Acceptance: the coordinator board section carries the transcript read with its disclosure and its stat-only posture; the operator-tier paragraph counts three reads; the Chapter records the two no-change confirmations with the lines read; targeted lane green.

## Out of Scope

- Authenticating any field. Every reading here narrows an honest writer without authenticating one, and the transcript leg is corroboration, never proof.
- Changing the naming convention or the `[ref]` mechanism the harness prints.
- The claim's write side, its waiter rule and its release tombstone, which `claude-kit_claim-protocol-writer-side_spec_v1.md` owns.
- The doctrine's own clearance clause, which the kaizen prose batch plan carries.

## Assumptions

- assumed 2026-09-08 (source: the audit's reads live in `plugins/claude-kit/hooks/kit-registry-stamp.js` (`auditDir`, the function the `audit` verb runs) and `test/registry-stamp.test.js` reads that file's exports directly): the transcript resolver section 3 adds lives in `kit-registry-stamp.js`, exported beside the audit predicates, rather than in `kit-compact-lib.js`'s transcript helpers beside `sessionTranscriptPath`; reversal: the first second consumer moves it to `kit-compact-lib.js` and the stamp CLI requires it from there, with the tests following the export.
- assumed 2026-09-08 (source: `grep -rn "never reaches it\|staleness prune" test/` returns no pin over the takeover ritual's wake step, its only hits being unrelated board-write and memq pins): dropping the wake step's prune claim in section 1 reds no parity pin; reversal: a pin that reds names the sentence it wants, and section 1 restates the wake reason without the prune claim in that sentence's shape.
- plan review 2026-09-08 (plan-reviewer at fable, effort high): 13 findings, 12 fixed, 0 assumed, 1 asked, 0 discarded.

## Related

- `claude-kit_claim-protocol-writer-side_spec_v1.md`: the same protocol's other half.
- `claude-kit_kaizen-prose-batch_spec_v1.md`: carries the doctrine half of decision 5.
- `../archive/claude-kit_board-routing-and-homing_spec_v1.md`: rewrites the coordinator skill ahead of section 2; anchors re-derive.
- Operator memory `a-self-stamped-liveness-field-cannot-establish-exit`: the measurement and the general rule.
- Kaizen triage record `kaizen/archive/2026-09-02-pass-triage.md`: the notes this plan dispositions.
