# Backlog

The living handoff and next-steps doc for the kit. It carries active items only. When an item is done, it moves out to a dated snapshot in `archive/` (`backlog-YYYY-QN.md`) rather than being struck through in place, so this file stays lean and readable.

Per-plan history does not live here. A plan's Chapters are its own append-only record and travel with the plan into `archive/` when it closes. This file is for cross-effort next-steps that do not belong to any single open plan.

## Active

- **The size ratchet creates a governed moment the ownership map does not cover, so the rule for raising a cap archives with the plan that wrote it (2026-09-05, from the subtraction-bars plan's finishing docs curation; confirmed at source).** That plan's Decision 1 states the rule: a cap is a ceiling only the audits lower, and raising one is a deliberate edit to `test/size-budget.json` that rides in the diff for a reviewer to see. Nothing in `plugins/claude-kit/skills/operating-instructions/references/ownership-map.md` covers the moment itself, which is a session at a red ratchet deciding whether a curated file may grow and who may raise its cap; the nearest row, `:42`, is the heavy-process box budget and a different subject. The doctrine treats a moment the map lists as unowned as a gap to declare rather than a silence to fill, and once the subtraction-bars plan archives, the only durable statement of the rule is the script's own header, which no row points at. Repair shape: one map row naming the moment, with `testing-discipline` or `writing-skills` as owner according to which corpus the cap sits in, or a single row for the ratchet naming the script and the budget file. Signal: the first cap raise after this plan archives, or the next edit to the ownership map.

- **The prose bar has no sentence-level trigger where the retire rule has an explicit one, so the two halves of the subtraction-bars mirror are unevenly discoverable (2026-09-05, from that plan's finishing docs curation; the report's stronger claim is corrected here rather than carried).** `plugins/claude-kit/skills/testing-discipline/SKILL.md:3` names the retirement moment in its own description ("whether a test already in the tree still earns its keep", "a test whose subject moved or was deleted, a suite audit"), so the retire rule loads at the moment it governs. `plugins/claude-kit/skills/writing-skills/SKILL.md:3` names no sentence-level or prose-audit trigger: not sentence length, not mannered prose, not whether a sentence earns its place. The curation report read that as the bar loading only where a session already loaded the skill for another reason, which is too strong and is not what the tree shows, since that description does trigger on amending curated prose the kit ships and on correcting a claim a curated document states, which is the moment the bar mostly applies at. The real gap is the missing audit-shaped trigger, which is the moment the corpus audit arrives at. Repair shape: add the sentence-level and prose-audit triggers to that description, matched to the sibling's phrasing rather than reworded, and price the addition against that file's word cap. Signal: the corpus audit being armed, or the next edit to either skill's description.

- **A committed kaizen note names another project's internal guard file and describes a working bypass of it, in a repository that is public (2026-09-05, from the subtraction-bars plan's finishing security review; escalated to the operator rather than edited).** The note dated 2026-09-04 in `kaizen/notes-SCOTT-CLAUDE.md` names a guard file belonging to another project on this fleet and states how the guard is got around. `gh repo view` reads this repository's visibility as PUBLIC, so the line is published. The finishing pass did not edit it, on two grounds: the line is another session's, in a file several sessions append to, and rewriting another seat's inbox line is neither that seat's business nor that plan's. What the operator decides is whether the line is rewritten to keep the friction and drop the bypass, or left standing because the guard it names is not a security boundary. Repair shape once decided: rewrite the note in place, since the inbox is append-only by convention rather than by contract, and remember that git history keeps the original either way, so a rewrite reduces discoverability rather than erasing the text. Signal: the next kaizen pass over this inbox, or any review of what this repository discloses.

- **The test-audit plan opens on suite figures the tree has outgrown (2026-09-05, from the subtraction-bars plan's finishing prose review).** `docs/plans/claude-kit_test-audit_spec_v1.md:11` states the suite as 96,900 lines and 3,034 tests across 52 files at HEAD. The tree now reads 101,092 test lines, 3,073 tests and 53 test files, from `node plugins/claude-kit/scripts/kit-size.js check` at the close of the subtraction-bars plan. The figures are the plan's own opening frame rather than an acceptance clause, so nothing reds on them, and a plan that starts by measuring the corpus is exactly the document where a stale measurement misdirects the work it scopes. Repair shape: re-read the figures from the ratchet's own verb when that plan starts, and record them with the moment pin a measured figure carries rather than as bare numbers. Signal: the test-audit plan being armed.

- **Three prose defects in the writing-skills skill, none inserted by the effort that found them (2026-09-05, from the subtraction-bars plan's finishing prose review; routed rather than fixed under that plan's Decision 3 routing rule).** Two are confirmed at source in this checkout. `plugins/claude-kit/skills/writing-skills/SKILL.md:21` cites a dated audit anecdote ("the 2026-07-14 stabilization audit found a dozen drifted copies, one in outright contradiction") inside a rule, which is journey where the surrounding text is state, and the doctrine's own delete-litmus is what convicts it. And `:42` restates the count of its own table's rows in prose ("not just the four forms above"), which is the restated-count shape Decision 3 of the subtraction-bars plan repairs by deleting the count rather than pinning it. The third is carried from that review's report and not re-derived here: the review found the skill's list of forms narrower than the set the doctrine admits, and its fuller account lived in the review's inline report rather than in a kept artifact, so the closing session re-derives the claim against the doctrine's Style section rather than looking it up. Interim board 9's ruling 83 in the subtraction-bars plan doc is the provenance for all three. Signal: the corpus audit reaching this skill, or the next edit to its form table.

- **The size-ratchet suite mutates the process environment at module scope, so its isolation is a property of load order rather than of any test (2026-09-05, from the subtraction-bars plan's finishing adversarial review; confirmed at source, not a defect in what the suite asserts).** `test/size-ratchet.test.js:148-150` assigns `process.env.HOME`, `process.env.USERPROFILE` and `process.env.XDG_CONFIG_HOME` at module scope, before any test runs, because the child processes it spawns inherit the environment and the untracked-path reading depends on it. Nothing here is wrong today: `node --test` runs each file in its own process, so the mutation cannot reach a sibling file, and the suite asserts the redirect held at `:2467-2470`. What it costs is that the guarantee sits in the runner's process model rather than in the test, so a future runner change, a concurrency mode that shares a process, or a helper imported from this file into another, silently moves the redirect's blast radius. Repair shape: move the assignment into a setup the tests call, or state the load-order dependence in a comment beside it so the next reader does not have to reconstruct why it is at module scope. Signal: any change to how the suite is invoked, or the first test file importing from this one.

- **The Haiku implementer charter is missing the settle-the-test-question sentence its three siblings carry (2026-09-05, from the subtraction-bars plan's finishing prose review; confirmed by comparing the four charters).** `plugins/claude-kit/agents/implementer-haiku.md` carries no statement of the duty to settle the test question past the brief's own `Tests:` floor, where `implementer-sonnet.md`, `implementer-opus.md` and `implementer-fable.md` each carry one. The gap matters most at exactly this tier: the brief for a Haiku section names its gate explicitly, so a charter silent on the duty leaves the cheapest implementer the one least likely to add cover the brief did not think to name. Repair shape: add the sibling sentence, matched to what the other three say rather than reworded, and check whether `test/readonly-agent-guard.test.js` or the doctrine parity suite should pin the duty across all four charters so the next charter added is held to it. Signal: the next edit to any implementer charter.

- **Two corpus-wide prose classes the subtraction-bars plan measured and deliberately did not cut (2026-09-05, from that plan's finishing prose review; the corpus audit is their home).** The finishing reviews found journey-tense clauses and sentences past the length diagnostic in curated prose that the subtraction-bars effort did not insert, and a density pattern the reviews named separately: the "X rather than Y" construction used as a default sentence shape rather than where a contrast is load-bearing. Both are class findings over the corpus rather than defects at a site, and the subtraction-bars plan cut nothing by design, its job being to write the bars the audits cut to. `docs/plans/claude-kit_corpus-audit_spec_v1.md` is the plan that cuts to them and is authored and parked. Repair shape: both classes are swept by a predicate over the class rather than by a list of sites, and the sweep's own control is a planted instance withheld from its literals. Signal: the corpus audit being armed.

- **Section 2's acceptance clause in the subtraction-bars plan is satisfiable by a reading nobody performs (2026-09-05, from that plan's own finishing review; recorded at close rather than repaired, since the section shipped and the plan is archived).** That section's acceptance reads that a fresh-context read of either reviewer charter names the classes and where their definitions live. A clause whose test is a reading no gate performs is discharged by judgment rather than by evidence, which is the shape the same plan's Standing Brief Amendment 4 rules out for a closure claim: where a class can be closed mechanically, the mechanical check is the closure and the prose is not. The section did land a mechanical closure, the cross-surface agreement sweep, so the defect is the wording of the clause rather than the state of the tree. Repair shape: when a future plan writes an acceptance clause whose subject is what a reader would understand, pair it with the predicate that a gate can run, or state plainly that the clause is adjudicated by a dispatched reader and name the reader. Signal: the next plan whose acceptance turns on comprehension rather than on a check.

- **The shared-tier `glob:` refusal is restated at both `triggers` producers instead of living in the gate they both already call (2026-09-03, from the memory-recognition-reach plan's finishing security review; one Minor, confirmed at source, not exploitable).** `plugins/claude-kit/scripts/memq.js:10533` and `:10815` each call `triggerEntryFault(one)` for the entry grammar, and each then restates the shared-tier `glob:` refusal on its own, at `:10552` and `:10820`. Both enforce it today, so nothing reaches past either copy and there is no exposure to fix. What it costs is the second-producer drift shape the doctrine names for a sanitising guard: the next writer of a `triggers:` line, a future `--update` path or a repair verb, inherits the grammar for free and the tier rule only if someone remembers it. The two copies have already stopped being identical, `:10552` guarding on `(toType || toOperator)` where `:10820` guards a bare `glob:`, which is the drift beginning rather than merely threatened. Fix as the reviewer proposed: a `sharedTriggerEntryFault(entry)` wrapping `triggerEntryFault` plus the glob refusal, called from both sites.
- **Two operator-tier records with confirmable handles are still handle-less, and no document names which two (2026-09-03, from the memory-recognition-reach plan's section 3 security review; routed to section 5 and never applied).** Section 3's review found two records that had genuinely confirmable handles and had been put on the handle-less list on an unconfirmed-identifier ground this checkout settles, and it routed the write to section 5 rather than making it at the time. The finishing pass checked the store and found that all 52 records Chapter 3 names as handle-less still declare no handle, so the write never landed anywhere. The two cannot be recovered from the plan doc, because that document names the pair only as a count and never by name. Closing this means re-running section 3's confirmable-identifier judgment over the current 73-member handle-less list rather than looking the answer up.
- **A trigger withdrawal is inferred from absent arguments rather than asked for, so an argument loss reaches the destructive form (2026-09-03, found by the blind lens at the fifth review round of the memory-recognition-reach plan's section 5; weighed there and deliberately not fixed).** `memq triggers <name> --replace` with no `<type>:<pattern>` entries removes the record's `triggers:` line outright, which is the intended withdrawal spelling. The hazard is that it is also what a truncated command looks like: the Windows command wrapper hands its line to a shell that cuts it at the first newline and drops everything after, so a narrowing replace whose trailing patterns are lost succeeds as a silent withdrawal, exit 0 with nothing on stdout. The shared-tier consent flag does not discriminate, because a caller typing a shared-tier replace types that flag anyway. Not fixed in the section that found it, on three grounds worth keeping with the item since they are what a future reader should re-test rather than re-derive: the loss is bounded and recoverable, the record keeping its body and its applied history while the previous line survives in the backup beside it and in the store's own git history; the shape needs the wrapper path plus a multi-line invocation to arrive at all; and the repair is a new consent token, a capability the specification never scoped, landing in the verb, its usage string, the skill's rows, four curated documents and the tests at the point where five review rounds had converged. Repair shape: refuse a zero-entry `--replace` unless it carries an explicit withdrawal token, so the destructive form is asked for rather than inferred, and pin both directions. Signal: the next edit to the verb's argument parsing, or the first report of a trigger line that went missing without anyone meaning it.

- **The memory-system skill's frontmatter `description` runs well past the documented cap for that field (2026-09-03, found by the blind lens reviewing the memq triggers change in the memory-recognition-reach plan).** `plugins/claude-kit/skills/memory-system/SKILL.md`'s `description` measures around 1817 characters against a 1024-character cap Claude Code documents for skill frontmatter. The overage predates the change that surfaced it and no repo test or doctor check pins the field's length, so nothing in this tree would ever report it. Two things are unconfirmed and are why this is a backlog item rather than a fix: whether the cap is enforced at all on the harness this kit runs against, and what the failure mode is if it is, since a silently truncated description degrades the skill's discoverability rather than erroring, which is the shape nobody notices. Not fixed in the section that found it because shrinking the description is a judgment about what the skill advertises to a session choosing skills, not a mechanical trim, and the same question applies to every other skill in the tree rather than to this one alone. Repair shape: measure every shipped skill's description against the live harness once, then either pin the length in the doctor or record that the cap does not bite here. Signal: the first time a session fails to find the memory-system skill by description, or the next documented change to the frontmatter cap.

- **Six index lines in the machine-wide operator memory tier name records that do not exist (2026-09-02, found while backfilling recognition triggers in the memory-recognition-reach plan).** `~/.claude/memory-operator/MEMORY.md` carries 248 entries against 242 record files, re-counted at the close of the memory-recognition-reach plan's section 6; the six names below are unchanged, so the gap is those six and the totals have simply grown with the tier. The six with no file behind them are `untracking-a-claim-file-does-not-hold`, `ollama-wedge-shapes-and-fixes`, `sidecar-divergence-alerts-are-a-mixed-signal`, `harness-snapshots-hook-config-at-session-start`, `shared-file-list-comes-from-the-diff-not-from-recall` and `a-retirement-leaves-its-mirror-image-standing`. None sits in the tier's `archive/`, none is tracked in the store repository now, and `git log` over one of those paths returns two `kit memory sync` commits, so each existed and went. The failure is silent in both directions a reader could notice it: following such a line returns nothing, and `memq recall` counts files rather than index entries and reports 242, so the digest and the index disagree with nothing saying so. Not repaired here because the repair is a write to a shared store whose right form is itself the decision, restore the six from the store's own history or drop the stale lines, and because the store syncs between SCOTT-CLAUDE and NEO-CLAUDE, which puts the other machine's copy inside the question. Signal: the next store sync reconciliation, or the first session that follows one of those lines and reports an empty read.

- **A homing round is auditable one round at a time and not in aggregate, because the journal that records it is folded away before an aggregate question is usually asked (2026-09-02, from the board-routing-and-homing plan, sections 2 and 3).** A homing round publishes a coordinator board line to the memory store or the kaizen inbox and then cuts the line, and the record of that irreversible move is a memq outcome-journal entry written at two moments. That entry is complete while it lives, so any single round can be audited. The decay pass folds entries into per-key rollups at thirty days and keeps the tally while discarding the prose, so after that window the fleet can say how many rounds ran and not what any of them moved. The plan declared this a residual rather than fixing it, because retention belongs to the memory-system skill and no deletion-shaped repair exists inside the coordinator contract. The fork is the operator's: accept the tally-only horizon, lengthen retention for this key class, or have a round write its own durable summary somewhere the rollup does not reach, each trading store growth against how far back a cut can be reconstructed. Signal: the first time someone asks what a board lost over a quarter, or the next retention change in the memory-system skill.

- **A machine-wide seat's audit record lands in a project-scoped store, so the account of an irreversible act can rest where no later reader resolves (parked 2026-09-04, from the board-routing-and-homing plan, section 6).** The coordinator seat is one per machine and has no home project. The record of a homing round, which publishes board content outward and then cuts the board line irreversibly, is two entries in the memq outcome journal, and that journal resolves per call to whichever store memq's own ladder answers with. Readers exist and are real, `memq get` by exact key, `find` by key substring, `recent` in a window, and `recall` read whole at every seat takeover, but each reads only the store its own call resolves, so a round logging from one checkout is invisible to a takeover standing in another, and `recall`'s digest is budget-bounded besides. Section 6 established that neither available fix belongs to this plan: a read-back inside the round needs a machine-stable key, and naming a home checkout resolves a store location out of a machine-local signpost. The memory-system skill owns both the journal key grammar and the store resolution, and it already carries a machine idiom for a record, so the direction is a machine-scoped journal home or a machine component for the key on that surface. Parked rather than specced because it is a change to another skill's owned contract and the appetite for one is the operator's call.

- **A branch cut for a parallel queue has no standing signal when the trunk moves the plan documents it will execute (parked 2026-09-02 by the kaizen pass; from two same-day instances across two repos on 2026-08-31).** A worktree branch ran for hours ten commits behind the trunk while three of its own queue's plan docs changed there, two amended after the cut and one authored after it and never visible in the executing tree. The instrument the machine coordinator supplied is a left-right count against the trunk plus a merge-base range filtered to `docs/plans/`, one command per branch, read at session start or at chapter boundaries. Its caveat is what parks it rather than ships it: both halves read the branch's published tip, which is a publication event and not a progress reading, so it false-alarms in the alarming direction for exactly the interval between a seat's in-tree merge and its push, observed live as an eighteen-behind reading against a tree whose refresh was already merged. Candidate homes: the session-start worktree hints, or the branch-hygiene strand check, which covers the frozen-branch direction and not this inverse. Signal: the next time a branch-executed plan is found to have run against a superseded plan doc, or a design for reading the worktree rather than the tip as the progress surface. Decision protocol: ship only with the tip-versus-tree caveat in the instrument's own words.

- **The Ollama watchdog's state logic has no test coverage, and no human drives it (2026-09-02, from the judge-partial-input finishing reviews; adjudicated a standalone rider).** `sidecar/ollama-watchdog.ps1`'s strike accounting, corrupt-strike-file degrade, and foreign-owner refusal are machine-only contracts: nothing exercises them until the host wedges, which is exactly when they must work. The repo already spawns PowerShell from node tests (the doctor tests are the sibling pattern), so the state logic is drivable without the host; the `Restart-Service` leg stays honestly host-bound. Fix shape: a small test file driving the script with a stubbed probe (the probe URL is a parameter) through strike accumulation, corrupt-file degrade, and the owner refusal. Signal: the next watchdog edit, or the first real firing on the host.

- **The judge request sets no num_ctx, so the instrument's context window is whatever the endpoint's server configuration happens to be (2026-09-01, from the judge-partial-input sections 2+3 review; declined for that changeset with the reload analysis).** `sidecar/judge.js`'s buildRequest sends num_predict and temperature only, and `sidecar/config.js` accepts url/model/timeoutMs, so a server whose default window is smaller than a worst-case judgment call (~7.5K tokens at the 6,000-char caps) silently drops the front of the prompt - SYSTEM's verdict definitions and fence rules - while the schema-constrained decode still returns a well-shaped verdict. It was declined in-changeset because the deployed server is configured at 16K (worst case fits) and a per-request num_ctx differing from the server's forces a model reload per alternation on the shared serial lane, which is its own outage shape. Fix shape: an optional config field the operator sets to match the server, sent only when present, with the mismatch-reload hazard documented beside it; size the worst case against the daemon's read-side cap (ENTRY_FIELD_CAP, 12,000 characters per judged field, so a hand-written spool line can present a ~30K-character triple), not the hook's 6,000, since on overflow the server drops the front of the prompt - SYSTEM's fence rules - while keeping the attacker-authored tail. Signal: the next endpoint resize, or a verdict log showing well-formed verdicts with reasons unmoored from the intent.

- **Five moments the ownership map lists as unowned or contested need the operator's ruling (2026-09-01, from the precedence-and-ownership pass).** The map is `plugins/claude-kit/skills/operating-instructions/references/ownership-map.md`, its last table; the doctrine's intake-gap rule governs each until ruled. Recommendations, one per row: (1) the Branch-and-PR pull-request moment goes to `executing-work` as a section-loop moment (one draft at the first section close, refreshed each section, which is what the plan-doc machine contract already states and the fleet reads) with `finishing-work` flipping it ready and `curating-docs` pointing rather than stating; the alternative, one PR opened at finishing, is fewer PRs at the cost of rewriting the fleet-facing contract. (2) The doctrine's authorization sentence names the Commit Model header generally rather than Commit-and-Push alone, so the pushes Branch-and-PR directs are authorized by the same header form; sequenced strictly behind the standing-grants plan's section 3, which replaces that exact sentence verbatim, since two edits to one anchor cannot both land. (3) `branch-hygiene`'s recovery step requires the recovery branch to sit in the merged set before the stranded original is deleted, making the recovery step and the auto-delete rule one rule; the "ideally merged" softener goes. (4) No commit-locally-never-push model is added: an unpushed commit is not a durable artifact under the doctrine's recovery rule, which is why the set has no such value; a seat asked for one declares the gap and takes Review-Only or Commit-and-Push as the operator names. (5) The seat-git-in-the-store row needs no new ruling: the standing-grants plan's section 2 is the fix, and the row leaves the map when it lands. Signal: the next session that meets one of these moments, or the arming of the scenario-probes plan, whose first probe set needs these answers.

- **The judgment battery has no partial-input cases, so the contract the judgment-v3 prompt added is unmeasured by the gate that scores it (2026-09-01, from the judge-partial-input section 1 review round).** The 13 frozen cases predate the capture-cut notice, the ACTION cut label, and the absence-leniency instruction, so a wording change to any of those can shift verdicts with no battery case moving. Extend `sidecar/batteries/judgment-v1/` (as a new battery version, keeping v1 frozen) with cases exercising a flagged-and-noticed input, a prompt-cut command with the label, and an unmarked-but-tool-cut result. Signal: the next judgment prompt wording change, which is the moment an unmeasured contract costs something.

- **The judge sends the fenced triple to the model with no invisible-character strip (2026-09-01, from the judge-partial-input section 1 review; a v2 carry-over).** `sidecar/judge.js` applies `neutralize()` to the model's answer only; the triple from `formatTriple` goes to the wire raw, so zero-width and bidi characters in a judged command or result reach the model unneutralized. There is a real design tension (the judge is supposed to see what ran, and stripping mutates evidence), which is why this is a design item rather than a fix: decide between stripping, marking, or documenting the pass-through as posture. Signal: the next judgment prompt version, or a finding that invisible characters steered a verdict.

- **The sidecar daemon's scheduled task runs production from a live git worktree, so uncommitted edits are production behavior (2026-09-01, from the judge-partial-input section 1 security review; candidate spec).** `sidecar/daemon-task.ps1` launches `node` on this checkout's files and the installer registers AtLogOn plus 15-minute repetition, so any daemon exit promotes whatever sits in the worktree, mid-edit states included, with no rollout decision. Candidate fix shapes: pin the task to a committed install (a copied payload or a tag checkout), or gate the relaunch on a clean-tree check. Deserves its own small spec rather than a drive-by. Signal: the next daemon incident traced to a half-edited worktree, or the next plan whose rollout section has to reason around this hazard.

- **recognition-v1 still opens with a blanket hedge, cites judgment-v2 as its exemplar, and its head-slices can now bisect an in-band cut marker (2026-09-01, from the judge-partial-input section 1 and sections 2+3 reviews).** The judgment prompt dropped its blanket hedge on the measured argument that a hedge on every input carries no information; recognition-v1 still carries one, and its header points at v2 as the pattern to follow, which is now a retired wording. Separately, its 800/1000-character head-slices are marker-unaware, so a field the capture pipeline cut (which now carries head, marker, tail) can reach the recognition prompt with a bisected marker fragment astride the slice point - the exact mangled-marking shape the capture side's own re-cut discipline exists to prevent, escaping through this one consumer. Bring the hedge posture in line (or record why recognition differs), repoint the exemplar, and route recognition's cuts through the shared marker-aware cutter or strip partial markers. Signal: the next recognition prompt edit, or the next harvest showing marker fragments in recognition inputs.

- **The judge misreads tool-side-truncated observations as diverged: case #13 regression, candidate judgment-v4 (2026-09-01, from the judge-partial-input section 1 battery adjudication).** Battery case #13 is a read-the-diff call whose output the tool cut before capture, so the pipeline correctly marks nothing; judgment-v3 reasons from the visible cut to a false diverged in three of three runs (v2 and the round-1 overclaiming text both got it right; the honest completeness claim costs the case, adjudicated and accepted at 12/13). Any repair must extend absence-restraint to visibly-cut-but-unmarked inputs without re-breaking cases #1/#5, the false-achieved traps v3 exists to fix. Pin a battery case for the tool-cut shape before any wording moves, so the next attempt is measured. Signal: the next judgment prompt version, or a fleet verdict log showing false diverged on truncated observations.

- **The two signpost installers create their temp file non-exclusively, where the library's own atomic writer uses an exclusive create (2026-09-01, from the durable-boundary finishing pass's security review; declined for that effort and routed here).** Section 5 of the durable-boundary plan gave `setup.sh` and `doctor.ps1` a temp-and-rename write for the kaizen signpost, so nothing is written through a link standing at the signpost path itself. The temp name is not held to the same standard: `setup.sh` writes `claude-kit.local.json.tmp-<pid>` with a plain `fs.writeFileSync` and `doctor.ps1` writes a fixed `$signpost.tmp`, so a link pre-planted at either temp name is followed, and the fixed name additionally means two concurrent doctor runs collide on one temp file. It was declined for that effort on the argument that the whole path sits inside the single-principal `~/.claude` boundary the security model places outside every control, so an actor able to plant a link there can write the signpost directly and the exclusivity buys nothing against them; `docs/security-model.md` makes no exclusivity claim about the temp path, so nothing untrue ships either way. What is worth doing is parity: both writers should take the exclusive create `atomicTmpPath` already uses, and the doctor should carry a per-process temp name, so a later reader of either writer does not conclude the weaker shape was chosen deliberately.

- **A Chapter registers one section, and this repository's plans routinely close several in one Chapter, so the machine-read section count under-reports without saying so (2026-09-01, from the instruments-not-prose finishing pass, found while archiving the plan).** The plan-doc machine contract reads the first `Completed:` line of each Chapter and matches it against one section number or one section title, so a Chapter closing three sections can register at most one of them, and a phrasing like `Completed: Section 1 (title), Section 2 (title)` registers none at all, since the value has to open with the bare number. The instruments-not-prose plan hit both halves: two of its Chapters carried the non-registering phrasing, and the first of them closed three sections. Repairing the phrasing at the close moved its registered count from four of eight to six of eight, and the remaining two cannot register from the Chapter that closed them. Nothing surfaces the shortfall: terminality rides on the `Status:` header, so a plan can read Complete while its own section count says otherwise, which is a reading instrument disagreeing with the record it reads. Fix shape is a contract question rather than a patch, and it is coordinated with the external engine rather than a drive-by: either the parser reads every `Completed:` line in a Chapter, or the kit's Chapter template stops closing more than one section at a time. Signal: the next plan whose Chapters close sections in batches, or any work on the plan-doc parser.

- **`kit-registry-stamp.js push --takeover` re-stamps `Started:` over an entry that already carries one (2026-09-01, from the instruments-not-prose finishing security review; routed rather than repaired).** `stampRegistryStatus` at `plugins/claude-kit/hooks/kit-registry-stamp.js:337-340` passes `['Started', 'Status-updated']` on every takeover with no guard against an already-stamped field, so nothing mechanical holds `Started:` to the one write per registration the calling convention assumes, and a second takeover rewrites the field with the moment of the push rather than of the takeover. The consequence is fail-safe rather than damaging: a `Started:` sitting ahead of `Heartbeat:` pushes the coordinator's exited predicate to unknown, which keeps the entry rather than pruning it. Not repaired in the finishing pass because it is a behaviour change arriving after both review gates had reported, it needs its own red-first test to be honest, and there is a real design question under it about whether a same-session second takeover is ever legitimate. Fix shape: refuse the stamp when `Started:` parses as a moment rather than as `none`, with the red written first. Signal: a second seat is seen taking over one registration, or the exited predicate is tightened.

- **`kit-registry-stamp.js audit` returns exit 1 both for a refusal and for a scan that completed and found something (2026-09-01, from the instruments-not-prose finishing security review; routed rather than repaired).** A scripted caller reading only the exit code cannot separate "scanned and dirty" from "never scanned", which is the same absence-reads-as-clean shape the plan exists to remove, arriving on the reading side rather than the writing side. The module header documents the conflation as design, and no caller gates on it today, which is why it is a guard item rather than a defect. Fix shape: give the refusal its own code, distinct from both the clean scan and the scan with findings, and pin all three under Standing Amendment 3's every-plausible-input rule. Signal: the first caller that branches on this command's exit code.

- **The machine-configuration epoch record is a decay candidate, and the convention that depends on it assumes it is not (2026-09-01, from the instruments-not-prose Section 2 review; routed rather than folded).** The moment-pin convention reads every measured figure against a per-machine epoch record at the canonical name `machine-configuration-epoch-<hostname>`, and the memory-system skill states the expiry rule that does the reading. The record itself carries no `pinned:` field and no write path emits or removes one, so it is an ordinary operator-tier record: a summarize candidate at 30 idle days, whose body condensation can drop the very figures the comparison needs, and an archive candidate at 60. It is read only when a comparison is actually made and stamped only on an explicit `touch --applied`, which is exactly the ambient under-stamped shape pins exist for. Once archived, the expiry rule's third case fires, a machine no epoch record covers, and the next session writes a duplicate over a shadowed name. Not folded because the honest fix is a memq change rather than a skill sentence: writing that the record is pinned would assert a mechanism the tool does not have, which is precisely the prose-wearing-a-number's-clothes defect the plan exists to remove. Fix shape: give the epoch name a decay exemption in memq's decay scan, or emit `pinned:` on the `add-operator` path for that canonical name, with the usual red-first decay tests. Signal: the first epoch record to reach 30 idle days, or the next decay pass run on a machine whose epoch was written more than a month earlier.

- **Two SessionStart hooks can each tell a session to restart over the kit's version, keyed on two different identities (2026-08-29, from the plan-lifecycle Section 3 review; routed rather than folded).** `session-start.js` now emits a session-level line when the plugin view this session loaded differs from the machine's install, naming the cache-directory sha, and `kit-version-nudge.js` has emitted its own restart nudge for longer, naming the build stamp's short git hash. Both are wired on startup, resume and compact, so one payload can carry two restart requests naming two different version strings for the same session, or one can speak while the other stays silent about the same underlying fact. Neither is wrong: they answer different questions, the first whether the installed payload moved under this session and the second whether the build stamp inside the payload changed, and the second is what catches a rebuild-in-place that leaves the cache directory name alone. What is missing is a decision about which of them owns the session-level restart claim, and single-sourcing the identity string so a reader is never handed two spellings of one version. Rated Minor by the blind lens and deliberately not folded, because picking an owner is a design call about two hooks that predate and postdate each other rather than a defect in either. Fix shape: give one hook the restart claim outright and have the other state its fact without the directive, or merge the two readings into one block that names both identities once. Signal: the next report of a restart nudge that a restart did not clear, or the next change to either hook's trigger.

- **The goal CLI's unknown-flag refusal covers `arm` alone, and `--version` still prints no version (2026-08-29, from the plan-lifecycle Section 1 review; the section shipped the `arm` half deliberately).** Section 1 of the plan-lifecycle effort made the `arm` dispatch refuse any leading-dash token that is not `--append`, with a usage error naming the token and the running build. The section body scoped the harm to `armGoal`, because that is where an unknown flag was silently read as a plan path and produced the "plan not found: --append" misdiagnosis, and the shipped code matches that scope. The plan's own Goal states the CLI unqualified, so two gaps stay open behind the fix. `clear` and `status` still accept an unknown leading-dash token in silence, which is a quieter failure than `arm`'s was rather than a safer one: nothing is misread as a path, so nothing is reported at all, and a session passing a flag a newer CLI would honour gets the unflagged behaviour with no signal that its flag was dropped. And `--version` itself, the one token a reader would reach for after seeing a version in a usage error, falls through to the bare usage line and prints no version, so the refusal teaches a spelling the CLI does not answer. Both reviewers raised the pair as one Minor and it was recorded as scope rather than widened, since widening the screen to every dispatch is a behaviour change to commands the section never tested. Fix shape: lift the leading-dash screen to the argv dispatch ahead of the command switch, with each command declaring its own known flags, and give `--version` a real handler over the same build-stamp read `arm`'s refusal already uses. Signal: the next flag added to any goal CLI command, which is the moment the silent-swallow costs something.
- **The store sync's staging is unattended and path-shaped, and two of that property's three consequences are unpriced (2026-08-29, coordinator-routed, adjudicated by the expert seat).** The sync pass stages with `git add -A` before committing (`plugins/claude-kit/doctor/install-memory-sync.ps1:808`, confirmed at source independently by two seats), so admitted content is committed with no seat's deliberate act, and the allowlist screens by path form alone. The security model prices this property for exactly one of its consequences, secrets: its Storage properties section states admitted content is committed "with no further deliberate act" and the boundary is "path-shaped rather than content-shaped". The second consequence, lifecycle, is live: `claims/heavy-process.md` is an ephemeral lock whose designed end is deletion, admitted as a coordinator-tier `.md`, and the role skill's sync-exposure sentence names the churn and the modify/delete conflict whose blast radius is the whole store's sync standing down, but prices the admission as documentation rather than as a choice with an exclusion option. Recommended for that leg: a `claims/` carve-out from the allowlist, since the heavy-process slot is one box's resource, fleet visibility of holds is no stated goal, and the conflict cost is store-wide. The third consequence, authorship, is priced nowhere: the doctrine's stage-exactly-your-target rule screens by authorship so one session never commits another's in-flight work, while the sync has no authorship screen and cannot express "not mine to commit", so a peer seat's in-flight store edits are staged and shipped unattended (field instance: a coordinator declined to stage peer-modified store files and the sync's screen would have carried them regardless; reported from that seat's account). The gap is a reasoning stop rather than a missing concept: the same document handles writer-scoping deliberately elsewhere, "neither release marker is scoped to its writer" being an argued accepted risk, and the secrets paragraph itself names the directory's multi-writer population, so an absence beside a worked example reads as a decision. Fix shape: the claims carve-out ships as an allowlist change with the usual red-first sync tests; the authorship leg is written in the security model's own accepted-risk form and either accepted on argument or closed with a mechanism. Signal: the next change to the sync allowlist or the claim protocol, or the next multi-seat store incident.

- **An engineless agent fan-out is invisible to the load poll and outside the claim-worthy class, so the box can be loaded with nothing declaring it (2026-08-29, from the kaizen pass; the parent finding is otherwise resolved).** The claim protocol already retired the process poll as a release verdict on exactly this ground ("absence hides in-process agent fan-out", role skill), puts claims at subagent spawn granularity through the dispatch-brief clause, and bars a clean poll reading from licensing any act, which resolves the routed finding's substance. The residue: the claim-worthy class is enumerated as a suite, a build, or an embedding pass, so a session driving several review-class agents that spawn no engine writes no claim and the coordinator's arbitration still cannot see that load, which is the shape of the one measured wrong grant (a slot cleared on a poll reading zero engines with at least five review agents live). Widening the class to every fan-out would put a claim ceremony on the routine two-reviewer section round, which is the ceremony-versus-window trade the index claim marker was declined on, so the widening waits for data. Signal: the next resource grant that goes wrong traced to an undeclared fan-out; decision then is whether fan-outs above some width join the claim-worthy class or the arbitration bullet gains a declaration ask instead.


- **An unscreened plan path reaches the goal CLI on the mid-run arm route (2026-08-29, from the review-and-record finishing security review; pre-existing).** The mid-run arm route passes a plan path into the goal CLI without the screen the token gate gives the CLI's other producer, a real second-producer gap beside that gate. Rated Minor and routed rather than fixed: the gap predates that effort, it is materially mitigated by the peer-sessions grant trace that runs before any arm, and closing it is a design change to a rule that plan does not own. Signal: the next change to the arm route or to the token gate.

- **Three prose Minors sit on surfaces the review-and-record effort did not write (2026-08-29, from that effort's finishing prose pass).** A dated audit narrative at `plugins/claude-kit/skills/writing-skills/SKILL.md:21`, a counted positional self-reference at `plugins/claude-kit/skills/peer-sessions/SKILL.md:137`, and a Chapter-field name at `plugins/claude-kit/skills/executing-work/SKILL.md:298` that the frozen Chapter format does not define. All three are real and all three fell outside that effort's scope, which is why they landed here rather than in its fix delta. Line numbers are as of that effort's close (commit `6d2e6cc`); re-locate by content.

- **`plugins/claude-kit/skills/executing-work/SKILL.md:351` records a reduced-effort review in the Chapter alone, where the kit's other recorded review downgrades reach the close-out (2026-08-29, from the review-and-record finishing fix round).** A justifying clause in `finishing-work` was narrowed to the downgrade set the evidence supports precisely because `:351` falsifies the universal: its reduced-effort record is Chapter-only, so a close-out reader never sees it. Routing that record to the close-out is the better rule and was deliberately not taken in the pass that found it, since no finding identified `:351` as defective, the site sat outside every section's scope, and editing an unrelated rule so a justifying sentence becomes true is scope creep wearing consistency's clothes. Take it as its own small change; the receipt is that finishing pass's third-pass finding.

- **The claim protocol's review instrument is still two one-directional enumeration tables, and neither can represent a destructive act that never fires (2026-08-28, from the seat-infrastructure Section 4 close).** Eight review rounds over that section kept returning in-class Majors with rising rather than falling yield, and the cause is that both tables encode a one-directional harm model in writing: the Class A table prices the harmful direction as the value that makes a destructive act fire, the Class B table resolves every ambiguity in the non-destructive direction, and neither prices a destructive act that never fires. For a lease over the machine's one heavy-process slot that second direction is the primary harm, the phantom hold with the longer arm that `plugins/claude-kit/skills/role/SKILL.md` names in its own words, so both tables rated the exact site of that round's confirmed security Critical as fail-closed and never-hastens, which for that act is the failure rather than the defence. The repair Section 4 specified and did not build: one state model keyed on claim state, transition, and both harm directions, moved out of `.kit/scratch/` into shipped payload and pinned in the suite. It is the mechanical leg the kit's own line prescribes wherever a duty must fire, and it is the thing that would have caught that Critical. Two questions are open and are why it is parked rather than folded into the seat-infrastructure finishing pass, both being design calls rather than transcription: which shipped surface owns the model, and what a pin over a review instrument asserts, given the instrument's subject is a reviewer's coverage rather than a code path. Signal: the next section whose subject is a protocol carrying a destructive act, or `claude-kit_review-and-record-discipline_spec_v1.md` reaching its own review-instrument work.

- **`test/memory-session.test.js:865` asserts a property of the host, not of the code, and is a false red on any machine with a short TEMP (2026-08-27, from the seat-infrastructure Section 1 baseline).** The case `a pinned directory too long to name faithfully stands the session down` pads a fixture path with 200 characters and relies on the host's temp prefix to carry the total past the `PATH_EMIT_CAP = 260` guard at `plugins/claude-kit/hooks/memory-session.js:1058`. Both lengths were computed rather than inferred: this machine's `TEMP` is `D:\Temp`, seven characters, landing the fixture at 254, so the guard is never reached and the case fails; a default Windows temp prefix lands the same fixture at 280 and it fires. The failure is silent in the dangerous direction on the other kind of box, since a machine with a long prefix gets a green that genuinely proves the guard fires while this one gets a red that proves nothing about the code at all, and a reader diffing a baseline here reads a known red as their own regression. The fix is for the test to derive its padding from the actual prefix length rather than assume a long one, which also makes the case prove the guard on every host instead of on some of them. Routed out of the seat-infrastructure plan rather than folded: it serves test soundness rather than that plan's goal, and Section 1 already carries a security review. Signal: the next session to run the suite on this machine and wonder what it broke.

- **The store-backed `.kit` redirect covers one writer family and two others reach the store directly (2026-08-28, from the seat-infrastructure Section 10 fix round).** `kitScratchDir` at `plugins/claude-kit/hooks/kit-compact-lib.js:62-69` redirects a `.kit` path to `~/.kit/store/<rel>` when the cwd sits under the store, which is what keeps the compaction gate's journal out of the replicated tree. Two other writers do not go through it: `plugins/claude-kit/hooks/kit-goal-lib.js:314` joins `.kit/goal-state.json` onto the goal root directly, and `plugins/claude-kit/scripts/kit-statusline.js:66` joins `.kit/statusline-cache.json` onto the cwd. So a seat whose project directory is the store's own coordinator directory drops both files into the replicated tree. Neither is a sync leak: both are `.json`, which no root's leaf set admits before or after Section 10's narrowing, so they are untracked residue that the operator's one-time `.kit/` sweep removes by directory. What the item is for is the asymmetry rather than the residue, since a redirect that one writer family honors and two do not is the shape that grows a leak the moment one of those two gains a form some root admits. The candidate fix is to export the resolver and have all three writers take it. Signal: any new `.kit` writer, or a root gaining a `.json` form.

- **`Test-MemorySyncPathAllowed` leaves three hardening gaps that predate the coordinator tier (2026-08-27, from the seat-infrastructure Section 1 blind and security reviews).** All three are pre-existing across all four tier prefixes in `plugins/claude-kit/doctor/install-memory-sync.ps1` rather than created by that section, which is why they are here instead of folded into it. First, the traversal guard is private to one caller: the predicate returns true for `coordinator/../board.md` and `coordinator/BOX/./board.md`, and the `.`/`..` refusal lives only in `sync-store.ps1`, so it protects the inbound screen and nothing else. Section 10's per-root narrowing bounded what a traversal reaches without closing the gap: the coordinator root admits `*.md` alone, so an escape can no longer land on a store-root file such as `history.jsonl`, and what remains is a traversal to any `.md` outside the root it was screened against, which is not benign residue: the store root holds `CLAUDE.md` and `claude-kit-doctrine.md`, and the predicate returns true for both by way of a memory tier's own prefix. What keeps that unreachable today is the precondition rather than the predicate: all four doctor probes are fed git-normalized output, and `sync-store.ps1` refuses any `.` or `..` segment before it calls the predicate at all. That is a property of every current caller rather than of the guard, and the doctrine's own rule is that a sanitizing guard belongs at the shared boundary rather than in the producer that first needed it. The predicate has five callers, the doctor's four probes and the inbound screen, and only the screen refuses a traversal segment, so the asymmetry already sits inside the predicate's own caller set rather than waiting on a new producer to arrive. Second, PowerShell `-match` is case-insensitive, so the predicate admits `Coordinator/x.md` while a case-sensitive peer's `.gitignore` refuses it, and the inbound screen is the one surface where the two sides are genuinely different operating systems. Third, the coordinator prefix's machine segment is host-derived and unconstrained, so an incoming tree from a POSIX peer can carry a segment legal there and illegal on NTFS, which the screen admits and the checkout then fails on, recording a conflict on every subsequent run. The first is the one worth doing on its own merits; the other two want a deliberate answer recorded rather than a silent default. Signal: a sixth caller of the predicate, or any existing caller reaching it with a path the inbound screen did not normalize first.

- **Two end-to-end observations of the compaction release paths are outstanding, and only the operator can make them (2026-08-26, from the compaction-boundaries finishing pass).** The unit-level gate behavior is pinned by tests and green, but neither release path has been watched land in a real session. First: in a long-running goalless session, run `node <plugin-root>/hooks/kit-compact-checkpoint.js boundary` at a banked moment, then let the session cross the harness's own auto-offer threshold, and confirm `.kit/compact-gate.jsonl` records the landing with reason `role-boundary` at that boundary rather than a `valve` landing at the context safety ceiling. Second: instruct a session over the relay to release its deferred compaction, and confirm the landing is journaled `operator-consent`. A failure on either reopens the effort as a new round. Signal: the next long session that actually defers.

- **`memq add-operator`'s prompt-free fleet grant was priced when the operator tier carried facts, and that tier now carries an authorization switch (2026-08-28, from the seat-infrastructure Section 8 security review).** `plugins/claude-kit/hooks/memq-grant.js` allows `add-operator` prompt-free and agent-blind under the engine store signals, and `docs/security-model.md` accepts that risk on the reasoning that the write lands inside a store the engine chose. Section 3 of the seat-infrastructure plan changed what such a write can be: the standing-delegation opt-in is one operator-tier record whose presence flips a seat's default treatment of scoped direction from the seats above it, so a write there is an authorization change rather than a fact. The grant's stated precondition is unchanged and the reach needs the documented environment-divergence residual to fire, which is why this is a re-pricing rather than a defect: decide whether the verb stays in the prompt-free set now that the tier carries a switch, or whether the grant narrows to records that are not delegation records. The security model states the reach and points here. Signal: the next change to either the grant's allow set or the delegation model.

  Widened 2026-08-31 by the standing-grants plan's section 1: the subject is no longer one switch. The role skill now states a general standing-grant rail, of which the delegation opt-in is the first instance and the memory store's push grant the second, and the rail admits further grants whenever an owning skill states one, with no edit to the security model or to this item required for that to happen. So the question to decide is not whether the delegation switch is worth the prompt-free grant but whether a tier whose privileged content grows by an owning skill's say-so is, and the second exit above narrows accordingly: withholding the grant from delegation records alone would leave every later grant's record inside it. Signal is unchanged, with a third trigger added: a new instance being added to the rail.

- **A `superseded by <name>` label is forgeable by a record description (parked 2026-08-23).** The label rides the same line as the record's own free-text description on `find`'s lexical hits and on `recall`'s digest lines, so a description ending in `superseded by <name>` renders indistinguishably from a real label, and on an archive line, where the genuine label prints after the description, the forgery is exact. A forged label steers a reading model and an adjudicator at a decay pass; it cannot demote a rank or nominate for archive, both of which derive from the pointer map rather than from the line. This is the free-text-beside-tool-tokens class the tier label already sits in, with a token of more decision weight added to it. The fix is positional rather than a filter: give the tool-written tokens a span the description cannot enter. Surfaced by the finishing security review of `claude-kit_memory-supersedes_spec_v1.md`.

- **A `--supersedes` target named in variant case writes a pointer that goes inert off Windows (parked 2026-08-23).** `supersedesTargetRefusal` resolves the target through the filesystem, which is case-insensitive on Windows, so `--supersedes OLDER` against `older.md` passes the typo guard and stores `supersedes: OLDER` verbatim. `memoryFileKey` lowercases on win32 only, so the same record synced to a case-sensitive checkout labels nothing: the one case mismatch the write-time guard exists to catch while the author is still present. Inert is the read side's safe direction, so the cost is a silently lost label rather than corruption. The fix is to canonicalize the written value to the on-disk filename's casing at write time. The off-Windows half is inferred rather than confirmed; confirming it takes one pointer written in variant case on a case-sensitive filesystem. Surfaced by the finishing adversarial review of `claude-kit_memory-supersedes_spec_v1.md`.

- **Give the `memq-grant` `--supersedes` screen a canary probe** (parked 2026-08-23). `hooks/hook-canary.js` probes the grant's withholding direction for `--body-file`, `--rollup` and `find`, and not for `--supersedes`, a body-carrying `--update`, `--trigger`, `--type=<type>`, or `--drop-malformed`, which the kaizen batch's section 10 added to the screen on 2026-08-25. All five unprobed flag shapes have a second lock, which is the stated ground for leaving them unprobed, though `--drop-malformed`'s is of a different kind from the other four: theirs is a CLI refusal under the store signals, while its is a coupling, since the CLI answers it with a usage error unless `--rollup` accompanies it and the hook withholds `--rollup` too (`docs/security-model.md`, the memq-grant compensating-control paragraph), so on those five this is an enhancement rather than a defect. The `anchor` verb is the one that is not: it has no second lock anywhere, the verb being the operator's own, so its screen rests on the hook alone and a regression on it degrades to nothing with no probe to say so. Probe `anchor` first. Cost is one probe block per shape plus the probe-count figures in `security-model.md` and the canary's own comment. Surfaced by the section 3 review of `claude-kit_memory-supersedes_spec_v1.md`.

- **Give `memqGrantProbes` the load check its own sibling already does** (parked 2026-08-26). `plugins/claude-kit/hooks/hook-canary.js:503-517` establishes that the grant's payload is there with `fs.statSync(memq).isFile()`, while `cacheSuppliesMemq` at `:666`, one site over in the same file, answers the stronger question by loading it. A stat passes for a file that is present and unloadable, so the canary can report the grant's payload healthy on an install where every granted command would still fail at require time, and that gap widened when `scripts/memq.js` took a load-time coupling to `hooks/kit-network-lib.js`: a cache missing the sibling now stats clean and throws on require. The two probes ask about the same file and should reach the same answer. Cost is one call swap plus whatever the load's failure mode needs saying on the failure line. Routed here by Chapter 4 of `claude-kit_memory-anchors-and-frontmatter-guard_spec_v1.md`; the finishing adversarial review of that plan found the routing had never landed, which is the second half of why it is dated today rather than at Chapter 4.

- **Sweep the plan-private referents out of the test comments** (parked 2026-08-26). Fourteen sites across `test/memq.test.js`, `test/memory-session.test.js`, `test/memory-frontmatter-guard.test.js` and `test/compact-deferral-nudge.test.js` name a plan's own sections ("Section 3's pin case", "this section widened hooks.json's matcher"), which no reader of the test can resolve once the plan is archived and which are the same change-narrative class the house rule bars from a code comment. The defining grep is `grep -nEi "this section|Section [0-9]|Chapter [0-9]"` over the test set, discounting three legitimate hits: two `KIT_RUN_SECTION` fixture values, and the operator-facing "this section's commit model" string in `hooks/chapter-boundary-nudge.js`, where the word means the reader's own section of work. Two of the fourteen sit in a test name and an assertion message rather than a comment, so they are behavioral surface and want more care than a comment edit. Held out of the anchors plan's finishing pass deliberately: the review scoped its finding to shipped code, which was fixed there, and a fourteen-site comment refactor inside a finishing pass is scope creep with a real chance of breaking a test name. Surfaced by the finishing adversarial review of `claude-kit_memory-anchors-and-frontmatter-guard_spec_v1.md`.

- **Check the channels broker for readers of the stop-failure marker files (2026-08-23, from the dormant-feature removal).** In the `sapplefeld-channels` repo, grep for `stop-failure-latest.json` and `stop-failure-resumed.json`. The archived stop-failure spec named the broker as a possible second reader of those markers, for a status line on the Discord card it posts. No reader found means done. A reader found means either confirming it tolerates the files being absent, which is already its steady state on any machine that never logged a failure, or removing that reader on the broker side. Nothing in the kit blocks on this.

- **Take the plugin update on each fleet machine (2026-08-23, from the dormant-feature removal).** Until a machine runs `claude plugin update`, its installed cache still carries the removed payload, and its build manifest still names `stop-failure-log.js`, so the session-start canary reports that file missing on every session. The update is what quiets it. The scheduled-task precondition this item used to carry is answered: no fleet machine holds the watcher task, so the update is unconditional. The per-machine acceptance check is internal consistency plus trunk match, not a version compare alone: the installed guard's frontmatter field list agrees with the installed reader's and with the trunk's (`MEMQ_FIELDS`, seven names), because guard and reader ship in one payload and the dangerous state is a partial update rather than an old one; a stale-but-consistent payload merely lags, while a mismatched pair would read fields its own guard never checks (2026-08-30, measured on the coordinator's box: both cache payloads at six names, internally consistent, trunk at seven; activating the box's newer cache entry was checked and changes nothing, so the action is the update, not an activation).

- **Never-worse invariant, banked for the first summarizer (2026-08-22, from the Warden candidate 5 disposition).** Any future spec that adds a reduction or summarization step includes this as a tested requirement: the reduction never emits more than it consumed, and ships the original when it would. Deliberately not retrofit to the `recall`/`recent` digests, settled 2026-08-22: their coverage lines exceed a tiny store's size on purpose, because a stated empty surface is the product and a silent absence is the failure. Signal: the first spec adding a summarizer writes the invariant in; until then this item just holds it.

- **Two guards from the shared-tier delete ship with no red on this host and need provisioned environments (2026-08-22, from the shared-tier authoring effort's named gaps).** Both are stated rather than assumed closed, and neither is reachable from a normal run here. The hook canary's POSIX path-spelling branch cannot go red on win32, where the reversal is the state that passes, so it needs a POSIX host in the gate; both branches were verified by evaluating the constant with `path.sep` faked, which is an argument rather than a test. And the `stat` to `lstat` change on the record path needs the symlink-creation privilege, meaning Developer Mode or an elevated shell, since `fs.symlinkSync` returns EPERM on this box and a junction is directory-shaped, which the guard refuses for a different reason. Standing Brief Amendment 8 says a guard closing a defect class ships with a per-site red, so these two are the named exceptions to that inventory and should stop being exceptions when either environment is available.

- **Collapse the surviving restatements of three shared-tier facts to single owners (2026-08-22, from the shared-tier authoring effort's finishing prose review).** Three facts are each written out in full at three or more sites, and every one of them is a set whose membership can grow. The delete's closing-sweep rule sits at `plugins/claude-kit/skills/memory-system/SKILL.md:14` (the owner, with two correct pointers at `:30` and `:136`) plus three independent paraphrases in `docs/security-model.md`, `docs/architecture.md` and `plugins/claude-kit/skills/finishing-work/SKILL.md`. The eleven granted verbs are enumerated in `docs/security-model.md`, `docs/fleet-integration.md` and `memory-system/SKILL.md`, plus twice more in comments inside `plugins/claude-kit/hooks/memq-grant.js`, with `GRANTED_VERBS` at `memq-grant.js:249-250` the only mechanical owner. The kit doctor's `-Fix` procedure is duplicated near-verbatim across `memory-system/SKILL.md:64` and `finishing-work/SKILL.md:66`, and that duplication is confirmed to have caused a defect: a false rule about the doctor's WARN handling landed in both copies and had to be fixed in both. All six sites currently agree, which is exactly why this is a backlog item and not a bug: the cost lands on the next correction, not today. The fix is to name one owner per fact and reduce the rest to pointers, which the two correct pointers at `memory-system/SKILL.md:30` and `:136` already show working. A fourth member joined at the finishing fix round (2026-08-22, found by the fable confirmation review): the canary's grant-probe roster, count and screen names, is enumerated in full in both `docs/architecture.md` and `docs/security-model.md` on top of its mechanical owner in `plugins/claude-kit/hooks/hook-canary.js`, so the next probe added must update two prose enumerations nobody owns.

- **Move `sameSessionId` from `kit-compact-lib.js` into `kit-goal-lib.js` so the two call-time requires can become load-time ones (2026-08-29, from the plan-lifecycle finishing review).** `armingSessionClaims` and `sessionHoldsLeash` in `kit-goal-lib.js` each `require('./kit-compact-lib.js')` inside the function body rather than at module load, because `kit-compact-lib.js` destructures `kit-goal-lib.js` at its own load and a top-level require back is a cycle. That is correct and documented at both sites, and `require` is cached, so the runtime cost is a map lookup. The closure is the other direction: `sameSessionId` is a session-id comparison rule with no compaction content, so it belongs in the goal library, and moving it there leaves the compaction library taking it through the load-time destructure it already performs. Parked rather than done because it rewrites every consumer in `kit-compact-lib.js` and their tests for no behavioral change, which is not work to fold into an effort's closing pass. Worth doing the next time either library is opened for its own reasons.

- **The nudge log's two security Minors ship recorded and unfixed (2026-08-30, from the memory-recognition finishing security review).** Both sit in `plugins/claude-kit/hooks/memory-recognition-nudge.js` and both were rated Minor and deliberately not folded. The first is a cross-session rotation gap: the log rotates to `<path>.old` past 1 MB under the emission lock, but a reader that opens only the live file after a rotation it did not observe sees a window it cannot tell from a quiet one, so `nudgeStampRate` can under-count nudges near a rotation boundary rather than refusing. The second is the state directory's ownership screen, which is a real check on POSIX and degenerate on win32, where the ownership predicate the sibling writers rely on has no equivalent, so the directory is trusted on its kind alone. Fix shape: have the rate reader take the rotated file into its window when the live file's first line postdates the window start, and state the win32 screen's actual strength where the security model describes it rather than inheriting the POSIX sentence. Signal: the first stamp-rate reading that disagrees with a hand count, or a second consumer of the state directory.

- **The nudge log's `O_NOFOLLOW` append is unexercised on the only machine the suite gates on (2026-08-30, from the memory-recognition finishing pass).** `memory-recognition-nudge.js` composes `O_NOFOLLOW` into its append open where the platform defines the constant, which refuses a final-component symlink at the open itself. Node exposes the constant only where the platform has it, and on win32 `fs.constants.O_NOFOLLOW` is `undefined`, so the branch resolves to a plain `'a'` open and the guarded path never executes here. The code is correct and the coverage is absent, which is the shape that reads green forever: no test on this box can distinguish the composed flags from the fallback. `docs/security-model.md` already states the residual in the shipped prose, so what is open is the proof rather than the disclosure. Fix shape: a POSIX runner, or a unit test over the flag-composition expression alone with the constant injected, which proves the composition without needing the syscall. Signal: the kit gaining a non-Windows gate, or a second writer reaching for the same idiom.

- **`anchor` and `triggers` are withheld from the fleet grant and probed by the canary in neither direction (2026-08-30, from the memory-recognition finishing pass).** `hooks/memq-grant.js` withholds five verbs, and for three of them, `find`, `anchor` and `triggers`, the grant list is the whole of what withholds them, with one qualification: `triggers` now refuses its own shared-tier and pinned-tier `--replace` under the store signals, so what rests on the grant list alone there is every other shape of the verb rather than all of it, while `find` and `anchor` carry no such refusal at all. `hooks/hook-canary.js` probes `find` in both directions and these two in neither, so a regression on either screen degrades silently to nothing and no probe would say so. This is the same gap as the parked `--supersedes` probe item above and shares its fix shape; it is filed separately because the two verbs are the operator's own authoring verbs, which is what makes an unnoticed grant worse here than a missing probe elsewhere. Fix shape: extend `memqGrantProbes` with a withheld-direction probe per verb. Signal: the next change to `GRANTED_VERBS`, or the next verb added to either authoring family.

- **`cmdAnchor` and `cmdTriggers` both name the wrong tier in one not-found message (2026-08-30, from the memory-recognition Section 1 review).** Both verbs compute a `where` holding the tier the record actually resolved to (the run's pending tier or the project tier), and both then hardcode `'in the project tier'` in the message for a record that vanished between the pre-lock stat and the read under the lock (`memq.js`, `cmdAnchor`'s not-found write and `cmdTriggers`' clone of it). The window is racy and narrow, so nothing is lost but the accuracy of one sentence, and the reader it misleads is one already chasing a record that disappeared mid-command. Parked rather than folded into Section 1 because the defect originates in the anchor verb, and that section's whole discipline was that file anchors stay untouched: repairing it there would have put an unrelated behaviour change inside a changeset whose reviewers were told anchors were out of scope. Fix both sites together, since fixing one leaves the clone stating the same falsehood.

- **The kit doctor's verdict is relative to whichever copy of itself you run, and its output never names the payload it derived from (2026-08-30).** Run read-only three times on one machine in one minute, against unchanged files: the active installed build reports memq shim PASS and Memory sync PASS with four sensitive paths proven ignored; this checkout's own copy reports both FAIL; an older cached build reports one of each. The detail lines say why, once you read them closely ("differs from the allowlist this doctor derives", "differs from this payload's copy"), but the PASS/FAIL banner does not, so two seats reading the banner reach opposite conclusions about one machine and each is correct about a different payload. Two consequences make it worth fixing rather than documenting. A FAIL of this shape is the expected mid-effort state of any checkout ahead of its install, so the check cries wolf for the whole length of every effort that touches the payload. And its remedy line says to re-run with the fix pass, which from a development checkout would install the in-development allowlist over the working one, a repair that is only correct from the payload the machine actually runs. The fix is to name the derivation in the output, and to distinguish a state that is unsafe from one that merely trails the checkout in hand.

- **Rule the fork between a shared-file hold and a finishing pass's clean-docs gate (2026-08-30, raised by the machine coordinator seat, no durable trace found anywhere before this entry).** Two rules this kit states both apply to one situation and point opposite ways, and nothing says which wins. The doctrine's scope rule says a file another session is mid-edit on is not yours to stage even where you also changed it, so a document left dirty under that hold stays dirty. The finishing pass expects the plan doc reaching its terminal state in the same delivery as the code, and its resting state is terminal-and-staged rather than uncommitted, which a held file cannot reach. So a finishing pass that meets a held document has to either violate the hold or report itself finished with an uncommitted doc, and today the choice falls to whoever notices, unrecorded either way. What a ruling needs to settle: whether the hold outranks the gate (and if so, what the pass records so the next session can tell a held doc from a forgotten one), or the gate outranks the hold for the plan doc specifically (and if so, what makes that safe when the other session's content is in the same file). This one is the operator's rather than a seat's, because it trades a correctness risk against a bookkeeping risk and the appetite for each is theirs.

- **Frame a memory record's provenance by the pin that resolved it, not by the tier it came from (2026-08-30, declined out of the memory-recognition Section 2 security review).** The store fences a record read whose writer is someone other than its reader: the shared tiers always, and the project tier under a store pin, where the pin is what makes the writer foreign. The framing that carries that fence is written in the vocabulary of tiers, so a reader learns which tier answered and has to infer from the pin whether the text in front of them was written by this project or handed to it by an environment that chose the store. The recognition nudge sharpens the question, because it names a record without being asked and its line is the only provenance a session may ever see for that record. What a fix would state is the resolving pin itself, in the fence and on any surface that names a record, so a pinned read reads as foreign at a glance rather than after a lookup. Declined inside that section as a MINOR whose blast radius is legibility rather than safety, and because the change reaches every surface that fences a read rather than the one that prompted it.

- **Decide what the public repository may carry of the operator's own identity, and make the answer one rule rather than three conventions (2026-08-29, from the plan-lifecycle Section 4 re-review).** Three surfaces carry it today and each arrived by a different route, which is why no single sweep has ever covered them. `kaizen/notes-<hostname>.md` is machine-scoped by documented design (`plugins/claude-kit/skills/kaizen/SKILL.md`), so the hostname is in a tracked filename on purpose and any change there is a change to the kaizen model. The prose in `docs/archive/backlog-2026-Q3.md` names machines and the operator by name inside item bodies, which is incidental rather than designed: nothing reads those tokens and nothing would break if they were roles; `docs/backlog.md` itself no longer names machines, the public-surface-hygiene plan having generalized its three machine-naming entries to roles, though it still names the operator in three item bodies, which is the half of this question that plan deliberately did not settle, and that plan's disposition table is where each surviving surface's rule is now recorded. And three test files key fixtures on the real marketplace name (`claude-kit@applefeld`): `test/session-start-plugin-view.test.js`, `test/kit-statusline.test.js` and `test/kit-goal-statusline.test.js`. That is the weakest of the three surfaces, since each hook deliberately matches on the plugin half alone and a neutral marketplace name would exercise the same path. The enumeration is stated at file granularity on purpose, because the first version of this item named one of the three and a sweep run to it would have left two behind. None of it is a credential and the marketplace name is published in the repository's own install path, so this is a posture question rather than a leak: the standing rule says operator specifics are recorded by reference and never by quotation, and three surfaces answer it three ways. Decide the rule, then sweep to it in one pass rather than per-effort. The fixture keys are the cheap third to fix whatever the wider answer is.
- **Decide whether a description-only `--update` should keep its prompt-free grant on the unattended vector (2026-08-22, from the shared-tier authoring effort's finishing security review).** `memq-grant.js:480` withholds `--update` only when `--body` rides with it, so a bare `--update` on `add-type`/`add-operator` keeps the grant and a fleet worker can replace an existing shared-tier record's index line with no prompt. The index line is the higher-reach surface of the two: it is emitted into every declaring project's session context with no command invoked (`docs/security-model.md:39`), while the body is only read on demand. The CLI's own stated reason for refusing the body half under the store signals, that the `.bak` which would recover it is local and unsynced so a repair there is as final as a deletion (`memq.js:6942`), applies verbatim to the index rewrite, which also leaves only a never-synced `.bak`. The counterweight is blast radius: 120 characters against 64 KB. Rated Minor because nothing is exploitable as shipped and the description channel is the repair path a fleet worker most plausibly needs. Two exits: state the asymmetry in `docs/security-model.md:188`, or withhold `--update` at the hook, which is a behavior change and so ships with a per-site red under Standing Brief Amendment 8.

- **Retitle or re-harness the repair-consent test whose title outruns its assertion (2026-08-22, from the shared-tier authoring effort's finishing adversarial review).** `test/memq.test.js:6685` is titled as refusing a body "without its consent flag first" but runs on the `makeStore`/`run` harness, where `memq.js:6949` refuses the repair under the engine store signals before the consent gate at `:7007` is ever reached; the assertion matches `/refused under the engine store signals/` and never exercises consent. The test's own comment concedes it. The consent-first claim is pinned only by the neighbouring test at `:6723`, so nothing is currently unpinned, but a title asserting what its body does not test is the small end of the class Amendment 8 exists to close. Either retitle it to the refusal it actually pins, or move it to `makeHomeStore`/`runHome` and assert the consent refusal.

- **Break the negation-then-correction cadence in the two long prose documents (2026-08-22, from the shared-tier authoring effort's finishing prose review).** `references/ai-tells.md` names "it is not X, it is Y" contrast framing against a position no reader proposed. It runs at roughly one instance every two lines in `docs/security-model.md` (161 occurrences) and `docs/architecture.md` (112), with `memory-system/SKILL.md` at 46 and `docs/fleet-integration.md` at 21, alongside 23/21/9 instances of "deliberate(ly)" and 9/4/3 of "is the point". It is a frequency and uniformity finding rather than a quarrel with any line, and it is the strongest machine-prose signature in the library. Adjacent, same source: six section-closing one-line morals in `memory-system/SKILL.md`, a bolded lead-in on nearly every argument-carrying paragraph in that file's second half, and four single-sentence blocks over 100 words whose announcing count is separated from its list by an interposed clause (`architecture.md:123` at 1,258 words, `security-model.md:182` at 1,228, `finishing-work/SKILL.md:62` at 635 carrying an executable procedure, `memory-system/SKILL.md:14` at 426). Deferred rather than fixed because it is a rewrite of two documents and not a correction, and because none of it is false; the finishing pass fixed what was wrong and left what was merely mannered.

- **The shared-tier delete's ordering argument claims a permanence its checks do not establish for a node sitting at `archive/<name>.md.bak` (2026-08-22, from the shared-tier authoring effort's section 3, raised by the blind review lens).** `deleteSharedRecord`'s header argues that the record-copy sweeps lead because a failure there can be permanent rather than transient, and that both directories are listed before either sweep unlinks, so a permanent failure leaves the store exactly as it was. The listing step stats the two directories and not the two backup names inside them, so a directory occupying `archive/<name>.md.bak`, which the header itself names as the permanent case, is discovered only when the archive sweep tries to unlink it, by which time the live-tier sweep has already spent the record's own `.bak`, the single copy of the body a repair replaced. The shape of the fix is to lstat both `<name>.md.bak` paths during the listing step alongside the directory checks, so a permanent occupier refuses before anything is unlinked, which is what the header already claims happens. Not urgent: reaching it needs a directory hand-placed at a `.bak` name inside a tier that bars hand edits, which is the same privilege class as the effort's other named gaps, and the loss is one recovery copy rather than a record. Deliberately not taken in the effort that found it: section 3 was a documentation pass and held its code edits to comments and one message string, and a change to the delete's ordering wants a red-first test per site under Standing Brief Amendment 8 rather than a tail-of-section edit.

- **A mistyped name under `--confirm-shared` spends the shared tier's three backup generations and reports nothing (2026-08-22, from the shared-tier authoring effort's section 3, raised by the blind review lens).** Under consent `cmdDeleteType` and its operator twin deliberately stand down the no-copy-present pre-check, so a name the tier does not hold reaches the delete proper, removes nothing, sets its completion flag, and runs the closing sweep over the tier index, the archive index and the usage sidecar. Those three `.bak` files are whatever the last locked rewrite left, which for a decay pass is its only local recovery, and they go. The loss is silent twice over: the line reporting what was swept is gated on a nonzero removal count, which a typo leaves at zero, and the sweep counts no backups in the first place, so stderr says only that no memory file of that name exists. Two shapes of fix, and they compose: report each backup the sweep actually removed, so the cost is visible whatever the run did; and reconsider whether the sweep belongs on the path where no record was found, since its stated justification is that a backup holds the record being deleted, which is not true when there was none. Deliberately not taken in the effort that found it: it changes a destructive verb's behavior and its reporting contract, which wants its own red-first test per site rather than a tail-of-section edit, and section 3 was a documentation pass. What section 3 did instead was write the behavior down, in `memory-system/SKILL.md` and in `docs/security-model.md`, so an operator meets it in the documentation rather than in a lost backup.

- **The stopped-delete pin in `test/memq.test.js` carries a dead assertion whose message contradicts the comment above it (2026-08-22, from the shared-tier authoring effort's section 3).** The `.bak` non-existence check near the end of "a delete of a name the tier does not hold spends no backup on the sidecar" cannot fail: the closing sweep unlinks the sidecar backup on every completed run, whoever wrote it, which is exactly what the comment three lines above now says. The real discriminator in that test is the byte-identity assertion that follows it. The assertion is therefore not merely redundant, it asserts the opposite of the comment, so whoever narrows the sweep later reads its failure message as evidence for a rule the file elsewhere denies. Fix is to drop it or re-point it at a fact the sweep does not erase. Do not confuse it with the neighbouring "a completed delete leaves no backup holding the record it removed", whose two `.bak` assertions are the live red for the closing sweep and must stay. Not taken here because section 3 held its code and test edits to comments, and changing an assertion is a test-logic change that belongs with the sweep work in the entry above it.

- **The shared-tier delete sweeps the `.bak` at its three index and usage targets but not a `.tmp.<pid>` stranded at the same paths, and the two documents describing that residue now carry the exception rather than an absolute (2026-08-22, from the shared-tier authoring effort's section 3).** `plugins/claude-kit/scripts/memq.js` closes a completed `delete-type` or `delete-operator` by unlinking `<path>.bak` at the tier index, the archive index and the usage sidecar. A rewrite killed between its `writeFileSync(tmp, ...)` and its `renameSync` strands `MEMORY.md.tmp.<pid>` at one of those paths, holding the index as it then stood: for a record authored without a body flag the index line carries that record's whole stored body, which is the same reason the `.bak` is swept. The record-side sweep already handles both shapes (`recordCopyEntry` matches `.bak` and `/^\.tmp\.\d+$/`), so the asymmetry is between the two sweeps in one verb rather than a missing concept. The shape of the fix is to extend the closing sweep to the temp shape at the same three paths, which needs the tail test factored out of `recordCopyEntry`, today bound to a record-name comparison the index sweep cannot make, so that both sweeps share one rule rather than minting a second, and then to restore the absolute wording the prose gave up. Not urgent, and deliberately not taken in the effort that found it: reaching the state needs a hard kill (SIGKILL or power loss) between two adjacent calls, the residue never leaves the machine because `*.tmp.*` is one of the sync's never-sync patterns, and closing it is a behavior change to a destructive verb, which wants its own red-first test per site rather than a tail-of-section edit. What section 3 did instead was bound the claim: `docs/architecture.md` and `docs/security-model.md` now say no copy the verb can name survives and name this one as the exception, so the documents are true of today's code and this entry is what makes them absolute again.

- **The hook canary's grant probes are bounded per child, so the phase they form has no bound at all (2026-08-22, from the shared-tier authoring effort's section 2).** `plugins/claude-kit/hooks/hook-canary.js` spawns six grant probes on Windows and five elsewhere, each capped at `PROBE_TIMEOUT_MS = 5000`, so the worst case before integrity hashing is about 35 seconds. That worst case is reached exactly when something is wrong, which is the moment a session start hanging half a minute reads as the kit being broken rather than the kit reporting, and it sits on the path a user waits on at every session start. The shape of the fix is a whole-phase wall-clock bound rather than a per-child one, with the probes that did not get to run reported as unrun rather than passed over in silence, which is what keeps the honesty property the probes exist for: a probe phase that quietly stops early would report a healthy grant hook on evidence it never gathered. It was deliberately not taken in the effort that grew the count, because it changes the canary's reporting contract rather than its correctness, and a reporting-contract change wants its own enumeration of what every consumer of a probe result does with an unrun one. Not urgent: nothing here is wrong, the cost is latency in the failure case, and each added probe was added to close a real detector gap.

- **A resumed archive name can carry its description onto a different retired record's archive index line (2026-08-22, from the shared-tier authoring effort's review round 10).** `carryArchiveIndex` in `plugins/claude-kit/scripts/memq.js` always writes the archive index line for a name the pass is retiring, including a name admitted as the resume of an earlier stopped run. The resume admission now requires three facts (a genuine ENOENT at the record path, a plain file in the archive slot, and the tier index still listing the name), which closes the case the review found. What remains needs all three to hold about two different memories at once: a live record gone from the tier while its index line survives, and an unrelated retired record already sitting under that name in `archive/`. The carry then overwrites the retired record's description with the live one's. No body is ever confused and no record is lost; one archive index line describes the wrong memory. Two paths reach the precondition, and the second is the one that keeps this from being pure operator error: a hand deletion of a record file, which the shared tiers bar but cannot prevent, and a partial or conflicted sync merge that carries a `delete-type`/`delete-operator` removal of the record without the matching index-line removal. The fix is to have the carry skip a name whose archive index line already exists when that name arrived as a resume. It was deliberately not taken in the effort that found it, because it changes the carry's contract rather than the validator's and wants its own enumeration of the archive pass's stop states: a genuine stop after the carry landed and before the rename would then be a name whose line exists and whose description the pass declines to refresh, which is believed harmless and is not proven so. Not urgent, and bounded to a description.

- **The memory-store destination probe can silently mislead in two directions, and neither is recorded as unproven (2026-08-21, from the sync-state-writer-and-push-pair effort's review round).** `Get-MemorySyncStatus` in `plugins/claude-kit/doctor/install-memory-sync.ps1` reads git through `Invoke-MemorySyncGit`, which merges stderr into stdout, and then takes `Output[0]`. Five reads in that function do this, including the branch, the upstream, and the two destination reads added for the push-pair check. A git warning printed ahead of the value is therefore read AS the value, which for a config read means the check either never fires or fires with a warning string standing in for a branch name. The kit already knows this hazard and solved it the other way in `plugins/claude-kit/doctor/sync-store.ps1`, which takes `Output[-1]` and says why in a comment on each site, so the two files disagree about the same helper. Separately, when either destination read simply fails, nothing records that it failed: an unreadable `push.default` silently falls back to git's default, and an unreadable upstream ref silently disables the name-pair check altogether while the report still prints a confident destination line. Every other probe in that file has an unverified state (`DestinationRead`, `RemoteBranchesRead`, `ProbesRan`); these do not. Both halves want one pass over the same function: switch the reads to the last-line convention with a shape filter, and give the pair a read flag that feeds the existing unverified-destination WARN. Left out of the effort that surfaced it because fixing only the two newest reads would leave three older ones carrying the same defect, which is half a class fixed and worse than none. Not urgent: `git config --get` does not warn on a healthy repository, and no instance has been observed.

- **The push-destination check assumes a central workflow and would misreport a triangular one (2026-08-21, from the sync-state-writer-and-push-pair effort's review round).** The check added to `Get-MemorySyncDestinationLines` in `plugins/claude-kit/doctor/doctor.ps1` reads `push.default` and compares the local branch name against its upstream's. Git only enforces that pairing when the push remote and the fetch remote are the same. With `remote.pushDefault` or `branch.<name>.pushRemote` naming a different remote, git behaves as though `push.default` were `current` and pushes a mismatched pair happily, so the doctor would emit a blocking FAIL and exit nonzero over a store whose pushes work. That is the failure mode the same function's own header comment warns about, since a check that fails over a state the operator chose teaches them to stop reading the section. It also means the real triangular hazard, pushes landing on a remote that the pulls never read, goes unnamed. The fix is to read the two push-remote keys and either skip the name-pair finding or reword it when the push remote differs from the fetch remote. Reachability is close to nil for a personal memory store, which is why it was left out rather than folded in.

- **The doctor prints the store's origin URL verbatim, which discloses an embedded credential (2026-08-21, from the sync-state-writer-and-push-pair effort's security review; pre-existing, not introduced by it).** `plugins/claude-kit/doctor/doctor.ps1` reports the origin URL read from `git remote get-url origin`. A remote configured in the credentials-in-URL form, `https://<user>:<token>@host/path`, therefore prints the token to the terminal on every doctor run, and the doctor's output is exactly the thing an operator pastes into a chat or an issue when asking why sync is broken. Maps to OWASP A09 and A02, and to SOC 2 CC6.1. The fix is to redact the userinfo component before printing, keeping the host and path, which is all the line is for. Not urgent on a store whose remote uses SSH or a credential helper, and neither machine is known to use the URL form, but the check is cheap and the disclosure is silent. The effort's finishing security review, which read the routing rather than the original finding, argued for scheduling this rather than letting it age here, on the ground that a credential printed on every run of a diagnostic is the finding class an audit writes up whatever the local likelihood.

- **`Write-SyncState` cannot report its own failure, so the defect class the null-marshalling fix closed would recur invisibly (2026-08-21, from the sync-state-writer-and-push-pair effort).** The state write in `plugins/claude-kit/doctor/sync-store.ps1` sits inside a `try` whose `catch` deletes the temp file and returns: no output, no exit code, no record anywhere. That is deliberate, because the runner is silent by design and a bookkeeping failure must not break a sync, and it is also exactly why the `$null` marshalling defect survived for the life of the file. It means any future `File.Replace` failure freezes the state file the same way with the same absence of signal, and two triggers remain reachable: a store root on a network path or a non-NTFS volume, and a sharing violation from a concurrent reader. The section that fixed the marshalling deliberately left the catch alone, because the remedy is a design question rather than an edit: the state file is the only channel the runner has, so a failure to write it cannot be reported through it, and each candidate (a sibling error file, a marker the SessionStart hook notices, a non-zero exit whose caller ignores it anyway) buys signal at the cost of the silence the runner exists to keep. Not urgent, since the one known trigger is fixed and the two remaining ones need an unusual store location. Worth settling before anyone adds a third consumer to that file.

- **The store's auto-sync stands down on a cross-machine MEMORY.md conflict: the root-cause diagnosis held, every conclusion drawn from the state file did not, and all three open items are now closed (2026-08-19, corrected 2026-08-21 from the sync-state-writer-and-push-pair effort).** The reading that opened this item was `~/.claude/kit-sync-state.json` on this machine showing `lastOk` empty, `lastResult: transient`, `reason: push-failed`, with the store ahead 4 / behind 31 of `origin/main`, and the diagnosis behind it stands: a genuine cross-machine content conflict, both machines having appended index lines to `memory-operator/MEMORY.md` and both having created `memory-operator/claude-code-bash-rule-token-matching.md` (an add/add), so a rebase of the local commits onto `origin/main` conflicted and the sync correctly stood down. That is the parent spec's own Out-of-Scope item #3 (auto-resolving cross-machine index and add/add conflicts is deferred to union-merge work), so it was expected behavior rather than a regression, and nothing corrupt reached origin. Everything the entry then concluded needs correcting. Its manual-reconcile item is done, by exactly the remedy it named: the store's reflog carries `refs/remotes/origin/main@{2026-08-20 20:45:43}: pull --rebase: fast-forward` followed by `update by push` at 20:46:50, and the store now reads `main` tracking `origin/main` at 0 ahead / 0 behind. Its open question, why the recorded reason was `push-failed` rather than `pull-conflict`, is answered, and the hypothesis it offered is refuted by the code: the guess was that the runner resolves `@{upstream}` to a fixed commit before the fetch, making the rebase a no-op against a stale base, but `sync-store.ps1` fetches at line 459 and resolves `@{upstream}` at line 469, in that order and in the same block, so the resolve always reads a freshly fetched ref. The real answer is the state writer's null-marshalling defect: `Write-SyncState` can write the file only once, through the `File.Move` branch that runs when no state file exists, so every later write is silently dropped and the recorded reason freezes at whichever reason happened to be current at that first write, `pull-conflict` included. And its central claim, that "the shipped `doctor/sync-store.ps1` has never once succeeded", is not merely unsupported but false. That claim was drawn from `lastOk` being empty, which a file whose only landed write is its first cannot ever set afterward. The reflog shows the runner's own signature sequence, a `fetch --quiet: fast-forward` followed by a rebase and an `update by push`, at 2026-08-21 00:29:58 through 00:30:01 and again at 09:26:12 through 09:26:16, with two further commit-and-push cycles at 10:05 and 10:08; `fetch` and `push` each appear exactly once in the whole `plugins/claude-kit/doctor/` tree, both in `sync-store.ps1` (lines 459 and 548), so nothing else the kit runs produces that trio. Those four successes on 2026-08-21 also confirm the null-marshalling defect on the machine this reading was taken on rather than only on the other syncing machine, because they sit against a `kit-sync-state.json` whose mtime is still 2026-08-19 18:00:05 and whose contents still read `"lastResult":"transient","reason":"push-failed","lastOk":""`: a working runner and a frozen file. What remains is a deployment check, not a diagnosis. The frozen file's `firstFailSince` reads `2026-08-19T22:00:05`, so the seven-day failure-streak nudge in `hooks/memory-session.js` fires on a demonstrably healthy store around 2026-08-26 unless the fixed script reaches both machines first, and since each machine runs the installed plugin copy rather than this clone, confirm on each after the fix ships that `kit-sync-state.json` starts moving and that `firstFailSince` clears.

- **Give the leash bind a compare-and-swap, as the queue advance already has (2026-08-18, from the compaction gate binding effort).** `bindSession` in `kit-goal-lib.js` is an unlocked read-modify-write that rewrites the whole goal state, and its own comment already records that a clear landing between its read and its write can be resurrected by it. The gate now calls it too, and past the compaction trigger the harness re-offers every assistant turn, so an unbound armed goal in a claiming session attempts that write far more often than at stops alone: the window did not open, its cadence widened. The concrete failure is an operator running `/kit-goal clear` at high context while an offer is in flight and finding the leash back on, recoverable by clearing again. `advanceGoal` already carries the fix's shape, refusing when the re-read plan or `armedAt` no longer matches the caller's snapshot, and the timestamp leg is load-bearing there because a re-arm putting the same plan back would otherwise pass a plan-only compare. Not urgent: not attacker-reachable, one command to recover, and the race needs a clear inside a single read-modify-write. Left out of the effort that surfaced it because the fix changes `bindSession`'s contract for its other caller, the Stop hook, which deserves its own round rather than riding a gate change.

- **The doctrine parity gates prove presence only for the bullets that defer (2026-08-18, narrowed 2026-08-23 by the process-rule-repairs effort).** The whole-body comparisons in `test/doctrine-parity.test.js` and `test/output-style-parity.test.js` pass whenever the copies agree, so a line deleted from all three leaves both green. The pinned-phrase fix this item asked for now exists in part: eight assertions in `doctrine-parity.test.js` hold seven bullets present beside the identity check, and the open question of what belongs on the list has an answer the last three efforts converged on rather than argued. Deferral is the criterion. A bullet carrying its own content fails loudly when deleted and needs no pin, while a bullet that moves its operative content to a skill and keeps a pointer fails by going quiet, which is the only green-passing deletion path. The residual gap is therefore scoped rather than total: every content-carrying bullet is still unpinned, deliberately, and the failure mode is a deletion nobody meant, which review normally catches. Reopen only if a content-carrying doctrine rule is ever found missing from both copies.

- **Confirm the intake gap check's declared assumptions actually reached the recap (2026-08-18, operator-only, from the intake-gap-check close-out).** The shipped rule says an assumption a session proceeds on must reach Scott on a surface he reads, and the brainstorming half of that rule is the recap he approves a plan from. The check is one no session can run: it needs the transcript of the session that wrote a spec, and by the time a run executes that spec the approving conversation is gone. Read `archive/claude-kit_intake-gap-check_spec_v1.md`'s own `## Assumptions` section, three entries, and confirm each appeared in the recap approved on 2026-08-18. An entry that did not appear reopens the brainstorming half of the surface rule as a new round; all three appearing closes it. Cheap to fold into the next plan approval instead: read the recap against the spec's `## Assumptions` section at the moment of approval, which is when both are in front of you.

- **Prove route (c), the ask branch, on a gap a model cannot wave off (2026-08-18, from the intake-gap-check proof run).** The proof run established the enumerate-and-declare half of the intake bullet against a genuine pre-change control and left the ask branch unexercised. Both GREEN arms enumerated their gaps, named the operator-owned one first, and declared it with a reversal rather than asking, judging it non-material. The cause the Chapter records is the framing: a scratch module makes almost any gap non-material, so the model routes to (b) honestly and route (c) never fires. A later proof has to remove that escape, which means a brief whose gap is expensive to get wrong (money, a customer-visible policy, a migration that cannot be undone) in a codebase the arm believes is real. Until then the ask branch is shipped on design reasoning alone. Not urgent: the failure mode if the branch is weak is a session declaring where it should have asked, which the close-out surface still shows.

- **Reconcile the close-out assumption filter between the doctrine and finishing-work (2026-08-18, from the intake-gap-check docs curation).** The doctrine bullet says the close-out status carries an assumption "made while I was away"; `finishing-work` step 5 lists every entry on the Chapters' `Assumptions:` lines unconditionally. The behaviours form a safe superset, finishing-work showing more and never less, so nothing is lost today and this is not a defect. What it costs is a future editor reading the doctrine alone and looking for a filter no skill implements, or implementing one. One clause in either text settles it; the open question is which way, since the unconditional list is the one that survives a session that cannot tell whether Scott was watching.

- **The intake gap check does not exist for a live session until the plugin cache updates (2026-08-18, operator-only, from the intake-gap-check close-out).** Same shape as the plugin-update items above and worth folding into the same update. The kit runs from the installed plugin cache, and the doctrine a session reads at every session start is rewritten from that cache, so the new bullet, the brainstorming assumptions block and fresh-eyes step, and the executing-work and finishing-work surfaces are all inert until the update lands. The `blind-reader` and `prose-reviewer` agents the document review battery shipped are in the same position and are not dispatchable at all, which is why this effort's own document sections took the code review pair instead.

- **Make `.kit/` self-ignoring instead of relying on the consuming repo (2026-08-16, from the kit-goal-queue security review).** Everything the kit writes under a project's `.kit/` is scratch that should never be committed: the goal state and its plan paths, the compaction checkpoint, and, since the compaction-deferral effort of 2026-08-25, the gate's decision state and its journal, which carries a session id and one line per verdict. Nothing enforces that. The kit's own `.gitignore` covers `.kit/`, but a consuming project that never added the line commits the directory on the next `git add -A`, and a committed goal state puts one clone's plan paths into history where every collaborator's session-start notice reads them back. The queue made that payload several times larger, which is what surfaced it. The cheap real mechanism is a nested ignore: whichever code creates `.kit/` also writes `.kit/.gitignore` containing `*`, and git honors it regardless of the root `.gitignore`. One creator of four already does it. `memory-recognition-nudge.js` writes that file when its nudge log first creates the directory, because it is the one creator firing from a hook on every tool call rather than behind an explicit operator action, so its own log is covered on a repository that never excluded `.kit/`. The other three do not, and they are the ones carrying the plan paths and the session ids: `kit-goal-lib.js` twice and `kit-compact-lib.js` once. What is left is the sweep rather than the mechanism, which wants one shared ensure-directory helper those three take rather than a fourth inline `mkdirSync`. What shipped instead is a doctor check that reports the `.kit` directory tracked (naming every tracked path it found), untracked-and-unignored, or ignored, with the fixing command; that detects the exposure but does not prevent it, and it only runs when somebody runs the doctor. The check was keyed on the single path `.kit/goal-state.json` until the compaction-deferral effort widened it to the directory, which is what makes it speak on a repository that tracks only the gate's journal. Do the helper when something else next touches those creators.

- **Give a finished queue's blocker notes a durable home (2026-08-16, from the kit-goal-queue reviews).** When `/kit-goal` arms several plans, each plan that ends on a `BLOCKED:` has its blocker's first line recorded into the goal state's `history`, and the last plan's release deletes that state file outright. So once a queue finishes, the content of every mid-queue blocker survives only in the session's own closing summary, which is a soft guarantee: a session that dies or is compacted before writing that summary leaves the operator with plan paths and no blockers. The `goal-blocked` event fires per plan and carries the plan path but no note, by contract. Two candidate fixes, neither taken because both are event-shape or new-surface changes the queue plan deliberately scoped out: add a note field to the event, or write the history to a `.kit/` sidecar before clearing. Judge it on the first real multi-plan queue that actually blocks somewhere: if the closing summary reliably carries the blockers, this stays closed. Raised by the blind reviewer over Sections 1 to 3, adjudicated as a known limitation rather than a defect and documented in `architecture.md` under Goal release events. **Amended 2026-08-27 by the blocked-escalation effort:** the survives-only-in-the-closing-summary premise is false for the window a goal is armed. The machine-coordinator seat's BLOCKED funnel reads a blocked project's goal state through the goal CLI and recovers the recorded note from its history, so a mid-queue blocker has a second reader for as long as the queue is live, and `architecture.md` was corrected in the same effort. What this item still owns is the narrower half, unchanged: the last plan of a queue records no note at all, and the state file is deleted when the queue completes and the goal clears, so the durable home is still missing once that happens.

- **Turn boundary-gated compaction on, then judge it on a real run (2026-08-15, operator-only, from the boundary-gated-compaction close-out).** **Order matters, and getting it backwards is worse than doing nothing: update the installed plugin first, then set the window.** The gate runs from the installed plugin cache, which at close-out was still at an older commit and carried no compaction hooks at all (verified). Setting `autoCompactWindow` against a cache with no gate makes the harness compact early *and* mid-chapter, which is strictly worse than the late-but-unmanaged status quo. So: update the plugin, confirm the cache carries `hooks/kit-compact-gate.js`, and only then set the window. The gate does nothing until `autoCompactWindow` is configured, and writing that value is a change to live machine config, so it waits on `.\doctor.ps1 -Fix` (it offers 285,000, backs the file up, verifies, and aborts rather than clobbering). That value, the 800,000 safety ceiling, and the 10-minute checkpoint bound were re-tuned on 2026-08-15 after the originals were found sized for a 200,000-token window when plan sessions run on roughly 1,000,000; see `archive/claude-kit_compaction-window-retune_spec_v1.md`. A machine carrying any other configured value reaches 285,000 through a `-Fix` re-run, which the doctor's mismatch report names; that one prompt is withheld from `-Yes`, because replacing a value the operator chose is not something an unattended run can judge. Once it is set, the verification is a real multi-day unattended plan run, which no session can produce: the scrollback should show compactions landing right after Chapter commits rather than mid-section, and the stuck or circling stretches that motivated the effort should shorten. Two outcomes reopen work rather than closing this: a run that wedges near the context limit with compaction still deferred means the safety valve failed its one job and reopens the gate, and compactions still landing mid-chapter means the checkpoint or its expiry is mistuned. A third thing to watch rather than act on: the ceiling assumes the roughly 1,000,000-token window plan sessions run on, and on a materially smaller one the valve would never fire and the gate could hold a session against its hard limit, which nothing detects. First real-run data (2026-08-17, a 12-hour two-plan run on the machine that is not the one this repository is checked out on): the gate held mid-section throughout and the valve landed the compaction, so both mechanisms did their job; but no checkpoint was ever opened, because the session was executing a compaction-truncated copy of executing-work that kept step 0 (clear) and lost step 8 (open), so the compaction fell wherever the valve fired, mid-section. That is a session-side omission the re-load instruction added the same day addresses (doctrine, executing-work, and the compact-source SessionStart block), not a gate defect; the next real run is still the verification, now with the re-load in place. The scrollback this item asks you to read is now a file instead: `.kit/compact-gate.jsonl` in the run's own project records one line per auto-compaction verdict, so "did compactions land at boundaries or at the valve" is answered by reading the `reason` field of each `allow` (`checkpoint` is a boundary landing, `valve` is not) rather than by scrolling. Second real-run data point (2026-08-25, this repo, the compaction-deferral plan's own leashed run): two `deny-boundary` records, then an `allow` with `reason: checkpoint` honoring a 3260-second-old checkpoint on the pending leg, so the compaction landed at the declared boundary and the checkpoint was opened this time. Both failure directions this item watches for stayed clear. It remains open because its verification is a multi-day unattended run and this is a single-session observation.

- **Make the compact-source SessionStart block carry a computed state slice, not only an instruction to re-read (2026-08-18, same read).** On `compact`, `session-start.js` tells the session to re-load the governing skill and re-read the plan doc, and lists each In Progress plan's path and commit model (`session-start.js:382-391`); everything past that is left to the model's next tool calls, which is the step the summary-truncated run in the kaizen note skipped for four sections. The candidate: fold a small deterministic slice from the durable artifacts into the block itself, computed by code rather than recalled by the model: the armed plan's current section (first heading whose Status is not Complete), the latest Chapter's `Next:` line, whether a compaction checkpoint is open, and the goal queue position. Bounded to a few lines so it never becomes a second copy of the plan doc in context; the doc stays the source and the slice is the pointer into it. Judge it on the same signal as the mechanize item: a run after the re-load fix that still resumes on the wrong section or skips the checkpoint. If the prose re-load holds, park it.

- **Confirm the new effort pins are live in the installed plugin cache (2026-08-11, operator-only, from the reviewer-effort-compensation close-out).** The four agent definitions carrying `effort` changes are verified by file content only. The kit runs from an installed plugin cache rather than this working tree, and no session has confirmed the cache picked the new values up; every reviewer dispatched during that effort carried an explicit Workflow effort override, which would mask a stale cache completely. Confirming it needs a plugin update followed by one ordinary Agent-tool reviewer dispatch observed at the expected effort, which is Scott's to run. If the cache does not carry them, the fix is a plugin update rather than a code change, and it reopens nothing: the shipped rule is correct either way, it would just be running at the old defaults until the cache refreshes. **Amended 2026-09-06 by the reviewer-fable-uncap close-out:** five reviewer agents now carry pins (`low` on adversarial-reviewer, blind-reviewer, blind-reader and prose-reviewer, `medium` on security-reviewer), and an Agent-tool reviewer dispatch at frontmatter effort is every per-section round over an opus- or fable-tier writer rather than a special observation, so the confirmation falls out of the worker's first review round after the next `claude plugin update` and restart.

- **Comparative lessons-learned across the three repos' update waves (parked 2026-08-25).** When the claude-kit, discord-channels, and ai-os update efforts now in flight have all finished, run one comparative retrospective over their records (Chapters, kaizen notes, the peer-messaging transcripts) rather than three separate close-outs. The operator's framing to carry in verbatim: the strongest observed result of the 2026-08-25 peer-messaging experiment was recursive improvement, context spread across warm sessions with different expertise feeding retrospection, capture, and planning through the kit machinery, against the prior cycle of backlog-then-eventually-cold-session with no knowledge of cause. Two questions the retro should answer beyond the comparison: whether consult answers tracked the target's role (an Opus executor gives detailed ground truth, not goals-and-directions altitude, so the mesh may need an explicit altitude map of who to ask what), and which lessons replicated across all three repos versus appearing in only one. Signal: all three efforts reach Complete.
- **The Dispatch Authorization by-reference convention is live; one line at the format's owner remains, post-queue (parked 2026-08-26, signal fired and narrowed 2026-08-27).** The remote is public, and the format definition (`plugins/claude-kit/skills/kit-goal/SKILL.md:46`) asks only who authorized the run, when, and which sessions, saying nothing about quotation. The sweep the original entry envisioned has no editable live surface left: the three plans carrying a grant today record it by reference (date, scope, the artifact holding the operator's words; landed at `21d68c9` and armed on that form the same day, which is the fired signal), the two live operator quotations a pattern-plus-control grep of `docs/plans/` found outside authorization sections (`claude-kit_session-roles_notes_v1.md`) were converted in the same change, and every quoted grant now sits in `docs/archive/` (coordinator-and-roles, compact-boundaries, dispatch-authority), immutable history whose innocuous approval sentences are in the public git history irrecoverably either way. Residue: one sentence at the format's owner stating the convention (in a public repository the grant is recorded by reference, date, scope, and artifact, never quotation), deferred because a plugin-payload edit while the five-plan queue runs stales the build stamp the leashed worker gates on. Signal: the armed queue completes.
- **Turn the in-context advisor off, and update the plugin (2026-08-15, operator-only, from the consult close-out).** Two machine actions the kit cannot take for itself. First, `/advisor off`, or remove the `advisorModel` line from `~/.claude/settings.json`; QA confirmed at close-out that it is still set to `opus` on this machine, and the kit never edits live machine settings. Second, update the installed plugin: every shipped change in that effort lives in the plugin cache, which is still at a pre-effort commit, so the consult skill and the consultant agent do not exist for a live session until the update lands, and the doctrine a session reads is rewritten from that cache at every session start. Order does not matter between the two, but the advisor flip is the one with a reopening condition: if long-run quality visibly degrades with no consults firing, that outcome reopens the trigger wording rather than the decommission.

- **Consult adoption watch (2026-08-15, from the consult close-out).** The consult's trigger floor was set by judgment, not by data: a counted floor (a second failed attempt, any decision-shaped BLOCKED, a debugging dead end) plus a general license, deliberately chosen to be recognizable by a re-reader rather than to depend on a stuck session's own sense of being stuck. Judge it on the first real long run, on three signals the run produces for free: the `consults <n>` counts in the Chapter Metrics lines, whether every BLOCKED that reached the operator arrived with a ruling attached, and how many rounds burned before the first consult fired. The two failure shapes point opposite ways and the fix differs. A BLOCKED with no ruling and no consult in the Metrics, or a third same-tier attempt on one problem, means the floor is too narrow or the wording is not firing, and reopens the trigger wording in `consult/SKILL.md` and `executing-work/SKILL.md`. Consults firing on questions the spec already answered means it is too wide, and the floor tightens. Do not widen or tighten speculatively; this item owns that call and it waits on a real run.

- **Open experiment: Haiku implementer-tier band (from kaizen, 2026-07-03; kept on the operator's call, decided 2026-08-22).** The haiku transcription band shipped ahead of demand, so its edges are provisional. First recorded data point (2026-08-22 sweep of every `Implemented By:` line in docs/, since transcript stores rotate out in weeks and Chapters are the durable dispatch record): one dispatch ever, `archive/claude-kit_hook-canary-and-goal-events_spec_v1.md` Chapter 3 (2026-07-25, commit 174991b), a faithful clone on the first attempt with 0 NEEDS_CONTEXT and 0 escalations, though its per-section review pair was skipped under the trivial-section rule, so the driving signal below has still never literally fired; the quality evidence there is the controller's full-diff read and a doctor PASS. The 2026-07-07 to 07-14 workstation corpus had zero (nothing was tiered haiku). Driving signal unchanged: haiku-tier sections passing first-round adversarial review keep the band; escalations to sonnet are the mis-band counter; do not widen speculatively. The same-day qa-verifier opus-to-sonnet re-pin reverts with one line in `agents/qa-verifier.md` if finishing passes start missing acceptance-criterion nuance.
- **Tune the memory store's invented constants on real evidence (2026-07-31).** `EXTEND_PER_APPLIED_DAY = 30` and `EXTEND_CAP_DAYS = 365` (`plugins/claude-kit/scripts/memq.js:497-498`), both digest budgets, `RECALL_MAX_LINES = 200` and `RECENT_MAX_LINES = 200` (`:379-380`), the successor display cap `SUPERSEDED_SHOWN = 4` (`:378`), `recent`'s `RECENT_DEFAULT_SINCE = '1d'` window (`:8350`), and the neighbour floor `NEIGHBOUR_FLOOR = 0.30` (`:564`), the similarity at which the authoring verbs' neighbours block and the decay scan's pairs block read two records as one fact, tuned at a decay pass that has a pairs block to read, were seeded by judgment with zero observations behind them, the same posture as the haiku implementer band above: tune on real numbers, do not widen speculatively. The instrument that produces those numbers now exists but has not accrued: nearly every record in this project's store scans `applied never`. The applied stamp is a self-report a session makes with `memq touch --applied`, and the store holds almost none, so the reinforcement formula is shipped, correct, and currently inert. Two separate triggers, neither of them a date. For the two extension constants, the first `decay-scan` whose standing usage line reports applied stamps across several files: check whether real tallies push memories past the thresholds too fast or not fast enough, and whether the 365-day cap ever binds on anything. For either digest budget, the first digest that prints a counted remainder at all, since a truncation only ever appears when the budget binds and it names which surface it cut. For the default window, a close-out whose session ran longer than a day and whose digest therefore had to be re-run with an explicit `--since`. The archive index's unbounded growth is the related pressure on the same surface: `recall` caps its read at 64 KB and says so when the cap binds, which converts that growth into a bounded read rather than a bounded digest, so a clip note is the signal to trim the index by hand. The same posture covers `memq.js`'s six semantic-ranking constants (`SEMANTIC_SHOWN`, `SEMANTIC_FLOOR`, `SEMANTIC_APPLIED_BOOST`, `SEMANTIC_BOOST_CAP_DAYS`, `SEMANTIC_ARCHIVE_DEMOTION`, `SEMANTIC_SUPERSEDED_DEMOTION`, at `plugins/claude-kit/scripts/memq.js:324-329`): seeded by judgment against one measurement of one model before any real semantic-search usage existed, so tune them against the first `find` runs whose rankings can be checked against felt relevance, not on a schedule.

- **A memory used mostly from other projects under-accrues applied stamps here (2026-08-03, from the synced-semantic-memory finishing reviews).** `memq touch --applied` resolves only the caller's own tiers, so a shared-tier memory another project applies constantly but this one merely reads takes no stamp from this project's sessions. The clock still ages it correctly for every project that does apply it, so this is a demotion risk for one under-using project's view rather than a loss: another project's stamps keep it alive machine-wide. Not a defect, since `touch` resolving only the caller's tiers is the scoping every memq command uses; flagged so a memory that scans idle from one vantage is checked with `memq find` across stores before being archived by hand.
- **Extend the integrity manifest beyond hooks/ (2026-07-31, from the section 2 security review).** The build's hash manifest covers the shipped hook files only, but the same user-writable plugin cache ships other executed payload with the same rationalized-edit exposure and no detection: `scripts/memq.js` and `scripts/memq-shim.js` (whose output is injected into session context and whose type-tier fencing the security model treats as a control) and `doctor/*.ps1`. One member still changes the sizing rather than lengthening the list, and it is no longer the watcher: `doctor/sync-store.ps1` is what the kit executes unwatched. `memory-session.js` spawns it detached, with its streams discarded and the child unreferenced, and it fetches, rebases, commits and pushes the memory store to a private remote out of that same writable cache. Its trigger is an attended session start, but nothing watches the run and no canary sees an in-place edit to it, because the manifest does not reach `doctor/` either. That is a narrower case than the watcher was, since the trigger is at least attended, and it is not the attended-session one this item was scoped for. Deliberate scope choice in the backlog-sweep effort, not a defect; extend the manifest and canary walk to those directories in a later pass. Ride-along from the same review: `build.sh`'s stamp block has never run end to end on a POSIX box (verified here by transcription and a read-only fragment run; the shasum and openssl fallback branches have run nowhere), so give it a one-time smoke on WSL or Linux before anything relies on a build.sh-produced stamp - the natural moment is the doctor.sh Linux-sibling item below.
- **Tag the multi-sidecar readers' malformed-line notes (2026-08-04, from the find-legibility finishing reviews).** `readUsage` takes an optional tier tag that rides in its stderr notes, so a run reading several sidecars can say which one held the bad line. `memq unstamped` passes it, and `decay-scan` was tagged on 2026-08-25 by this repository's kaizen batch, whose section 10 gave that scan a candidate-suppression clause that sends an operator to remove the exact line, so an ambiguous note there had become actively misleading. Three commands still emit the bare `skipping malformed usage line N`, which names no file to go fix: `recall` (three or four sidecars), `recent` (one per tier), and `find`, which is the widest of them all and the one to do first: its `tallyForTier` reads a sidecar per tier per store across every project store on the machine, so its note is ambiguous across stores as well as tiers. One argument per call site, plus a store segment in `find`'s tag.
- **`memq recent`'s added-versus-updated split does not mean what it reads as (2026-08-01, found by running the shipped command against the real store).** The label is correct against its stated rule and its tests, and still misleads: the Edit and Write tools rewrite a memory file rather than patching it in place, which resets `birthtimeMs` to the moment of the edit, so a memory that has existed for a week and was edited today reports `added`. Measured on this store: `subagent-wait-mechanics.md` and `plugin-cache-snapshot-inert-hooks.md`, both days old and both edited in that close-out, carry a birthtime equal to their mtime to the millisecond. The platform gate is doing its job (Windows keeps a real creation time); what the birthtime tracks is the writing tool's behavior, not the record's history, so on this toolchain `added` effectively means "written by a tool that recreates the file" and fires for nearly every memory write. No test caught it because fixtures construct files directly. Three ways out, cheapest first: drop the split and label every memory file `updated`, which is true either way and costs the platform gate; key `added` on the memory's own `created:` frontmatter date falling inside the window (`readFrontmatterCreated` already exists and `lastAliveMs` already reads it), which is author-asserted and absent on some records; or keep the split and say plainly in `memory-system` that `added` means first written by this toolchain. The middle option is the recommended one, since the frontmatter date is the only signal that describes the record rather than the tool.
- **Fence `memq recall`'s journal lines under a store pin (2026-08-01, from the memq-session-recap finishing reviews).** `cmdRecall` prints journal lines at column zero whatever the pin state, so under a pin each carries the latest summary another of the instance's workers wrote for that key, in the tool's own voice. `memq recent` fences its journal lines for exactly this reason, and the security model's keyed-on-the-pin rule reaches both. Deliberately deferred rather than missed: `recall`'s surface is narrower (one line per key against `recent`'s one per entry inside a window), and closing it changes a shipped command's output and its expected-output tests, which is a separate call from adding a new command. The counterweight is frequency, and it argues for doing it: `recall` runs at every effort start while `recent` runs at close-out. The fix is one `projectIndent` on the journal line at `plugins/claude-kit/scripts/memq.js:3486`, plus the recall tests' expected output and the exception paragraph in `docs/security-model.md`.
- **Workstation (ASR) config repairs (2026-07-15 verification pass).** A user-level graphify PreToolUse hook is dead machine-wide and noisy - it fires on virtually every tool call and exits 127 with the mangled path `C:UsersASR.localbingraphify.EXE` (backslashes stripped before reaching bash), so the guard silently never runs; fix the hook command quoting or remove the hook.
- **Explore channels for unattended interactive runs (research preview) (2026-08-23, parked 2026-07-08).** Kept and re-scoped: a gated channel relay is now live in-session, delivering operator messages from a Discord thread through an allowlist that names one account, so the "is a channel usable at all" half of this exploration is answered by a running system rather than by a prototype. What is unexplored is the other two halves the item named, a scheduler or webhook nudging an overnight run's next turn, and permission relay answering tool-approval prompts from a phone. Neither has been stood up. A channel is an MCP server that injects external events into the interactive session as model input (code.claude.com/docs/en/channels-reference). Two uses for long runs, both keeping the work on subscription billing: a local webhook or scheduler nudging an overnight run's next turn, and permission relay (v2.1.81+) answering tool-approval prompts from a phone. A channel cannot execute `/resume` (events are conversation input, not UI commands), so it nudges a live session rather than starting or swapping one. Caveats: custom channels need `--dangerously-load-development-channels` at every session start while off the allowlist, senders must be gated (an ungated channel is a prompt-injection vector), and undelivered events drop silently. Decided 2026-07-08: prototype when the next overnight-run need materializes.
- **A `doctor.sh` sibling for WSL/Linux (2026-07-09).** The Windows doctor is verified on both machines (see the Q3 snapshot); the frictions it fixes were all Windows-shaped, so the Linux sibling was deliberately skipped. When it lands it should absorb `setup.sh` the way doctor's `-Fix` absorbed setup.ps1/setup.cmd on Windows, leaving doctor as the single first-run tool everywhere.
- **Push-time stranded-branch guard (candidate, 2026-07-17).** When a repo auto-deletes merged head branches, a late push to a just-merged branch silently recreates it off the integration line (the stranded-commit trap), signaled only by `git push` printing `[new branch]` for a branch that should already exist. The kit already ships merge-strand-guard and branch-hygiene, and the manual strand-check caught a live instance on the neo project, so this is defense-in-depth, not a gap. A mechanical guard would flag an unexpected `[new branch]` on push. Build only if the trap recurs despite the existing guards; the strand-check working is why it is parked, not prioritized.


- **Open experiment: effort dials and reviewer tiering in flight (2026-07-29; dials set 2026-07-31).** The effort knob exists after all, as subagent frontmatter rather than per-dispatch: `effort` (low/medium/high/xhigh/max, overrides session effort; code.claude.com/docs/en/sub-agents.md). Current pins, after the 2026-08-11 revision: `effort: medium` on implementer-sonnet, implementer-opus, qa-verifier, docs-curator; `effort: high` on adversarial-reviewer, blind-reviewer, security-reviewer, implementer-fable, consultant, and, since the document review battery shipped on 2026-08-18, blind-reader and prose-reviewer; no field on implementer-haiku (Haiku 4.5 lacks effort support), council-member (it runs in-line during a brainstorm, where the session's own effort is the right call), design-facilitator. The same change added the reviewer model rule (executing-work step 3, finishing-work step 3): per-section reviewers one tier above the writer, finishing adversarial review always fable. A frontmatter dial cannot split per-section from finishing reviews (same agent type). **Partly closed by the reviewer-effort-compensation effort (2026-08-11).** The claim flagged here as most likely wrong, that the finishing gate riding fable-at-medium was acceptable, was retired rather than tested: reviewers now pin `effort: high`, and effort is chosen per dispatch from the model the reviewer actually runs at and whether that model is the aimed-at tier or a stand-in for one above it. The split this item called unreachable is reachable, by a route the note never considered: a per-dispatch effort override does not exist on the Agent tool and never did, but `Workflow`'s `agent()` carries `model`, `effort`, and `agentType` per call, and an `agentType` dispatch there stays governed by the read-only guard. What stays open is the measurement, unchanged: judge the new levels on Chapter Metrics against the pre-change baseline (review rounds, NEEDS_CONTEXT, escalations, findings quality). Revert is one frontmatter line per agent plus the table rows. The Haiku implementer band above is still untested and still waiting on its own data. **Amended 2026-08-19 by the reviewer-tier-cap effort:** the per-section bump is now capped at opus and the security-reviewer is inside the rule rather than outside it, with one exception keeping it off fable per-section. That change folds into this experiment rather than opening a second one, so the levels being judged are the post-cap levels; the pre-change baseline table it is judged against is in Chapter 1 of `docs/archive/claude-kit_reviewer-tier-cap_spec_v1.md`. The effort generator described earlier in this item is superseded by the same change: effort now turns on whether the reviewer's model sits above the writer's tier, not on the aimed-at-versus-stand-in distinction, so an Opus reviewer reaches `max` whenever it has no headroom over what it is reading. **Amended 2026-09-05 by the reviewer-fable-uncap effort:** the cap is retired and every per-section reviewer runs at Fable, effort by lens (`low` for the code and document pairs, `medium` for the security-reviewer, finishing unchanged at `high`), on the operator's reading that Fable 5.1 at `low` barely moves the allotment and that review rounds are where runs spin. The per-section effort now lives in the reviewer agents' frontmatter, the finishing pass takes the Workflow route to `high`, and the Opus `max` row is back to its 2026-08-11 ground, unavailability alone. The levels being judged are these; the post-cap baseline they are judged against is in Chapter 1 of `docs/archive/claude-kit_reviewer-fable-uncap_spec_v1.md`, and its Chapter 2 carries the per-row table. When reading later Chapter Metrics, count the rounds in which a `low` reviewer made no tool call at all as their own class: the one known trait of Fable 5.1 at `low` is a reluctance to call tools, and a diff reviewer that read no file returned a green it did not earn. **Amended 2026-09-06 by the reviewer-tier-hybrid effort:** before the uncap reached any worker, the operator's weekly Fable reading under the capped rule was a few percent ahead of pace, so the per-section reviewer returns to one tier above the writer, with Fable rather than Opus as the ceiling: opus- and fable-tier sections keep the Fable reviewer at the uncap's frontmatter efforts, and a haiku- or sonnet-tier section's reviewer (Sonnet or Opus) runs at `high` through the Workflow route. The levels being judged are these, against the same Chapter 2 baseline; no worker round having run under the uncap on this machine, it produced no Chapter Metrics. The next lever if the bars keep climbing is the Opus cap over opus-tier sections, per `docs/plans/claude-kit_reviewer-tier-hybrid_spec_v1.md`.
- **Pin executing-work's reviewer-effort table rows to the agent frontmatter map (2026-09-05).** `test/readonly-agent-guard.test.js` pins each reviewer's frontmatter effort to a literal map, and the table at executing-work step 3 carries the same values as a third copy nothing reads, so the table can drift from the frontmatter while the suite stays green. Have the test parse the table's backticked efforts and compare them to the map. Deferred from the reviewer-fable-uncap effort because that test file sits at its line cap and the budget file was another session's in-flight edit.
- **State in `docs/security-model.md` that the finishing route's read-only guard coverage is conditional on the Workflow `agentType` field (2026-09-05).** The finishing reviews now take `Workflow`'s `agent()` by default rather than only as the compensation exception, and no hook matches the Workflow tool, so the read-only guard binds a finishing reviewer only where the orchestrator fills `agentType` as the Reviewer Dispatch template requires. The guard's coverage in code is unchanged; what an auditor cannot see from the security model is that the finishing security pass now rests on that instruction-level field by default. One sentence naming the precondition, beside the guard's existing description. Deferred from the reviewer-fable-uncap effort's finishing security review because that file was another session's in-flight edit. Under the reviewer-tier-hybrid rule (2026-09-06) the per-section rounds over haiku- and sonnet-tier writers, their security-reviewer included, take the same route, so the precondition covers those passes too.
- **Install the reviewer-fable-uncap and reviewer-tier-hybrid rules on this machine (2026-09-06, operator-only, from the two close-outs).** Run `claude plugin update`, then restart the KIT: Worker session, since a running session reads skills and agents from its frozen plugin view and every review it runs until then is under the retired Opus cap; then watch the Fable usage bars over the next few worker sections. The next lever is the Opus cap over opus-tier sections, one commit over the hybrid plan's section 1 files, and the open reviewer-tiering experiment above is where the levels are judged.

- **Fable 5 prescriptiveness audit, trigger-based (2026-07-29).** Anthropic's Fable 5 guidance says skills written for prior models are often too prescriptive for it and can degrade output, and that brief instructions now steer as well as enumerated case lists. The kit's heavy enumerations (the completion contract's rationalization table and red-flags list, the dispatch-brief template) each trace to an observed failure, so nothing is thinned speculatively; the writing-skills bar cuts both ways. The trigger: a Fable-led run observed fighting or over-applying a specific enumerated rule makes that rule the candidate to thin, RED/GREEN-tested like any behavior-shaping change.

- **Kaizen scoping companion against over-generalization (deferred 2026-07-26).** The kaizen skill's "State the lesson, not the incident" rule generalizes captured notes; the symmetric failure (Twain's cat: over-generalizing to "never touch stoves") has a pre-written fix, a companion line "Generalize to the cause, not the scene. The lesson covers everything hot, and nothing that's merely a stove." Deliberately left out per writing-skills' observed-failure bar. Add it only when an actually over-generalized kaizen note or memory is observed; the "one level more general" phrasing already bounds the swing, which is why this is expected to stay parked.

- **Decide whether the read-only guard should deny outbound-network commands (2026-08-18, from the document-review-battery security review).** The guard's strict class denies write-shaped commands and says nothing about `curl`, `wget`, `Invoke-WebRequest`, `Invoke-RestMethod`, or `nslookup`, and the tree-state capture that backstops a review round sees only worktree deltas, which a network call never leaves. So the sole control against a read-only reviewer being talked into exfiltrating by a document it was told to review is the charter sentence telling it not to. Both new document agents state that prohibition with its rationale, which is the strongest form the kit's model currently offers, and the exposure predates them; what the effort changed is that two agents now exist whose entire job is reading documents that may come from outside. The candidate is naming the outbound-network commands in the strict-class denylist, which is cheap and mechanical. The argument against is that a read-only reviewer sometimes has a legitimate reason to fetch (a version, a published doc), so the denial would need an escape and the escape is where this gets expensive. Judge it the next time a reviewer agent is given a document from a source the operator does not control.

- **Decide whether the disclosure sweep needs a section-time trigger (2026-08-18, from the document-review-battery adversarial review).** The disclosure sweep runs at finishing, because that is where the audience predicate lives. Under Commit-and-Push, a section closes by committing and pushing, so an outward-facing document carrying a leak is already on the remote before the sweep that would catch it ever runs. The executing-work security-reviewer trigger list (input handling, auth, SQL construction, secrets or configuration, an external boundary) does not name a section whose spec carries a `Disclosure:` list, so nothing pulls the sweep earlier. This matches what the spec designed rather than drifting from it, which is why it was recorded rather than fixed at close-out; the proof runs are the counter-evidence, since they exercised the sweep at what was effectively section scope and it fit naturally there. The fix, if taken, is one entry on that trigger list. Judge it on the first real effort that ships an outward-facing document under Commit-and-Push.


- **Silent store auto-sync: the fail-safe hardening deferred at ship (2026-08-19, from the worktree-store-and-autosync Section 2 close-out, `docs/plans/claude-kit_worktree-store-and-autosync_spec_v1.md`).** The auto-sync shipped after five review rounds, security-clean of any Critical or security-Major (the inbound screen converged to a completeness-proven mode+path allowlist). The operator authorized shipping now with these residuals parked (Option A, 2026-08-19); every one is fail-safe (it wedges the sync loudly or misses a nudge, never leaks or silently corrupts) and most need a party who can push to the private remote. Judge each on real multi-machine use; none is urgent. (1) **Intermediate-commit history wedge:** the inbound screen reads the upstream TIP tree, so an add-then-remove of a disallowed path across incoming commits presents a clean tip, the rebase latches the intermediate commit into local history, and the post-rebase probe then wedges a loud `leaks` gate (push blocked, no leak). The complete fix screens the incoming range, which is fragile across merge commits; the tip screen covers the common case. (2) **Hostile managed-file intake:** the screen admits an incoming `.gitignore`/`.gitattributes` without a content check, so a peer's non-canonical one materializes over the live allowlist, the drift gate blocks the push, and `doctor -Fix` refuses to rewrite a Foreign managed file, so the loud line's own remedy cannot clear it (the operator fixes the file by hand). Fix: screen the two managed files' content (canonical or marker) on intake. (3) **Overridden-root silence on win32:** a pending store at a non-default `KIT_MEMORY_ROOT` gets neither the spawn (by design) nor any text (the state-file and marker paths never run there), so its drift is silent, where off-Windows and the base hook nudge. Fix: emit the text fallback for a pending owned non-default-root store; the cost that parked it is that the transient-streak and backstop tests use non-default stores as proxies and would need reworking onto default-store fixtures. (4) **Outbound probes' own blindness:** `install-memory-sync.ps1`'s tracked/history leak probes read paths only (no mode axis) and dedupe objects, the exact two gaps the inbound screen just closed, so a mode-120000 entry or a rename in local history could publish; same-user reachable, low value, and in the shared installer the section otherwise left alone. (5) **PATH steering of the bare `git`:** the detached sync invokes `git` by name, so a repo-carried PATH could choose the binary the unattended credential-using run executes, the same env-steering class as the `USERPROFILE` anchor the section did close; the fix is an absolute git path across the sync's invocations.

- **Type-check the session-id comparison the leash and the compaction gate both rest on (2026-08-21, from the boundary-ritual-reinforcement Section 2 security review).** `sameSessionId` in `plugins/claude-kit/hooks/kit-compact-lib.js` coerces both sides with `String()` before comparing, so a non-string `session_id` can compare equal: a payload carrying the bound id inside an array fired the chapter-boundary nudge in the reviewer's probe. The nudge's own consequence is one spurious reminder, which is why it was not fixed inside that section, but the same comparator decides the PreCompact gate's verdict and the Stop hook's leash binding, where a type-confused match is worth considerably more. Not reachable from a real harness payload, which always sends a string, so this is hardening against a shared primitive rather than a live defect. The fix is one line, requiring `typeof` string on both sides, and its cost is that every caller must be checked for a non-string it currently gets away with.

- **The integrity manifest reports on the files it knows, and is silent about a hook it does not (2026-08-21, from the boundary-ritual-reinforcement Section 2 security review).** `integrityProbe` in `plugins/claude-kit/hooks/hook-canary.js` iterates the build manifest's own keys, so a `.js` file present in the installed plugin cache but absent from the manifest is uncovered silently rather than reported. The manifest is currently complete, and the build regenerates it, so nothing is wrong today; what is missing is the reverse check that would notice a hook file the manifest does not name. This matters more now that a PostToolUse hook writes into the model's context on every edit, which raises the value of a tamper against the cache from one injection per session to one per edit.

- **The checkpoint command does not ask who is calling it (2026-08-21, from the boundary-ritual-reinforcement Section 2 security review).** `kit-compact-checkpoint.js open` writes a checkpoint using the armed goal's `boundSession` with no check on the invoking agent, and `readonly-agent-guard.js` permits writes under `.kit/` for every governed read-only class. The chapter-boundary nudge stands subagents down on the payload's agent keys, which is the only guard that can do it (a subagent's payload carries the parent session's own id, so the bound-session check passes for one), but that stand-down rests on the harness continuing to emit those keys, a field this kit established by probe rather than from documentation. If a future harness drops or renames them, a nudged read-only reviewer could open the compaction checkpoint on the parent's behalf mid-chapter, defeating the control the nudge exists to reinforce. The fix is a refusal in the checkpoint CLI itself when the environment or payload marks a subagent, which is defence in depth rather than a replacement for the guard.

- **Judge the chapter-boundary nudge on a real leashed run (2026-08-21, operator-only, from the boundary-ritual-reinforcement close-out).** The whole effort is a feedback loop for a failure only a live multi-hour run produces: a leashed session that never loads the executing-work skill, closes Chapters from the doctrine alone, and so never opens a compaction checkpoint. Everything shipped is verified in test and by direct hook invocation, and none of that can answer the question the effort exists for, which is whether the reminder actually changes what a session does. Each machine runs the installed plugin copy rather than this clone, so the check waits on the fix reaching both machines. What to look for on the next real unattended plan run: compactions landing right after Chapter commits rather than mid-section, and, in the scrollback, the gate's boundary note NOT still firing after a Chapter has closed. Two outcomes reopen work rather than closing this. A run where the nudge fires and the session still skips the checkpoint means the reminder is reaching the model and losing to something else, which is a wording or a placement question rather than a mechanism one. A run where the nudge never fires at a real Chapter append means one of the seven guards is standing the hook down wrongly on live payloads, and guard 3's agent-key stand-down is the first suspect, since it rests on payload fields established by a probe against one harness version. This item shares its signal with the older "Turn boundary-gated compaction on, then judge it on a real run" item (2026-08-15), and both are now read from the same file rather than from scrollback: `.kit/compact-gate.jsonl` records one line per auto-compaction verdict, so "did the boundary note keep firing after a Chapter closed" is answered by reading the `reason` on each record instead of scrolling for the note.

- **The docs-write guard's redirect heuristic has named blind spots its test table reads as exhaustive (2026-08-23, from the dormant-feature-removal Section 3 security review; narrowed 2026-08-27 by the testing-discipline effort).** `docs-write-guard.js`'s redirect heuristic misses `>|` (the bash noclobber override, because `|` is excluded from the character class before `docs` and so cannot be consumed) and `tee --append docs/x` (the alternation accepts only the `-a` spelling). It sits inside the heuristic-with-a-Stop-scan-backstop risk `docs/security-model.md` already accepts, and the guard is not a security boundary, so this is not a new exposure. What makes it worth a note is that the case table now living in `test/docs-write-guard.test.js` reads as exhaustive and is not. The two misses were traced through the character classes by hand rather than executed, because the reviewer's own probe command was blocked by `docs-write-guard` for containing a `> docs/` string, so confirm them by running the regex probe from a session the guard does not govern. The push-guard half of this entry is closed: `merged-pr-push-guard.js` now finds the push at a real command position, so the newline, bare `&` and subshell shapes it used to miss are guarded and asserted end to end, and the residue it still carries (a quoted string executed later, a backslash-newline continuation, an interposed runner, and every push after the first in one compound) is asserted by name in `test/merged-pr-push-guard.test.js`. The two remaining push-guard items are dated 2026-08-27 further down this file.

- **The README's payload map reads as complete and is not (2026-08-23, from the dormant-feature-removal Section 4 blind review).** The `plugins/claude-kit/` tree in `README.md` enumerates directory contents file by file, which reads as exhaustive, and two entries are absent: `scripts/memory-index.js`, which the doctor invokes and which has its own test file, and `doctor/sync-store.ps1`, which `memory-session.js` spawns detached and which pushes the memory store to a private remote. The second is the one that costs something, because that script is the kit's remaining unwatched executor and the integrity-manifest item above now turns on it. The `doctor/` block listing six of the seven files in that directory is the shape that misleads, since the one it omits is `sync-store.ps1` itself. The skills half of this item is retired on receipts: `standing-watch/` is listed in the map today, and the skills block now carries an entry per directory on disk. Either add the two entries or say in one line that the map is illustrative; adding them is cheaper and keeps the map's own contract.

- **The frontmatter read has no size bound (parked 2026-08-23, from the memq harness-shape section security review).** `frontmatterField` reads the whole file with `fs.readFileSync(file, 'utf8')`, and `frontmatterBlock` then splits the entire file into lines, while only the first 40 are ever consulted: `FRONTMATTER_MAX_LINES` bounds the scan, not the read. Six readers reach the store through that function, so the bound is missing at each of them. The claim this item opened with, that every frontmatter read in `memq.js` is whole-file, no longer holds: the anchors work gave `listMemories` and `readFrontmatterAnchors` a `readHead(file, FRONTMATTER_READ_CAP)` prefix read, which is this item's own remedy pattern applied at two doors. So the scope is now the readers still going through `frontmatterField`, not the file. Records arrive by git sync from other machines, so an oversized record is inside the threat model; the effect is local memory and CPU amplification only, and past roughly 2 GB `readFileSync` throws and fails safe to `FRONTMATTER_UNREADABLE`. The file already ships the remedy pattern elsewhere: an `fstat` on the descriptor plus a fixed-size prefix read. Pre-existing rather than introduced by that section, which is why it is parked here.

- **Check whether the harness rewrites the pending tier the way it rewrites project memory (parked 2026-08-23, from the memq harness-shape section adversarial review).** The SessionStart hook instructs a session to write provenance frontmatter (`run:`, `vector:`, `section:`, `written:`) at column 0 with the Write tool on records it creates under `memory/pending/<run-id>/`. On Claude Code a Write into a project memory directory is relocated into a `metadata:` map; whether that rewriter reaches the `pending/` subtree is unconfirmed. Nothing in this repo reads those four keys back, since only `tags`, `created`, `pinned`, `machine`, `supersedes`, `anchors` and `triggers` go through `frontmatterValue`, so the consequence would land on the external adjudicator that groups a run's writes by `run:`. Confirming it costs one probe: write a record into a pending directory with the Write tool and read it back. If the rewrite does reach it, either the adjudicator learns the two placements the way memq did, or those keys move to a reader that does.

- **The doctor calls an oversized goal state a healthy armed goal while every hook reads it as absent (parked 2026-08-24, from the compaction-deferral Section 6 security review).** `readGoal` now refuses a goal state past 64 KB and returns the same null an absent file returns, but `doctor.ps1`:1217-1225 warns only when the state is unparseable or missing a plan field, which was the pre-change refusal set. An oversized but valid file parses there, so the doctor prints the armed plan and its queue while the Stop hook releases and the compaction gate stops deferring. The security model names the doctor as the surface that flags the damage for a state file the hooks refuse, and it does not flag this leg. Detectable after the fact, since the gate records a `no-goal` reason in `.kit/compact-gate.jsonl`, but not by the diagnostic an operator actually runs. Parked rather than folded because Section 5 shipped the doctor work and closed, and no real run approaches the cap (a fifty-plan queue with history is roughly 18 KB).

- **The agent-identity key list is spelled four times and read two different ways (parked 2026-08-24, from the compaction-deferral Section 3 review).** `readonly-agent-guard.js`, `docs-write-guard.js`, `chapter-boundary-nudge.js` and `compact-deferral-nudge.js` each carry their own copy of the five keys that mark a payload as a subagent's, and the two PostToolUse nudges disagree on how to read them: one tests key presence, the other truthiness. So a harness change to how those keys are emitted gets handled inconsistently across four detectors and only the one someone happens to look at is fixed. The fix is one exported predicate all four call, which settles presence-against-truthiness in a single place. Parked rather than folded because three of the four files sit outside every section of the plan that surfaced it.

- **The Stop hook's boundary directive names its CLI by bare filename and sends the model to a session-unscoped report (parked 2026-08-25, from the compaction-deferral-signal plan, Section 4 review).** Both block reasons that carry the boundary directive name `kit-compact-checkpoint.js open` and `kit-compact-checkpoint.js status` with no path, while the sibling `compact-deferral-nudge.js` composes an absolute path from `__dirname` precisely because its text lands in a model's context as a command to run. A session that runs the bare name from a cwd that is not the hooks directory gets module-not-found. The bare form is pre-existing, but this plan raised the stakes on it: the directive now sends the model to `status` to evaluate one of its own two conditions, so a failed call silently degrades the directive to the Chapter-only behaviour the change exists to replace. Riding with it: `status`'s deferral-episode report is deliberately session-unscoped, so after a crash and re-arm a dead binding's episode reads as a live hold for up to the four-hour idle bound, which is the state most likely to push an operator into opening a checkpoint mid-chapter. The fix for the first half is one interpolation mirroring the nudge; the second half is a reporting decision (a session-scoped line beside the unscoped one) rather than a defect. Parked rather than folded because the fix moves the string the section's test pins match on, and the section it surfaced in closed on a review round with no Critical. **Widened 2026-08-25 at the plan's finishing review**, which found the same bare form at a third site the original wording did not enumerate: the boundary paragraph in `plugins/claude-kit/skills/finishing-work/SKILL.md` names `kit-compact-checkpoint.js status` and `open` the same way, with no plugin-root resolution rule beside it. Three sites, then, not two, and a fix that reaches only the two block reasons leaves the skill body wrong. The under-enumeration is itself the class the plan's third Standing Brief Amendment exists to catch, which is why it is recorded here rather than quietly fixed: the skill file is a concurrent effort's live workspace at the time of this writing and is not this plan's to edit.

- **The deferral nudge parses the whole PostToolUse payload before any guard can stand it down (parked 2026-08-24, from the compaction-deferral-signal plan, Section 6 round-5 review).** The hook drains and JSON-parses stdin on every covered tool return, including every dispatched agent's, before the guards that would stand it down can run, and its own header prices the absence of a payload cap without pricing the parse on that hot path. The obvious reorder, checking the external-engine marker before draining stdin, carries its own risk, so this is a measure-then-decide item rather than a known defect: the first move is to measure the parse cost against a real payload rather than to reorder on suspicion.

- **The status-line widget can attribute a Next: line from an interim board to the Chapter above it (parked 2026-08-25, from the compaction-deferral-signal plan's finishing adversarial review).** `parsePlan` in `plugins/claude-kit/scripts/kit-goal-statusline.js` walks the `## Chapters` block and treats a `### Chapter` heading as the boundary between entries, but not a `### Interim board` heading. A column-0 `Next:` line sitting inside a board would therefore be read as the preceding Chapter's, and only where that Chapter carries no `Next:` line of its own. No plan doc in this repo produces that shape, because interim boards write their pointer lines bolded rather than at column zero, so this is a latent parse quirk with no current misrender rather than a live defect. It is recorded because the widget's whole job is to state a run's position accurately on a surface the operator reads at a glance, and a wrong `Next:` there is indistinguishable from a right one. The fix is one more heading in the boundary test.

- **The compaction gate's decision writer has no compare-and-set, so a bystander session can restore a cleared episode (parked 2026-08-25, from the compaction-deferral-signal plan's finishing security review).** `recordGateDecision` in `plugins/claude-kit/hooks/kit-compact-lib.js` does a read-modify-write with no fingerprint verify, unlike the deferral nudge's own stamp, which was given a compare-and-set for exactly this shape. A bystander session's `deny-interactive` carries the deferral episode through from its own read; where that read predates the bound session's safety-valve or illegible allow, writing it back restores the episode the allow just cleared, and a pending checkpoint the allow left standing gets vouched for a second time. Reaching it needs two gate processes in the same project within the same few milliseconds, and the cost is the same bounded one already accepted throughout this mechanism: one mistimed compaction, which is the pre-gate status quo. The residual is now stated in the code beside the undercount note; this item carries the optional hardening, which is to give the writer the `gateOwnedFingerprint` verify the stamp uses. Parked rather than folded because a lock-free writer gaining a retry loop is a change worth its own red-first cases.

- **Confirm the shared-checkout advisory fires with two genuinely live sessions in one working tree (2026-08-26, operator-only, from the dispatch-authority close-out, `docs/archive/claude-kit_dispatch-authority_spec_v1.md`).** Section 4 of that plan added a SessionStart line that says another session may be live in the same checkout, triggered when a sibling transcript in `~/.claude/projects/<flattened-cwd>/` was written inside the last ten minutes. Fifteen fixture tests cover the detector's logic, including the crowded-store case and every fail-open path, and none of them can answer the question the feature exists for, which is whether the line appears in a real second session. It could not be observed during the effort: the only concurrent session on this machine works a different checkout, which flattens to a different directory, so the state that fires the advisory never existed. Each machine runs the installed plugin copy rather than this clone, so the check waits on `claude plugin update`. What to do: with one session already running in a checkout, start a second in that same directory and read its session-start output. What to look for: one line saying another session may be live, carrying a count and an age and no path, session id or filename. Silence in that state reopens Section 4 as a new round, and the first suspect is `ownTranscriptDir`, which trusts a payload `transcript_path` only when its stem matches this session's own id and otherwise falls back to the home store.

- **Confirm the peer-sessions skill and its doctrine bullet actually reach a machine (2026-08-25, operator-only, from the peer-sessions close-out).** After `claude plugin update` on a machine, a fresh session should list `peer-sessions` among its available skills and its loaded doctrine should carry the bullet beginning "Peer sessions are a coordination surface, not a record." Either one absent reopens the install path, not the content: the repo copies are verified present on origin/main (`plugins/claude-kit/skills/peer-sessions/SKILL.md` tracked, the bullet present once in each of the two hand-edited doctrine copies, parity gate green), so an absence is a delivery failure rather than a defect in the change.

- **The shared-tier resolvers collapse a stat failure into absence, so a refused tier leaves the sweep silently (parked 2026-08-25, from the kaizen batch's section 5 round-3 review and its consult).** Three readings decide which tiers exist, and all three collapse a failure into absence. `typedTierOrNull` and `operatorTierOrNull` in `plugins/claude-kit/scripts/memq.js` (around :1024-1031 and :1052-1057) answer `statSync` with `catch { return null }`, so an EACCES or EPERM on a shared-tier directory that genuinely exists is indistinguishable from the directory not existing. `projectType` (:1012-1016) is the third and the most reachable: it swallows any failure reading the project's own `MEMORY.md` as "this project declares no type", so on Windows a transient sharing violation on that file silently drops the whole type tier from every cross-tier command, with no stderr note, because the note that would say which type was declared depends on re-reading the same file. The tier then drops out of `memq unstamped` entirely: no coverage line, no loss flag, and the report can close with its clean-sweep clause while that tier's sidecar holds unadjudicated reads. It also makes the stderr line that says the tier directory does not exist a false statement in the refused case. The store's own doctrine everywhere else is that only ENOENT reads as absence (`readUsage` and `recentFileNames` both carry an explicit three-value status for exactly this reason), so the resolvers are the outlier rather than the pattern. Not fixed inside the kaizen batch, and the reason is scope rather than severity: this is tier *discovery* failing before the sweep runs, not a granularity of the sweep's own readings, and the resolvers are consumed by `recall`, `recent`, `find`, `decay-scan` and `touch --type`, whose single-answer property the code pins deliberately. A section-local re-stat would fork that single answer, which is a worse defect than the one it closes. Confirmed pre-existing: neither resolver appears in any hunk of that section's diff. Repro recipe for whoever takes it: make an existing operator-tier directory unreadable (EACCES on the directory itself, not its contents), then run `memq unstamped` from a project that declares a type tier, and observe that the operator tier is absent from the report rather than reported as unread. The EACCES-on-`statSync` behaviour is inferred from a code read rather than reproduced, because the box this surfaced on is Windows; confirming it is the first step of the work.

- **Rules in `finishing-work`, `executing-work` and `memory-system` that contradict a neighbour or the code they describe (parked 2026-08-25, from the kaizen batch's section 9 round-8 blind review).** That round briefed the blind lens to read three whole skill files rather than a diff, per project memory `skill-amendments-collide-with-neighbours`, and it returned eleven defects in text section 9 never touched. They are parked rather than folded because each needs acceptance section 9 does not carry, and they serve the kit's general correctness rather than that section's goal. Two are confirmed by this orchestrator; the rest carry the reviewer's citation and are unconfirmed, so confirming each is the first step of the work.
  - **Confirmed.** `finishing-work/SKILL.md:40`'s base-ref cross-check names a bookkeeping set of the plan doc plus `docs/README.md`, but `docs/backlog.md` is in the changeset of any healthy pass that parked a surface (executing-work step 4 writes it and step 7 commits it) and no `Files in scope:` line ever names it; a jotted `kaizen/notes-<machine>.md` is the same shape. So the check fires on the designed-normal case, which is the standing false alarm that same paragraph warns against, while `docs/README.md` cannot be dirty at that moment because the check runs before the curator. Verified against this very plan: none of its ten sections names either file. Fix: put `docs/backlog.md` and the kaizen note path in the bookkeeping set and re-time or drop `docs/README.md`.
  - **Reviewer-cited, unconfirmed.** `finishing-work:40`'s Commit-and-Push base-ref walk takes the earliest Chapter commit, which on a reopened plan reaches back into the previous round's already-reviewed changeset, and the stated cross-check cannot catch it because those files sit inside the plan's own scope union. `finishing-work:14,20,32`'s unavailability rule admits an unavailable growth reading as evidence for the never-wrote condition at :32 while :14 says a wedge failing any condition proves nothing about reachability, so a session with no control can permanently downgrade the finishing gate on evidence the rule itself disclaims. `finishing-work:52`'s cross-reference selection takes whole-token equality against repo-relative plan paths while the curator's charter writes cross-ref gaps as free-form plan names, so step 5's act-on-the-gap instruction is unreachable on the ordinary entry. `finishing-work:84` counts the decay pass as three moves where `memory-system` counts four and this step's own later prose requires the fourth (`decay-done`). `executing-work`'s Branch-and-PR bullet says the section's code commits with its Chapter and then that it commits at first green, before step 6 has written any Chapter. `executing-work`'s dispatch site states the never-park rule unqualified while the completion contract sanctions a leading `WAITING:` park when dispatched agents are the only remaining work. `memory-system:202`'s frontmatter budget arithmetic does not close against `frontmatterBlock`, which allows the closing fence at line 41. `memory-system:58`'s warning that every edit resets a file's birthtime contradicts `recentFileRecord`'s own platform gate, and one of the two is wrong. (`memory-system:114`'s closed frontmatter field set was the eleventh; it is resolved rather than parked. The memory-anchors work merged into this branch at the finishing pass, which made the omission confirmable against this branch's own code: `frontmatterAnchors` reads `anchors:` through the same `frontmatterValue` path at both placements, so the five-field enumeration was closed and false. `anchors:` is now the sixth member there and in `docs/architecture.md`'s copy of the same list.)

- **The frontmatter guard does not refuse a hand-written mutual `supersedes:` pair (2026-08-25, from Section 4's first review round in the memory-anchors plan).** memq's own CLI refuses the case where a pointer's target already supersedes the writer, on the reasoning that a pair asserts no replacement and so every reader drops both halves. The new PreToolUse guard, which is the only door in front of a hand-written project-tier pointer, does not: it checks that the target exists, is live, is not the record itself and is named in exact casing, and stops there. A hand-written pair naming each other therefore lands, and both records lose their label and their archive nomination with nothing said. Ruled out of v1 rather than folded, because it is a new deny rule the section's closed deny list did not name and it needs an acceptance criterion that section did not carry; the detection itself is cheap, since the target is a readable file in the same directory the guard already lists.

- **`memq`’s handshake-failure stderr write is bare where the goal library’s is wrapped (parked 2026-08-26, from kaizen batch 2 section 1’s blind review).** The two implementations of the worktree handshake agree on every resolution decision and are pinned against each other, but they diverge on one exotic path: `plugins/claude-kit/hooks/kit-goal-lib.js` wraps its note write in a try/catch while `plugins/claude-kit/scripts/memq.js` leaves its bare, so a throwing stderr write (a closed descriptor) propagates out of memq’s resolver and is swallowed by the library’s. The fix belongs on memq’s side, which the section held read-only, and the drift pin cannot see it because the pin asserts resolution answers rather than failure behaviour.

- **The type-lock contention test can still red on a loaded box, and the cause is a two-second lock wait rather than shared machine state (parked 2026-08-26, root cause corrected 2026-08-27 from the testing-discipline effort's section 4).** The cause originally recorded here was that the test's subject is the machine-global type tier, so that a sibling Claude session running `memq touch` or `memq add-type` would be indistinguishable from the rival writer the test measures. The code refutes it. `concurrent add-type writers from two projects serialize under the type lock` in `test/memq.test.js` already spawned its writers with `KIT_MEMORY_ROOT` pointed at the test's own `fs.mkdtempSync` root, `memoryRoot()` honors that pin at `plugins/claude-kit/scripts/memq.js:412-414`, and the type tier derives from it at `:1091`, so no other session's memq can reach the store under test and no concurrent writer can reproduce the red. The live cause is a budget. `cmdAddType` calls `acquireLock` with no options at `:10546`, so `waitMs` takes its 2000 ms default at `:1290`, every writer shares that one budget, and a box slow enough pushes the tail writer past it, at which point memq exits 1 with `memq: type store locked, nothing written` and the test's exit-code assertion reds. Measured on this box by a throwaway probe script that runs the test's own spawn shape against a fresh temp store at five repetitions per writer count: the slowest of six writers finishes 294 ms after the parent spawned it, of which 249 ms is time the tail writer spent waiting rather than working (a lone writer takes 45 ms), so the wait it must fit inside 2000 ms has a factor of 8.0 to spare; four writers put the same figure at 10.9 and the two writers the test now spawns at 32.8, against the roughly 10x load factor this suite has been observed to take under full-suite contention (the sibling entry at 2026-08-04 recorded 11.1 s against 1.1 s alone). Section 4 took the writer count from six to two and made a future red self-diagnosing, so a starved writer now names the tier lock instead of dumping stderr. That buys margin and does not repair the cause. What remains is the design call the margin defers: either give `cmdAddType` a lock wait proportionate to the writers a real store can carry, or make the wait injectable so a test can ask for a generous one. Both change a production default or widen memq's environment surface, which is the most carefully gated surface in this repository, so each earns its own decision rather than a drive-by edit.

- **The doctor reports the armed queue position from the stored index, where the node-side reporters now read it from the plan docs (parked 2026-08-26, from kaizen batch 2 section 3).** `plugins/claude-kit/doctor/doctor.ps1:1255` prints `Plan N of M in the armed queue.` straight from `queueIndex`, so after section 3 it disagrees with the SessionStart advisory and with `kit-goal.js status` in exactly the stale-index state those two now correct. It is parked rather than fixed, and the reason is a real design argument rather than cost: the doctor is the surface that deliberately reads the raw state file with no hooks-side guard, so that it can report a file the hooks refuse. A doctor that silently corrects the number it is there to show you would defeat the one job that makes it the reporting control this repository credits it as. The right shape is probably to print both, the stored position and the position the evidence supports, and say when they differ. That needs its own spelling of the rule in PowerShell, since the node library is not reachable from it, which is a second implementation to keep in step and earns a decision rather than a drive-by edit.

- **No kit goal is armed for this project, and nothing holds a run here to completion (parked 2026-08-27, from the blocked-escalation finishing pass).** Confirmed by running the goal CLI from the effort's worktree, which reported no goal armed for the tree the reading was taken in. The consequence is restart readiness rather than a defect: with no leash there is no `goal-complete` event to point the operator at a closing status, so a fleet restart landing mid-pass leaves the plan doc as the only recovery surface. It costs nothing while every boundary is pushed, which was the case throughout that pass. Arming is artifact-authorized and traces to the operator, so neither a seat nor an executor may do it: the remedy is the operator arming the plan they want held, or deciding that unleashed runs are acceptable here and retiring this item.

- **Seven declared memq boundaries await the operator's adjudication (parked 2026-08-27, from the blocked-escalation effort).** Each of that effort's six section Chapters, and its finishing Chapter, declares that memq was not run at that boundary, under the operator's standing workspace constraint that a write from those sessions can flip a concurrency test in a sibling session's gate. So seven stamp adjudications were never performed, and the effort read, wrote, and stamped nothing in the memory store. They are declarations rather than omissions, which is what makes this the operator's call rather than a gap to close quietly: a sweep run later would retroactively change what those seven declarations describe. Decide whether to run the sweep against that stretch and accept the mismatch, or to let the declarations stand as the record. The plan is `docs/archive/claude-kit_blocked-escalation_spec_v1.md`, whose Chapters carry the declarations verbatim.

- **The merged-PR push guard checks only the first command-position push in a compound command (parked 2026-08-27, from the testing-discipline effort's finishing adversarial review).** `pushArgs` in `plugins/claude-kit/hooks/merged-pr-push-guard.js` iterates command positions but returns on its first match, so in `git push origin safe && git push origin merged-branch` the guard judges `safe`, allows, and the second push rides through unguarded with no output. Fail-open direction and a fringe command shape. Declined during that effort's finishing fix round rather than taken, because closing it changes the function's contract from one branch to many and so changes the guard's whole decision shape, which is a redesign rather than a fix-round edit. The parser's own comment names it as a known unfixed gap.

- **The push guard's git reads can be redirected by `HOME` or `XDG_CONFIG_HOME`, which its environment scrub does not strip (parked 2026-08-27, from the testing-discipline effort's finishing security review).** `queryEnv` strips every `GIT_*` key, which closes `GIT_DIR` and `GIT_CONFIG_GLOBAL`, but git also locates `~/.gitconfig` through `HOME` and `XDG_CONFIG_HOME`, so a repo-carried value for either redirects the global config behind the three reads the block decision rests on. Bounded: the guard only ever blocks, so a redirect yields a spurious block or the fail-open default rather than any grant, and the three commands used do not reach the config-driven command hooks that would give execution. Declined during that effort's finishing fix round because git needs `HOME` to find legitimate config, so stripping it risks breaking the guard's reads in ordinary use to close a hole whose worst outcome is a needless block. Reported rather than confirmed: the redirect was reasoned from git's lookup order, not exercised against these commands on this box.

- **The doctrine states a background run's death as settled by the process list while its box-check bullet states a clean poll as never a basis for starting (parked 2026-08-29, from the review-and-record effort's Section 12 review).** The two bullets sit six apart in one section of one document and read as opposite verdicts on one instrument. They are not: the death reading is of a named run whose process you launched and can identify, where the box check scans for unknown foreign processes and cannot see what starts after its sample. That distinction is stated on neither surface, so a reader reconciles them or picks one. Left here rather than folded into Section 12 because the fix is a clarifying clause in a bullet outside that section's subject, and because Section 12's rescoping of its own leg to starting a suite already removed the direct contradiction; what remains is a clarity gap rather than a conflict.

- **The absence-control rule is precisely stated and reliably violated, and the violations split into three classes with different repairs (2026-08-29, coordinator-routed, adjudicated by the expert seat).** Four instances within roughly one hour, across four seats and four unrelated instruments, every seat loading the doctrine that states the rule near-verbatim: twice a path bar that errored and printed empty on the board and its control alike; a diff filter that swallowed the index lines it was meant to inspect; and a fixed-name process scan whose true silence was reported as "the box is free" when what it established was "no declared heavy process". The adjudicated split. Class one, errored-empty (two of four): the check failed as a process and its empty output was read as a clean result; this class has a mechanical discriminator already, the check's own exit code (grep and rg distinguish no-match 1 from error 2), so its repair is an exit-code read beside every absence claim, with the armed standing-lines exit-code clause as the sibling and a shared checked-search helper as the stronger form. Class two, wrong-pattern silence (one of four): the check ran clean because its pattern could never match; no mechanism recognizes this in arbitrary output, so the control rule stands as the repair. Class three, scope over-claim (one of four): the check was correct and its silence true, and the claim shipped wider than the check's subject; the repair is a claim form that forces naming the instrument and its scope ("nothing found by scan S over scope Y"), which no control run would have caught. The finding that outranks the count: the rule fires at the claim-maker's moment of maximum confidence and demonstrably does not fire there, while three of the four were caught by a consumer reading the control or re-asking, and the fourth by the maker re-reading its own shipped claim, so the enforcement point that works is the claim's consumer, not its maker. Refined the same day by a fifth instance and a cross-seat hypothesis, recorded here so the thread's insight outlives the thread. The AI-OS worker's testable hypothesis: the four instances share one authorship shape, the check and its control written by one seat in one sitting from one mental model, so the control cannot detect the omission its author did not imagine, and the highest-risk checks are therefore the CONTROLLED ones, which carry diligence evidence while sharing the blind spot; a sweep for uncontrolled checks misses the class entirely, and a consumer who asks only whether a control existed is falsely satisfied by a same-model control. The coordinator tested it live and confirmed on its own board, the fifth instance: a nine-term enumerative bar whose control was the term list grepped against itself, incapable of failing by construction, already reported clean to the operator; a second list generated from a different model of the class produced one hit, cleared on inspection, so the verdict survived and the warrant did not. A sixth instance arrived from the worker seat while this record was being written, and it fired while verifying that the rule exists: a hand-authored character-class grep over the doctrine returned empty with the rule present in the file the whole time, and the catch was neither a control nor a consumer but redundancy, two narrower phrase greps in the same batch that both matched; so the discriminating move in every catch so far is one shape, the same question asked through a second, independently formulated instrument, whether that second instrument is a consumer's challenge, a maker's re-read, a different-model term list, or a redundant sibling query in the same batch. A seventh instance the same evening sits at a surface none of the six touched and adds a fourth class, certification moment against artifact moment: the store sync, fired by a peer seat's deliberate doctor fix pass rather than any scheduler, committed and pushed a board mid-edit under its whole-tree add, publishing bytes that failed two bars the seat had run minutes earlier, with every bar correct and every control speaking, because a bar certifies a moment and not a file while the sync commits at a different moment, so no exit-code read, control, or consumer challenge reaches the gap; the sharper reading, settled by the firing seat's own unprompted report, is that this class needs no malfunction and no timer, one seat's legitimate act carrying a blast radius neither seat could see from its own side. An eighth instance landed on the adjudicating coordinator itself inside the same pass, self-caught and self-corrected: a size figure taken partway through a rewrite was published as the finished one, with the input figure right and the arithmetic sound, because the finished size was never measured at all; nothing in the owning loop found it, and the catch was the close-out re-ground of a number about to go outward, the summary-outlives-its-source rule firing outside the loop that owned the figure. The refinement that instance adds to class four's repair, the coordinator's own reading adopted here: the repair is a measurement point as well as a claim form, since a loop that neither measures nor certifies its own output leaves both jobs to whoever happens to look later, and a moment-pin can only name the moment a measurement actually happened at. That class's repair is already armed in another costume, the instruments-not-prose moment-pin: a bar report names the commit it certifies, and the seat commits promptly after a sweep rather than editing a file it has certified; the coordinator adopted both locally the same evening. The unattended whole-tree sync staging is the adjacent item above, and this is a live instance of its unpriced consequences. The through-line of every instance above, which is this item's real subject: the gap between what an instrument measured and what the claim or the artifact actually covers, with the exit code, the scoped claim form, the different-model control, and the named commit as four faces of one coverage question. And the complement rule earned itself inside the hour it was proposed: the published board's stray soft hyphen was caught by the complement-formulated non-ASCII bar, a character no enumerative list would have thought to include. The distinction that fell out, the coordinator's own: the weakness is specific to enumerative checks, a hand-authored list of things to look for, while a complement check, defined by what it is not, has no list to be incomplete, and where a complement formulation exists it is strictly stronger than an enumeration plus a control. The kit holds independent corroboration in one narrow costume already, the recorded control-derivation lesson that a control derived from the instrument tests its mechanism and never its coverage. Fix shape: a reviewer-charter and status-round clause in the two-direction idiom, with the consumer's challenge asking the question that discriminates, whether the control could have failed at all, not merely whether one ran, and whether it exercised the direction opposite its subject's expected answer, because a control that agrees with the result it checks is one defect speaking twice (the field instance: a marker pattern truncated by one character and an absent-marker control both returned absent, two agreeing absences reading as confirmation, caught only by a positive control built to hold the marker); the exit-code helper for class one; the scoped-claim form for class three; and two write-time rules for the testing-discipline surface, prefer the complement formulation where one exists, and where a check must enumerate, its control is specified from a model other than the check author's (another seat, the consumer's challenge, or a list derived from the spec rather than from the check). Sized for the follow-on after the queue drains, or folded into the corpus-audit follow-on if its census lands first. Signal: the next errored-empty incident, or the next absence claim consumed without a challenge.

- **docs/architecture.md still describes the retired kaizen routing (2026-08-29, from the standing-capture grant; file was worker-held at edit time).** The seat-duties paragraph (around line 278) states that every non-coordinator seat routes captured friction to the coordinator by message because only that seat holds the operator's nod. The grant retired both the nod and the mandatory route: capture is standing-authorized for every session, adjudication is standing at the coordinator and the kit expert seat, and the kaizen, coordinator, role, and peer-sessions skills already say so. Fix shape: align the paragraph with role/SKILL.md's rewritten duty (append directly, public-board cap, adjudication seats named by the kaizen skill). Signal: the file leaving the worker's dirty set; any session may take it then.

- **normalizeState never shape-checks `boundSession`, so a hand-edited value leaves a leash every surface reports as bound and nothing enforces (parked 2026-08-29, from Section 11's security review).** `plugins/claude-kit/hooks/kit-goal-lib.js` repairs `boundTranscript` and, as of Section 11, `armingSession` to their shapes on every read, but `boundSession` is passed through whatever it holds. A non-string value reaches `sameSessionId`'s `String()` coercion, matches no session, and produces a goal that reads armed-and-bound on the status line, at session start and to the doctor while no session is leashed, which is this plan's own subject arriving through the one field it did not touch. Pre-existing and out of Section 11's scope, which is why it is here rather than folded; it is the guard-at-some-sites-only shape Standing Amendment 3 names, now visible because the sibling field beside it gained the guard.

- **Six of the doctor's file reads carry no `-ErrorAction Stop`, so a present-but-unreadable file is diagnosed as a wrong answer rather than an unreadable file (parked 2026-08-30, from Section 12's review round).** `plugins/claude-kit/doctor/doctor.ps1` now spells its encoding one way at every read, and its read-failure contract in two: the plan-head and compaction-gate reads stop on error, while the doctrine-import, two signpost, `hooks.json`, canary `hooks.json` and goal-state reads do not. `Get-Content` emits a non-terminating error there, so the surrounding `catch` never runs and the check proceeds on an empty value. The concrete cost the reviewer named: an unreadable `hooks.json` leaves the error variable null, so the doctor reports that the Stop array does not reference the goal hook, naming a cause that did not fire against a correct install. Still a FAIL rather than a false clean, so the exposure is a wrong diagnosis rather than a missed one. Pre-existing at all six, which is why it is here rather than folded; the seventh site of the same class was introduced by Section 12's own conversion and fixed inside it, which is what made the pattern visible. It is the guard-at-some-sites-only shape Standing Amendment 3 names, one layer below the encoding question that surfaced it.

- **The doctor decodes the files it reads but not the child processes it runs (parked 2026-08-30, recorded as a Section 12 exclusion rather than a miss).** Windows PowerShell 5.1 decodes a child process's stdout through the console output codepage, which is the OEM codepage rather than UTF-8, so the doctor's `git ls-files` and `git config core.hooksPath` calls can mangle a non-ASCII path before it reaches a report. Section 12's scope was every file the doctor parses, and this is the same defect class one boundary over, at a channel that section deliberately did not open. The fix shape is not the same one either: the file reads took a flag, and this takes either an explicit decode of the captured bytes or a scoped codepage change that is restored, since setting the console encoding leaks to every later process on the host. Signal: the first report that shows a mangled path from a git call, or any work that touches those call sites.
- **Absorb the discord-channels relay into the kit repo, so one repo and one doctor run own the whole machine (parked 2026-08-30, operator wish at the keyboard).** The operator standard is recorded in the operator tier (customization-converges-on-the-kit-repo-and-its-doctor): every customization comes from claude-kit and a single doctor run checks full machine health; the relay is the standing exception, installed and run from its own repo (memory stores exist for two checkouts, sapplefeld-channels and discord-channels, so a scout starts by resolving which is live). The effort when called: scout the relay repo, then a design round on what moves (the broker service, the plugin, the allowlist config), what the doctor gains (install, update, health), and what the migration costs on a machine mid-service. The judgment sidecar post-field-trial packaging lands the same direction first and is the smaller instance to learn from. Signal: the operator calls for it, or the sidecar field trial closes and its packaging round opens.

- **The memory store root is gated behind an explicit allow-data flag while the endpoint config path reads a bare home directory (parked 2026-08-30, from the judgment sidecar's Section 6 review round; deliberately outside that section's bound).** Two paths that both decide where machine-local state is read from are screened to different standards in the same tree. `memq` refuses an inherited `KIT_MEMORY_ROOT` unless `KIT_MEMORY_ROOT_ALLOW_DATA` is also set, on the reasoning that an environment variable arriving from an unknown parent must not silently move where memories are written. The endpoint configuration path, which decides which machine every judged query and every candidate set is posted to, resolves from the home directory with no comparable gate. Neither is wrong on its own terms: the store path is a write surface and the config path is a read of a file the operator authors, and no exploit follows from the asymmetry by itself. What makes it worth a decision rather than a shrug is that an auditor comparing the two will ask why one answer was reached twice, and the tree currently has no written answer. Section 6 could not close it because its stated bound excludes memq's store verbs, and changing the store gate is a behaviour change to a rule that section does not own. Fix shape: either state the difference as an argued accepted risk in `docs/security-model.md` in the form that document already uses for writer-scoping, or give the endpoint config path a screen of its own and single-source the predicate. Signal: the next change to either gate, or the sidecar's post-field-trial packaging round, which is when the endpoint config stops being one machine's hand-authored file.

- **Claude-kit gains branch protections and a release channel (queued 2026-08-30 by the operator's direction in the gate-cadence design dialog; wakes when the armed queue drains).** Direction: the repo follows the recognizable pattern rather than carrying a testing-rule exemption: PR-required protection on main, no direct pushes. Sequencing: land after the armed queue drains, because every queued plan's header reads Commit-and-Push and direct-pushes main per section, so flipping mid-queue invalidates the commit model every plan names. Phasing: protection first with self-merge allowed and no required checks; CI becomes a required check only once a runner-green suite definition exists, since the suite carries box-conditional expectations (the temp-prefix path-length test reds only under a default-length temp prefix, and zip sizes differ by PowerShell host), so a required-but-red check would freeze merges. PR-per-plan makes main the release channel, which subsumes explicit kit versioning for the update nudge unless sub-plan releases are wanted. The open cost to price at pickup: spec and index work currently direct-pushes main several times a session, so doc-only work needs either self-merge without review or batched doc PRs. The gate-cadence plan's pre-push clause is keyed on the surface, so it stops matching claude-kit the day protections land, with no rule edit.

- **The stranded-recovery path integrates a tree neither side had and no gate reaches it (2026-08-31, from the gate-cadence Section 7 carrier sweep; found and deliberately not repaired there).** `plugins/claude-kit/skills/branch-hygiene/SKILL.md`'s stranded recovery cherry-picks a commit range onto a fresh base and then pushes that branch and opens a PR. A cherry-pick onto a fresh base produces a tree neither parent had, which is exactly the property the gate cadence's merge moment rests on, and yet no gate runs anywhere on that path: it sits outside the finishing pass, so the Branch-and-PR merge gate never reaches it, and the push lands on a recovery branch rather than an install-surface trunk, so the pre-push condition genuinely does not fire either. The sweep left it because closing it means extending the merge moment to name cherry-pick, which is a change to the cadence's design rather than a carrier repair, and Standing Brief Amendment 1 permits reporting over fixing on exactly that boundary. Fix shape: decide whether the whole-gate moment keys on merge-the-git-operation or on integration-produces-an-unseen-tree, and if the latter, say so in the doctrine's gate bullet and name the lane in branch-hygiene's step 4. The second reading is the one the cadence's own reason supports, since the failure it names is redness in files neither parent changed. Signal: the next stranded-recovery run, or the next change to either the merge moment or branch-hygiene's procedure.

- **A doc-only push to an install-surface trunk now prices a full gate, and nothing exempts it (2026-08-31, from the gate-cadence Section 7 carrier sweep; declined there as a change to operator-approved text).** The pre-push whole-gate condition keys on the surface a push lands on, so under Commit-and-Push every push to claude-kit's main earns the whole gate whatever the diff carries. Two rituals push doc-only changes to main under that model: executing-work's interim-board entry, and the kaizen note capture, which commits and pushes a note file alone so the inbox syncs across machines without waiting on a pass. The kaizen case is the sharper of the two, because that capture is standing-authorized in every session and is routinely executed from a session working in a different repo, which has no lane, no baseline, and often no checkout of the repo whose gate the push would owe. On the plain reading each now owes a full suite to ship a paragraph, which on a box where the gate runs twenty-five minutes is a real cost attached to the cheapest possible change, and a rule that expensive on a one-line note is one sessions will quietly skip, which is worse than no rule. The backlog-entry ride is not a member: it is committed with the Chapter and the section's code, so it travels on the section-close push, which owes its gate on its own account. One of the two is now settled and the other is not. The kaizen note push carries an exemption stated at its own step, on a narrow rule about what the push can break rather than about a file path: with the note file alone staged it appends one line to an inbox no test takes as a subject, and a commit carrying anything beside that file is a different push taking whatever lane its own surface earns. The premise was verified rather than assumed, every test touching `kaizen/` building its own temp fixture and none reading the repo's real inbox. The interim-board push is untouched and still prices a full gate, and no exemption is written for it, because the general narrowing is a change to a sentence the operator approved verbatim and is a design call rather than a carrier repair. Worth noting that this dissolves on its own the day branch protections land, since the clause stops matching claude-kit entirely; so the decision is whether to carve a doc-only exemption now or accept the cost for the window until then. Fix shape, if taken: state the exemption in terms of what the push can break rather than in terms of file paths, since a docs path is not what makes a change safe. Signal: the first time a session pays a full gate to push an interim board entry, or the branch-protections item above being picked up.

- **The kaizen routing duty lost its stated reason and now has none (2026-08-31, from the gate-cadence finishing pass's docs curation; the false clause was excised there, the design question was not answered).** `docs/architecture.md` gave every non-coordinator seat a duty to route captured kit friction to the machine's coordinator by message, and justified the message route by the authority: it claimed the kaizen capture bar puts the inbox behind the operator's nod, that only the coordinator's function holds that nod, and that there is therefore no artifact a capturing seat could write that would reach the party who acts on the note. `plugins/claude-kit/skills/kaizen/SKILL.md` now states the opposite in terms, that capture is standing-authorized for every session, seated or not, with no per-note approval and no routing through any seat, the append itself being the capture. So the premise the route rested on is gone: a capturing seat can and does write exactly the artifact the document says it cannot. The falsifying change belongs to a sibling effort rather than to the gate cadence, and the curation pass repaired only the untrue clause, replacing it with the skill's own words plus the narrow consequence that what routes to the coordinator is the dispositioning rather than the authority to capture. What is left open is whether the duty itself still earns its place: if the append is the capture and the inbox syncs across machines on its own, a routing message to the coordinator may be redundant with the artifact, or it may still be worth its cost as an interrupt that says look at the inbox now, which is the same doc-is-the-record-message-is-the-interrupt split the peer-sessions skill draws. Answering it inside the curation pass would have meant inventing a rationale to replace a false one, which is worse than the falsehood. Fix shape: decide whether the routing message survives as an interrupt, is narrowed to friction that cannot wait for a pass, or is retired in favour of the append plus the coordinator's own inbox poll; then state the surviving reason in `docs/architecture.md` rather than leaving the duty reason-free as it now stands. Signal: the next kaizen pass, or the next edit to either the capture bar or the seat-duty paragraph.
- **The suite has never run on a non-Windows platform, so every platform-conditional claim in it is argued rather than measured (parked 2026-08-31, from the judgment sidecar's section 7; recorded as a declared assumption of that section rather than a miss).** This repository has no CI configuration and this machine has no WSL, so `node --test test/*.test.js` has only ever been run on win32. Most of the suite is platform-neutral and this costs nothing; the exception is code whose whole subject is filesystem semantics. The live-store screen in `sidecar/state-screen.js` is the current instance: its Windows behaviour is measured against OS-derived spellings (UNC, extended length, volume GUID, 8.3, junctions) and its POSIX behaviour is reasoning about `path.posix` and `realpath(3)`. That module's own history is why the gap is worth a line rather than a shrug: it was found accepting a path inside the live tree six times in one section, every time through a case nobody had thought to write down, which is exactly the shape an unrun platform hides. What closes it is a runner, container or CI, that executes the suite on Linux and macOS. Note the ordering constraint the branch-protections item above already records: the suite carries box-conditional expectations (a temp-prefix path-length red, zip sizes differing by PowerShell host), so a runner-green suite definition has to exist before any of this becomes a required check. Signal: the branch-protections item is picked up, or any further work on filesystem-semantics code.
- **The judgment sidecar's transcript distiller is parked by design, waiting on a field trial and an operator call (parked 2026-08-31, from the judgment sidecar plan's section 8).** This is a designed deferral rather than work left undone, and the distinction is load-bearing: the plan reached Complete at section 7 deliberately, because section 8's own acceptance reads "deferred to the design pass", so there is no buildable acceptance a run could have reached. What it would build: end-of-session transcript extraction, chunked map-reduce distillation sized well inside the standing context, candidate memory drafts and contradiction flags written to a pending file for session or operator adjudication, with budget stated in the prompt plus validate-and-re-ask, since the model composes past schema caps and constrained decoding then truncates mid-sentence. Two things get decided at the design pass rather than now: how it interacts with the shipped memory-recognition hook, which did not exist when section 8 was written, and whether a distiller earns its keep at all once recognition is running in the field. The pass itself is specified as fable-tier. The field trial of sections 1 through 7 has run: the daemon is live against the real store on this machine and both operator-only acceptance criteria passed on 2026-09-03 (receipts in `archive/backlog-2026-Q3.md` under that date). Signal: the operator calls for the design pass.


- **The invisible-character neutralizer is shared by four consumers that each compile it with their own flags, and the fourth was found by breakage rather than by a check (2026-08-31, from the judgment sidecar's finishing fix round).** `UNSAFE_PATTERN` in `plugins/claude-kit/scripts/kit-endpoint-lib.js` is a joined list of escape strings rather than a literal character class, deliberately, so the source file does not contain the bytes it screens for and does not drop out of the repository's own hygiene sweeps. The cost of that shape is that each consumer calls `new RegExp` itself and chooses its own flags. When the class gained a supplementary-plane range and the `u` flag with it, `sidecar/harvest.js` was still compiling the shared pattern with `g` alone and became a load-time `SyntaxError`, taking a whole suite down. That was caught because it was loud; a future consumer whose flag mismatch is merely wrong rather than fatal would not be. There is also a spelling pin asserting the hook's copy and the library's copy are byte-identical, and it is worth knowing what that pin cannot do: it pins the two equal, so when the class was too narrow it propagated the gap to both surfaces faithfully and reported green. Fix shape: export a compiled regular expression, or a `neutralize` helper, from the library so no consumer chooses flags, and keep the byte-identity pin for the hook copy that cannot require the library. Signal: a fifth consumer, or the next change to the character class.

- **Stripping the variation-selector range degrades emoji in the sidecar's advisory text (2026-08-31, from the judgment sidecar's finishing fix round; accepted rather than fixed).** The neutralizer now strips `U+FE00` through `U+FE0F`, which includes VS16, the selector that asks for the emoji rendering of a dual-presentation character. So a heart or a warning sign in captured text can arrive at the session as its monochrome text form rather than as the emoji. This is accepted because the exposure is narrow and the alternative is worse: the consumers are sidecar-internal advisory text plus the capture hook, `memq` does not consume the neutralizer, and carving VS16 out of the range would reopen a genuine steering channel to close a cosmetic one. Recorded so that a later report of "the sidecar mangles my emoji" is met with a decision rather than a bug hunt. Signal: a consumer of the neutralizer whose output is user-facing prose rather than advisory pointers.

- **`test/kit-sidecar-capture.test.js` closes the live-store class by per-call-site discipline where its two sibling suites close it by structure (2026-08-31, from the judgment sidecar's finishing fix round).** The live-store class reached ten instances in that effort, and the thing that finally held was structural rather than behavioural: one spawn helper naming the interpreter at a single site, so a later call site cannot be written without a fixture home. `test/kit-sidecar-battery.test.js`, `test/kit-sidecar-daemon.test.js` and `test/kit-sidecar-rollup.test.js` all carry that shape and a pin asserting the interpreter token appears exactly once. The capture suite does not: it has three separate `process.execPath` sites, and all three pin `HOME` and `USERPROFILE` to a fixture home correctly today. That is per-call-site discipline, which is precisely the discipline that failed eight consecutive times in the battery file, once under a file header asserting the opposite. Nothing is wrong today and this is a guard item, not a defect. Fix shape: give the capture suite the same single-site spawn helper and the same structural pin. Signal: a fourth spawn site added to that file, which is the moment discipline has to be remembered rather than enforced.

- **The sidecar inbox lock's stale-reap has a race that duplicates advisory text (2026-08-31, from the judgment sidecar's finishing adversarial review; real, inferred rather than reproduced, and deliberately shipped).** `acquireInboxLock` reaps a lock it judges stale, and two writers can judge the same lock stale in the same window and both proceed. The blast radius is bounded and was priced before the call was made: the worst outcome is a delivery item written twice, so a session sees one advisory pointer duplicated, and there is no corruption of the inbox file and no loss. It was not fixed in the finishing round because the fix is a concurrency rewrite of the acquire path rather than a pin, and a concurrency rewrite landing in a fix round whose other five items were all red-first pins is the shape that ships a worse bug than it closes. Fix shape: an atomic create-exclusive acquire with the staleness judgment made by the holder of the new lock rather than by each contender independently. Signal: the first duplicated pointer observed in the field, or the daemon gaining a second writer.

- **Three surfaces restate the coordinator board's commitment categories by hand, and nothing pins them to the ledger they summarize (2026-08-31, from the standing-lines Section 4 security and adversarial reviews; the three restatements themselves were brought current by that plan's finishing curation, which leaves the durable half open).** The categories are enumerated inside `plugins/claude-kit/skills/coordinator/SKILL.md`, and three summaries of them live outside that file: the board-holdings list and the board-carried-path classes in `docs/security-model.md`, and the count of branches read for defects and never performed in `docs/architecture.md`. All three were short before the standing-lines plan widened the set with the routed-finding category, and all three were repaired by hand during that plan's finishing pass, so none of them is wrong today. What is unresolved is that a hand-edit is the only thing keeping them right: the set has now grown once with three separate summaries needing three separate repairs, and nothing would catch the next growth. The cheap durable answer is a pin over the category set rather than a fourth round of hand-edits. Fix shape: a test that reads the category list out of the coordinator skill and asserts each restatement covers it, or a single-sourced list the three surfaces cite rather than restate. Signal: the next commitment category added to the ledger.
- **A finding routed to a seat that never answers has no terminal path, and the spec deliberately did not invent one (2026-08-31, from the standing-lines finishing pass; routed to the operator as a design decision rather than filled).** The coordinator skill now carries routed findings as a board category: the line stays open until it carries a disposition pointer or an explicit open marker, and a successor does not sweep past an open one however stale it reads. That is exactly the rule the spec asked for, and it is silent on the one state it cannot resolve by itself, a finding sent to a seat that is retired, wedged, or simply never replies, whose line then rides every successor's board forever with nothing that can ever close it. Three fix rounds during Section 4 each spawned their own defect while building machinery to close that state (a re-ask cadence, an escalation threshold, an operator disposal), and round three reverted all of it to the spec's own scope, which is why the gap is recorded here rather than filled: picking a terminal path is a design call about what a seat owes a commitment it cannot discharge, and it is the operator's. Fix shape, if wanted: a bounded number of re-asks before the line converts to an operator escalation, or an explicit expiry that retires the line with a recorded reason. Signal: a board carrying a routed-finding line nobody can answer, or any further work on the coordinator's board categories.

- **Five wording questions raised by the doctrine's withheld-control split are unanswered, because the text is operator-approved verbatim and every repair edits it (2026-08-31, from the standing-lines finishing pass; sections 3 and 5 each routed rather than took theirs).** The rule that a silent check earns its silence with a control that spoke now sits under its own bold lead, split out of "Make the test earn its green", and the split left five seams that an implementer could not repair without editing signed-off wording. (1) "The bar covers any check that proves an absence..." opens the new bullet with no antecedent inside it; in the merged bullet the phrase read "The same bar reaches past tests" and pointed back at the earn-its-green bar. Repair: "This bar" or "The control bar". (2) The bold lead states an absolute the body then qualifies: the lead says a silent check earns its silence with a withheld control, while the body's last sentence provides for the case where none can be run and directs that the silence be called unproven. Repair: a qualifier in the lead. (3) The tree-mutating-probe discipline (run one with no agent reading the tree, restore from pre-probe byte copies, verify against those copies) stayed in the retained bullet, while the act that most needs it, running a check against a state known to hold the thing, is now in the new one with no pointer to it. Repair: a pointer sentence. (4) Section 3's widened exit-code rule is unreachable from where it now binds: it sits inside a bullet whose bold lead names test lanes, while the reader it targets is authoring a silent grep and is reading the absence-check bullet. Repair: a pointer from the new bullet to the exit-code rule. (5) "A check whose subject is a class owes the coverage answer either way" now follows the sibling-subject sentence, so the two ways it names, a control drawn from the pattern's own literals versus one withheld and matched on shape, sit two sentences back. Repair: name them. All five take the same vehicle, a further sentence appended to the new bullet rather than an edit inside the approved wording, so answering them is cheap and answering none of them costs only the seams above. Signal: the operator rules on any of them, or the next edit to that bullet.

- **The standing-lines plan's Standing Amendment 3 names the doctrine's source and mirror in reverse, and the amendment is operator-approved text (2026-08-31, from the standing-lines finishing pass; surfaced rather than edited).** The amendment names `home/claude-kit-doctrine.md` as the source a doctrine edit is made in. `test/doctrine-parity.test.js` names the opposite pair: `plugins/claude-kit/skills/operating-instructions/SKILL.md` is its `SKILL` (the source) and `home/claude-kit-doctrine.md` is its `MIRROR`. The amendment also omits the operating-instructions copy entirely, so a run following it literally edits one file where the parity pin requires two and reds the suite. Both of this effort's doctrine sections resolved the edit set from the test's own constants instead and shipped green, so nothing is broken; what is unresolved is which file the kit means to call the source, since the amendment and the pin disagree and only one of them is enforced. Note the third copy is not a candidate either way: `plugins/claude-kit/claude-kit-doctrine.md` is gitignored build staging regenerated by `build.ps1:77` and is never hand-edited. Fix shape: correct the amendment's wording in whichever plan carries it forward, or rename the test's constants if the intended source is `home/`. Signal: the next plan that writes a Standing Amendment about doctrine edits.

- **A plugin build measured 4.0 KB apart from a sibling where project memory records the host-to-host gap as about 1.5 KB, and the discrepancy is unexplained (2026-08-31, noticed during the standing-lines finishing pass; not investigated, because nothing in that effort depended on it).** The memory record `build-size-differs-by-powershell-host` exists precisely so a build figure is compared only against one from the same PowerShell host, and it states the expected gap as roughly 1.5 KB between pwsh 7 and Windows PowerShell 5.1. The gap observed here is larger by enough that the recorded figure may be stale, or the comparison may have crossed something other than the host (a payload that genuinely grew, a different compression path). This is recorded rather than chased because it is a measurement question with no defect behind it yet, and because a memory contradicted by evidence gets fixed rather than routed around, so whoever confirms the real figure should correct or delete that record in the same turn. Fix shape: build once on each host from one clean tree and record both sizes. Signal: any effort that compares build sizes, or the next doctor run that reports one.

- **The memq-shim stale-detection test fails every run on SCOTT-CLAUDE, hardened from the intermittent the baseline memory used to record, and the miss is real detection scope: content-swapped JS shims are not flagged (2026-08-31, from the sidecar staleness effort's whole gate; routed because the memq shim doctor is a different goal than the sidecar pipe).** `test/memq-shim.test.js` > "the status check reports a content-swapped shim as stale, which is what makes -Fix reach it" fails 3/3 on 2026-08-31 (one whole gate, two solo runs) with the stale list coming back `['memq.cmd', 'memq.ps1']` against an expectation that also names `kit-statusline.js` and `memq-shim.js`, so the status check sees the wrapper pair and misses the two content-swapped JS shims. Reproduced at HEAD bc57e15 in a clean worktree with no in-flight diff, and the commits since the last green measurement touch only a plan doc, so no changeset explains the flip; machine state (an installed-shim change, a build-stamp interaction) or a latent code path are the open suspects. The project memory `suite-baseline-is-not-zero-fail` is already corrected to carry it as the box's second standing fail. Fix shape: run the status check by hand against a deliberately content-swapped JS shim, read which comparison source it consults for the JS pair versus the wrappers, and either fix the check or the test's expectation. Signal: any doctor -Fix run that claims shim health, or the next baseline diff on this box.

- **Two hooks read files their own writers create through the shared bounded reader with no link refusal, where the reader now offers one (2026-08-31, from the durable-boundary plan's section 2; routed out because hardening those hooks is a different goal than this plan's).** `readFileBounded` in `plugins/claude-kit/hooks/kit-read-lib.js` follows a symlink at the final component by design and now takes an opt-in `{ refuseLink: true }`. Two callers meet the reader's own stated criterion for wanting it and do not take it: `memory-recognition-nudge.js:1295` reads `.kit/memory-recognition-nudges.jsonl`, a file that same hook creates and appends to, on a PostToolUse path where a link into a dead network mount stalls the open; and `session-start.js:708` reads `~/.claude/plugins/installed_plugins.json` on a session-blocking hook with neither a link refusal nor a network screen. A third, `memory-recognition-nudge.js:720`, hand-writes the lstat guard the option would now retire. Enumerated with a per-caller verdict by that section's fix round 4 and correctly left unedited, since editing outside a section's file list leaves the committed tree calling a symbol that exists only in an unstaged worktree. Fix shape: adopt the option at the two unguarded sites and replace the hand-written copy at the third, then run each hook's own suite. Signal: any further work on the recognition nudge or on session-start's plugin-view read.

- **The sidecar staleness-and-liveness rollout is operator-gated on two acts, and each carries its own reopen condition (parked 2026-09-01, operator-only, from that plan's finishing pass; the plan is archived with the same items in its final Chapter).** The daemon and wrapper changes run only after the live daemon (pid 25828 at close-out) is stopped and the scheduled task's next tick relaunches it from the updated source: until then the real state root has no `logs/heartbeat.json` and the real `daemon-task.log` stand-down lines read `heartbeat unreadable (no file)`, which is correct for old code and would be a section 2 defect after the restart (no stamp within a minute of a new daemon starting reopens it). The capture-hook cap split reaches sessions only after `claude plugin update` plus session restarts (the standing fleet-update item above); a delivered advisory block still breaking at one item after that reopens section 3. Signal: the operator restarts the daemon, or the next fleet plugin update lands.

- **The memory-sync state file writes timestamps in two formats, a structural defect conceded to the kit's backlog rather than any in-flight changeset (parked 2026-09-01; found by the machine coordinator, its board carries the finding and the concession dated 2026-08-31; reported from that board, not re-derived here).** `kit-sync-state.json` at the store root round-trips its timestamps through two spellings, so a reader parsing one format meets the other. Fix shape: one writer, one format, and a read that refuses rather than guesses. Signal: any work on the store's sync runner or its state file.

- **Two disagreeing copies of the sanctioned memory-sync script exist by construction, and the durable fix is a resolution rule rather than a path (parked 2026-09-01; from the machine coordinator's board finding, conceded by this seat 2026-08-31 after verifying the ancestry first-hand: the installed revision is an ancestor of the trunk, 13 commits behind at the check).** The installed plugin cache and the checkout both carry the script, the fleet update closes the gap only until the next commit reopens it, and a wrong copy fails silently (exit 0). The rule to state wherever the script is named: THE SANCTIONED COPY IS THE ONE THE UNATTENDED RUNNER ITSELF EXECUTES, so a check compares against what the runner runs, never against whichever copy is nearest. Signal: the next edit to the sync script or to any doc naming its path.

- **`logs.js` `saveState` keeps a check-then-write shape its heartbeat sibling just abandoned, out of the sidecar effort's scope and recorded rather than swept in (parked 2026-09-01, from that effort's section 2 reviews).** The heartbeat writer now runs guard, then clear, then exclusive create, precisely because a check-then-write at a guarded name can be raced by a same-user writer; `saveState` still writes its sibling without the exclusive-create step. Same-user trust makes this a robustness gap rather than a vulnerability, which is why it did not ride the section. Fix shape: mirror the heartbeat writer's order at `saveState`'s sibling write. Signal: any further hardening pass over `sidecar/logs.js`.

- **The kit's two other ancestor-climbing `.git` detectors still fail open where memq's has just learned to fail closed (parked 2026-09-01; from the memory-read-side plan's section 2 round-6 security review, which rated it Minor and named it for routing rather than as a defect that round introduced).** `plugins/claude-kit/hooks/docs-write-guard.js:79` and `plugins/claude-kit/hooks/readonly-agent-guard.js:718` climb ancestors testing `fs.existsSync` for a `.git` entry, and that predicate answers false both for a dangling link and for an entry that is there but cannot be examined. memq's `isRepositoryRoot` now reads only ENOENT and ENOTDIR as absence, on the reasoning that a climb continuing past a checkout's root on an unreadable entry is the nested-checkout misdirection reached with no link in the path at all. This is the amendment's named class: a rule moving in one resolver while its siblings keep the old derivation. Not reachable today, which is why it parks rather than blocking: both guards derive their root from the payload cwd, which for a launched session is the checkout root that holds `.git`, so the climb terminates at step 0 and neither overshoot (which would relocate the KIT_ONLY `.kit` write allowance to a directory above the true root) nor undershoot reaches. Fix shape: adopt memq's error-code discrimination in both, or single-source one detector that all three call. Signal: any change to either guard's root derivation, or a caller that hands one of them a working directory below the checkout root.

- **`memoryRoot` reaches `os.homedir()` unguarded, so an unresolvable home directory still silences the whole SessionStart hook (parked 2026-09-01; from the memory-read-side plan's section 2 round-7 review, where two lenses rated it Major and the security lens Minor).** `plugins/claude-kit/scripts/memq.js:512` ends in `path.join(os.homedir(), '.claude')` with no guard, and `os.homedir` throws on a POSIX process whose `HOME` is unset with no passwd entry for the effective uid. The hook's ordinary branch reaches it immediately, `decayNudge` to `projectMemoryDir` to `projectsRootPath` at `memq.js:1505` to `memoryRoot`, and `memory-session.js`'s tail catch turns the throw into total silence: no memory blocks at all, which is the outcome the refused-cwd guard exists to prevent while testing only the working directory's spelling. Section 2 guarded the same reach inside `sessionProjectFiling` and corrected the comment that claimed the closure was end to end, so what remains here is the reach itself. It routes rather than folding because the fix reaches every verb's contract: `memoryRoot` returns a path today and every caller assumes one, so making it decline is a contract change across the CLI rather than a change within this section's spirit. It is also invisible to the suite by construction, and that is the more useful signal: `test/memq.test.js:133-142` pins `KIT_MEMORY_ROOT` with `KIT_MEMORY_ROOT_ALLOW_DATA=1` in every child, so the override short-circuits the call and no test on the current harness can reach the unguarded branch. A pin for it needs a child that runs with the store override absent. Fix shape: give `memoryRoot`'s homedir reach a declining envelope with an explicit answer its callers can read, or hoist the resolution to one guarded site the whole module shares. Signal: any work on memq's failure envelope, any new caller of `memoryRoot`, or a harness change that stops pinning the store override in test children.

- **The two payload-reading guards each hold a hand-copied agent-type spelling chain that omits the fifth spelling the shared module carries (parked 2026-09-01; from the memory-read-side plan's section 3 second fix round, which could name the sites but not edit them, and confirmed at the code by the orchestrator before parking).** `plugins/claude-kit/hooks/docs-write-guard.js:46` and `plugins/claude-kit/hooks/readonly-agent-guard.js:88` each read `p.agent_type || p.agentType || p.subagent_type || p.subagentType`, while the canonical set at `plugins/claude-kit/hooks/kit-agent-identity-lib.js:62` is `['subagent_type', 'subagentType', 'agent_type', 'agentType', 'type']`, five spellings to their four. Both guards read a missing type as null and null means allow, so a payload naming the type under the bare `type` key would pass a dispatched agent through the docs write guard and the read-only guard alike. It is not a live bypass and that is why it parks rather than blocking: the bare `type` arm is defensive breadth rather than an observed spelling, added on the library's own stated reasoning that a spelling the list misses is a whole class of dispatch invisible to every reader at once, and the one payload actually measured on this harness (`SubagentStart` on v2.1.252, recorded in that plan's interim board 9) spells `agent_type`, which both chains do carry. It routes out of the memory-read-side plan rather than folding into it because closing it serves guard hardening rather than that plan's goal, which is the memory store's read side, and the two guards sit in no section's file list. The sweep that found them was structural over the chain's shape rather than over a list of file names, and it found exactly these two hand-copies among `plugins/claude-kit/hooks/*.js`. The section's own widened single-source pin in `test/kit-sidecar-capture.test.js` names both as routed exemptions with this reason rather than skipping them silently, so the pin does not read as covering them. Fix shape: point both at `AGENT_TYPE_KEYS` from the shared module, which is the same single-sourcing that section's FIX 4 applied to the recognition nudge, and drop the two exemptions from the pin in the same change so the pin then covers the whole class. Signal: any change to either guard's type derivation, any new hook that reads an agent type off a payload, or a harness release that renames the field.


- **The recognition nudge's per-call match budget charges one op per trigger, so one charged match op covers a glob's whole inner sweep while a `cmd` op covers a single containment test (parked 2026-09-02; from the memory-read-side plan's finishing security review, Minor 2, confirmed at the code by the orchestrator before parking).** `MATCH_OPS_MAX` is 2048 at `plugins/claude-kit/hooks/memory-recognition-nudge.js:507`, and `collectHits` decrements `ops.left` exactly once per trigger considered (`:1727`) and once per anchor (`:1758`). The file's own ceiling comment at `:1520` states the bound as "the matrix of triggers against subjects", which is true of the count and not of the work: a `cmd:` or `err:` op is one containment test, where a `glob:` op runs `globMatchesPath` (`:775`) over the call's touched paths, and that function itself retries `matchSegments` at every start offset of the path, bounded per path by `PATH_SEGMENTS_MAX` rather than by the op budget. So the real per-call work is not proportional to the ops charged, and the budget's unit is not uniform across trigger types. It is not a live denial of service and that is why it parks rather than blocking: both inner loops carry their own segment cap, the touched-path list on a single tool call is small, and glob triggers are admitted from the project tier alone and from no tier at all under a store pin, so the population that can reach this path is one checkout's own authored records. It routes out of the memory-read-side plan because closing it is budget accounting rather than that plan's goal, which is the memory store's read side. Fix shape: charge the glob arm per touched path rather than per trigger, or give `globMatchesPath` its own budget drawn from the same `ops` object, and correct the `:1520` ceiling comment in the same change so the stated bound and the charged unit agree. Signal: any new trigger type whose match is a sweep rather than a test, any rise in `MATCH_OPS_MAX`, or a boundary that starts handing the nudge a large touched-path list.
- **The PIANO substrate: loops on mixed timers outside the main thinking loop (direction recorded 2026-09-03 by the operator in the design dialog; note at `docs/plans/claude-kit_piano-substrate_notes_v1.md`).** Fast loops that cancel or redirect a running task and slow loops for goal-setting, measurement and memory curation, hosted outside the harness, with the judgment sidecar as the first organ and the Claude Agent SDK as the candidate host. The note carries the inventory of loops the kit already runs and the five questions to settle first, authority and SDK billing ahead of mechanics. Signal: the armed queue and the subtraction cycle (bars, test audit, corpus audit and its cut) drain; then a brainstorm, with the design council offered on the authority fork.

- **The watch ledger has no destination for a durable fact about the watched system itself (parked 2026-09-03, surfaced by `claude-kit_gating-definitions_spec_v1.md` section 2 review).** The destination rule at `plugins/claude-kit/skills/standing-watch/SKILL.md` line 81 routes a plan decision to that plan's Chapters and an instrument lesson to the memory store, and names no home for a durable fact about the watched system that is neither. The flipped placement default sends more traffic through that rule, so the gap is more reachable than it was, but it is pre-existing rather than this section's and the section is not widening line 81. Likely belongs with the corpus audit.
- **The watch chassis's own kind definitions fail the gating-definition rule this plan shipped (parked 2026-09-03, surfaced by `claude-kit_gating-definitions_spec_v1.md` section 2 review).** Section 1 landed a rule requiring an enumeration followed by a trailing general clause to say which the clause is, a summary of the examples or an extension past them. The situational kind at `plugins/claude-kit/skills/standing-watch/SKILL.md` line 24 is exactly that shape and says neither, and the standing kind at line 23 is the same. The kind fork at line 32 settles the reading in practice for the doubt case, so this is a compliance gap in the chassis rather than a live ambiguity, and section 2 does not touch either line. Likely belongs with the corpus audit.
- **The watch chassis places a situational member that cannot satisfy its own re-derive obligation (parked 2026-09-03, surfaced by `claude-kit_gating-definitions_spec_v1.md` section 2 review round 15).** `plugins/claude-kit/skills/standing-watch/SKILL.md` line 24 places recent pings on the situational list by name, line 34 requires every situational sentence to carry the command that re-derives it, and line 47 bars re-deriving from anything but the watched system's own source. A ping the loop itself sent is reproduced by no such probe, so a recent ping is a situational member no probe reproduces and has no re-derive command it can carry. Both halves are pre-existing: they are byte-identical at base commit 8a50326, and section 2's edits only state the consequence aloud. The likely resolutions are to give the class a stated substitute for the re-derive field, as the coordinator's own told-not-derived carve-out already does by carrying the time and source of the evidence instead, or to carve the class out of line 34. Likely belongs with the corpus audit.
- **The coordinator's homing round keeps a line on the board on the sole ground that the board is its only copy (parked 2026-09-03, surfaced by `claude-kit_gating-definitions_spec_v1.md` section 2 review round 17).** `plugins/claude-kit/skills/coordinator/SKILL.md` line 91 runs an already-boarded line through the routing as though it were being written now, and its cut invariant cuts only a line whose content it has confirmed landed somewhere else, so a line no destination takes stays on the board and becomes a finding for the operator. Run over the founding incident's board that keeps every narrative line no destination takes, at size, which is the rescue-by-only-record shape principle 3 of that plan calls not the load-bearing half. The invariant is pre-existing, predating base commit 8a50326, and it is a deliberate bound on a destructive act rather than an oversight, so the resolution is a design question rather than a correction: whether a boarded line no destination takes should be cut with its loss accepted, as the chassis accepts at write time, or kept as the operator finding the paragraph now makes it. Likely belongs with the corpus audit.
- **The watch chassis does not say whether one pass record survives the next pass (parked 2026-09-03, surfaced by `claude-kit_gating-definitions_spec_v1.md` section 2 review round 17).** `plugins/claude-kit/skills/standing-watch/SKILL.md` line 79 says to write every pass in a form that survives distillation, which reads as one record per pass; line 30 names the pass record in the singular as a class the destination rule places; and line 15 says the ledger holds what is true of the watched system right now, rewritten in place. Whether pass N's record survives pass N+1 is then left to the admission test's per-line refusal, which line 28 says is asked once at placement rather than on every pass. All three passages predate base commit 8a50326 and section 2's edits do not touch the tension, which matters because an unbounded accumulation of pass records is the founding incident's own failure mode arriving through a rule rather than through drift. Likely belongs with the corpus audit.
- **The coordinator's board carries a class its own two-kinds statement excludes (parked 2026-09-03, surfaced by `claude-kit_gating-definitions_spec_v1.md` section 2 review round 19).** `plugins/claude-kit/skills/coordinator/SKILL.md` line 79 states that every line on the board is situational or a commitment and that the board carries no standing section at all, while inheriting the chassis's ledger rules unchanged save its named overrides. The chassis at `plugins/claude-kit/skills/standing-watch/SKILL.md` line 30 names the read protocol as a class its own rules place on the ledger, and a read protocol is neither situational nor a commitment, so the coordinator's exhaustive statement excludes a class it inherits. Both statements predate base commit 8a50326 and section 2 did not create the tension; what the section changed is that the chassis now names its placed classes explicitly, which makes the seam visible where it was latent. The resolutions are for the coordinator to name the read protocol as the one non-kind thing its board carries, or to override it away outright. Likely belongs with the corpus audit.
- **A memq test's malformed-line fixture collides with its own valid fixture in a single ~10-hour window on 2026-09-04 and on no later date in any year (found 2026-09-04 while running `claude-kit_gating-definitions_spec_v1.md` section 2's close gate; the product code is correct).** `test/memq.test.js:5394` hardcodes the malformed usage line as the literal `{"ts":"2026-08-25T0` and seeds a valid applied stamp at `daysAgo(10)` in the same fixture. When `daysAgo(10)` resolves to 2026-08-25 and the UTC hour begins with `0`, the surviving valid line reads `{"ts":"2026-08-25T00:...` and contains the malformed literal as a substring, so the closing assertion at `:5433`, `!includes(torn)` under the message `the malformed line left the sidecar`, fails on correct output. The drop itself works: the same run's stderr matches `dropping 1 malformed line`, its stdout reports `dropped 1 malformed line`, and the `.bak` assertion beside it passes. The collision was reproduced directly rather than inferred, and the red reproduces solo as well as under the whole gate, so it is not contention. It is a test defect and not a product defect. Its window is one-time rather than annual, because the literal hardcodes the year: `daysAgo(10)` on 2027-09-04 serializes `2027-08-25`, which cannot contain a 2026 literal, so the collision recurs on no later date in any year. That one-window reach is why nothing caught it, and it is also why the red clears itself when 2026-09-04 10:00 UTC passes rather than coming back. Fix shape: derive the malformed literal from a date the fixture's own stamps cannot produce, or assert on the parsed line set rather than on a raw substring, which is the discriminating-assertion class this plan's own Standing Brief Amendment 6 names. Fixing it still earns its keep after the window closes, because the fixture is one year-bump away from colliding again and the raw-substring assertion is the defect rather than the date. Signal: the next edit to that test.

- **The watch chassis names no author for a keeper-learned standing DO-NOT, and no destination for one either (parked 2026-09-04, surfaced by `claude-kit_gating-definitions_spec_v1.md`'s finishing security review).** `plugins/claude-kit/skills/standing-watch/SKILL.md` names two sources for a prohibition, the operator's word and a governing document behind it (lines 30 and 32), and names the keeper as author only of a do-not-reopen trap (lines 30 and 40). A DO-NOT the keeper learned from the watched system itself, of the shape "do not restart this service, the restart corrupts its cache", is unconditioned, is about the watched system rather than the keeper's reasoning, states rather than argues, and re-opens nothing, so it is a genuine standing constraint that matches no named author. Line 81's destination legs are a plan decision and a lesson about the watch's own instruments, so such a line kept off the ledger is dropped, and line 28's cost table licenses that drop on the premise that what the default keeps off is by its nature the keeper's reasoning or an episode's narrative, which is false for it. The resolution is a design question rather than a correction, because it changes the chassis's author model: either name the keeper as a third author of a standing DO-NOT, or widen the trap class to cover a keeper-learned do-not about the watched system and say so, and in either case give line 81 a leg for a durable fact about the watched system. No live consumer exercises it today, since the coordinator carries no standing section at all (`plugins/claude-kit/skills/coordinator/SKILL.md` line 79). Belongs with the corpus audit or a chassis design pass.
- **The watch chassis's admission default still drops a DO-NOT-shaped line whose membership the keeper cannot call (parked 2026-09-04, surfaced by `claude-kit_gating-definitions_spec_v1.md`'s finishing adversarial and security reviews).** The default's doubt branch at `plugins/claude-kit/skills/standing-watch/SKILL.md` line 30 takes three shapes, and the finishing round closed only the third, a recognised member whose particular line the keeper cannot say a successor needs. The second shape, a line the keeper cannot recognise as a member at all, still sends its doubt off the ledger, so a DO-NOT-shaped line of uncertain membership is kept off and then dropped for want of a destination leg. Line 28's own cost principle points the other way for that line, since per lines 32 and 53 the off-error licenses an act no later pass takes back while the on-error only forgoes an act that remains available. The second shape's text is unchanged from base commit 8a50326, so this is pre-existing rather than that round's, and it resolves with the author-model question above rather than separately: a recognition criterion for a prohibition is what removes the doubt, and stating one is the same contract change. Belongs with the item above.
- **The watch chassis's tick order names no ledger read, so a trap's binding point is unstated (parked 2026-09-04, surfaced by `claude-kit_gating-definitions_spec_v1.md`'s finishing security review).** At `plugins/claude-kit/skills/standing-watch/SKILL.md` the tick order runs from line 42, its step 2 re-derives from the watched system and its step 3 presumes the standing list is already in hand, and no step names the ledger read the read protocol at line 38 describes. The self-authored wake prompt at line 53 carries the standing prohibitions and a pointer to the ledger, so it carries no trap. Line 32 states that re-deriving a trap is the re-opening the trap exists to forbid, so a pass that re-derives before it reads the ledger can trip a trap the ledger holds. Pre-existing and untouched by that plan. The resolutions are to name the ledger read as the step between 1 and 2, or to carry traps in the wake prompt beside the prohibitions. Belongs with the corpus audit.
- **Two consequence claims in the watch chassis name mechanisms its own steps do not run (parked 2026-09-04, surfaced by `claude-kit_gating-definitions_spec_v1.md`'s finishing reviews).** Both are in `plugins/claude-kit/skills/standing-watch/SKILL.md` and both are the Standing Brief Amendment 3 class, a consequence stated without checking the rules that would produce it. Line 48's re-measure step re-reads the governing document only for a standing DO or DO-NOT that cites a condition, so a document-backed prohibition citing no condition is obeyed after its document drops it until the operator says otherwise; the cost is over-binding, which is the recoverable direction, so this is a liveness gap rather than a safety one. And line 28 prices the loss of a confirmed mechanism as a re-derivation under the tick order's re-derive step, while that step at line 47 re-derives the board rather than re-reading the system's source, so the cost of dropping a mechanism is a source read the tick order never schedules. Line 32 carries a related over-promise, that a prohibition kept wrongly forgoes an act "which a later pass or the operator's word can still take", which the finishing round's own text narrows by putting the binding question outside the keeper's hands. Belongs with the corpus audit.
- **Confirm the blind reader's gating-definition charge actually reaches a dispatched reader (2026-09-04, operator-only, from the gating-definitions close-out, `docs/archive/claude-kit_gating-definitions_spec_v1.md`).** The charter part that asks a reader for near-miss pairs per gating definition was verified only as a labelled proxy, an agent handed the new part's text verbatim over one document, because a dispatched `blind-reader` resolves the installed plugin copy and the install trails this checkout. The check needs `claude plugin update` and a fresh session, then one `blind-reader` dispatch over a document carrying a gating definition: the report should come back with three near-miss pairs per definition and a separating feature on each. A report without them means the wiring never delivered the instruction, whatever the shipped text says, and section 1 reopens as a new round.
- **Four queued plans carry no `## Dispatch Authorization` section at all (2026-09-04, from the gating-definitions close-out).** `claude-kit_subtraction-bars_spec_v1.md`, `claude-kit_checkpoint-session-validation_spec_v1.md`, `claude-kit_test-audit_spec_v1.md` and `claude-kit_coordinator-sync-machine-scope_spec_v1.md` carry no such heading, confirmed by an anchored grep over each with a control that spoke on a plan whose authorization is recorded. It bears on nothing about the current arming, whose `armedBy` field reads `operator` for all fourteen queued plans, and it does bear on a self-armed run and on a peer-dispatched handoff, where the committed section is the durable grant a receiver traces. Add the section to each, above `## Sections of Work`, which is the one position the frozen v1 machine contract leaves bounding nothing.
- **The audit artifact's account of what the machine coordinator seat writes omits the seat's own artifact-production write (parked 2026-09-04, surfaced by `claude-kit_board-routing-and-homing_spec_v1.md` section 5 review round 2).** `docs/security-model.md` enumerates the write surfaces the four-kinds routing sends content to, and that enumeration is now scoped to the routing rather than to the seat. What it does not describe anywhere is the write the coordinator skill's line 63 states as a charter function, that the seat writes the spec or backlog entry in the repo that owns the work, which is a file write into an arbitrary repository on the machine including public ones, and the kaizen skill's disposition step goes further by writing a brief and then clearing the note lines it handled on a multi-writer inbox. Both were confirmed at their own sources. Nothing states whether the board's two line bars and its readership reduction reach that artifact, and a Dispatch Authorization section is designed to carry operator words. It is out of scope for the plan that found it, whose section charters the routing's writes alone. Signal: the next edit to that document's coordinator section, or the first time a seat is asked what bars reach a spec it authors.
- **The coordinator's outcome journal is a directly writable, unauthenticated account of an irreversible act, and only its first moment's landing is confirmed (parked 2026-09-04, surfaced by `claude-kit_board-routing-and-homing_spec_v1.md` section 5 review round 2).** The frontmatter guard puts sidecars out of scope on every tier and `outcomes.jsonl` sits in the sync allowlist under a union merge, so any local session with the plain file tools and any machine on the store's remote can append or rewrite an entry with nothing authenticating one. Separately, the homing round now confirms that its first journal moment landed and confirms nothing about the second, so a round that completes the publish and the cut while its closing entry fails to land leaves a journal showing a lone `fail` entry, which reads as a round that died partway rather than as a completed irreversible cut. Both bounds belong to the memory-system skill's surface rather than to the coordinator contract, which is why the plan that found them declared them instead of closing them. Signal: any change to the journal's integrity story, or the first time a fail-only key has to be diagnosed.
- **The homing round's new confirming read of its own journal entry runs on an unfenced channel (parked 2026-09-04, surfaced by `claude-kit_board-routing-and-homing_spec_v1.md` section 5 review round 2).** The coordinator skill now requires a round to establish that its first journal entry landed before any destination write begins, and defers how to the memory-system skill, which is what that section's acceptance requires of it. Of the two journal readers that skill states, `memq recent` is fenced and `memq get` is not: it prints the entry's fields charset-reduced but unframed, and the guard table's fence exception names recall only. The journal replicates, so a confirming read can pull store content another machine wrote into the seat's context at a privileged moment. Naming a specific reader in the coordinator skill is refused rather than pending: that section's acceptance requires the how be deferred, and restating another skill's mechanism is the class this plan's amendments 8, 9 and 10 already cut. The repair belongs to the fence's owner. Signal: the next change to the guard table's fence exceptions.
- **A homing round's cut-remedy entry has no stated verdict, so a round's record can be three entries where the contract describes two (parked 2026-09-04, surfaced by `claude-kit_board-routing-and-homing_spec_v1.md` section 5 review round 2).** The coordinator skill states that a round writes at two moments, the first carrying `fail` and the second `pass`, and now states that an entry that landed cut is the memory-system skill's to remedy, whose remedy is to re-log the lost tail as its own entry. Nothing states what verdict that third entry carries or how a successor auditing the journal reads a round whose record is three entries. A cut is the expected outcome for exactly the rounds that earn one, since the first moment must name every line it intends to move for a board that has already failed the one-read test while the summary caps at 120 characters, so this is the ordinary case rather than an edge. The verdict grammar is the memory-system skill's. Signal: the first homing round whose first entry lands cut.

- **Both curated indexes promise a per-plan archive list that neither carries (parked 2026-09-04, surfaced by the board-routing-and-homing close-out).** `docs/README.md` and `docs/plans/README.md` each carry a sentence saying that each entry below describes what that plan delivered when it shipped, and neither has a single entry below it, while `docs/archive/` holds 97 plan documents. The repository reflects the archive by a pointer instead, which is a working convention, so the choice is to delete the two sentences or to build the lists; either way one plan's close-out cannot answer it, since the sentence is a promise about 97 other plans.

- **The journey annotation on curated index lines is that file's convention rather than one effort's addition (parked 2026-09-04, surfaced by the board-routing-and-homing finishing pass).** Index entries narrate when an effort was resumed or which update wave it followed, which the house style bars from a curated document, and sibling lines across both indexes carry it. Sweeping it is a pass over both files rather than a repair inside any one plan's charter.

- **The ownership map's third column cannot distinguish a pointer from a co-owner or an enforcer (parked 2026-09-04, surfaced by `docs/plans/claude-kit_subtraction-bars_spec_v1.md` sections 1 through 3 review rounds).** The column names the surfaces that speak to an owner's moment, and three review rounds across three sections circled it because a row cannot say whether a listed surface points at the owner, shares ownership, or mechanically enforces the rule. Rows 40 and 93 both went stale this plan in the same way: a new doctrine pointer landed and neither row could record it as a pointer rather than a second owner. Section 5 adds the two missing entries; the column's own expressiveness is the item here.

- **No precedence between `writing-skills` and `scott-writing-style` for a kit-shipped document in the operator's voice (parked 2026-09-04, surfaced by `docs/plans/claude-kit_subtraction-bars_spec_v1.md` section 3, both review lenses independently).** Ownership-map row 45 gives operator-voice documents to `scott-writing-style`, whose `:85` licenses long explanatory sentences of thirty to fifty words, and row 93 gives kit-shipped curated prose to `writing-skills`, whose new sentence-shape bar treats a count past forty as a second idea. A README or a doc under `docs/` written in the operator's voice sits in both sets. Section 3 scoped its own bar to the kit's voice, which removes the contradiction at the point of action and leaves the precedence unstated. The map's own amend rule says a two-owner moment belongs in its contested table.

- **`plugins/claude-kit/skills/scott-writing-style/SKILL.md` carries en dashes, against a repo-wide ban (parked 2026-09-04, surfaced by `docs/plans/claude-kit_subtraction-bars_spec_v1.md` section 3's fix round).** The doctrine bans em and en dashes anywhere, prose and code comments alike. That file uses them for numeric ranges in at least three places (`30-50 words` at `:85`, `2-4 short sentences` at `:93`, and a further instance at `:134`, each currently spelled with an en dash). The fix is a hyphen per instance and is mechanical; the reason it is parked rather than folded is that no section of this plan owns that file and a dash sweep of another owner's file has no gate in this plan's lanes. The corpus audit is the natural home, and a sweep should cover the whole tracked tree rather than this one file, since a ban violated in one curated file is worth checking everywhere.

- **`prose-reviewer.md`'s severity ladder lists no style item under Major, while its new Surplus bullet rates one Major (parked 2026-09-04, surfaced by `docs/plans/claude-kit_subtraction-bars_spec_v1.md` section 2, both review lenses).** The charter's ladder at `:59` and `:60` puts style deviations under Minor and names no style case under Major, and the file's other `[style]` case is Minor. Section 2's Surplus bullet rates a restatement of an owner Major and tags it `[style]`, so a reviewer reading the ladder and a reviewer reading the bullet rate the same finding differently. Either the ladder gains the case or the bullet drops to Minor; the plan's Assumption 3 records the reversal as one severity line per charter, so this is a small deliberate call rather than a defect to fix blind.

- **`writing-skills/SKILL.md:21` states which forms a mention may take, and it is narrower than the doctrine (parked 2026-09-04, surfaced by `docs/plans/claude-kit_subtraction-bars_spec_v1.md` section 3's fix round).** That line permits a pointer or an operational residue at its point of action and says never a restatement, while the doctrine at `home/claude-kit-doctrine.md:41` also permits a surface that copies the rule whole under a parity pin or a build step, and `:74` of the same skill names a deliberate restatement across covered surfaces as a live category with its own disposition. So the kit's own owner of the one-owner rule omits two forms the doctrine and its own later section allow. Section 3 left `:21` alone because it is unchanged text outside that section's delta, which Standing Amendment 3 routes here. The fix is one clause at `:21`.

- **`kit-git-lib.js` offers no way to raise its 1 MiB output ceiling for one call, so a reader of a large tracked file has no supported route to git's copy of it (2026-09-04, from the subtraction-bars plan's section 4; routed out because the fix is another file's contract).** `plugins/claude-kit/hooks/kit-git-lib.js` exports `gitRun`, `gitOutput`, `gitChildEnv`, `DEFAULT_TIMEOUT_MS` and `MAX_OUTPUT_BYTES`, and that last constant is 1,048,576 bytes hardcoded into the `spawnSync` `maxBuffer`. The `options` object every caller passes carries `timeoutMs` alone, so the ceiling is a module constant rather than a per-call argument and a caller needing more cannot ask. It bites today: `test/memq.test.js` is 1,481,542 bytes, over the cap, and it sits inside the corpus the new size reader measures, so `kit-size.js` cannot take a HEAD reading of the single largest file in the very set it exists to bound. Section 4 worked around it rather than fixing it, reading HEAD content only for the files git reports as changed and naming a HEAD read it needs and cannot get rather than omitting one silently, because a silent omission reads exactly like a file that did not change. What the workaround cannot do is give any caller the whole-corpus HEAD reading, so the next tool that wants one rediscovers this. Repair shape: let `options` carry a `maxOutputBytes` that defaults to the constant, so the ceiling stays where it is for every existing caller and a caller with a reason can raise it, with the reason that the ceiling exists (a runaway git command should not exhaust the box) preserved by keeping the default rather than removing the bound. Signal: the next tool that wants git's copy of a file it cannot size, or the next edit to that library's signature.

- **A repo-only development tool ships inside the plugin payload every install downloads, and nothing in the layout distinguishes the two kinds (2026-09-04, from the subtraction-bars plan's section 4; the section put it there because that is where the payload's script directory is).** `plugins/claude-kit/scripts/kit-size.js` reads this repository's own curated-file sizes against `test/size-budget.json` and fails a ratchet. Its subject is this repo and nothing else: an installed kit has no `test/` directory, no budget file, and none of the measured roots outside the payload, so the verb is inert wherever it lands after an install while still riding in the download. Its neighbours in that directory are genuinely shipped tools, which is why it went there and why the shape is worth deciding rather than drifting: the directory now holds two kinds of script with no marker separating them, so the next author reads the precedent rather than the intent. The cost today is bytes and a reader's confusion rather than a defect, which is why this is a backlog item. Repair shape: decide whether the payload gains a stated convention for repo-only tooling (a `dev/` sibling excluded from the build, or a header line the build reads) or whether this tool moves out of the payload entirely, and record the decision where the build can enforce it. Signal: the second repo-only tool that wants a home, or the next build-size question.

- **`test/helpers/` is the natural home for the fixture-git helper two test files now want, and the new size classifier reds any tracked file it finds there (2026-09-04, from the subtraction-bars plan's section 4).** Section 4's `test/size-ratchet.test.js` builds real git fixture repositories under the system temp directory, and the same helper steps are the kind a sibling test file would reuse rather than re-author. The obvious home is a shared module under `test/helpers/`. The obstacle is the ratchet's own coverage control: it classifies `test/*.test.js` by lines and a test count, and any other tracked file under a measured root that no shape classifies and no named exclusion covers is a hard failure, which is deliberate, since that control is what proves a ratchet cannot green on a corpus it never measured. So a helper committed at `test/helpers/fixture-git.js` reds the gate until either the classifier gains a shape for it or the exclusion list gains its name, and neither choice is obvious: a shape means deciding what a helper's budget should even measure, while an exclusion means a tracked file under a measured root that nothing bounds. Repair shape: rule on how the size budget treats test support code before the first shared helper lands, so the decision is made once rather than as a gate-unblocking edit under time pressure. Signal: the second test file that wants the fixture builder, or the first `unclassified` failure naming a path under `test/helpers/`.

- **A `Bash(node:*)` permission rule would fail both screens of the grant audit, and the shape is inviting enough to be worth writing down before someone reaches for it (2026-09-04, from the subtraction-bars plan's section 4; a grant-hygiene note rather than a defect, since no such rule exists in this tree).** The security reviewer's checklist asks two questions of any command grant: does it name a specific command rather than an interpreter, and can the argument position be steered into something else. A rule permitting `node` with any arguments fails both. It names an interpreter, so the grant is really a grant of arbitrary code execution: `node -e '<anything>'` is inside it, as is `node <any path>`, which reaches any script a repository happens to carry and any file an earlier step wrote. And the argument position is the whole payload rather than a parameter, so there is nothing narrower to bound. The reason to record it is that the temptation is structural rather than careless: this kit's own tooling is node scripts, a session runs many of them, and each one otherwise costs a prompt, so `Bash(node:*)` is exactly the rule a tired operator writes to stop being asked. Repair shape: where a grant for this tooling is wanted, name the scripts rather than the interpreter, one rule per script path, and keep the interpreter-level grant out of the settings file; and where a reviewer meets a proposed one, this is the entry that says the audit was already run on it. Signal: the first proposal of an interpreter-level grant in any settings file this kit reads.

- **The fenced-block and heading screen that keeps a printed path from forging a Chapter line lives inside its first producer, so the next tool printing repository-supplied text into that block reimplements it by not implementing it (2026-09-04, from the subtraction-bars plan's section 4; routed out because the shared home is another file's export surface).** `plugins/claude-kit/scripts/kit-size.js` renders every path it prints through `safePath`, which wraps the shared `safeForAuthorization` from `plugins/claude-kit/hooks/kit-goal-lib.js` and then adds two strips of its own: a backtick run, which would close the fenced block the reading sits in, and a leading hash run, which would open a markdown heading. Both strips are properties of the destination rather than of this producer, that destination being the fenced block inside a plan-doc Chapter whose body the status line's plan parser reads line by line, keying on column zero exactly as the strips do. The kit's own rule is that a sanitizing guard belongs at the shared boundary the moment a channel gains a second producer, and this channel now has one producer carrying the guard and a shared library without it, so the second author inherits the weaker half by writing nothing. Repair shape: export the destination screen from `kit-goal-lib.js` beside `safeForAuthorization`, have `kit-size.js` call it, and leave the printable-ASCII screen where it already is. Signal: the second tool that prints repository-supplied text into a Chapter's fenced block, or the next edit to that library's export list.

- **The size ratchet's own suite reaches its subject through a child process at nearly every case, where the same functions are callable in process, so the file's wall clock is set by process spawns rather than by the work under test (2026-09-04, from the subtraction-bars plan's section 4; routed out because it is a cost judgment over a file the test audit will re-read in full anyway).** `test/size-ratchet.test.js` stood at 2,737 lines carrying 101 call sites of its `runScript` helper, which spawns the reader as a child process, beside 155 in-process call sites of the same module's exported functions and 5 spawns of git for fixture construction, measured on SCOTT-CLAUDE on 2026-09-04 at that section's sixth fix round. Its lane read 154 tests in 119.9 s under a named foreign contention, so the per-case process cost is the dominant term rather than the assertions. The two shapes are not interchangeable and the file is right to hold both: a case whose subject is the command-line boundary itself, argument parsing, an exit code, or which stream a refusal lands on, is only testable through a spawn, while a case whose subject is a function's own return value is testable in process and pays the spawn for nothing. What is not established is the split between them, which is the question this item asks: how many of the 101 are boundary cases by subject. Repair shape: classify each `runScript` site by what its assertion actually reads, reduce the non-boundary ones to in-process calls against the already-exported surface, and keep one spawned case per distinct boundary claim rather than per behaviour. Signal: the test audit reading this file, or the lane's wall clock becoming a gate concern on a slower box.

- **`docs/security-model.md` accounts for the configuration the kit suppresses when it runs git, and not for the configuration it cannot suppress: a repository handed to a reading tool carries its own git configuration, which no environment key the kit sets can reach (2026-09-04, from the subtraction-bars plan's section 4; routed out because the file it wants is not in that section's scope).** The kit's shared git runner strips every `GIT_*` key from the children it spawns, and the size reader's suite additionally pins `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` at absent files, so the global and system scopes are accounted for. A repository's own `.git/config` is a third scope, read by git by construction because it belongs to the repository being operated on, and the size reader operates on whatever directory its `--repo` argument names. That much is confirmed by reading the runner's strip and the reader's argument handling. What is NOT established, and is the whole question this item exists to settle rather than to assert: whether any repository-local configuration key executes a command for the specific subcommands this tool runs, which are `rev-parse`, `ls-files`, `diff` and `show`. Some git configuration does name commands (pagers, filters, external diff drivers, a filesystem monitor), and whether any of those fire on this command set is unverified here. Treat the exposure as unquantified rather than as demonstrated. Repair shape: determine which repository-local keys reach those four subcommands, then either state the residual in that document beside the mutating-CLI exposure it already names, on the same pattern, or record that the command set reaches none of them, which is equally worth writing down. Signal: the next tool that reads a repository it did not create, or the next audit of that document's exposure list.
- **The repo README's payload map lists five of the eight tracked files under the plugin's `scripts/` directory, so three ship undescribed on the surface a reader treats as the map (2026-09-04, from the subtraction-bars plan's section 5 sighted review; routed out because only the one entry the new size reader falsified was in that section's scope).** `kit-endpoint-lib.js`, `memory-index.js` and `prompts/relevance-v1.js` appear nowhere in `README.md`, while `docs/architecture.md` names all eight, so the two surfaces disagree about what the directory holds and the shorter one is the one a newcomer opens first. Section 5 added the size reader's entry because the section owned that falsified claim; completing the map is a README audit rather than a claim repair, which is why it is here. Repair shape: add the three entries in the map's own aligned form, and decide at the same time whether the map is meant to be exhaustive, since a map that is complete by accident drifts back the next time a script lands. Signal: the next script added under that directory, or the next pass over the README's payload map.

- **Two doctrine pointer pins assert that the skill copy's bullet equals the mirror copy's, which the whole-body identity pin above them already implies, so each is a duplicate by the retire classes the kit now ships (2026-09-04, from the subtraction-bars plan's section 5 blind review; routed out because the instances sit in tests this section did not touch).** `test/doctrine-parity.test.js` asserts `mirrorBody() === skillBody()` for the two doctrine copies whole, and the gate-bullet and authoring-bullet pointer pins then each assert their own filtered line is equal across those same two bodies. Equal bodies imply equal filtered lines, so neither assertion can fail unless the whole-body pin has already failed, which is the owner's own definition of a duplicate: a test whose failure implies another's and which catches nothing the other misses. The two presence counts beside them are not duplicates and stay. Section 5's own new pointer pins had the same leg and it was removed there, so the tree is inconsistent until these follow. Repair shape: drop the redundant equality leg from each pre-existing pointer pin, keeping the two `length === 1` legs, and check the same shape has not spread to the other pointer pairs. Signal: the test audit plan, which adjudicates every test against these classes.

- **`test/doctrine-parity.test.js` asks git a separate question per assertion, fifteen child spawns per run where the tracked listing one of them already takes answers every membership question in the file (2026-09-05, from the subtraction-bars plan's section 5 second review round; routed out because the repair rewrites a shared helper many tests call, which is not a change to make in a section's last fix round).** The `assertTrackedInIndex` helper spawns `git ls-files --error-unmatch` once per path it is handed, and fourteen call sites use it, while the cross-surface retire-class sweep in the same file already lists the whole tracked tree once. The testing-discipline skill names the cheaper form directly: a spawn per assertion becomes a spawn per batch. Repair shape: derive the tracked set once per run and have the helper test membership in that set, keeping its contract (a throw naming the untracked path) byte-identical so no caller changes, and confirm the set is derived lazily so a file whose tests never run pays nothing. Signal: the test audit plan, which re-reads this file against the retire classes anyway, or the next time this suite's wall clock is measured under contention.

- **`test/doctrine-parity.test.js` guards the read and enumeration sites its newest sweep added and leaves every sibling site in the same file unguarded, so one file holds two contradictory standards for the same boundary (2026-09-05, from the subtraction-bars plan's section 5 third review round; routed out because the file's whole read-site population is one audit rather than one site, and auditing seventy call sites is not a change to make in a section's last fix round).** The section's new cross-surface sweep enumerates through `git ls-files`, screens each path with `containedRealPath` and reads it with `readFileBounded`. Three sibling whole-tree sweeps in the same file enumerate with `readdirSync` over the worktree instead, which includes untracked files, follows a symlinked final component out of the checkout, applies no size ceiling, and in one case puts matched line text into an assertion message; and the new sweep's own vocabulary read, which derives every class pattern the sweep then judges against, goes through the file's bare `readRepoFile` rather than through the containment it applies to every other path, so a path resolving out of the tree there empties the carrier set and reads exactly like a clean tree. Honest bound on all of it: each needs local write access to the repository, and in that scenario the assertions themselves are attacker-controlled, so the value is consistency and defence in depth rather than a live exposure. The doctrine's own rule is the shape of the repair: a guard belongs to the channel rather than to the producer that first needed it, so the boundary's guard is applied at every call site or at none. Repair shape: enumerate the file's read and directory-walk sites, route each through the exported `readFileBounded` and `containedRealPath` its newest sweep already uses, and make the file-header disclosure state the result rather than an aspiration. Signal: the test audit plan, which re-reads this file against the retire classes anyway.


- **The chat-completions translation sends a JSON schema without `strict`, so a gateway that treats a non-strict schema as advisory returns an unconstrained answer and the degrade line cannot say why (2026-09-05, from section 1 of the endpoint-dialect-key plan's review round; confirmed at source, no observed failure).** `plugins/claude-kit/scripts/kit-endpoint-lib.js` builds `response_format` as `{ type: "json_schema", json_schema: { name: "answer", schema: src.format } }` in `toChatCompletionsRequest` and sets no `strict: true`. On this fleet's llama.cpp server the schema does constrain, confirmed by a live `memq find` returning a well-formed ranking that `parseJudgedAnswer` accepted, so nothing is broken today. The exposure is a gateway whose json_schema support is advisory rather than enforcing: there the ollama path's native `format` grammar constrains and the chat path's does not, the answer fails validation instead, and the reader sees `the endpoint's answer was not a ranking` with nothing naming the dialect as the cause, since only the refusal branch names it. The translator is shared, so the sidecar daemon's judge and recognition calls inherit the property. Repair shape: decide whether the translation owes `strict: true`, which is not free because some gateways reject a strict schema outright and the daemon would inherit that too, and if it is added, pin both dialects' constraint behaviour against a server that enforces; the cheaper half, independent of that call, is to name the resolved dialect beside the endpoint fingerprint note so every degrade is placeable rather than only the refusal. Not handed off as its own spec because there is no observed defect to reproduce and the answer turns on gateway behaviour this fleet cannot exercise. Signal: a fleet machine moving to a gateway other than llama.cpp, or a judged or recognition degrade reading `was not a ranking` on a healthy endpoint.

- **The endpoint config loader accepts a url carrying a path and never says so, so the OpenAI convention produces a doubled path that only fails at the first real call (2026-09-05, from section 1 of the endpoint-dialect-key plan's blind review; confirmed at source).** `plugins/claude-kit/scripts/kit-endpoint-lib.js`'s `loadEndpointConfig` validates the url with `/^https?:\/\/[^\s/]+/`, which is unanchored at its end, and strips only trailing slashes, so `http://gw:8080/v1` passes as valid. Every generation call then appends the dialect's own suffix, so that config posts to `/v1/v1/chat/completions` and the endpoint answers 404. Naming a base url ending in `/v1` is the standard OpenAI convention, so this is the misconfiguration an operator is most likely to write. Section 1 made the failure legible rather than silent: the refusal detail now names the path the request actually reached, so the doubled prefix shows up in the degrade line instead of reading as a correct path. What is still absent is a reading at load time, where the cost is one 404 round trip and a puzzled reader rather than a wrong answer. Repair shape: have the loader report a url whose parsed pathname is anything but empty or `/`, and equally a url carrying a query or a fragment, which the same unanchored check admits and which swallow the appended dialect suffix into the query or the fragment while the pathname still parses as `/`, so a check reading the pathname alone stays silent on exactly the two shapes where the suffix never reaches the wire. The reading is a warning beside the existing `api ignored` one rather than a refusal, since refusing would break a deployment that genuinely serves the dialect suffix under a prefix; the warning names the pathname it found and what the call will append. Deliberately not done in the endpoint-dialect-key plan, whose Approach states that the config parser stays as it is. Signal: the next plan that touches `loadEndpointConfig`, or an operator report of a 404 from a healthy endpoint.

- **The non-2xx refusal detail carries no length cap, so the 120-character print on memq's degrade line silently drops the dialect clause for a long posted path (2026-09-05, from section 2 of the endpoint-dialect-key plan's review rounds; confirmed at source, trigger unreachable on this fleet).** `plugins/claude-kit/scripts/kit-endpoint-lib.js:611` builds the detail as `HTTP ${res.status} from ${postedPath} (${api} dialect)` and applies `MAX_DETAIL_CHARS` (200) nowhere on it, while the sibling refusal at `:631` does apply it. memq prints the detail through `sanitize(sent.detail, 120)` at `memq.js:6443`, a bare slice with no truncation marker. The fixed part of that string is 31 characters, so the dialect clause starts falling off the end once `postedPath` passes 89; `postedPath` is the configured pathname joined with the dialect's own suffix, so in configured-path terms that is roughly 69 characters under `/v1/chat/completions` or 76 under `/api/generate`. The dropped clause is the one fact the change that added it exists to supply. This is not a defect in the transport's own contract, which does name all three values, so the surface is the printing one rather than the naming one, which is why it took the route out of the plan rather than an appended section. The daemon's own leg carries that same uncapped detail into a durable gap record and onto stderr with no bound at all before the rollup's 2000-character render, which is unbounded operator-authored text rather than an exposure and so rides here as a clause rather than as an item of its own. The trigger is empty on this fleet, where every machine's config names a bare host and port with no path at all. Repair shape: capping the assembled detail at the transport as its sibling does fixes nothing by itself, since 200 leaves memq's 120 the tighter bound; the two shapes that actually hold the clause are bounding the path component so the assembled detail fits inside the printing surface's own cap, or ordering the dialect ahead of the variable-length path, which section 1 of that plan pins as `HTTP <status> from <path> (<dialect> dialect)` and which would therefore be approval drift. Pin whichever shape is chosen against a long configured path, in both dialects. Signal: a fleet machine configuring an endpoint url with a path, which the sibling entry above on the loader's unanchored url check makes likelier than it reads.

- **The address screen is restated at eight sites outside the security model, and none carries the path-versus-address distinction the shipped refusal now needs (2026-09-05, from section 2 of the endpoint-dialect-key plan's security review, widened by that session's own sweep; confirmed at source).** The non-2xx refusal branch at `plugins/claude-kit/scripts/kit-endpoint-lib.js:611` writes the configured URL's own pathname into log lines, records, reports and test assertions. The screen it narrows is claimed at `kit-endpoint-lib.js:32` ("lands in no comment, log line, record or test") and again at `:325` and inside `classifyThrow` at `:421`, which states the promise in the document's own terms, that the endpoint address reaches no log line, record or report, and which is the site the next author of the refusal branch reads, at `plugins/claude-kit/scripts/memq.js:6419`, at `sidecar/daemon.js:524` ("not printed, here or anywhere", the widest phrasing in the tree and sitting in the file that emits the line), at `sidecar/battery.js:152` and `:1489`, and on two surfaces that face outward: the printed banner at `sidecar/battery.js:1493` ("the endpoint address is never printed; its fingerprint is below") and `sidecar/batteries/README.md:284`, which ships in this public repository. Every one of them survives literally, because the address is the host and its port while a pathname is neither, and section 2 of that plan drew exactly that distinction in `docs/security-model.md` on both the memq and the daemon legs. What is left is that these are the sites the next author of a print statement in these files reads, two of them make the promise to an operator or to a stranger, and `sidecar/battery.js` wires the daemon's report dependency to its own stderr, so a dialect-mismatched or path-doubled config prints a pathname into the very report whose banner denies it. Coverage of that list, since it is a claim about a class: the predicate swept was a grep over `*.js`, `*.md` and `*.ps1` in the working tree for the phrasings that assert the address is not printed, excluding `docs/security-model.md` and `.kit/`; it found six of the eight sites above and did not find the `kit-endpoint-lib.js:32` header form or the `:421` restatement, both of which the reviews named instead, so the class of prose asserting this screen is not provably swept and a repair pass re-sweeps rather than working from this list alone. The tests that assert the screen (`test/kit-sidecar-daemon.test.js:2165` and `test/memq.test.js:26986` among them) compare against the host rather than the path, so they hold as written. Repair shape: one clause at each prose site naming the path as outside the screen and pointing at the security model's two paragraphs rather than restating them, which keeps the single-source discipline those sites already follow. Signal: the next change to any of these files that prints anything, or a reader citing one of them as authority for what a refusal discloses.

- **The architecture document credits a host-side generation-lane watchdog whose probe cannot answer against this fleet's server, so a reader counts a restart-on-wedge protection that is structurally inert here (2026-09-05, from the endpoint-dialect-key plan's finishing curation; confirmed at source, predating that plan).** `docs/architecture.md` describes, under Repo layout, a scheduled task that probes the generation lane every few minutes and restarts the service after two consecutive failed probes. `sidecar/ollama-watchdog.ps1:109` posts to `$BaseUrl/api/generate`, which is the Ollama dialect's own path and the path a llama.cpp server answers 404 on, and every machine of this fleet runs such a server. So the probe fails permanently rather than on a wedge, which is at once a protection the document credits and does not have, and a probe that would read a healthy server as one needing a restart. The endpoint-dialect-key plan named the watchdog in its Out of Scope list as host state the operator holds, and the operator-tier record `the-host-endpoint-speaks-only-the-openai-dialect` carries the endpoint's shape; what stays open is the watchdog's own path and the sentence that credits it. Repair shape: either teach the watchdog the dialect its host actually serves, reading the same `api` key the kit's own transport reads, or retire it and say so in the document. The choice is the operator's, the scheduled task being host state rather than repository state, which is why this entry does not pick one. Signal: the next plan that touches `sidecar/ollama-watchdog.ps1`, or an operator ruling on whether the watchdog stays.

- **The memory-system skill's degrade enumeration for the judged ranking carries no clause for a refusal, so the surface a session reads before running `memq find` cannot place the line this plan added (2026-09-05, from the endpoint-dialect-key plan's finishing curation; confirmed at source, deliberately out of that plan's scope).** `plugins/claude-kit/skills/memory-system/SKILL.md` states the judged block's degrades as one line for a dead or slow endpoint and silence for an absent config. A dialect mismatch is neither: the transport returns a refusal whose detail names the status, the path the request reached and the dialect it posted in (`plugins/claude-kit/scripts/kit-endpoint-lib.js:609-613`, printed at `memq.js:6439-6444`). The skill is the surface a session loads before it reaches the store, so a reader who meets that refusal line there has nothing to place it against, where `docs/architecture.md` now carries the full degrade set. The plan ruled the skill out of scope for a stated cost, that the clause would spend a size-ratchet cap raise for a sentence the architecture document already carries, and that cost is what makes this a backlog item rather than a defect the plan should have fixed. Repair shape: add one clause to the skill's degrade sentence naming the refusal, in the same edit as the next cap raise that file takes for another reason, so the raise is spent once rather than twice. Signal: the next plan that edits `plugins/claude-kit/skills/memory-system/SKILL.md`.
## Snapshots

Completed items are archived to `archive/backlog-YYYY-QN.md`. An item retired because an effort removed what it was about goes to a dated snapshot naming that effort.

- `archive/backlog-retired-2026-08-29.md` (retired by the memory-store shared-tier close-out)
- `archive/backlog-retired-2026-08-23.md` (retired by the dormant-feature removal)
- `archive/backlog-retired-2026-07-31.md` (retired by the compact-session removal)
- `archive/backlog-retired-2026-07-24.md` (retired by the compaction-automation unwind)
- `archive/backlog-2026-Q3.md`
- `archive/backlog-2026-Q2.md`
