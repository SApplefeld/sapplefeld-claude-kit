# Kaizen inbox: NEO-CLAUDE
- 2026-09-02 | NEO-CLAUDE | claude-kit | RE-CAPTURED after being cleared without disposition by `ade15e9`; full original text at commit `d09d099`, which is the very commit the clearing pass names as its own base. The judgment sidecar scores a tool call's output against the intent string the caller stated, so it raises a divergence alert on every DIAGNOSTIC whose whole purpose is to find divergence. For those calls a mismatch between expectation and result is the success condition rather than a fault, and the sidecar has the sign backwards: it alerts loudest exactly when the instrument is working. Reproducible class rather than an incident, confirmed by repeat firings on the same shape, both on the coordinator board's mandatory duplicate-subject sweep, whose entire job is to compare per-line expected occurrence counts against actual ones and surface the ones that differ; on one of those occasions the sweep had just caught a real self-contradiction on the board, so the false alert sat directly on top of a true positive. The cost is calibration rather than a bad act: a seat running a verification ritual every pass draws a guaranteed false alert every pass from the instrument that exists to flag its mistakes, and a channel that cries wolf on schedule is one a seat learns to skim, which matters because the true positives arrive through that same channel. Candidates: let a caller mark an intent as a divergence-seeking check so the sidecar scores whether the check RAN correctly rather than whether results matched expectations, since a check stating expected values inline is announcing it expects some to fail; or have the sidecar treat a stated expectation as a hypothesis under test rather than a prediction of the output. The general lesson: an oversight instrument that infers success from "output matched what the caller predicted" will systematically misjudge every tool whose function is to discover that a prediction was wrong, which is most of the verification surface.
- 2026-09-02 | NEO-CLAUDE | claude-kit | QUALIFIES THE SIDECAR NOTE ABOVE and is filed separately because an adjudicator reading that note alone would reasonably weaken or gate the instrument, and the same session produced the evidence against that disposition. That note's cost argument holds and its remedy candidates stand; what it lacks is the other half of the ledger. Across one session this seat drew ten alerts and five were FAIR, and the fair ones increasingly landed inside the very divergence-seeking class the note identifies as structurally false-positive, which retires "clusters on divergence-seeking checks" as a dismissal rule while leaving it true as a description. THE PART WORTH GENERALISING IS WHAT THEY CAUGHT: in each, the seat's CONCLUSION was correct on re-derivation and its ROUTE to the conclusion was unsound. One read an exit code produced by `head` at the end of a pipe rather than by the check it claimed to report; three others printed a fixed legend ("empty above = none present", "empty list = no overlap") beneath output that was not empty, with a prose claim built on top. A defect whose answer happens to come out right emits no symptom anywhere in the artifact: no test reddens, no count moves, no later step trips, so no internal check the kit has or could add would surface it, and the only thing that did was an outside reader scoring the route rather than the result. That is a capability argument rather than a defence of the implementation, and it cuts against the obvious remedy, since suppressing the divergence-seeking class to fix the false-positive rate would have suppressed every one of these: a check announcing expected values inline is exactly the shape such a suppression keys on. Candidate that keeps both halves: score the ROUTE independently of the match, so a check whose stated expectations fail is not alerted while a check whose reported status could not have come from the command still is, a hardcoded verdict and a hijacked exit code both being visible in the call's own text without knowing what the answer should be. THE GENERAL LESSON, one level above the sidecar: the defects an oversight instrument is uniquely worth keeping for are the ones that leave no trace in the artifact, so its value cannot be read off how often it was right about outcomes, and a false-positive rate measured against outcomes will always argue for removing exactly the instrument that catches them.
- 2026-09-03 | NEO-CLAUDE | Neuro-Evolution-Operations | The goal CLI records who ran an arm or append as the operator by default, so a session that runs the command on a relayed operator instruction writes a record claiming a keyboard act that did not happen, and nothing in the CLI corrects that field afterwards: append refuses a plan already queued and the state writer is not exported, so the only remedy was a hand edit of .kit/goal-state.json written temp-then-rename. Evidence: two seats on one day each ran an arming on a relayed ruling without --self-armed, one caught by a peer before the write and one after, leaving a six-plan queue whose arming field read two ways until edited. Candidate: default the arming record to self whenever CLAUDE_CODE_SESSION_ID is set, since that means a session is running it, and require an explicit flag for the operator claim; or add a kit-goal.js verb that rewrites armedBy for a named plan through the library writer.
- 2026-09-04 | NEO-CLAUDE | Neuro-Evolution-Operations | Two kit surfaces resolve a plan archive as a flat `docs/archive/<file>` while a long-running kit-driven repo has grown a nested `docs/archive/plans/<file>` (136 plans there against 4 left flat), and nothing reconciles them, so every archived plan is invisible to the readers that walk the archive. curating-docs prescribes the flat `git mv docs/plans/<file> docs/archive/<file>`; kit-goal position walk looks in `docs/plans/` and `docs/archive/` only. The failure is silent in the worst direction: the session-start advisory reports the armed plan doc as being in neither directory and keeps its queue position rather than erroring, which reads identically to a plan doc that was deleted or never written, so a completed-and-archived plan and a lost one are the same observation. Candidates: have both walks search the archive recursively, since an archive is history and a recursive read costs one directory walk; or have curating-docs state the archive layout as a per-project fact the walks read, rather than as a path literal each surface repeats. General lesson: where a kit surface writes a path literal and another surface reads it back, the pair drifts the moment a project subdivides the directory, and a reader that reports not-found rather than erroring turns that drift into a false negative that looks like real data.
- 2026-09-04 | NEO-CLAUDE | Neuro-Evolution-Operations | A dispatch brief put a `docs/` file in a fix section's scope for an implementer subagent, and the kit's own docs-write-guard hook blocks that agent type from writing anywhere under `docs/`, so the section could not be completed as briefed: the code half landed (test comments repointed at a durable doc passage, replacing a plan-doc citation that archives away inside the effort) while the doc half had to be handed back as report text for the orchestrator to paste, leaving those citations dangling until someone else acts. The dispatching session cannot see the guard when it scopes the brief, because the guard is enforced at the subagent's tool call and declared nowhere the composing session reads. General lesson: where a hook restricts writes by agent type, that restriction is part of the agent type's contract and belongs where briefs are composed; otherwise briefs keep scoping work the agent is structurally unable to do, and the refusal lands mid-section, after the dependent edits are already written, rather than at dispatch.
- 2026-09-04 | NEO-CLAUDE | claude-kit | The stamp audit resolves the coordinator board by the runbook default filename only, so on a machine where the operator settled a different board filename it reports "the board absent" on every run and its against-the-clock reading over the board stamps never executes. The registry and claim legs still run, so the exit code and report look like a working audit; the board leg is silently uncovered, and a reader who trusts the run has no signal that one of three legs did not fire. This is the silent-check failure mode the kit warns about, inside the kit own instrument: absence of a finding reads identically to a clean board. Candidates: have audit take the board filename as a flag or read it from a per-machine fact, as the coordinator runbook already defers other per-machine figures; or have it report the board leg as UNCOVERED rather than the board as absent, so the report distinguishes "checked and clean" from "never looked". General lesson: a multi-leg checker that resolves one leg by a hardcoded path turns a naming decision into permanently missing coverage, and reporting the subject as absent rather than the leg as uncovered is what makes it invisible.
- 2026-09-04 | NEO-CLAUDE | claude-kit | The store has no write-time duplicate check, so a memory is authored against the author's own recall rather than against the store, and an overlapping record is noticed only later by a reader who happens to search first. The authoring verbs take a name, a description and a body and write them; nothing in that path runs the search the store already implements well, so "search before you bank" is carried entirely by the author remembering to, against an operator tier now 268 records deep carrying 1080 read stamps. Evidence from one session on one seat, same store and same author, opposite outcomes with the search as the only variable: a claim-protocol record was banked and only afterwards found to overlap two existing records that covered part of it better, costing an update to repair; later the same session searched first for an orphan-testhost lesson, found an existing record at 0.59 in the right tier already applied eight times, and the write became unnecessary. What the store already holds makes the gap plainer rather than smaller, because both halves are recording rather than proposing: the decay scan reports a supersession explicitly once a human has judged two records to conflict, and a record can carry its own superseded-by prose, so the store records a replacement judgment well and has no instrument that proposes one. Candidates: have the authoring verbs run the existing hybrid search over the incoming name plus description and print the nearest ranked neighbours above a threshold before writing, warning rather than refusing so a legitimately distinct record is not blocked; and have the decay scan flag live pairs above that same threshold as replacement candidates rather than reporting only supersessions already recorded. General lesson: where a store's retrieval is strong and its write path consults nothing, deduplication and scoping degrade into disciplines the author must remember, which is the class of duty that fails silently as a corpus grows, and the cheap fix is to spend the retrieval the store already has at the moment of writing rather than only at the moment of reading.
- 2026-09-04 | NEO-CLAUDE | claude-kit | The memory search's model-judged ranking block is dead against any endpoint speaking only the openai dialect, because its single call site drops the api key when handing a hand-built config to the endpoint library. The endpoint config declares api openai; the judged-ranking call passes url, model and timeoutMs and no api; the library resolves the dialect as the config's api where it names a known flavor and the default otherwise, so it falls to ollama, posts an untranslated ollama-shaped body to the ollama generate path, and an openai-only gateway answers 404. Receipts from one machine: a GET of the openai models path returns 200 listing exactly the configured model, a POST of a minimal body to the openai chat-completions path returns 200 with a real completion, a POST to the ollama generate path returns 404, and the ollama tags path returns the same 404 shape, so the endpoint, the model name and the openai route are all healthy and only the path the library chose is wrong. The failure is shaped to be unreadable: the probe GETs the bare base url and is dialect-blind, so it passes, and the block then reports the call refused with a bare HTTP 404 naming neither a dialect nor a path, which reads as a remote fault rather than a caller bug and degrades quietly on every search. This is the only call site of that function in the tree, so the api key is threaded nowhere and the openai dialect has no working caller at all. Candidates: pass the api through at that call site; have the library refuse a config carrying a url and a model but no api rather than silently defaulting, since a caller that supplies one key of the pair and not the other is a caller that forgot; and have the refusal name the dialect and path it tried, so a 404 distinguishes a wrong route from a dead endpoint. General lesson: a library resolving behaviour from an optional key with a silent default will be miscalled by exactly the caller that hand-builds its config object, and where that default is also the wrong wire protocol the symptom presents as the remote party's fault.
- 2026-09-04 | NEO-CLAUDE | claude-kit | The judgment sidecar judges a tool call's completeness against its own truncated copy of the output and bills the shortfall to the caller, without consulting the fidelity flag it recorded on the same row. Receipts from one call: a read of three memory records drew a diverged verdict reasoning that the third record's content was cut mid-sentence with 1701 characters missing and so was not read in full, while that call's own verdict-log row carries truncated true, and the subject record's file on disk is 3278 bytes ending exactly where the read ended, its final paragraph whole. So the caller read the whole record, the instrument saw part of it, and the report names the caller's route as the defect. The failure is indistinguishable from a real finding to any reader who cannot see the flag, which is every reader of the advisory block, since the block carries the reason and not the row. It also lands on the exact shape the instrument is most valuable for, an absence or incompleteness claim, so it spends the channel's credibility where that credibility is worth most. Candidates: consult the truncated flag before scoring any completeness or absence dimension, and either skip that dimension or state in the reason that the capture was cut rather than that the output was; or carry the flag into the advisory block so a reader can discount the item without opening the log. General lesson: an instrument that records a fidelity flag about its own input and then judges as though the input were complete converts its own limits into findings against the party it audits, and because the flag never travels with the finding, the audited party cannot tell that class from a true one.
- 2026-09-04 | NEO-CLAUDE | claude-kit | QUALIFIES THE MATCHED SIDECAR PAIR ABOVE with the measurement neither note could make, and changes what the remedy should be. Both notes argue about a threshold; the logs say the defect is the verdict vocabulary. Measured across all twelve per-session verdict logs on this machine: 12596 judged calls, 1459 diverged, an 11.6 percent divergence rate stable to within a point in every session, which retires the calibration claim that a seat draws a guaranteed false alert every pass. Of one session's 20 diverged this seat judges 18 fair, and of 12 sampled from the 222 verification-shaped diverged in another session, 12 fair, so the fair rate among alerts is far higher than the pair records and note 2's warning against suppressing the class is stronger than note 2 states. The first note's structural claim is partly upheld and wrongly framed: fleet-wide only 12 diverged verdicts sit on a duplicate-subject sweep and only four show the predicted shape, a sweep that ran correctly and found counts differing from the ones the caller stated inline. Those four are not false. The alert text is true and useful, telling a reader the board holds duplicates; what is wrong is that it carries the same label as a route defect. So diverged currently means two incompatible things, that a check did not establish what the caller claimed, and that a check worked while the world disagreed with the caller, and a reader cannot separate them without the reason prose. Candidate that keeps every true positive and removes the whole appearance of a false-positive class: split the verdict into two labels rather than reweighting, suppressing, or inverting anything. General lesson: where an oversight instrument files a true finding under a label that reads as the caller's fault, the resulting apparent false-positive rate is an artifact of vocabulary, and every remedy aimed at the rate will remove real coverage while leaving the vocabulary that produced the impression intact.
- 2026-09-04 | NEO-CLAUDE | claude-kit | Branch-and-PR states no rule for when a section-accumulating draft pull request flips ready, so an executing seat at plan completion reasoned from the repository arm-on-ready workflow that the flip was the merge and held two Complete plans pull requests in draft, which silently broke the operator review signal (ready means review me) while every gate read green and every plan read Complete. On a trunk whose ruleset requires a non-pusher approval, arming auto-merge merges nothing; the approval is the operator merge call. Candidates: state in the Branch-and-PR model that the finishing pass flips the PR ready and that arming is not merging where the target protection requires review; state the one exception, a PR based on an unprotected branch, which stays draft until retargeted, since an unprotected base has no gate and the flip merges into the parent branch. General lesson: a commit model that names an artifact opening state and not its terminal state leaves the terminal transition to a seat own risk reading, and the conservative reading (hold) fails silently against every gate while the operator signal is what breaks.
- 2026-09-04 | NEO-CLAUDE | claude-kit | A memory supersession has a second step that nothing at the first step names, so the contradicted record stays live and recallable: add-operator --supersedes writes the pointer and reports success, decay-scan then marks the predecessor archive, and only decay-prune retires it. Until that runs both records answer a recall and they disagree, which is worse than the stale one alone. Four records in this operator tier sit in that state, three of them superseded by different sessions, so the half-finished lifecycle is the normal outcome rather than one seat forgetting. Suggest the supersede path either archive the predecessor itself or print the owed prune command with the confirm flag already spelled.
- 2026-09-04 | NEO-CLAUDE | claude-kit | The sidecar intent-divergence verdict fires on a stated intent that names its TARGET with a verb-initial proper name, reading the target name as the action. A board write described as "Board batch 1: read protocol and roster" drew a divergence alert reasoning that a read-only intent had performed a write, when "Read protocol" and "Roster" are the two board headings being written and no read was claimed. The general shape: a check that parses free-text intent prose for the action will misread any description whose object is a document, section, file, or command whose own name begins with a verb (read protocol, run book, watch list, make target), and it misreads in the alerting direction, so the cost is a false positive on exactly the sessions that describe their targets precisely. Candidates: compare the stated intent against the tool call operation class the harness already knows (a Write or an Edit is a write whatever the prose says) rather than inferring the class from the prose; or discount an apparent verb that sits inside a capitalized or quoted name. General lesson: an intent checker that reads prose for verbs inherits every ambiguity of the naming conventions around it, so it needs a structural signal beside the prose before an alert is worth raising.
- 2026-09-04 | NEO-CLAUDE | Neuro-Evolution-Operations | A doctrine pointer spelled as a repo-relative path under "the kit plugin root" (skills/testing-discipline/SKILL.md and its siblings) resolves against the wrong root inside a dispatched subagent: two separate review rounds resolved it to a session additional-working-directory holding a partial kit checkout, found no testing-discipline directory there, and skipped that skill rules while reporting they had done so. The skipping was declared, which is the good case; the same resolution failure in an agent that does not narrate it is silent. The pointer form is the problem rather than either agent: a relative path is only meaningful beside the root it is relative to, and a subagent inherits the session working directories rather than the plugin cache path, so the one root the pointer means is the one root the agent does not have. Candidates: have the dispatch brief template carry the absolute plugin root as a named value the pointers resolve against; or make the doctrine pointers self-resolving (a skill name the Skill tool takes) rather than filesystem paths. General lesson: a cross-surface pointer written as a relative path silently retargets whenever the reader stands somewhere the writer did not, and the failure reads as an absent rule rather than as a broken pointer.
2026-09-05 | NEO-CLAUDE | Neuro-Evolution-Operations | A plan's Standing Brief Amendments are delivered by dispatch briefs and the section-open grep, so they reach implementers and inline sections but not the orchestrator's own hand edits made while adjudicating a review round (a declared-bonus fix, a fold). That is the least-guarded authoring site in the loop, and a review Critical landed on one. Executing-work's recurrence rule could name the adjudication-time edit as a delivery point too.
- 2026-09-06 | NEO-CLAUDE | claude-kit | The judgment sidecar's divergence verdicts rest on comparisons it performs over the tool output, and it performs some of them wrong in ways no threshold change reaches. Two in one session: it reported a deliberately-corrupted control copy as IDENTICAL when the printed line said DIFFERS, inverting the polarity of a safety check and so alarming on the one result that proved the check was working; and it judged a commit stamped 16:39:36-04:00 to fall BEFORE a 16:43Z cutoff, comparing a local wall reading against a UTC bound with the offset unapplied. Both arrived as confident, specific findings, which is what makes them expensive: a reader who trusts the verdict acts on an inverted or mis-timed reading, and a reader who learns not to trust it stops reading the true ones too. The remedy is arithmetic and polarity rather than sensitivity: normalize an offset-bearing instant before comparing it, and quote the output line a verdict rests on so its polarity can be checked without re-running the call.
- 2026-09-07 | NEO-CLAUDE | claude-kit | The seat takeover ritual arms the recurring wake as step 1, before the board read at step 2 and the store read at step 4, and the crash-safety reason for that order is sound. The cost is that a self-authored wake prompt is necessarily composed at the session's point of maximum ignorance of the machine it will wake on, so it can only carry the runbook's defaults. This pass armed a reconciliation cron naming the board at the runbook's default filename; this machine's board is at a different name the operator settled months ago, and the record of that settlement lives in the store the ritual does not read until step 4. The wake would have fired into a false path and read absence as a state. That an operator-tier record already documents this exact shape is what makes it structural rather than a slip: the ordering guarantees the error class, so it recurs per takeover regardless of who takes the seat. Candidate remedy: make the ritual's own last step a re-authoring of the wake it armed at step 1, now that the board, the store and the machine's facts are resolved, rather than leaving the first arm as the standing one. General lesson: where a procedure requires an agent to author a durable artifact before the reads that would inform it, the artifact's content is a property of the ordering rather than of the author's care, and no amount of diligence at authoring time removes it; the fix belongs at the ordering, as a revision pass once the facts are in hand.
- 2026-09-07 | NEO-CLAUDE | claude-kit | The coordinator board's location is operator-settable in practice but no kit instrument reads that setting, so every instrument hardcodes the runbook's default filename and reports a relocated board as ABSENT. On this machine the operator settled a non-default board filename months ago and recorded it as an operator-tier memory; five instruments have now reported that board absent while it sits on disk at 25 KB, including kit-registry-stamp.js audit, which printed the board absent in the same run that scanned sixteen registry entries beside it. The cost is not the wrong word in a report: absent is a live branch in the coordinator runbook, where a cold start finding no board holds the seat and reports to the operator, and a pass that believed any of the five would either stand the seat down over a healthy board or create a second board over a predecessor's commitments. Four separate sessions have now had to be corrected on this reading, so the round trip is paid per seat rather than once. Candidate remedies: have the instruments resolve the board through the same operator-tier record the seats do, rather than by filename; or make the location a field of the coordinator directory contract with one resolver every reader calls; or, at minimum, have an instrument that cannot resolve a board say it looked for one specific filename rather than asserting absence. General lesson: where a value is configurable by the operator but each reader hardcodes its default, absence is indistinguishable from relocation at every reader at once, and a report of absence is a statement about the reader's assumption rather than about the world; an instrument that asserts a negative should name the specific thing it looked for, so the reading can be falsified without knowing the instrument's internals.
- 2026-09-07 | NEO-CLAUDE | claude-kit | The doctrine tells a session to stop leftover processes before a build and never bounds the blast radius of doing so, and an implementer subagent read that as license for a machine-wide kill. The environment-discipline rules say a running app host or leftover testhost locks the DLLs and yields stale-binary false-greens, so stop it before every build, and they name testhost, dotnet and node by image name. An implementer that hit a hung test run reached for a kill by image name across the whole box. It took down MCP server children in five unrelated sessions, including the operator's own web session and BOTH relay children, which is the channel that reaches the operator's phone, so the blast radius included the only route by which the incident could have been reported to the operator while they were away. Every peer session survived; what died were their children, which is the failure mode that makes this cheap to cause and expensive to notice. The rule's own framing invites it: it names images to stop rather than saying whose processes are yours to stop, and a subagent has no roster, no claim file and no way to know which PIDs belong to a peer. Candidate remedies: state the stop rule in terms of the agent's OWN process tree and require scoping to PIDs it can attribute, with a machine-wide kill by image name named as barred; and treat a kill that reaches processes the actor did not spawn as an irreversible outward act under the stop-for-a-yes rule, since that is exactly what it is. General lesson: a rule phrased as which THINGS to act on rather than which of them are YOURS scales its blast radius with the machine's occupancy, so it is safe on a single-tenant box and destructive on a shared one, and the day it becomes shared nothing in the rule's text changes to say so.
