# Kaizen inbox: SCOTT-CLAUDE

- 2026-09-02 | SCOTT-CLAUDE | claude-kit | A CONTRACT THAT LICENSES AN IN-PLACE REWRITE OF A SOLE-RECORD SURFACE OWES A RULE BINDING THE WRITE'S COVERAGE TO THE READ'S, and the coordinator skill has none: `plugins/claude-kit/skills/coordinator/SKILL.md:85` licenses a pass to rewrite the board in place and `:91` licenses the homing cut, while nothing says the rewrite must cover what the read did not reach, so a pass that read only part of a grown board can drop commitment lines the board is the only record of. The obvious mitigation is foreclosed on this surface: the chassis requires a read protocol at the top of a ledger (`plugins/claude-kit/skills/standing-watch/SKILL.md:34`) so a partial read still reads what stops it doing damage, but the coordinator states the board carries no standing section at all (`:79`), leaving that inherited requirement with no home. Confirmed at both files; the sub-question of whether `:79` admits a read protocol at all is the part that needs an owner's ruling. Surfaced by two review lenses during the board-routing-and-homing plan's section 3 and attributed pre-existing by the security lens and by a consult, both on the diff: the section neither creates nor widens it, and the fix belongs on the write rule rather than in the readability rule that section edits.
- 2026-09-02 | SCOTT-CLAUDE | claude-kit | A DAEMON THAT LOADS ITS CONFIG AND CODE ONCE, UNDER A SUPERVISOR THAT NEVER REPLACES A LIVE PROCESS, OWES THE OPERATOR A RESTART COMMAND: the sidecar daemon (`sidecar/daemon.js:510`) reads `kit-endpoint.json` at startup only and Node holds `kit-endpoint-lib.js` in memory, while the task wrapper (`sidecar/daemon-task.ps1:213`) stands down on a live heartbeat and by design kills nothing. So an endpoint or config change (the llama-server move) leaves the running daemon on the old route, every call refused with a fresh heartbeat, and `Start-ScheduledTask` exits at once with "stood down", which reads to the operator as the daemon failing to start. There is no kit-native stop or restart; the working path today is `Stop-ScheduledTask` (Task Scheduler tears down the process tree) then `Start-ScheduledTask`, and nothing documents it. Wanted: a `daemon.js --restart` or `--stop` (signal or stop-file the watch loop honors), a wrapper line that says "to replace this daemon, stop the task first", and a config-mtime check in the watch loop so a config edit takes effect without a kill. Confirmed at both files and in the daemon log for pid 6032 on this date.
  - Refinement, same date: the Task Scheduler GUI "End" flips the task to Ready without reliably killing the tree. Evidence: manual runs at 19:18:04Z and 19:18:21Z and the 5-minute tick at 19:20:01Z all executed the wrapper, which under MultipleInstances=IgnoreNew means the scheduler held the task as not running, yet the wrapper pwsh (pid 4856) and its node child (pid 6032), both from 09:33 local, were alive at 19:19:45Z. The wrapper launches node as `& $node $daemonJs *>> $logFile`, so node is a plain child with no job-object tie the GUI End is guaranteed to reach. Any kit restart path must kill by command-line match (`sidecar[\/]daemon\.js`) rather than trust the task stop, and the Task Scheduler operational log is disabled on this box, so nothing records what End actually did.
- 2026-09-02 | SCOTT-CLAUDE | claude-kit | A PROTOCOL WHOSE READ IS MEANT TO GATE A WRITE OWES AN EXPLICIT BAR ON CHAINING THE TWO INTO ONE COMMAND, and the machine's one-slot heavy-process claim protocol has none. The doctrine states that bar for git staging, where the staged-list read is required to be its own step because a chained pipeline commits before anyone has looked at what the read returned. The claim protocol in the role skill has the identical shape, a session reading the claim file to see whether the slot is free and then writing its own claim, and states no such bar, so a pre-spawn step that chains the two passes every rule as written and silently overwrites a live peer's claim. Observed live on this box: a peer session's chained read-and-write replaced this seat's claim for about a minute while both a JS suite and a .NET lane ran against the same window. Two further gaps ride along and are worth the same pass. The protocol reads a claim's age from the file's modification time, so a restoration write by whoever overwrote it resets that clock and makes the claim read younger than it is, which is how a claim near its expected end gets reaped as stale. And nothing in the protocol says what a session owes when it discovers it overwrote a live claim, so the restore-and-notify this peer performed was invented on the spot rather than followed.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: a writer-facing template shows a placeholder the machine reader does not accept, and the pointer to the frozen shape leads to a document that never states it. The executing-work Chapter template reads `Completed: <section name>`, and its note sends the writer to the curating-docs machine-contract section for which values register, which carries no Completed text at all; the only statement of the two registering forms (bare number, or the heading tail exactly, model tag included) is the comment block in the status-line widget. A run that follows the template literally writes `Completed: section 1, the title` on every Chapter and the widget reads 0/N on a plan with three sections closed (recognition-reach, 2026-09-03). Code batch s9 makes the miss loud at the stop; the template and the pointer still teach the miss. Direction: put the two registering forms in the template itself, and make the pointer land on text that states them.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: a compaction-gate allow can consume a chapter checkpoint without a compaction landing, leaving the run back in deferral with nothing to say so. Gate journal for the worker session 9d5f5037: allow/checkpoint at 07:54:20Z (checkpoint age 133s, pendingOffer true, corroborated), no compaction summary in the transcript until 08:50:46Z, and the consumed reading at 08:39Z still 543K, so the 07:54 allow spent the checkpoint and compacted nothing; the run then sat mid-chapter at 560K until the next chapter close opened another at 08:48Z, which did compact. Sixteen allows against fourteen compaction summaries over the session. Lesson: consumption of a single-shot token has to be keyed on the act it licenses actually happening, or the token must be re-issued when the act does not follow; a gate that spends the token at the offer trusts the harness to finish what it offered. Direction: read the transcript after an allow for the summary marker and restore the checkpoint if none follows within the offer window, or journal the miss loudly.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: the manual role-boundary declaration is stated as a moment that lapses at the next turn, and the gate is stated as offering only inside a turn, but no surface joins the two into the operating rule: a declared boundary is spent only by an offer arriving before the declaring turn ends, so the verb is never a turn's last act (declare, then take at least one more tool round in the same turn), and it is worth declaring only once context is already past the harness's trigger, since below it no offer exists to catch. Evidence: three declarations by the expert seat followed by a further tool round each landed within two minutes (gate journal allows 2026-09-02T14:39Z, 20:26Z, 2026-09-03T08:58Z); one declared below the trigger as the turn's last act (2026-09-03T00:19Z) lapsed unspent. Direction: state the rule in the peer-sessions banking paragraph, the owner, with the coordinator runbook and the CLI's boundary line pointing at it.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: a skill states a usage-accounting fact it cannot verify from any surface it holds (brainstorming tier text: Fable rides the plan allotment rather than being metered separately), and the operator reports it is half right: Fable is not billed separately but is metered separately, on its own weekly limit that runs out independently of the session and weekly limits; so a tier assignment is a meter-balancing decision after all, and the text should state the meter and its balancing rule rather than deny one, with the numbers themselves kept off the artifact.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: the doctrine's found-work bullet (do it now if quick, spin it to a subagent, take the cheap adjacent win as a bonus) is contradicted by a harness-injected task-scope block that ranks above it (found work is a suggestion at the end, never a change in this delivery); under the doctrine's own ranking the lower surface is the defect, so the bullet needs restating to route found work to a handoff or follow-up rather than into the current change.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: a section acceptance condition can require a dataset (dispositioned per record, with its decision) while the Chapter template has no slot for one, so the data has nowhere to land and evaporates at the section close; one plan produced three such datasets and its close-out had to re-derive two from the live store and record the third as unrecoverable, because the document that demanded them never held them.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: the judgment sidecar attributes a dispatched subagent's tool calls to the parent session, so an orchestrator that dispatches gets advisory items about commands it never ran and cannot verify from its own transcript, and the pointer it offers (check the call id in the sidecar log) resolves to the subagent's call rather than to anything the parent did; an advisory keyed on a session should be bounded to that session's own calls, or state whose call it is describing.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: the judgment sidecar reads an absence check with a positive control backwards, reporting zero hits in the subject beside one hit in the control as a contradiction of the stated intent when that is exactly the passing shape the doctrine's silent-check rule mandates; a judge that flags the correct result of the kit's own standing verification pattern trains a reader to discard its items wholesale.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: the executing-work Dispatch Brief template has a field requiring every load-bearing assertion to be marked confirmed, inferred or reported, and no field covering the argument a brief supplies for why a rule is right; so an orchestrator's own reasoning rides into shipped text as required content wearing no epistemic mark, the implementer transcribes it as compliance, and it surfaces only at review, three times in one section here before a plan-local amendment caught it. A cost or consequence clause is the shape that escapes hardest, because it reads as motivation rather than as an assertion about the file's own rules.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: the review loop has no exit condition for a section whose deliverable is claims (skill prose, contract text): rounds keep finding new seams with unchanged neighbours at a declining but non-zero yield, the tier ladder and the consult trigger both fire and neither ends the loop, and a run reached fifteen rounds and two consults on one section before deriving its own condition (a Major is the section's to fix only where it sits in the delta and contradicts an acceptance clause or principle; anything else is a file-audit finding routed to the backlog). The claims-sections operator memory already states rounds do not converge on this class; executing-work step 4 should carry the exit condition rather than each plan re-deriving it.
- 2026-09-03, SCOTT-CLAUDE, claude-kit: finishing-work puts the unstamped memory sweep in step 7, after the step 6 commit, while step 5 writes a final Chapter carrying a Stamps line whose count only that sweep produces, so a pass following the written order either pre-writes a figure it has not measured, against the doctrine bullet forbidding exactly that, or reorders the step with no rule saying it may.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: an audit record for a machine-wide actor is placed in a project-scoped store, so the account of an irreversible act can land where no later reader resolves, and the surface that owns where a record lives states no location rule for an actor with no project. The coordinator seat is one per machine and has no home project; the record of a homing round, which publishes board content outward and then cuts it irreversibly, is two entries in the memq outcome journal, which resolves per call through the ladder the memory-system skill owns, a store pin, a worktree fold and the session's own transcript filing all sitting ahead of any working-directory derivation (`plugins/claude-kit/skills/memory-system/SKILL.md:8`, and `projectSegment` in `plugins/claude-kit/scripts/memq.js`), so the store a round logs to is whichever that ladder answers with and never a fixed one. The readers exist and are reached by key or by digest (`memq get` filters on exact key equality at `memq.js:7115`; `memq recall` is read whole at every seat takeover per `plugins/claude-kit/skills/role/SKILL.md:73`), but each reads only the store its own directory resolves, so a round logging from one checkout is invisible to a takeover standing in another. Two attempts to fix it from the reading side both failed on surfaces the fixer does not own: a read-back inside the round needs a machine-stable key, and the owning skill states a dot-namespaced key grammar (its Action keys section) while naming no machine component in it, so the component would be a rule this plan wrote on that surface own behalf, and naming a home checkout resolves a store location out of a machine-local signpost. Direction: the memory-system skill, which owns both the journal key grammar and the store resolution, states either a machine-scoped journal home or a stable key for a machine-wide actor, so an audit that is written can also be found.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: a runbook orders a record written before an irreversible act and states that the record therefore exists, while requiring confirmation only of the later and larger write, so the audit is best-effort at exactly the entry it exists for. The coordinator skill homing order writes an intent entry to the memq outcome journal first, on the stated ground that a round dying partway leaves a record of what it was doing, then writes the destinations, then rewrites the board irreversibly with no pointer and no tombstone. The only landing it requires a round to confirm is the destination write. The journal write can fail without writing: `memq log` refuses a working directory naming a network share and says in terms that nothing was logged, and its append has a catch that reports a write failure, both setting a non-zero exit (`plugins/claude-kit/scripts/memq.js`, the cmdLog refusal and append paths). No sentence in the coordinator skill mentions a success line, and none ever has in its committed history, so this is a gap rather than a regression. Direction, one level up from the incident: where a procedure orders an artifact written so that a later reader can account for an interrupted run, the step that writes it owes a confirmation of its own landing, read off the command that wrote it, before the irreversible step begins.
- 2026-09-04, SCOTT-CLAUDE, ai-os: a write guard that also screens shell commands screens them by redirect shape, so an interpreter invoked inline writes straight past it. `hooks/docs-write-guard.js` denies Write, Edit and MultiEdit by file path and denies a Bash `>`, `>>`, `tee` or heredoc landing under `docs/`, and its own header states that pairing as its coverage. A subagent whose role the guard refuses the Edit tool reached the same files with an inline `python -c` string replacement, which is neither a redirect nor a named write tool, and the guard allowed it. The guard documents itself as exiting 0 when uncertain and denying only when certain, so this is a gap in what the shell screen can see rather than a deny that broke. Direction, one level up from the incident: a guard that screens a shell command for an effect rather than for a tool name can enumerate only the writers it already knows, and an interpreter taking a one-line script is always a writer it does not, so that leg is worth stating as advisory and the load-bearing control is the tool-name deny plus withholding shell from the role, rather than a shape match over command text that reads as though it closed the path.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: finishing-work's base-ref derivation anchors the Chapter walk on the added line `^\+### Chapter `, and a document that ships its own Chapter template as literal body text matches that anchor at the commit that authored the document rather than at the commit that closed its first section. The rule already anticipates one instance of this shape, stating that the sentence declaring the rule contains the bare string and so requiring the added-line form; the template case defeats that fix, because the template is an added line. Here the walk selected the spec-authoring commit, whose parent sits behind an entire sibling effort, and the base ref is the input that sets the reviewers' scope and step 4's selection listing together, so a wrong one widens both with no error to show for it. Anchoring on `^\+### Chapter [0-9]` selects a real Chapter, the template's own `N` placeholder being what separates them. Direction, one level up from the incident: a walk that identifies a document's instances by matching its own template is defeated by any document that ships that template, so the anchor belongs to the shape only a filled instance can take, and a rule stating a pattern for finding instances of its own format owes that distinction rather than one carve-out per way the format can appear verbatim.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: the judgment sidecar evaluates a call's stated intent against output the call cannot have produced yet, so every dispatch whose result is asynchronous by design draws a divergence item. A backgrounded run returns only a task id, and the sidecar reports that the command produced no build or test output and so may not have run; the same shape fires on a backgrounded wait loop, which is silent until its condition is met, and it fired four times in one finishing pass, each time on a healthy run that later completed at exit 0. The judge is reading the launcher's return as the run's result, which is the exact confusion the doctrine's own background-marker bullet exists to stop a session making, so the instrument contradicts a rule the kit teaches. Direction, one level up from the incident: a judge that scores intent against a call's immediate output owes a notion of calls whose output is not yet due, and should either withhold a verdict on a dispatch that returns a handle rather than a result, or evaluate it against the artifact the run was told to write rather than against its stdout.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: the compaction checkpoint's `boundary` and `status` verbs disagreed within a minute of each other, with no compaction landing between them, so a seat cannot tell from a status read whether the boundary it just declared is live. Right after a compaction landed, `kit-compact-checkpoint.js boundary` reported the role-boundary marker open for this session, and `status` run about a minute later reported no role-boundary marker open beside a gate decision of allow (role-boundary) dated before the declaration; a second `boundary` followed by `status` in the same command agreed with each other and named the marker's write moment. Nothing on either surface said what retired the first marker, so the reading is unexplained rather than wrong. Direction, one level up from the incident: a status verb that reports a marker absent owes the reason it is absent where a retirement path exists (consumed by a landing, aged out, swept at a turn boundary), because absent-with-no-reason reads identically to never-declared, and the seat that declared it a minute ago has no way to act on the difference.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: the read-only agent guard admitted a review agent's creation of a file inside the tree under review and denied that same agent's removal of it, so a lens barred from changing the tree is nonetheless able to litter it and unable to tidy up, and the orchestrator learns of the residue only because the agent chose to report it. A blind-reviewer dispatched over a section wrote a scratch listing to `.kit-tmp-budget.txt` at the repository top level, then reported that its own `rm` was refused; the file is untracked and `git check-ignore` does not match it, so a later blanket add would have committed a review artifact, and the round's own tree-state bracket is what has to catch it rather than the guard. The asymmetry is the finding rather than either half: denying the delete is defensible on its own terms, a delete inside a reviewed tree being the more destructive act, but pairing that deny with an admitted create produces the one outcome neither rule wants. Direction, one level up from the incident: where a guard screens an agent class out of mutating a tree, the screen belongs to the whole mutation rather than to its destructive half, since admitting a create while denying its cleanup converts a would-be refusal into permanent residue; and where a create is deliberately admitted, the allowed region and the deletable region are the same region, so an agent can always undo what it was permitted to do.
- 2026-09-04, SCOTT-CLAUDE, claude-kit: the memory store's sync script exits 0 whether it synced or jammed, so the caller that could act on a failure cannot see one. Measured twice in one pass on the same box: a run whose rebase halted on a merge conflict exited 0 and printed nothing, recording `lastResult: transient` with `reason: pull-conflict` in `kit-sync-state.json`, and a later run that fully succeeded exited 0 and printed nothing either, recording `lastResult: ok`. The two are indistinguishable to any caller reading an exit status or output, which is the reading the doctrine tells every session to take, and the background session-start spawn discards output entirely, so a store that has stopped reaching its remote fails silently until someone reads branch divergence by hand; here it had been jammed for twenty and a half hours across a day in which the coordinator board was rewritten five times and reached no other machine. The jam itself is ordinary and expected, two machines appending to one memory record body, which carries no union merge attribute by design. Direction, one level up from the incident: where a script's outcome is something a caller must act on, that outcome belongs in the exit status, and a state file is a record beside it rather than the only channel; a run that ends in a state its own state file names a failure should not exit 0, and a background spawn that discards output makes the exit status the only channel there is.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: the registry stamp self-check reads every timestamp in the coordinator board as an evidence time and flags any that sits ahead of the clock, but a coordination ledger has to carry declared future bounds as well as evidence times, so the check reports findings that are correct by construction and cannot be cleared. Measured this pass: a board carrying a probe window's close and a claim's honoured bound, both deliberately in the future and both re-derivable from the protocol, produced two `sits N minutes ahead of the clock` findings under the same `stamp findings` heading as the whole-second and leads-heartbeat readings that do indicate a composed stamp. The two classes are opposite in meaning, one being a value the artifact is supposed to hold and the other a sign it was not read from a clock, and nothing in the output separates them. The cost is not the noise: it is that a recurring finding nobody can clear teaches every later reader to skim the section that would also carry a real one. Direction, one level up from the incident: where a checker reads a field class that legitimately contains both measurements and declarations, the check distinguishes them before reporting, by scoping the against-the-clock reading to the fields that are evidence times or by having the artifact mark a declared bound as such; and where it cannot distinguish them, it reports the two classes under separate headings rather than merging a by-construction finding into a list of suspicion indicators.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: the goal CLI can extend an armed queue only at its end, so an operator's placement instruction (run this plan third) is met by a hand edit of the goal state that bypasses the arm path's validation and that the CLI has no supported way to recover from if the write is malformed. Measured this pass: the operator asked on the relay thread that a Ready plan run behind the second plan of a fifteen-plan queue bound to another session; `arm --append` could only add it last, a bare `arm` would have replaced the queue and rebound the leash to the arming session, and no verb inserts, moves or removes a single entry. The edit that met the ask reused the library's exported reader and condition composer and mirrored its unexported writer, so it came out byte-equivalent to a CLI write, but nothing validated the entries it moved, and the failure mode is exact: a state the CLI cannot parse makes `--append` refuse while deliberately naming no command, because the bare form would replace a live leash, so a malformed hand write leaves an armed run with no supported way forward. Direction, one level up from the incident: where a CLI owns a durable state that a session other than its writer depends on, every edit an operator can reasonably ask for is a verb of that CLI, with the same validation and the same re-read-before-write the existing verbs take, and a hand edit of that state is a gap in the verb set rather than a workaround; here that is an insert-or-move verb taking a plan path and the path it should follow, refusing any entry the current index has already passed.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: the judgment sidecar judges a tool call against its stated intent one call at a time, so a lookup step that exists to produce coordinates for the next call is reported as a divergence for not being the edit it precedes. Measured twice in one pass on the same shape: a `grep -n` run to find a block's current line numbers, and a `sed -n` run to narrow a staged replacement text, each drew a `stated intent diverged` alert reasoning that the action only inspected the file rather than modifying it. Both calls did exactly what their intent described; what diverged was the sidecar's reading of an English verb, and no read-then-write procedure can satisfy a check that expects the described change to be present in the same call that located where to make it. The cost is not the noise: a channel meant to surface a real divergence teaches its reader to dismiss it, which is the same failure a by-construction finding produces in any other check. Direction, one level up from the incident: where a checker judges an action against a natural-language intent, the unit of judgment is the smallest span in which that intent could have been satisfied rather than the single call, since multi-call procedures make a read-then-write pair the real unit; and where the checker cannot see that span, it reports the lookup call as unjudged rather than as diverged, because a verdict a reader must routinely overrule is worse than no verdict.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: the memory store's sync script does not always write its state file on a successful run, so a run that repairs the store leaves the previous run's failure recorded and a watcher keyed on that record alarms forever on a healthy store. Measured this pass: a first run recorded `transient` with reason `unproven` and set `firstFailSince`, leaving the store one ahead and two behind; a second run took it to level with the remote, confirmed by comparing the remote's own head and by a normalized blob hash against a control that spoke, and the state file's own mtime still read the first run's time, so the successful run wrote nothing. This is the same defect as the exit-status one from the other side: there the file was the only channel and the status lied, here the status is silent and the file is stale, and both leave a caller unable to read the outcome from anything the script produces. The reason code compounds it, `unproven` naming about a dozen distinct sites in that script as a deliberate silent-retry state rather than a diagnosis, so it cannot tell a reader which check fired. Direction, one level up from the incident: a run that changes the outcome writes the outcome, success included, because a state file read as a health signal has to be a record of the last run rather than of the last run that happened to write; and where one reason code covers many sites, it carries the site with it or the record cannot support the retry-or-escalate decision it exists for.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: memq refuses a malformed invocation by printing its whole usage block and naming nothing it rejected, so a caller passing several flags cannot tell which one failed and bisects across attempts; one add-operator call took three tries whose refusal reason is still unknown, the successful one differing from the refused one only in dropping a second tag flag and shortening the description.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: the executing-work dispatch brief binds an implementer to a standing absence-check clause, with a control and a stated predicate behind any check whose acceptance is an absence, and neither reviewer template carries that clause, so a dispatched judgment agent reports its own clean sweep with nothing establishing that the sweep could have spoken. Measured this pass: a per-section review round reported NO_UNICODE_DASHES after its own grep had failed with `character value in \x{} or \o{} is too large`, a false negative from a broken command that reads exactly like a clean result, and the orchestrator would have credited it had the error text not happened to surface beside it. The asymmetry is the point rather than the one lens: an implementer's unproven silence is caught by the reviewers, while a reviewer's unproven silence is the last check there is, so the clause is owed more at the reviewer's dispatch than at the implementer's.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: the heavy-process claim protocol makes one field, `Expected-seconds`, do two jobs it cannot both do: it is an estimate written before the run starts, and it is also the claim's whole lifetime, with no verb for a holder to revise it; a run that outlives its own estimate is still holding the box while the board reads it as aged, and the next session to check is correct to proceed. Measured this pass on SCOTT-CLAUDE: a dispatched implementer's claim aged past its stated 900 seconds while the implementer was still working, another repo's gate read it as stale at 912 seconds and proceeded to a Release build plus a full suite, and the displaced run was a node lane plus in-process agent fan-out that the proceeding session's dotnet-filtered poll could not have seen. The gap is the protocol's rather than either session's: both parties followed the rule as written. The general defect is one field doing two jobs, and it fails in both directions: an estimate that runs short holds the box while the board reads the claim aged, which is the case measured here, and an estimate that runs long is indistinguishable from a long run, so every waiting session waits out the number instead of the work with nothing surfacing that the box is already free. Reported from the AI-OS: Worker seat on this machine, which hit the second direction against this session's own 5400-second claim and held a runner open behind it; not measured from here. The two need different remedies, which is why the pair matters rather than either alone: a renewal verb fixes the short case and only a release-on-finish discipline fixes the long one, so reading a claim's liveness from its holder rather than from an estimate the holder cannot revise is the change that answers both.
- 2026-09-05, SCOTT-CLAUDE, claude-kit: no durable artifact records which model a seat ran, so the fleet cannot answer any later question about the executor, including whether a model change helped. The registry entry shape in `skills/role/SKILL.md` carries name, role, repo, workdir, session and four stamps with no model field, and none of this machine's twenty-one entries carries one; the goal state's history records plan, outcome and archive time only; a plan section's `Model:` tier is a dispatch instruction rather than a record of what executed. Measured when one seat's model was switched mid-queue and the only baseline available for the comparison turned out to be commit timestamps in the repo, which conflate the executor with the plan's own difficulty and with how many sections it happened to have. The general defect is an execution record that omits the executor: it cannot support the questions people actually ask of it afterward, and the omission is invisible until someone asks. Direction: write the seat's model into the registry entry beside its role at takeover, and into the goal state's history entry beside the outcome at archive, so the attribution is recorded rather than reconstructed.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: the heavy-process claim contract states its five field names only inside a very long prose paragraph and ships no template, so a writer extracts the schema by reading the rule that consumes it and the claims that result carry the right values under the wrong keys. Measured this pass on SCOTT-CLAUDE: a live, honest claim carried `Session:`, `Seat:`, `Claimed:`, `For:` and `Expected:` where `skills/role/SKILL.md` defines `Name:`, `Repo:`, `Session:`, `Started:` and `Expected-seconds:`, and this is not version skew, the pinned contract and the trunk's being the same bytes. The claim was substantively complete, its self-written stamp agreeing with the file's own modification time to the second and its seat resolving to a live roster row, so nothing was blocked; what fails is every mechanical reading the contract itself prescribes, because the readings keyed on `Expected-seconds:` and `Started:` find no such field and the absent-field dispositions then fire on an honest claim, reporting it as an untracked or over-bound hold. The general defect: a schema that exists only as prose inside the rule that consumes it gets satisfied in substance and missed in form, and the contract's own absent-field branches are what turn that near-miss into a false verdict rather than a shrug. Direction: ship the claim file as a copyable template beside the prose, and have the reader resolve the values it can rather than treating a renamed key as an absent field.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: kaizen capture tells every session to append to a shared file inside a working repo, so a note written while another session is mid-work sits in that session's dirty tree and rides into whatever it commits next, under a message about something else. Measured this pass on SCOTT-CLAUDE: two notes by two authors sat uncommitted on one inbox file and both landed in a commit whose subject is an unrelated feature, alongside seven other files; the content survived and the attribution did not, so the inbox's own history no longer says when either friction was captured or by whom, which is the one thing a dated append exists to record. The capture rule's push exemption assumes the note commit is the whole branch delta, which holds only when the capturing session owns the tree, and the doctrine's staged-list guard fires at the committing session rather than at the appending one, so neither protection reaches this case. The general defect: a capture surface placed inside someone else's working tree makes the capturer's record depend on a third party's commit discipline, and the failure is silent because the text is all still there. Direction: either give capture its own commit at append time, or move the inbox out of any working tree so an append cannot be swept.
- 2026-09-06, SCOTT-CLAUDE, ai-os: the executing-work and finishing-work dispatch briefs let an orchestrator carry a prior round's adjudication into a later reviewer's brief as settled fact, and neither skill warns that doing so removes the lens rather than saving its budget. A brief saying a finding was raised and rejected, and that disagreeing needs a new fact rather than the same argument, forecloses exactly the re-derivation that would catch the rejection being wrong, and the reviewer's resulting agreement reads like independent corroboration while being a restatement of what it was handed. Measured this pass on SCOTT-CLAUDE: three fresh-context lenses read one changeset, two carried that sentence about a member the plan had kept as a deliberate divergence and both confirmed it correct, while the third was handed the same item as a criterion to check with no verdict attached, built a probe project, ran the member and disproved the premise in one step; the member was a byte-equivalent alias and the reviewer whose finding had been rejected was right. The same shape fired again inside the fix round, where an implementer asked to check a class summary was pointed at a member's own doc comment and inherited that comment's false claim rather than reading the declaration the comment describes. The general defect: handing an agent a conclusion where its task is to test that conclusion converts an independent check into a restatement, and it fails silently because the report looks like corroboration rather than like an unrun check. Direction: state a prior adjudication in a brief as a criterion to re-derive with its evidence path named, never as a settled verdict, and where a finding genuinely must be foreclosed, foreclose it in the orchestrator's own triage of the returned findings rather than inside the lens's brief.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: a built-in Explore scout wrote four grep spools to the repository root through shell redirection during a read-only extraction, so the readonly-agent-guard's scope (kit agent types on Bash and PowerShell) leaves the built-in scouts executing-work bands free to write, and the tree-state bracket is the only thing that catches it; direction: state the gap where executing-work bands a scout, and either extend the guard to the built-in types or have the brief name the scratch path.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: the heavy-process claim is written once and carries no verb to revise any part of it, so its purpose field goes stale exactly as its duration field does, and an arbitrator reads a live-looking description of work that has already ended. Measured first-hand this pass on SCOTT-CLAUDE: a claim taken for one plan's finishing pass, declaring about fifteen to twenty minutes, ran 3h58m44s, and the plan it named completed 2h50m before the claim was released, the completion read off the event stream and corroborated by that queue's own history entry. The holder had gone on to the next plan and kept the slot, and that repo's tree took commits until thirteen minutes before the release, so the box was plausibly busy throughout and the hold itself was defensible; what was wrong was the record. That is the harder failure to catch, because a stale claim that happens to be producing the right arbitration answer gives no signal that it is stale, where an idle hold eventually shows up as a free box. The protocol's own release path never fired and missed by 21 minutes: a declared duration is honoured to one full cadence past its end, which put the bound 21m16s after the release, so the quiet came from the holder's own discipline rather than from the protocol working. The general defect: an artifact that states both what a shared resource is being used for and how long it will be held, with no way to amend either, is a snapshot published as live state, and its two fields go stale independently and silently. Direction: give the claim one renewal verb that revises purpose and duration together, and state that a claim carried past the work it names is released and re-taken rather than silently extended.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: a dispatch brief that names a runner flag without its position lets an implementer put the pattern flag after the path, where node --test reads it as a file; the executing-work brief template could carry the invocation verbatim (flag before path) for the repo's runner.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: a gate chain longer than the Bash tool's 600 s cap has to be backgrounded with its own exit marker, and no kit surface says so at the point finishing-work sends a session to run the whole gate; the testing-discipline lane mechanics could name the cap and the marker shape.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: an editing agent's Edit writes a file back in the checkout's autocrlf form, so a close-out script guarding on LF refuses a curated doc the curator just touched; the docs-curator charter or curating-docs could state that line endings follow the checkout, and scripts should match on normalized text.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: a one-clause carve-out folded into a skill sitting at its word cap costs a trim in that skill's own neighbouring paragraphs for every round that touches it, eight rounds here; the size ratchet could carry a small per-fold allowance, or writing-skills could state the trim-in-touched-sentences rule so the cost is paid once, deliberately.
- 2026-09-06, SCOTT-CLAUDE, claude-kit: a dispatched subagent compacts under its parent session id, so its auto-compaction spends the leash holder open chapter checkpoint and the parent lands at the safety valve; the gate reads no agent key off the PreCompact payload, and the checkpoint discipline has no subagent leg
- 2026-09-06 (Worker, checkpoint-session-validation finishing): a skill clause that restates a CLI remedy line dropped the line's bound and drew a security Major at finishing; the writing-skills or executing-work skill could state that a restated remedy carries the same condition and negative as its source, or the parity pin pattern could cover remedy sentences a skill mirrors from a hook.
- 2026-09-07 (Worker, scenario-probes section 2): the Bash tool collapses backslash escapes inside a heredoc, so a test fixture written that way carried a real NUL where the source spelled an escape; the kit could name the Write tool as the only route for source that carries escapes, and a brief could carry that line beside its build and test commands.
- 2026-09-07 (Worker, scenario-probes sections 3 and 5): a sonnet-tier prose section drew thirteen opus review rounds, each partly reversing the last on the same three passages, with nothing in the kit bounding a review seesaw; executing-work could state a round cap for a prose section after which the surviving findings go to the Chapter rather than to another rewrite, or a consult on the passage's framing could be the mandated exit.
- 2026-09-07 (Expert, queue dequeue on the main checkout): the doctrine's commit-then-push guard reads the landed commit's own file list, but on a shared checkout a push carries every commit from origin to the branch tip, so a peer's commit landed beneath yours between your check and your push rides to origin unread (a held plan-doc interim commit here); the scope-and-safety bullet could name the pre-push read as the origin-to-tip log rather than the HEAD commit, taken immediately before the push and not chained into it.
- 2026-09-07 (Worker, output-channel eliding finishing): finishing-work says a fix round whose delta touches the guarded boundary earns another review round, but names no stopping rule for the loop, so a session decides for itself whether a round that returns Minors alone ends it; the skill could state the terminal condition (a round with no Major over the latest delta, Minors fixed inline or declined and recorded) so two sessions stop at the same place.
- 2026-09-08, SCOTT-CLAUDE, claude-kit: a section whose deliverable is a rule owes the consult at trigger (a)'s second failed round rather than at the sixth, since every seam in rule text states a reachable input and so passes a class-keyed exit as owed; executing-work step 4 or the consult skill could name rule-text sections as the shape where the second round is the floor and not a suggestion.
- 2026-09-08, SCOTT-CLAUDE, claude-kit: finishing-work's drift table routes a curator mistake to a BLOCKED with no carve-out for a doc-only correction that widens no authorization (a doctrine list that a section made incomplete), so a one-word fix in a pinned copy either costs an operator round-trip or is declared a deviation from the stop rule; the table needs a low-blast row: a mistake whose fix is a pointer or an enumeration repair in prose, changing no grant, is taken and recorded as a deviation with its reversal.
- 2026-09-08, SCOTT-CLAUDE, claude-kit: the curating-docs machine contract describes the section model line as "the first one following a section heading" while every plan in the tree, executed ones included, carries `Model: <tier>` on the heading line itself and no kit hook parses the line at all, so a fresh reviewer reading the contract literally reports every plan as silently dispatching at sonnet; the contract row could state the heading-line form it admits, or name the consumer that reads it, so a contract with no known reader stops reading as a defect in every plan that follows the convention.
- 2026-09-08, SCOTT-CLAUDE, claude-kit: a whole gate run from a git worktree under .kit/scratch carries two reds the main checkout does not, the memory-index loader's relative-cwd refusal (test/kit-sidecar-memory-index.test.js) and memq's cross-store hit-line composer (test/memq.test.js), because project resolution follows the worktree's .git pointer to the main repository and both tests assume the process sits in a primary checkout; the suite-baseline memory names one standing fail and says nothing about the worktree case, so a seat gating in a scratch worktree reads two collateral reds as unexplained and spends a control run to clear its own delta. Evidence: both red solo in a fresh worktree at beb9ecd with no edits, green solo in D:/claude-kit, 2026-09-08. Candidate: the two tests pin their fixture cwd absolutely or resolve the worktree's own top level, and the suite-baseline memory names the worktree case until they do.
- 2026-09-09, SCOTT-CLAUDE, claude-kit: the rule that a security Major is fixed or raised and never routed says nothing about where the fix lives, so a section absorbs whatever the finding's fix turns out to need, and the section text never grows a bullet the fix can be reviewed against; the sync machine-scope plan's section 2, specified as three sonnet-tier tests, took a cmd.exe wrapper hardening in round 3 and then ran rounds 5 through 11 on a 1,672-line test harness that no section text describes, every round judging it against cmd.exe itself rather than against a spec. Candidate: the rule names the "raised" leg as the default when the fix needs an instrument the section's own text does not ask for, raising it as a new section at its own tier with its own acceptance line, and the "fixed" leg as the case where the fix is a delta the section's existing acceptance already covers; the review-loop-provenance plan's design stop is the mechanism that would have made the fork visible at round 3.
