# Kaizen brief: dispatch briefs name the pin tests a cross-cutting addition must update

Friction: when a dispatched section adds a member to a counted cross-cutting set (a role grant, a hosted worker), the brief omitting the exact-count pin test means the section lands red and only the finishing-work full suite catches it late. Evidence: a backlog-prep session's Section 1 brief omitted the DevAgentPrincipalGrant 7-to-11 pin update; the implementer caught it via deployment-parity Principle 4, not the brief. The orchestrator knows the addition is cross-cutting at brief-writing time, so the omission is a brief-template gap, not an implementer failure.

Change: executing-work step 1 (the dispatch-brief contents list) gains the pin-test line: when the section adds or removes a member of a counted cross-cutting set, name the exact-count or exact-set pin tests that enumerate it and the new expected values. The doctrine's implementer-briefing bullet (Orchestrating fan-out work, the "Subagents inherit the catalog" bullet) gains the matching sentence, mirrored into operating-instructions per the doctrine sync. Same placement precedent as the sibling-breadth line from fable-metering v1.

Acceptance: executing-work's brief list and both doctrine copies carry the pin-test wording; `diff` of the doctrine body against the skill body is clean.

Discipline: follow writing-skills; baseline-test any behavior-shaping wording.
