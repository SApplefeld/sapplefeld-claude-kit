# A Major enters a fix round on its provenance, and a goal-versus-output judge sits on demand and at finishing

Status: In Progress
Commit Model: Commit-and-Push
Created: 2026-09-08

Session model: any executor session in the kit repo; seven sections in order, since section 1 mints the charter every later section routes through. Authored by the KIT: Expert seat on the operator's relay rulings of 2026-09-08, taken after a NEO-CLAUDE worker's diagnosis of a twenty-round section, and on the operator's keyboard rulings the same day at that machine, recorded in `kaizen/notes-NEO-CLAUDE.md` under the 2026-09-08 date. Anchors are authoring-time and named by passage; re-locate every hit by content. Every plugin file in scope sat exactly at its size-ratchet cap at authoring. Under the ledger rule the review-loop-exit plan's section 4 wrote, a section raises the cap of a file it grows in the same change and records the net in its Chapter, and the sentences each section supersedes are named as the first place to shrink.

## Dispatch Authorization

Authorized 2026-09-08 by the operator, first-hand on the allowlisted relay thread, adopting as one plan the four NEO-CLAUDE rulings of 2026-09-08 (provenance over severity, the design stop, the five-round backstop, Minor deferral) and the seat split the operator agreed on that thread (the goal-versus-output judge on demand mid-section and once at finishing, never every round), to be inserted at position 2 of the kit worker's armed queue immediately behind the plan in flight, the grant covering any session holding this plan. The Expert seat authored the spec; the operator's relay instruction is the arm and directed the Expert seat to perform the queue insert, so author and grantor are different parties, and the receiver traces the grant to this commit.

## Related

- `docs/archive/claude-kit_review-loop-exit_spec_v1.md` (Complete 2026-09-08): keyed the loop's exit on the behavior-or-claim class. This plan extends it on a different axis and reverses one of its decisions, named under Decisions 5 and 6 below.

## Goal

The executing-work skill's findings step admits a Major to a fix round on its severity and never asks whether the finding traces to the plan's Goal or to an acceptance bullet, so a reviewer finding that names a new requirement becomes an unreviewed design built one repair per round; the tier-escalation ladder fires only on surviving Criticals, so a section flat at four Majors a round never trips it, and even when it fires nothing it can do asks whether the mechanism under repair should exist; no count of rounds on one section reaches the operator; and Minors fixed every round at the writer tier grow the diff the next round reads without moving the loop toward its exit. The kit recorded this shape on 2026-09-08 as a twenty-round section over twenty-six hours on NEO-CLAUDE, and the class-keyed exit that closed the same day would not have stopped it, since every Major that mattered there was a real behavior defect in a mechanism nobody asked for. When this plan is done: every accepted Major carries a provenance read at adjudication, spec-traceable, fix-introduced or new-requirement, with the sighted lenses citing the bullet and the orchestrator tracing the blind lens's findings; a new-requirement Major never enters a fix round and is bucketed refuse, accept-and-declare or ask by the repo's live Expert seat or, where none is seated, by a fresh scope adjudicator dispatched with a fixed brief that carries the plan's what and never its how; two consecutive rounds of Majors in code the previous fix round wrote convene a consult on the premise rather than another fix round; a section's fifth review round that leaves the loop open stops it on the BLOCKED path with a phase analysis and one journal entry; Minors accumulate to one pass at section close; the finishing pass dispatches the same judge once over the whole changeset in both directions; the Chapter Metrics line carries the provenance counts; the read-only guard governs the new charter; and two scenario probes plus one cross-component pin hold the moments.

## Evidence

- `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4 (`**Address findings.**`): `Critical: must be fixed before the section closes. Major: fix, or record the justification for not fixing in the Chapter. Minor: note in the Chapter; fix only if trivial and in-scope`; the `KIT-CLAIM-CLASS` marker region; `**The loop ends on the class of what remains, never on a count or a rating:**` with `That round's claim findings are dispositioned in the same fix round`; `An owed Major's second reversal on one passage ... mandates a consult`; `**The recurrence rule:**`; `**A fix delta can owe a review round of its own.**`; the out-of-scope route with its `Fold`, `Add it to the plan` and `Route it out` destinations. No sentence in the step reads a Major's provenance.
- The same skill, step 1's `**Tier escalation:**` bullet: `a review round is the full set of reviewers dispatched for the section, security included when it ran, and a round fails when any of them returns a Critical that survives adjudication`; the repeating-findings comparison; `If no class repeats ... convene a consult on the spec's premise instead`. The ladder keys on Criticals alone.
- The same skill, the completion contract's paragraph `**Before any BLOCKED at all, the expert ask goes out, and it goes ahead of the consult.**`: the ask goes to a live expert seat on the roster, `it never gates: send it, keep working whatever is still workable`, an answer discharges only what the asker verifies on its own surface, and `Where no expert seat is live there is no ask at all`. `test/doctrine-parity.test.js` pins that paragraph's public-board-cap wording by a slice between its lead and the next paragraph's lead.
- The same skill, the paragraph `**Before any BLOCKED that turns on a decision, the consult runs too**`, and the BLOCKED first-line rule: `BLOCKED: <exactly what you need from me>`, the body in the client-briefing register, the first line inside 120 printable-ASCII characters.
- The same skill, the Chapter format: `Metrics: <review rounds <n>, closed <clean | claim-exit | major-closed>; NEEDS_CONTEXT count; escalations; consults <n>>`, read by `docs/backlog.md`'s consult-adoption watch for the `consults <n>` token, and by `finishing-work/SKILL.md` step 5 as the field `Recap:` sits immediately after. No hook parses the line.
- The same skill, step 3's `**Reviewer effort.**` and route paragraphs, and the Reviewer Dispatch template, which every reviewer dispatch fills; `**A brief forbids nothing the guard does not govern.**`, which names the read-only guard's fixed roster.
- `plugins/claude-kit/skills/consult/SKILL.md`, `## The trigger floor`, trigger (a): `A second failed attempt at the same problem, whatever its shape: an implementation round, a debugging hypothesis, a review round, a review seesaw (the second reversal of one passage's fix)`; `## The brief`, which carries the querent's current lean labeled an instinct to test.
- `plugins/claude-kit/agents/consultant.md`: frontmatter `tools: Read, Grep, Glob, Bash`, `effort: high`; `## Your brief` names the lean the orchestrator supplies; `**Test the framing.**` makes that lean a claim to check; output `RULING`, `EVIDENCE`, `CONFIDENCE`, `OPERATOR FORK`, status `RULED` or `NEEDS_CONTEXT`. Its shape is the sibling; its mandate to receive and test the querent's framing is what the new charter must not inherit.
- `plugins/claude-kit/agents/adversarial-reviewer.md`, the output line: `[CRITICAL|MAJOR|MINOR] [claim]? [confidence: high|medium|low] file:line - what is wrong, why it matters, suggested fix (one line).` It is dispatched with the spec path. `plugins/claude-kit/agents/security-reviewer.md`, the output line: `[CRITICAL|MAJOR|MINOR] [confidence: high|medium|low] file:line - finding.`, dispatched with the spec path. `plugins/claude-kit/agents/blind-reviewer.md`, the output line with `[claim]?`, dispatched with the base ref or changed-file list only, never the spec, per its own description.
- `plugins/claude-kit/hooks/kit-agent-identity-lib.js`, `reviewAgentClass`: one anchored regex naming the strict roster (`adversarial-reviewer|blind-reviewer|security-reviewer|council-member|design-facilitator|consultant|blind-reader|prose-reviewer|plan-reviewer`), `qa-verifier` as `gate`, null otherwise; the comment above it states why one list serves two hooks. `plugins/claude-kit/hooks/readonly-agent-guard.js` lines 1 to 20 restate the roster in a comment. `test/readonly-agent-guard.test.js`, the test `every read-only agent definition is a governed seat, derived from the definitions themselves`, reads every `agents/*.md` frontmatter `tools:` line and asserts each write-tool-less definition resolves to `strict` or `gate`, so a new charter absent from the regex reddens the suite.
- `plugins/claude-kit/skills/finishing-work/SKILL.md`, `## Steps`: 1 QA verification, 2 security review, 3 final adversarial review, 4 documentation curation, 5 close and archive, 6 apply the commit model, 7 bank the learnings, 8 kaizen check; step 2's `Major findings: fix or present to me with the tradeoff`; the sentence `A fix round this pass runs, here or at step 3, ends on executing-work's terminal condition`. The qa-verifier checks the spec's acceptance criteria with evidence; nothing at finishing asks what was built that no criterion named.
- `plugins/claude-kit/skills/responding-to-review/SKILL.md`: `## The two sources`, `## How to respond`, `## Corroboration across lenses outranks severity`, `## A claim finding's fix is a deletion or a check`, `## A clearance is a claim of the same standing as a finding`, `## No performative agreement`.
- `docs/architecture.md`: the executing-work paragraph (opening `Orchestration mechanics are the largest thing that boundary keeps out`) states the loop's mechanics and carries no ladder sentence; the consult paragraph: `Four triggers form a counted floor`; line 13 counts the effort pins, `effort: high` on implementer-fable and the consultant. `README.md`, one sentence: `the escalation after a section fails review twice`. `docs/security-model.md` line 645 enumerates the strict class by name, `(adversarial-reviewer, ... plan-reviewer) get a read-only repo tree`. `plugins/claude-kit/skills/operating-instructions/references/ownership-map.md`, the row for the `Standing Brief Amendments` block and its re-read.
- `plugins/claude-kit/skills/executing-work/SKILL.md`, the completion contract: `The set is closed` (the blocker set), so a stop reasoned from a round count is not a blocker until the set names it. `plugins/claude-kit/skills/memory-system/SKILL.md`, the verb `memq log <key> pass|fail "<summary>" [--tag t]... [--detail "..."]`, the summary capped at 120 characters and the detail at 500.
- `test/probes/README.md` and `test/probes/review-round-returning-only-claim-majors.md`: frontmatter `moment`, `tier`, `verdict`, `answer`, `ruling`, `options` (four to six), `shapes` with file lists, then the scenario verbatim; a cap entry per probe in `test/size-budget.json`; `test/probe-set.test.js` pins the shape.
- `test/size-budget.json`, read 2026-09-08 in the worktree at `ba93bb6`: executing-work 21,783; finishing-work 18,094; consult 776; consultant 457; adversarial-reviewer 3,336; blind-reviewer 1,463; security-reviewer 1,705; responding-to-review 1,867; ownership-map 2,252. The ratchet lowers and never rises except under the ledger rule.
- Every Chapter on record at `ba93bb6`, `git grep` over `docs/archive` and `docs/plans` for `Metrics: review rounds <n>`: 175 sections; 17 at zero rounds, 84 at one, 26 at two, 16 at three, 8 at four, 8 at five, 1 at six, 5 at seven, 3 at eight, 2 at nine, 1 at ten, 1 at eleven, 3 at fifteen; 24 at five or more.
- `kaizen/notes-NEO-CLAUDE.md`, the four 2026-09-08 notes, reported by that worker and not re-derived here: dsh-bridge section 2, rounds 8 to 20, grew an ownership lease on a state file from a round 7 finding no acceptance bullet named, reopening a risk round 6 had accepted; thirteen rounds, seven fixes each leaving open the case it was written for; the Expert seat live for the last nine rounds and sent nothing; three consults each asked how the lease should work and none whether it should; eight to sixteen Minors a round fixed at fable for twenty rounds. Each note carries the operator's keyboard ruling of the shape.

## Approach

Read every accepted Major's provenance at adjudication, before any fix exists, and let the provenance rather than the severity decide what the finding enters. Three values, with a precedence that makes them exclusive: a finding that traces to no acceptance bullet and no Goal sentence is new-requirement, wherever its lines sit; a finding that traces to one and sits in lines a fix round of this section wrote is fix-introduced; every other traced finding is spec-traceable. The trace is supplied by whichever party holds the spec: the adversarial and security lenses are dispatched with the spec path and cite the bullet on their output line, and the blind lens, dispatched without the spec by design, cites nothing, so the orchestrator traces its Majors at adjudication and the Chapter says the trace was orchestrator-made. A spec-traceable or fix-introduced Major takes the fix round as today. A new-requirement Major never enters one: it is held, the section continues on every other finding, no section closes with a finding still held (one the live seat has not answered by the time the section's other work reaches the close gate falls to the adjudicator, dispatched then), a bucket is a ruling adopted and recorded in the plan doc rather than a source the asker verifies, and the held finding goes to the judge, which is the repo's live Expert seat where the `ListAgents` roster the peer-sessions skill owns shows one (through the completion contract's existing expert-ask paragraph, which already never gates) and otherwise a scope adjudicator, a new read-only charter dispatched through the Agent tool with the fable model override at its charter's frontmatter effort, high (through Workflow only under executing-work's compensation route, where the session cannot set that effort), with a fixed brief carrying the plan's what (the Goal, every section's acceptance bullets, and the `## Out of Scope` list, which is the what's negative half), the finding verbatim with its lens and round, the provenance fact as a diff reference, and the three buckets with their tests, and never the plan's how (Approach, Decisions, Chapters), the orchestrator's lean, prior consults or the fix narrative, since the question's framing is what colored every consult on the NEO-CLAUDE run. The judge buckets, a set closed at three, and a finding that meets none of the tests below is an ask: refuse (off the goal path as the Goal and the bullets draw it, or inside what `## Out of Scope` keeps out), recorded in the plan doc; accept-and-declare (serves the Goal, bounded, no new mechanism), recorded as approval drift in the Chapter and surfaced as a line in the next board recap; ask (a new mechanism, a changed recorded decision, a reopened accepted risk, or section-sized work), to the operator through the BLOCKED path with the judge's recommendation answering why it serves the goal, what it accomplishes, what the design missed and what hole it fills. The design stop rides the same reading: when two consecutive rounds each carry at least one owed fix-introduced Major, which can first happen at round 3, the next step is a consult on the premise, whether the mechanism the fix rounds are building should exist and if so what its design is, routed to the same judge and bucketed the same way, rather than a fix round. The five-round backstop sits behind both as the operator's stated upper bound, an instrument rather than the primary control: a fifth review round whose adjudication leaves the loop open stops the section on the BLOCKED path with a phase analysis (rounds grouped by generator, Majors per round with their provenance, which findings were fix-introduced) as the decision brief, and writes one journal entry under one key so that the firing rate is a query when the operator revisits the number. Minors stop driving the cadence: they accumulate in the section's findings record and are fixed in one pass at section close before the close gate, with no review round after that pass, while the orchestrator still reads each at adjudication and upgrades on a stated consequence. The finishing pass gains one read: the scope adjudicator dispatched once over the whole changeset with the plan's what and the base ref, asked in both directions what is built that nothing asked for and what was asked for that is not built, its built-but-unasked items bucketed as above and its asked-but-unbuilt items entering finishing's fix path as spec-traceable Majors.

The coverage sweep (Explore at sonnet, 2026-09-08, read-only over the shared checkout) ran the eight questions the sections answer: the disposition rule, the tier-escalation ladder, the consult triggers, the reviewer output line, the Chapter Metrics line, the Standing Brief Amendments rule, the expert-ask route, and the tests that pin them. It returned: executing-work step 4, step 1's tier-escalation bullet, the completion contract's two pre-BLOCKED paragraphs, the Reviewer Dispatch template and the Chapter format; the consult skill's trigger floor and brief; the consultant, adversarial-reviewer, blind-reviewer, security-reviewer, blind-reader, plan-reviewer and prose-reviewer charters' output lines; `kit-agent-identity-lib.js`'s `reviewAgentClass` and the guard's header comment; finishing-work steps 2, 3 and 5; responding-to-review whole; `docs/architecture.md`'s executing-work and consult paragraphs; `README.md`'s two ladder sentences; the ownership map's amendments row; `test/claim-class-parity.test.js`, `test/doctrine-parity.test.js`'s expert-ask slice and standing-dispatch pins, `test/readonly-agent-guard.test.js`'s derivation test, `test/probes/review-round-returning-only-claim-majors.md`; and comments in `readonly-agent-guard.js`, `kit-goal-stop.js` and `compact-deferral-nudge.js` that cite a review round as a boundary example without stating its rule. It found no test pinning the ladder, trigger (a) or the Metrics format as a string. The blind-reader, plan-reviewer and prose-reviewer output lines and the three hook comments are under `## Out of Scope` with the reason.

## Decisions

1. **Provenance is read at adjudication on three exclusive values, trace first.** A finding with no bullet is new-requirement wherever it sits; a traced finding in fix-written lines is fix-introduced; the rest is spec-traceable. Precedence matters because a defect in an unasked mechanism is both fix-introduced and unasked, and the question the loop must answer first is whether the mechanism should exist. The values are read at adjudication, before any fix, from the trace and the diff, never from the fix that follows.
2. **The sighted lenses cite the bullet; the orchestrator traces the blind lens.** The adversarial and security lenses hold the spec and add a trace field to their output line for every Critical and Major; the blind lens's line is unchanged, because handing it the bullets ends the blindness that is its value, and the orchestrator, who holds the spec, traces its Majors and records that the trace was orchestrator-made. The alternative of the orchestrator tracing everything loses the independent trace on the lenses that can give one, and the orchestrator is the party whose framing colored every consult on the NEO-CLAUDE run.
3. **The judge is a new charter, not the consultant with a brief.** The consultant's mandate receives the querent's lean and tests the framing; the scope adjudicator must never receive the lean, the prior consults or the fix narrative, and its brief is fixed. One charter cannot carry both mandates, and a brief cannot subtract a mandate a charter states. The charter clones the consultant's shape (frontmatter, read-only tools, effort high, the RULED or NEEDS_CONTEXT status) and states its own inputs and output. It joins the strict roster in `reviewAgentClass`, which the derivation test enforces.
4. **The judge route is the live Expert seat first, the adjudicator otherwise.** The completion contract's expert-ask paragraph already carries the route, its never-gates rule and its bounds, so a new-requirement Major goes through that paragraph where the `ListAgents` roster shows a live expert seat, with one difference that paragraph does not have: a bucket is a ruling the asker adopts and records in the plan doc, not a source it verifies, since the question is scope rather than fact. That paragraph states no answer window, and the NEO-CLAUDE run had a live seat that sent nothing for nine rounds, so the window is stated here: an ask unanswered by the time the section's other work reaches the close gate falls to the adjudicator, dispatched then, and no section closes with a Major held. Where no expert seat is live, the adjudicator is dispatched at once. The hold applies to the disputed finding alone; the section continues on everything else, and the held finding is recorded in the plan doc's Chapter with what it waits on, since a hold that lives only in context dies at the next compaction. Expect more asks and fewer refusals on the adjudicator route, which is the designed cost of a judge that holds no context.
5. **The design stop is a premise consult, fired on fix-introduced Majors in two consecutive rounds.** The tier-escalation ladder keys on Criticals and stays as it is; this is a second trigger beside it, an instance of the consult skill's trigger (a) named in that skill, whose question is whether the mechanism should exist rather than how the finding should be fixed. Earliest at round 3 by construction, since round 1 has no prior fix and round 2 is the first that can carry a fix-introduced Major. It routes to the same judge as Decision 4 and takes the same buckets; a refuse ruling removes the mechanism to the form the spec asked for, and the ruling applies whatever session inside the loop approved the widening. A widening the operator approved, through an earlier ask bucket, a ruling adopted at the keyboard or a relay word recorded in the plan doc, is on the goal path by that approval, so refuse never reaches it.
6. **The five-round backstop reverses the review-loop-exit plan's Decision 3 ("no numeric cap is added").** The operator ruled the cap on 2026-09-08 at the NEO-CLAUDE keyboard and confirmed on the relay the same day that it is a backstop behind Decisions 1 to 5 and a look at what the kit keeps missing, to be revisited on evidence. So it is built as an instrument: the number lives in one sentence with a named lead so that changing it is one word; the stop is a decision ask on the BLOCKED path, never a kill, carrying the phase analysis as its brief, and it enters the completion contract's closed blocker set as an instance of a decision the spec does not cover (whether the mechanism under repair should continue), so the pre-BLOCKED expert ask and consult run as that set requires, the design stop's consult standing as the consult where it already ran in this section, and the doctrine's `Pause only for a true blocker` bullet names it as the operator-ruled fourth interrupt so that skill and doctrine agree; every firing writes one journal entry under `kit.review.cap`; and its firing rate after this plan lands is the measure of whether Decisions 1 to 5 cut the tail. Historically 24 of 175 recorded sections reached five rounds, so the cap will fire on roughly one section in seven until those decisions bite. One cost named: BLOCKED mid-queue hands the plan's slot to the next plan, so a capped section waits on the operator's answer and a re-arm rather than holding the worker.
7. **Minors defer to one pass at section close, and the claim-finding disposition moves with them.** The review-loop-exit plan dispositioned a round's claim findings in the same fix round; under this plan a claim finding rated Minor accumulates with the other Minors and is dispositioned in the close pass, while a claim either exception holds to the behavior bar keeps its Major standing and its round. The close pass runs after the loop's terminal condition is met and before the close gate, and no review round follows it; the orchestrator's adjudication still reads every Minor and upgrades on a stated consequence, which is what caught two silent data-loss defects on the NEO-CLAUDE run.
8. **Finishing gains one goal read, with the plan's what and never its how.** Goal-only would flag every mechanism a section legitimately specified; the whole spec would hand the judge the design story. The brief carries the Goal, every section's acceptance bullets and the `## Out of Scope` list, the base ref, and the two questions. It does not duplicate the qa-verifier, which checks that the criteria are met; it asks what was built that no criterion named, and what a Goal sentence promised that no criterion delivered.
9. **Caps rise under the ledger rule.** Every plugin file in scope sits at its cap; a section that grows one raises the cap in the same change, records the net in its Chapter, and names the sentence it supersedes as the first place to shrink.

## Sections of Work

### 1. The scope adjudicator charter and its roster entry
Model: opus
Locus: inline

Create `plugins/claude-kit/agents/scope-adjudicator.md`, cloned from `consultant.md`'s shape: frontmatter `name: scope-adjudicator`, a `description` stating that it is a fresh-context judge of whether a review finding, or a mechanism a section's fix rounds are building, serves the plan's Goal, dispatched with the plan's what and never its how, returning one of three buckets; `tools: Read, Grep, Glob, Bash`; `effort: high`. The body states its inputs as the fixed brief and nothing else: the plan's Goal paragraph, every section's acceptance bullets and the plan's `## Out of Scope` list, quoted or by path with the sections named; the finding verbatim with its lens, severity and round, or for a premise consult the mechanism named with the rounds that built it; the provenance fact as a diff reference (the base ref and the fix commits or the fix delta path under `.kit/`); and the three buckets with their tests as the Approach states them, the set closed at three with a finding meeting no test being an ask. It states what it must not receive and refuses with `NEEDS_CONTEXT` when the brief carries it, six inputs: the orchestrator's lean, prior consults, the fix narrative, the plan's Approach, its Decisions, its Chapters. It states the whole-changeset shape section 6 uses: the base ref instead of one finding, and the two questions. Output: `BUCKET:` one of `REFUSE`, `ACCEPT-AND-DECLARE`, `ASK`, with the test that decided it; `GROUNDS:` the bullet or Goal sentence the finding serves or fails to serve, or the absence of one; for `ASK`, `RECOMMENDATION:` answering why it serves the goal, what it accomplishes, what the design missed and what hole it fills; for the whole-changeset shape, two lists, `BUILT-BUT-UNASKED` and `ASKED-BUT-UNBUILT`, each item carrying its bucket or its bullet; status `RULED` or `NEEDS_CONTEXT`. Read-only commands only, the guard denying the rest.

Add `scope-adjudicator` to the strict alternation in `reviewAgentClass` in `plugins/claude-kit/hooks/kit-agent-identity-lib.js`, and to the roster comment at the head of `plugins/claude-kit/hooks/readonly-agent-guard.js`. Add `claude-kit:scope-adjudicator` to the Reviewer Dispatch template's `agentType` enumeration in `plugins/claude-kit/skills/executing-work/SKILL.md`, since that line warns that an unlisted name yields an ungoverned type. Add the name to the strict-class enumeration in `docs/security-model.md` (the sentence ending `get a read-only repo tree`), and to `docs/architecture.md`'s effort-pin sentence (line 13 at authoring), whose opening agent enumeration gains the scope adjudicator, whose count of pinned charters rises by one, and whose `effort: high` enumeration gains it too. Add the charter's cap entry to `test/size-budget.json`.

Acceptance:
- `test/readonly-agent-guard.test.js`'s derivation test is green with the new charter present, and red-first: with the regex unchanged and the charter present, the test names `scope-adjudicator` as an ungoverned write-tool-less definition. That test's own rule governs which charters the roster must carry; this section adds one name to the roster it reads and defines nothing.
- The charter carries no sentence inviting the querent's lean, and its refusal list names the six inputs above.

Files in scope: `plugins/claude-kit/agents/scope-adjudicator.md` (new), `plugins/claude-kit/hooks/kit-agent-identity-lib.js`, `plugins/claude-kit/hooks/readonly-agent-guard.js` (comment only), `plugins/claude-kit/hooks/memory-recognition-nudge.js` (comment only; folded at the section's round 1, the same roster enumerated for the stand-down that the strict class drives), `plugins/claude-kit/skills/executing-work/SKILL.md` (the template's `agentType` line only), `docs/security-model.md`, `docs/architecture.md`, `test/size-budget.json`. `test/readonly-agent-guard.test.js` is read and left unchanged: its derivation test is the gate, not a file this section edits.
Tests: the derivation test is this section's gate; section 7's pin locks both roster directions (the namespaced name resolves strict, a longer name containing it does not), since a silent admission of an ungoverned charter is the expensive failure.

### 2. Provenance at adjudication and the new-requirement route
Model: fable

In `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4: after the `KIT-CLAIM-CLASS` region and before `**The loop ends on the class of what remains**`, add a paragraph with the bold lead `**A Major enters a fix round on its provenance, never on its severity alone.**` stating Decision 1's three values with their precedence and the moment they are read; Decision 2's trace supply (the sighted lenses' trace field, the orchestrator's trace for the blind lens, recorded as orchestrator-made in the Chapter); the rule that a spec-traceable or fix-introduced Major takes the fix round as today and a new-requirement Major never does; the hold on the disputed finding alone with the section continuing on every other finding; the route of Decision 4 (the expert-ask paragraph where a live expert seat is on the `ListAgents` roster, the scope adjudicator through the Agent tool with the fable override at its frontmatter effort otherwise, with the fixed brief's contents and exclusions named by pointer to the charter; the answer window, an ask unanswered when the section's other work reaches the close gate falling to the adjudicator then; and a bucket being a ruling adopted and recorded, not a source verified); the three buckets with their tests and each bucket's record (refuse in the plan doc, accept-and-declare as approval drift in the Chapter and a line in the next board recap, ask through the BLOCKED path with the judge's recommendation, the BLOCKED first line under its public-board cap); the recording of a held finding in the Chapter with what it waits on; and, in one sentence, why (a severity is a reviewer's reading of consequence and says nothing about whether anyone asked for the thing).

Amend the terminal condition's sentence `leave no owed Major neither fixed, routed under the out-of-scope route below, nor recorded in the Chapter as justified-not-fixed` to add a fourth disposition, `nor bucketed under the provenance paragraph above with its record in the Chapter`, and state beside it that a finding still held when the section's other work reaches the close gate falls to the adjudicator then, so a held new-requirement Major neither keeps the loop open on the section's other work nor rides into a closed section.

Beside the Reviewer Dispatch template, as the sighted-only rule's second line in the two paragraphs where `Amendments in effect:` is stated for reviewer dispatches (step 3's opening paragraph, which governs the Agent-tool route, and the paragraph beside the Reviewer Dispatch template, which governs the Workflow route; never inside the template, whose three fields are one anti-downgrade class, and never in the Document Review Brief), add `Trace target:` for every dispatch that carries the plan's what (the adversarial and security lenses, and the scope adjudicator in every shape sections 2, 3 and 6 dispatch), naming the spec path's Goal and acceptance bullets as what a trace cites, on both the Agent-tool and Workflow routes, and state that the blind lens's dispatch carries no such line. Add a row to the reviewer-effort table for the scope adjudicator (fable, its frontmatter `high`, Agent tool), routed here at section 1's round 1 by the blind lens: the table decides route and effort for every seat the skill dispatches, and a seat dispatched by sections 2, 3 and 6 with no row leaves an orchestrator unable to tell the charter's pin from a compensation notch.

In the Chapter format, extend the `Metrics:` line to `Metrics: <review rounds <n>, closed <clean | claim-exit | major-closed>; provenance <s> spec-traceable, <f> fix-introduced, <r> new-requirement (<a> refused, <b> declared, <c> asked); NEEDS_CONTEXT count; escalations; consults <n>>`, keeping every existing token in place so the `consults <n>` reader and the `Recap:` position are unchanged, and stating in the line's description that `consults <n>` counts consultant dispatches alone while judge rulings (Expert seat or scope adjudicator, for a new-requirement Major or a design stop) are counted under the provenance tokens, so the backlog's consult-adoption series keeps its meaning; add to the `Review Findings:` field's description that an orchestrator-made trace is named as such.

In `docs/architecture.md`, the executing-work paragraph's parenthetical stating the terminal condition (`the first round leaving no Critical and no owed Major unfixed, unrouted and unrecorded`) gains the bucketed fourth disposition, so the doc is true at this section's commit rather than at section 3's.

In `plugins/claude-kit/agents/adversarial-reviewer.md` and `plugins/claude-kit/agents/security-reviewer.md`, extend the output line with a trace field after the severity and any `[claim]` tag: `[trace: <section N, bullet quoted in five words or fewer> | trace: Goal, <five words> | trace: none]`, required on every Critical and Major and optional on a Minor, with one sentence in each charter stating that `trace: none` is a finding about the plan rather than a weaker finding, and that the trace names what the code fails, never what the reviewer would have asked for. Leave `blind-reviewer.md` unchanged.

In `plugins/claude-kit/skills/responding-to-review/SKILL.md`, `## The two sources`, add one sentence: a finding's trace is read before its severity, and a `trace: none` on a Major sends it to the provenance paragraph in executing-work's step 4 rather than to a fix.

Raise the caps of executing-work, adversarial-reviewer, security-reviewer and responding-to-review by the net each grows, naming as first place to shrink the sentence in step 4's opening line `Major: fix, or record the justification for not fixing in the Chapter`, which the new paragraph now qualifies.

Acceptance:
- Step 4 carries the provenance paragraph with the three values, the precedence, the two trace sources, the hold, the route and the three buckets with their records; the terminal condition names the fourth disposition; the Metrics template carries the provenance tokens with every prior token intact.
- Both sighted charters carry the trace field on the output line and the two sentences; `blind-reviewer.md` is byte-identical to its state at the section's base ref.
- `test/claim-class-parity.test.js` is green (the marker region is untouched).
- `test/doctrine-parity.test.js`'s expert-ask slice pin is green (that paragraph is pointed at, not edited).
- The size ratchet is green with the caps raised and the nets recorded in the Chapter.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md`, `plugins/claude-kit/agents/adversarial-reviewer.md`, `plugins/claude-kit/agents/security-reviewer.md`, `plugins/claude-kit/skills/responding-to-review/SKILL.md`, `docs/architecture.md` (the terminal-condition parenthetical only), `test/size-budget.json`.
Tests: none new in this section; section 7 pins the trace field's presence on the sighted lines and absence on the blind one, and the Metrics tokens.

### 3. The design stop
Model: fable

In `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4, immediately after the provenance paragraph section 2 added, add a paragraph with the bold lead `**Two consecutive rounds of fix-introduced Majors are a design stop, not a third fix round.**` stating Decision 5: the trigger (round N and round N+1 each carry at least one owed Major read as fix-introduced), the earliest round it can fire (3) and why, the question the consult puts (whether the mechanism the fix rounds are building should exist, and if so what its design is), the route (the same judge as the provenance paragraph, the Expert seat where live and the scope adjudicator otherwise, dispatched with the mechanism named and the rounds that built it as the finding), the buckets and their records, and what a refuse ruling does (the mechanism is removed to the form the spec's bullets ask for, in one fix round whose delta takes the fix-delta bar). State in one sentence that the ladder in step 1 is untouched and keys on Criticals, and that this stop keys on provenance.

In step 1's `**Tier escalation:**` bullet, after `convene a consult on the spec's premise instead`, add one clause pointing at the design stop as the provenance-keyed sibling of that branch.

In `plugins/claude-kit/skills/consult/SKILL.md`, trigger (a), extend the shape list: `a review seesaw (the second reversal of one passage's fix), or a design stop (two consecutive rounds of Majors in code the previous fix round wrote, which executing-work's step 4 routes to the judge its provenance paragraph names rather than to the consultant, since that judge must not receive the querent's lean)`. In `## The brief`, add one sentence: the design-stop consult is the one shape whose brief omits the querent's lean, and the scope adjudicator's charter fixes what it carries.

In `docs/architecture.md`, the executing-work paragraph (opening `Orchestration mechanics are the largest thing that boundary keeps out`), add one sentence naming the provenance read and the design stop beside its statement of the loop's mechanics; the consult paragraph's `Four triggers form a counted floor` sentence gains the design stop as a named shape of the second-attempt trigger. In `README.md`, the one sentence reading `the escalation after a section fails review twice` gains `or hits the design stop`, and the `agents/` tree listing gains a `scope-adjudicator.md` line after `consultant.md` in the same register (routed here at section 1's round 1, that listing being a per-charter enumeration section 1 falsified and README sitting outside section 1's scope).

Raise the caps of executing-work and consult by the net, naming as first place to shrink the tier-escalation bullet's `This is the intra-section form of step 4's recurrence rule` sentence, which now has a sibling that states the relation from the other side.

Acceptance:
- Step 4 carries the design-stop paragraph with the trigger, the round-3 floor and its reason, the question, the route, the buckets and the refuse action; step 1's bullet points at it.
- The consult skill's trigger (a) names the design stop and the brief names the lean omission.
- The architecture and README sentences are updated; `test/doctrine-parity.test.js` and every other test that reads those files is green.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md`, `plugins/claude-kit/skills/consult/SKILL.md`, `docs/architecture.md`, `README.md`, `test/size-budget.json`.
Tests: none new here; section 7's probe holds the moment.

### 4. The five-round backstop
Model: opus

In `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4, immediately after the design-stop paragraph, add a paragraph with the bold lead `**The fifth review round is the operator's backstop.**` stating Decision 6: a section whose fifth review round's adjudication leaves the terminal condition unmet stops on the BLOCKED path rather than opening a sixth fix round; the round count is the one step 1's tier-escalation bullet defines (the full reviewer set), fix-delta-owed rounds included; the first line reads `BLOCKED: section <n> hit the review-round backstop; phase analysis attached` inside the public-board cap; the body is the phase analysis in the client-briefing register: the rounds grouped by what generated them (the spec, a fix delta, a reviewer's new requirement), the Majors per round with their provenance counts, which findings were fix-introduced, and the orchestrator's read of whether a cause the kit's rules missed is visible; the phrase `five review rounds` appears once in the tree's skill text, in that paragraph's first sentence, and the paragraph says so, so a revision is one word (the lead's `fifth` is the ordinal and not the number's home, and no other sentence in the tree restates the count); the stop enters the completion contract's closed blocker set as an instance of a decision the spec does not cover (whether the mechanism under repair should continue), so the pre-BLOCKED expert ask and consult run as that set requires, the design stop's consult standing as the consult where it already ran in this section; and before declaring, one journal entry is logged as `memq log kit.review.cap fail "<plan slug> section <n>: <rounds> rounds"` with the provenance totals under `--detail`, the verdict `fail` recording that the section did not close on its own, so the firing rate is a query. State in one sentence that the backstop is behind the provenance paragraph and the design stop rather than the primary control, and that its firing rate after those two land measures whether they worked. State the mid-queue cost: BLOCKED advances the queue, so a capped section waits on the operator's answer and a re-arm.

In `plugins/claude-kit/skills/finishing-work/SKILL.md`, the sentence `A fix round this pass runs, here or at step 3, ends on executing-work's terminal condition`, extend with: and stops on that skill's review-round backstop, counted over the finishing pass's own rounds (never restating the number).

In `plugins/claude-kit/skills/operating-instructions/SKILL.md`, the bullet `**Pause only for a true blocker.**`, extend the sentence `Interrupt me only for a contradiction in the spec, a decision with material consequences the spec doesn't cover, or a destructive/irreversible action` with a fourth interrupt: `or the review-round backstop the executing-work skill states, which is a stop I asked for by name`, so the skill and the doctrine agree and the doctrine's ranking bullet has nothing to resolve. Mirror the edit byte-for-byte in `home/claude-kit-doctrine.md`, which `test/doctrine-parity.test.js` pins to the skill body; the installed copy under the operator's home directory follows at the next `claude plugin update`, per Operator Verification.

Raise the caps of executing-work, finishing-work, operating-instructions and the doctrine mirror by the net.

Acceptance:
- The paragraph exists with the lead named, the phrase `five review rounds` appearing exactly once in it, the BLOCKED first line under 120 printable-ASCII characters, the phase-analysis contents, the journal key, and the two cost sentences.
- `grep -o "five review rounds" plugins/claude-kit/skills/executing-work/SKILL.md | wc -l` prints 1, and every other statement of the bound in the tree points at this paragraph rather than restating the number.
- finishing-work's sentence is extended and its parity pins are green; the doctrine bullet carries the fourth interrupt in both copies and `test/doctrine-parity.test.js` is green.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md`, `plugins/claude-kit/skills/finishing-work/SKILL.md`, `plugins/claude-kit/skills/operating-instructions/SKILL.md`, `home/claude-kit-doctrine.md`, `test/size-budget.json`.
Tests: none new here; section 7 pins the single occurrence of the number.

### 5. Minor deferral
Model: opus

In `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4, rewrite the opening line's Minor clause from `Minor: note in the Chapter; fix only if trivial and in-scope, and where a trivial finding lands outside the section's scope the out-of-scope route below owns its disposition rather than this line` to: a Minor is read at adjudication and upgraded on a stated consequence, otherwise recorded in the section's Minor list, a file the orchestrator keeps at `.kit/scratch/<plan-slug>/minors-section-<n>.md` (gitignored, created at the section's first Minor, one line per Minor as the lens printed it with the round number prefixed, read by the close pass and deleted by nothing, since `.kit/` is scratch), and fixed in one pass at section close, after the terminal condition is met and before the close gate, with no review round after that pass; a Minor whose fix would meet the fix-delta bar, its three triggers (an outward action, a new module, a security surface) and its judgment clause (a subject the area's tests route around) alike, is left with the reason rather than fixed in the pass, so the pass never owes a round, and the pass's delta takes step 5's below-bar author re-read, recorded in the Chapter as an author re-read rather than a round, so main never carries a close-pass delta nobody read; a claim finding rated Major that no exception holds keeps its same-fix-round disposition; a Minor outside the section's scope takes the out-of-scope route as before. In the terminal condition, change `That round's claim findings are dispositioned in the same fix round` to state that a claim finding rated Minor accumulates with the Minors and is dispositioned in the close pass, while a claim finding rated Major, whether an exception holds it to the behavior bar or not, keeps its same-fix-round disposition; carry the rest of that sentence (the four disposition forms and the prose-deletions-only clause) unchanged in meaning. Add one sentence of why: a Minor fixed at the writer tier every round grows the diff the next three lenses read, and text edits breed text findings, so per-round Minor fixing drives cost and churn without moving the loop toward its exit. In `**This step runs the section's close gate**`, name the Minor pass as running before the gate.

In `plugins/claude-kit/skills/responding-to-review/SKILL.md`, `## A claim finding's fix is a deletion or a check`, add one sentence: the fix lands in the section's close pass with the other Minors unless an exception held the finding to the behavior bar.

In `plugins/claude-kit/skills/finishing-work/SKILL.md`, step 3, add one sentence: Minors the finishing reviews return take one pass before documentation curation rather than a round each, on the same rule (name the step by its title, never its number, since section 6 renumbers).

In the Chapter format's `Review Findings:` description, change `Minors noted` to `Minors: <n> fixed in the close pass, <m> upgraded on a stated consequence, <k> left with the reason`.

Raise the caps by the net; name the superseded Minor clause as the first place to shrink.

Acceptance:
- Step 4's opening line and terminal condition read as above; the close pass is named before the gate; the why sentence is present.
- responding-to-review and finishing-work carry their one sentence each; the Chapter format's Minor field reads as above.
- `test/claim-class-parity.test.js` green; the probe `review-round-returning-only-claim-majors` still resolves to its ruled answer under the probe pair's after leg, since deferring a Minor's fix changes nothing about closing the section after one fix round.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md`, `plugins/claude-kit/skills/responding-to-review/SKILL.md`, `plugins/claude-kit/skills/finishing-work/SKILL.md`, `test/size-budget.json`.
Tests: none new here.

### 6. The finishing goal read
Model: opus

In `plugins/claude-kit/skills/finishing-work/SKILL.md`, `## Steps`, insert a step between the final adversarial review and documentation curation, renumbering the later steps and every reference to them inside the skill and in any file that names finishing-work's steps by number. The sweep is two patterns: `grep -rn -E "finishing-work.{0,60}step [1-8]|step [1-8].{0,60}finishing-work" --include=*.md --include=*.js plugins/ docs/ test/ README.md` for step numbers, and `grep -rn -E "'[1-8]\. \*\*" test/` for the heading literals the parity test slices on, which renumber with the steps and which the first pattern never returns. Every hit that refers to a renumbered step is fixed; hits under `docs/archive/` are left as they are, being dated journal, and `kaizen/` is not swept for the same reason. Sites known at authoring: executing-work's Chapter format `Gate:` line (`finishing-work's step 5`); `test/doctrine-parity.test.js`, seven sites between its slices at `6. **Apply the commit model.**` and the assertion on executing-work's Gate line, which name finishing-work's steps 1 and 5 by number; `plugins/claude-kit/hooks/memory-session.js`, two comments naming step 7; and finishing-work's own cross-references. The new step, `**Goal read.**`: dispatch the `scope-adjudicator` agent once, through the Agent tool with the fable override at its frontmatter effort, carrying the `Trace target:` line, with the whole-changeset brief the charter states: the plan's Goal paragraph, every section's acceptance bullets and the plan's `## Out of Scope` list, the base ref, and the two questions; never the Approach, Decisions or Chapters. Its `BUILT-BUT-UNASKED` items take the three buckets and their records as executing-work's provenance paragraph states them, the ask bucket going to the operator in the finishing close-out rather than through BLOCKED; its `ASKED-BUT-UNBUILT` items enter this pass's fix path as spec-traceable Majors under step 2's `fix or present to me with the tradeoff` rule. State in one sentence how it differs from step 1: the qa-verifier checks that the criteria are met, and this asks what was built that no criterion named and what a Goal sentence promised that no criterion delivered. Record the read on the finishing Chapter's `Review Findings:` field as `goal read: <b> built-but-unasked (<a> refused, <d> declared, <c> asked), <u> asked-but-unbuilt`.

Raise finishing-work's cap by the net.

Acceptance:
- The step exists in the stated position with the brief contents, the two item routes, the difference sentence and the Chapter field; every step-number reference in the tree resolves to the renumbered step.
- The finishing-work parity pins and the doctrine-parity tests are green.

Files in scope: `plugins/claude-kit/skills/finishing-work/SKILL.md`, `plugins/claude-kit/skills/executing-work/SKILL.md`, `test/doctrine-parity.test.js`, `plugins/claude-kit/hooks/memory-session.js`, every further file the renumbering sweep returns, `test/size-budget.json`.
Tests: none new here.

### 7. Scenario probes and the cross-component pin
Model: sonnet

Add two probes under `test/probes/`, cloned from `review-round-returning-only-claim-majors.md`'s shape, each with its cap entry in `test/size-budget.json`, `tier: opus`, `verdict: RESOLVED`, `ruling: proposed 2026-09-08`, and shapes named `executing-work` and `executing-work-plus-adjudicator` naming `executing-work/SKILL.md` alone and `executing-work/SKILL.md` with `agents/scope-adjudicator.md` and `skills/consult/SKILL.md` together:
- `major-traces-to-no-acceptance-bullet.md`: round 3 of a section returns a `[MAJOR] [trace: none]` from the adversarial lens proposing an ownership lease on a state file no bullet names, beside two spec-traceable Majors; the operator is away. Options: `fix-all-three-this-round`, `hold-the-untraced-major-route-it-to-the-judge-and-fix-the-other-two`, `declare-blocked-on-the-lease`, `write-a-standing-brief-amendment`, `escalate-the-implementer-tier`, `convene-the-consultant-on-the-lease`. Answer: `hold-the-untraced-major-route-it-to-the-judge-and-fix-the-other-two`.
- `third-round-majors-sit-in-the-second-rounds-fix.md`: rounds 2 and 3 each returned Majors whose lines the previous fix round wrote, all traced; no Critical. Options: `run-a-third-fix-round`, `consult-the-judge-on-whether-the-mechanism-should-exist`, `escalate-the-implementer-tier`, `declare-blocked`, `close-the-section`, `write-a-standing-brief-amendment`. Answer: `consult-the-judge-on-whether-the-mechanism-should-exist`.

Add `test/review-loop-provenance.test.js`, a cross-component pin: (1) the adversarial and security charters' output lines carry `trace:` and the blind-reviewer's does not; (2) executing-work's Chapter format `Metrics:` line carries `provenance`, `spec-traceable`, `fix-introduced`, `new-requirement` and still `consults <n>`; (3) the string `five review rounds` occurs exactly once in executing-work; (4) `reviewAgentClass` resolves `claude-kit:scope-adjudicator` to `strict` and `scope-adjudicator-helper` to null; (5) `consult/SKILL.md`'s trigger (a) names `design stop`; (6) the three hand-copied rosters that enumerate the strict seats by name gain `scope-adjudicator`, routed here at section 1's round 1 by the blind and security lenses together as the regression pin the new seat ships without: `test/readonly-agent-guard.test.js`'s no-file-writing-tool list and its per-name deny pins, its effort-pin map (which is what holds `docs/architecture.md`'s `effort: high` claim, and whose absence lets a dropped `effort:` line pass green), and `test/memory-recognition-nudge.test.js`'s stand-down list. Each assertion has a mutation control in the style of `test/claim-class-parity.test.js`.

Acceptance:
- `test/probe-set.test.js` green with both probes; the probe runner's after leg reads each probe at its ruled answer under both shapes, or the mismatch is recorded in the Chapter as the finding it is.
- `node --test test/review-loop-provenance.test.js` green, and each control reddens when its subject is mutated in a temp copy.

Files in scope: `test/probes/major-traces-to-no-acceptance-bullet.md` (new), `test/probes/third-round-majors-sit-in-the-second-rounds-fix.md` (new), `test/review-loop-provenance.test.js` (new), `test/size-budget.json`.
Tests: the file is the test; lock the withheld control for the trace field (a charter that carries `trace:` in prose but not on the output line must not pass).

## Out of Scope

- A Critical, and a security finding of Critical or Major weight: they keep their route (fixed before the section closes or raised), and provenance is read on them for the Metrics line only.
- The `blind-reviewer`, `blind-reader`, `plan-reviewer` and `prose-reviewer` charters: their output lines are unchanged. The blind lens by Decision 2; the plan reviewer because it reviews a spec, not a diff, and already reads against the Goal; the reader and prose reviewer because their findings are comprehension and prose, which the provenance read does not apply to.
- The tier-escalation ladder's Critical trigger and its remedies: untouched, per Decision 5.
- The Expert seat's wake cadence and the peer-sessions Roles table: a new-requirement ask rides the existing expert-ask paragraph and its bounds.
- A queue-insert verb for the goal CLI: the CLI has `arm`, `clear` and `status`, none of which inserts, and this plan's own insert is a direct state-file edit with a backup, recorded under Operator Verification. Routed to `docs/backlog.md` at the close-out as found work serving another goal.
- The three hook comments (`readonly-agent-guard.js`, `kit-goal-stop.js`, `compact-deferral-nudge.js`) that cite a review round as a boundary example: they state no rule this plan changes.
- The ownership map: the provenance read, the design stop and the backstop are step 4 mechanics executing-work already owns under the map's existing row; no new row. Executing-work's step 4 owns the design stop whole (trigger, route, judge, buckets), and the consult skill's trigger (a) carries a pointer clause, on the precedent of the tier-escalation bullet's premise-consult branch, which that skill already owns whole while the consult skill points at it.

## Assumptions

- assumed 2026-09-08 (default): one plan rather than two, since every rule lands in executing-work's step 4 and two plans would edit one paragraph concurrently; reversal: split sections 4 to 6 into a second spec and re-order the queue.
- assumed 2026-09-08 (operator's relay ruling of 2026-09-08): the five-round backstop reverses the review-loop-exit plan's Decision 3; reversal: delete section 4 and the pin's clause (3).
- assumed 2026-09-08 (default): Minor deferral moves the claim-finding disposition from the same fix round to the close pass; reversal: restore the `same fix round` clause and drop section 5's terminal-condition edit.
- assumed 2026-09-08 (default): the judge is a new charter rather than the consultant with a brief, per Decision 3; reversal: delete the charter, add a lean-omitting brief shape to the consultant, and repoint sections 2, 3 and 6.
- assumed 2026-09-08 (default): finishing's goal read reuses the charter with a second brief shape rather than a second charter; reversal: split the whole-changeset shape into `goal-reader.md` and add it to the roster.
- assumed 2026-09-08 (default): the trace rides the sighted lenses' output line as a bracketed field in the position stated; reversal: move it to a trailing clause and repoint the pin.
- assumed 2026-09-08 (default): the journal key is `kit.review.cap`; reversal: rename in section 4 and the pin.
- assumed 2026-09-08 (operator's relay instruction of 2026-09-08): the plan is armed by inserting its path at position 2 of the worker's queue in `.kit/goal-state.json`, with a timestamped backup beside it in the CLI's own naming; reversal: restore the backup.
- assumed 2026-09-08 (default): dated journal surfaces, `docs/archive/` Chapters and `kaizen/` notes, keep the finishing step numbers they name, since each is read against the skill as of its date; reversal: add `kaizen/` to section 6's sweep roots.
- assumed 2026-09-08 (default): the spec was written and committed in a worktree at `origin/main` because the worker holds the main checkout on its plan-1 branch; the worker's tree receives it at the anchor commit named in the handoff message to the worker, which is the commit that added this file (`git log --diff-filter=A -- docs/plans/claude-kit_review-loop-provenance_spec_v1.md`); reversal: none needed, the file is on `main`.
- blind read 2026-09-08 (blind-reader, implementer persona): 11 questions, 11 answered, 0 assumed, 0 asked; gating litmus: 3 definitions, 2 one-sided, 1 crossed, 0 unplaced, 1 under-length, each rewritten before the plan review.
- plan review 2026-09-08 (plan-reviewer at fable, effort high), first pass: 16 findings, 15 fixed, 1 assumed, 0 asked; verdict NOT_READY on a Critical (a held Major had no answer window), rewritten and re-dispatched once.
- plan review 2026-09-08 (plan-reviewer at fable, effort high), second pass: 13 findings, 13 fixed, 0 assumed, 0 asked; verdict READY_WITH_FINDINGS. Totals across both: 29 findings, 28 fixed, 1 assumed, 0 asked.

## Operator Verification

- After the plan closes, run `claude plugin update` on each machine and the global restart the operator named on 2026-09-08, so sessions execute the provenance read and the finishing goal read; a section run after that on the old text reopens the work.
- On the first five-round firing, read the phase analysis and decide whether it exposed a missed cause or a legitimately long section; a cap that fires on legitimate sections three times running is the operator's signal to raise the number, which is one word in section 4's paragraph.
- Confirm the queue insert: `node <plugin-root>/hooks/kit-goal.js status` from `D:\claude-kit` shows this plan at position 2 of 9 behind the sync machine-scope plan; a status showing it elsewhere reopens the insert.

## Open Questions

- Whether the number five holds after the first few firings: the operator's to answer on the evidence section 4's journal key accumulates.

## Chapters

### Chapter 1 - 2026-09-09
Completed: 1. The scope adjudicator charter and its roster entry
Implemented By: main session, inline
Metrics: review rounds 3, closed major-closed; NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: The section carries `Model: opus` and ran inline under executing-work's docs-write routing override, its Files in scope naming `docs/security-model.md` and `docs/architecture.md` and the docs-write guard denying any non-curator subagent a write there; `Locus: inline` is recorded on the section as that step directs. The charter shipped the red-first reading the acceptance bullet asks for: with the file present and `reviewAgentClass` unchanged, `test/readonly-agent-guard.test.js` failed with `scope-adjudicator.md grants no file-writing tool, so it is a read-only seat, and no policy class governs it`, and the classifier edit turned it green. Placing the seat in the strict class also withholds every memory pointer from its dispatches, `memory-recognition-nudge.js` keying its stand-down on the same export; that is what a fresh-context judge wants and is declared rather than incidental, and the nudge's own roster comment was folded in so the two do not drift. Probe pair: the change touches one shape-named file, `plugins/claude-kit/skills/executing-work/SKILL.md`, and its only hunk there is one name added to the Reviewer Dispatch template's `agentType` enumeration, a passage no probe's scenario turns on, so no pair ran and this line is the record of that reading rather than a clean result. Worth the operator's eye at the close: three review rounds grew the charter from 988 to 1,804 words, every addition closing a real finding, against an operator decision of 2026-09-09 that a seat adds no net words to a rule file (agent charters among them) until the corpus audit runs.
Assumptions: assumed 2026-09-09, section 1 (default): the strict-class placement's pointer withholding is the intended behavior for this seat rather than a side effect to be worked around; reversal: give the nudge its own roster separate from the guard's. assumed 2026-09-09, section 1 (route (a), executing-work's docs-write routing override): the section runs inline at the session's model rather than dispatching to `implementer-opus`; reversal: none available while the guard stands.
Review Findings: review: code pair and security at fable, Agent tool, 3 rounds. Round 1: 0 Critical, 3 Major (blind), 8 Minor across lenses; security CLEAR. Round 2: 0 Critical, 4 Major, 10 Minor. Round 3: 0 Critical, 3 Major, 11 Minor; security CONCERNS with its round-2 findings confirmed closed. Criticals: none in any round. Majors addressed: the charter's `ASK` tests turning on plan sections the charter forbids (both lenses, round 1) and the by-path delivery handing over those same forbidden sections (blind, round 1); the required-input list demanding a round index that forbidden input 3 excluded (blind, round 2); the blindness leaking through commit messages, which a pathspec cannot bound (security, round 2, Major); the ASK-precedence paragraph making `ACCEPT-AND-DECLARE` unreachable, a defect round 1's own fix introduced (adversarial, round 2); the required-input list unmarked for the whole-changeset shape (adversarial, round 3). Majors justified-not-fixed: the charter promising orchestrator routes and a finishing-pass dispatch no skill performs (blind, rounds 2 and 3) is a forward reference inside this plan, sections 2, 3 and 6 building every one of those sites before the plan closes; the charter is inert until something dispatches it, so what main carries between this commit and section 6's is a doc ahead of its wiring rather than a live defect. Majors routed under the out-of-scope route: the missing regression pins on the new seat (blind round 1 Major, security Minor, corroborated again in rounds 2 and 3) to section 7, whose pin list this section widened to name the three hand-copied rosters and the effort-pin map; the reviewer-effort table carrying no row for the seat (blind, round 3) to section 2. Minors: 6 fixed in the charter and the two hook comments, 2 routed (README's agents listing to section 3, the test rosters to section 7), the rest recorded here. One surface folded: `memory-recognition-nudge.js`'s roster comment, same directory, comment only, covered by the gate. One document re-opened by this section's own change and corrected in scope: `docs/security-model.md`'s count sentence, which read `Three members of the class read prose that need not be the operator's own` and was falsified by a fourth seat that reads a plan doc by path.
Stamps: adjudicated 13, stamped 5 (`claude-kit-hook-edits-need-a-build-stamp-refresh`, `kit-state-keyed-on-project-directory-follows-the-shell-cwd`, `scott-claude-standing-delegation-granted`, `proceeding-past-an-aged-claim-is-not-taking-it`, `suite-baseline-is-not-zero-fail`); the other 8 were read at the seat takeover rather than applied to this section's work. Window 3h, covering the section's whole span from the takeover; no hand walk owed.
Gate: targeted lane, `node --test` over `readonly-agent-guard`, `size-ratchet`, `hook-canary`, `claim-class-parity`, `doctrine-parity`, `probe-set`, `probe-runner`: 541 tests, 538 pass, 0 fail, 3 skipped, exit 0. Whole gate, run before the push because it lands on `main`, which is the install surface: 3511 tests, 3498 pass, 1 fail, 12 skipped, exit 1, 446 s. Delta against the baseline recorded on the whole gate in `suite-baseline-is-not-zero-fail`: baseline 1 failing {`memory-session.test.js` > `a pinned directory too long to name faithfully stands the session down`} to still 1 failing, the same test, which fails on this box because its `TEMP` is seven characters and the fixture never reaches the `PATH_EMIT_CAP` guard it means to exercise. No regressions. The push was then rejected non-fast-forward, `main` having gained `5a541ba` (three documents under `docs/`, no overlap with this section's files) while the section ran, so the commit was rebased onto it and the whole gate was run a second time on the combined tree, a merge or rebase being able to redden a suite in files neither side touched: 3511 tests, 3498 pass, 1 fail, 12 skipped, exit 1, 477 s, the same single baseline red, so the integration carried no redness of its own. The plugin was rebuilt before that second run, the rebase replaying a commit that touches `plugins/claude-kit/hooks/` while the build stamp is machine-local and untracked. Contention lane not run: the section's delta touches no machine-shared state. Contention carried: none, the machine's heavy-process claim was this session's own for all three runs and was released at the last one's end.
Next: 2. Provenance at adjudication and the new-requirement route
Commit Model: Commit-and-Push
Delta: read 2026-09-09T02:15Z on SCOTT-CLAUDE against HEAD `eac399b`, main checkout, this section's delta uncommitted and no foreign worktree content present. The section's commit was afterwards rebased onto `5a541ba`, which changed no file this reading names.

```
repository: claude-kit
plugins/claude-kit/skills/executing-work/SKILL.md: 21784 words, cap 21784, +1
plugins/claude-kit/agents/scope-adjudicator.md: 1804 words, cap 1804, untracked so HEAD holds no blob and the totals below exclude it
words: 222646 of cap 222646 across 61 curated files
test lines: 114443 of cap 114443 across 59 test files
tests: 3429
changed paths under no measured root: 6 (6 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
excluded from those totals, untracked under a measured root: plugins/claude-kit/agents/scope-adjudicator.md
```
