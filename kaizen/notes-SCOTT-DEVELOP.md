2026-07-12 | SCOTT-DEVELOP | Neuro-Evolution-Operations | **Subagent liveness has no reliable signal, and silence reads as death.** I judged a running implementer "stalled" from a stale transcript mtime plus an idle-looking process list, and dispatched a second implementer onto the same worktree. It had not stalled: it was inside a 10-minute test run, which is indistinguishable from death by every signal I checked. The authoritative signal existed and I ignored it: the harness had never sent a completion notification. Proposed rule for the orchestration doctrine: the completion notification is the ONLY liveness signal; absence of one means the agent is alive, regardless of transcript mtime or process list. Never infer death from silence. If an agent must die, kill it explicitly (TaskStop), never by dispatching a rival and hoping.

2026-07-12 | SCOTT-DEVELOP | Neuro-Evolution-Operations | **A mid-flight contract change was briefed into a NEW agent while the old agent was still executing the OLD contract.** "Lock the contract before you fan out" covers the fan-out moment but not the case where a review OVERTURNS a locked decision while agents are in flight. A review changed the em-dash rule (the missing-value placeholder must be preserved, not stripped); I briefed the correction into a fresh agent while the first was still faithfully stripping placeholders per its original brief. Two agents, two truths, one tree, hours of mutual overwrites. Each even hallucinated a cause for the other's edits (one blamed a nonexistent "environment style hook"). Proposed rule: a contract change INVALIDATES every in-flight agent that depends on it. Stop them first, then re-dispatch. A contradiction between two live briefs is the controller's bug, not the agents'.

2026-07-12 | SCOTT-DEVELOP | Neuro-Evolution-Operations | **Implementer agents `git add` their work, which silently poisons the controller's commits.** I ran `git add docs/foo.md && git commit -m "docs: ..."` believing it scoped to docs. It was not: `git commit` with no pathspec commits the ENTIRE INDEX, including 28 src files an implementer had already staged. A "docs-only" commit shipped a half-finished sweep to origin and made the known-good baseline silently red, which then poisoned every later "no regressions" claim measured against it. The existing rule ("stage only the files you changed") reads as being about `git add`, and I followed it; the trap is in `git commit`. Two proposed fixes, both cheap and worth having together: (a) brief implementers to leave changes UNSTAGED (the controller stages and commits), which the implementer-agent prompts should say outright; (b) the controller commits with an explicit pathspec, or runs `git diff --cached --name-only` as a pre-commit check, so a foreign staged file is impossible to miss.
