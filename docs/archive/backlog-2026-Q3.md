# Backlog Snapshot 2026 Q3

Completed cross-effort items moved out of `../backlog.md` during this quarter. Append-only within the quarter.

- 2026-08-22: Dispositioned all five Warden-derived candidates, closing the brainstorm-or-retire item banked the same day. Adopted the supersedes field (`plans/claude-kit_memory-supersedes_spec_v1.md`) and the cheap outline-first shape of AST-aware reading (`plans/claude-kit_process-rule-repairs_spec_v1.md` section 3, validated by probes of NEO, ASR.Eleos, and the kit's own memq.js); parked impact analysis on the NEO boundary evidence (recorded in the candidates doc so it is not re-proposed); declined the context-cost measurement on the operator's call; retired the never-worse invariant's test-now half and banked the invariant as its own backlog item. The candidates doc stays in `plans/` as the evidence record, header marked Dispositioned. Source: the 2026-08-22 review-and-organize session.

- 2026-08-22: Retired the plugin-reinstall item from the shared-tier close-out, on receipts rather than action: the installed plugin cache on NEO-CLAUDE now sits at the effort's HEAD commit (`3563944f7e72` under `plugins/cache/applefeld/claude-kit/`), and the PATH shim answers `memq delete-type` with its usage refusal rather than `unknown option`, so the repair and delete verbs are live here with nothing left to reinstall. A whole-body `--update --confirm-shared` repair also ran through the shim this session and landed. Verified on NEO-CLAUDE only; a lagging cache on another machine surfaces through that machine's own session-start version nudge. Source: the 2026-08-22 review-and-organize session.

- 2026-08-20: Delivered arm-time leash binding (parked 2026-08-18 as "Consider arm-time leash binding as a supplement, once the session-id environment variable is pinned"). Both unpinned facts were pinned live the day a multi-line `/kit-goal` arming left a 12-hour ai-os run armed-but-unbound: `CLAUDE_CODE_SESSION_ID` equals the session id hook payloads carry (triangulated through the run's own claim-time bind and two sessions' shells against transcript basenames), and a dispatched subagent's shell carries the parent session's id (probed with a throwaway agent). The item's "not a shape the kit produces" judgment was what the incident disproved: the operator's own one-plan-per-line typing is that shape. Shipped as `claude-kit_arm-time-binding_spec_v1.md` with a two-key gate (UUID shape plus existence-checked transcript) rather than the shape-only bind the item sketched, after the review round showed shape alone cannot authenticate. The item's "supplement, never a replacement" posture held: both claim points remain as the fallback. Source: arm-time-binding effort.

- 2026-08-15: Closed the Opus advisor experiment as SUPERSEDED rather than disproven. The in-context advisor is decommissioned in favor of the consult, a fresh-context ruling instrument (`claude-kit_consult_spec_v1.md`). The closing evidence is what ended it: the advisor runs as a server-side tool on this build (`[AdvisorTool] Server-side tool enabled` in `~/.claude/debug/`), and its consultations persist in no local transcript, debug log, or metric, so the experiment's own compare protocol was unrunnable by construction. The operator observed consultations firing frequently on live Opus sessions, so the feature worked; it simply could not participate in the kit's evidence discipline. The structural argument that decided it: a shared-context instrument is good at "check my step" and unable to check "is my frame right", because it inherits the session's blind spots along with its transcript, and the incident that motivated the consult had exactly that failure, with the advisor confirming a wrong framing at the right moment. The Chapter Metrics `advisor` field is retired with it and replaced by `consults <n>`. Reversal, if the consult ever proves worse: `/advisor opus` plus restoring the Metrics field.

- 2026-07-15: Confirmed the PowerShell tool's PreToolUse payload field. The command string is at `tool_input.command`, proven by a live block on the Windows desktop: the pr-docs-guard denied a real PowerShell-tool `gh pr create` while `docs/` was dirty and passed it clean, which requires both the `PowerShell` matcher and the field read to be correct. Source: stabilization effort residual (`claude-kit_stabilization_spec_v1.md` Chapter 7).
- 2026-07-15: Confirmed the docs-write-guard's live payload field and behavior. The subagent-identity field is `agent_type` and the deny path works: four wild blocks on the workstation during the 2026-07-13 admin-portal build (implementer subagents denied Write/Edit into `docs/`, each correctly rerouting to `.kit/`), plus a same-day probe on the desktop (subagent Write and PowerShell redirect into `docs/` both denied; the subagent's `.kit/scratch` write and a main-session `docs/` write both pass). Source: docs-write-guard effort.
- 2026-07-15: Confirmed the pr-docs-guard fires on a real PR command. Live on the Windows desktop through the PowerShell tool: blocked with uncommitted `docs/` changes, passed once clean. The `gh pr create` path is confirmed; `az repos pr create` shares the same command-string detection and was not separately exercised (no ADO host available). Source: pr-docs-gate effort.
- 2026-07-15: Confirmed the merged-pr-push-guard against the real GitHub host. Blocked a push (dry-run) to the merged PR #4 branch with the correct message; allowed a push to a branch with no PR (fail-open path); host-query latency ~2s and only on pushes. The ADO invocation remains an active backlog item. Source: merge-strand-guard effort.
- 2026-07-15: Report-hygiene wording verified GREEN in the wild. Across the workstation's 29-session Eleos corpus (2026-07-07 to 07-14), zero subagent reports were routed into `docs/` (no `docs/reviews/` or `_impl_reports/` writes exist); the docs-write-guard blocked four real subagent attempts and every one was adjudicated working-as-designed, with the orchestrator curating the deliverable from the main thread. The RED case from the NEO close-out did not recur. Source: docs-write-guard / executing-work wording effort.
- 2026-07-15: Verified doctor on the workstation. kit-doctor ran `doctor.cmd` (check mode) on the workstation 2026-07-14: exit 0, all PASS, bun found via the WinGet Packages-dir probe (the Links shim is absent there, the exact branch the item wanted), ANTHROPIC_API_KEY not set so the scrub branch is moot. Real compactions are proven on that machine: six successful engine compaction+resume cycles on 2026-07-10. The desktop ran `doctor.cmd` 2026-07-15: healthy with one WARN (resume relay `window.txt` unconfigured, now a machine-repairs backlog item). The `doctor.sh` Linux sibling remains an active item.
- 2026-07-15: Retired the chain-vs-subagents benchmark item. The posture (chain is for unattended stretches only) is recorded in `claude-kit_operating-model_spec_v1.md`; the 2026-07-07 to 07-14 corpus adds real-scale operational validation: an overnight unattended chain ran seven spec sections (2026-07-10 19:30 to 07-11 05:17) with per-section gate counts and commit hashes in the handoffs, plus two supervisor+worker chains of 12 to 21 resume turns each.
- 2026-07-15: Closed the Sonnet 5 tier boundary experiment as CONFIRMED. In the workstation corpus, `implementer-sonnet` took 29 dispatches across roughly 11 distinct sections (API client, tool-surface clones, Dockerfile/CI, telemetry/Bicep, admin procs/BackgroundService, live-suite harness): one review round each, zero NEEDS_CONTEXT beyond answered ones, zero escalations, matching the opus baseline of one round. The lone outlier (2026-07-07 API-client section, before the Metrics convention) took two same-tier fix rounds, still no escalation. The sonnet band holds at S1-class scope; the forced-dispatch clause was removed from executing-work step 1 (reversal: restore the one sentence). Widening sonnet to absorb opus-tier sections is a separate deliberate tiering call at brainstorming time, not part of this close.
- 2026-07-15: Ran the 200k-gate re-study (the verification pass for the operating-model posture). Result negative: post-gate orchestrators did not sit near the trigger. Findings moved to the active follow-up item; method: `tools/transcript-study/` over the workstation corpus (no ledger was available, so per-event ROI went unmeasured).
- 2026-07-15: Resolved the compaction summarizer count-mismatch defect, and a second defect found during verification. Root cause (live-reproduced): the summarizer template anchored turns by first-line user text, and orchestrator sessions make most anchors the identical string `<task-notification>` (21 of 26 in the failing transcript), so the model merged or dropped turns and the exact-count check refused. Fix (`claude-kit_summarizer-robustness_spec_v1.md`): indexed template pairs with an echoed-anchor cross-check, sparse preserve-verbatim fallback, whole-text sanitized anchors, and a legacy positional fallback for attribute-ignoring models. Second defect: the 240s summarizer timeout sat below the ~250s a ~933k-token compaction measurably takes; raised to 600s with a timeout-aware error. Evidence: the wild-failing 933k transcript compacted end-to-end twice (26/26 summaries, 11.6MB to 2.3MB, ~249s); bun test 20/20; two review rounds.
- 2026-07-15: Completed the two guard-hardening candidates plus review-driven fixes. merged-pr-push-guard: strict branch allowlist ahead of every host query (injection closed; quoted and `+`force refspecs normalized first), sh() fixed-literal discipline documented. docs-write-guard: PowerShell cmdlet writes (Out-File, Set-Content, Add-Content, Tee-Object) caught with parameter-order tolerance and a command-position anchor (the `Reset-Content` embedded-name false-deny is gone). Permanent regression harness at `tools/hook-tests/guards.test.js` (49 tests: both directions per guard, both tool names, injection non-reachability via recorder shims, deny paths via MERGED shims, ReDoS timing). Three reviews (adversarial, blind, security CLEAR).
- 2026-07-15: Confirmed the merged-pr-push-guard's ADO path against the real Azure DevOps host (asr-solutions org, EleosIntegration/EleosCore, completed PR 361). Test rig was an empty `git init` directory with only the ADO remote URL set (no work code transferred). Both the installed guard and the hardened working-tree guard blocked a push to the merged PR's source branch (exit 2, correct message) and allowed a branch with no PR; `az repos` auto-detected org and project from the remote URL with no defaults configured; host-query latency 1.2-1.8s, incurred only on push-shaped commands. Every guard-confirmation item from the stabilization effort is now closed on live evidence.
- 2026-07-15: Desktop resume relay armed and self-verifying. window.txt configured (`ahk_exe WindowsTerminal.exe`, process-only match because tab titles vary), watcher running at logon, arm script now seeds that default on machines where window.txt is absent, and kit-doctor gained a dryrun round-trip probe (writes a synthetic request under the watcher's dryrun flag and confirms the DRYRUN log line), verified PASS live on the desktop. Caveat that rides with the process-only match: during an unattended run, keep the run's Windows Terminal window the only one open.
- 2026-07-31: Baseline-tested the backstop wording GREEN (backlog-sweep spec section 4). Five fresh Sonnet probes against scratch fixture repos, every end state verified by direct inspection. The stop-docs-hygiene block reason: 2/2 ran the full curating-docs close path (git-mv archive, backlog pruned to a dated snapshot, index refreshed, committed under the plan's recorded model). The session-start nudge: 2/2 clean GREEN (did the user's task, then ran the archive pass at close-out, correctly leaving it uncommitted absent authorization), plus one rep that refused to archive because the fixture's plan claimed work its code did not contain - a fixture-authoring error on our side, and the refusal-plus-escalation was the drift rule working, not the wording failing. No wording change needed; the doctrine-contamination caveat applies (probes inherit the global doctrine, which is production-faithful). Incidental find during probe design, filed as its own backlog item: docs-write-guard has no repo-containment on its docs/ path match.
- 2026-07-31: Closed the guard access-model item: `docs/security-model.md` now carries "The guard hooks" - the four guards and the invariant each enforces, the two-class agent policy, the fail-open deny-only-on-positive-confirmation posture with its per-guard accepted risks, and the compensating controls with the gap each cannot see. Written from the guard headers and the readonly-agent-guard effort's security-review reconstruction; the headers remain the per-guard operational detail, the security model is where the policy gets re-tested.
- 2026-07-31: Decided and closed the writable-guard-executables item: detection over prevention (backlog-sweep spec section 2). Scott's framing sealed it: the fear is a model mistake or misstep, not an attack, and the Principals analysis already places a deliberate same-account actor outside every control. The build stamps a SHA-256 manifest of the shipped hook files (hooks.json included) into build-info.json and the canary verifies the executing cache at session start, converting a permanent silent disarm into a bounded loud one. The relocation/ACL fork was rejected as hardening a boundary the security model says cannot exist on a single-user account.
- 2026-07-31: Retired the leash-across-native-compaction item on field evidence. Scott observed an armed `/kit-goal` carry through an overnight session that native-auto-compacted: the run continued pursuing its goal after the compaction and ended with the proper blocked/completion messaging the next morning. Since `kit-goal-stop.js` enforces on session identity and only the kit's own compact CLI writes ledger rows, the leash holding through a native compaction implies the harness preserves the session id across it, which was the open question. One field observation rather than the planned id-comparison probe; if a future armed run is ever silently unleashed at an auto-compaction, that contradicts this close and reopens the item.
- 2026-07-31: Decided the 200k gate follow-up: ride-native-plus-plan-doc-recovery is the accepted default, and no new orchestrator-context lever gets built. Scott has moved off compact-session entirely in practice - a fresh session or `/clear` between chapters when context runs heavy, with native auto-compaction covering unattended overnight stretches, and it runs fine. That matches Anthropic's current guidance endorsing filesystem-state recovery over compaction. The study's open question is answered by adoption: the plan doc plus durable artifacts are the recovery mechanism, and deliberate compaction remains available on request only. Revisit only if a real run demonstrably loses work across a native compaction. (Decided 2026-07-31 in the backlog-review session; the item's full study data stays in this snapshot's 2026-07-15 re-study entry.)
- 2026-07-31: Closed the sibling exit-after-stdout truncation item. All four named SessionStart hooks (`session-start.js`, `doctrine-refresh.js`, `kit-version-nudge.js`, `branch-reaper-nudge.js`) now end with `process.exitCode = 0` instead of `process.exit(0)`, the hook-canary pattern; verified none sets a nonzero code and none holds an async handle, so each process drains stdout and exits 0 naturally. Extended in the same pass to the two Stop hooks sharing the exact pattern, `stop-docs-hygiene.js` and `kit-goal-stop.js`, where the stakes are higher: a truncated block decision reads as silence and allows the stop (for kit-goal-stop, that silently releases an armed leash). `format-on-edit.js` writes nothing to stdout and was left alone; the PreToolUse guards' deny paths exit 2, where the exit code blocks regardless of stdout, so they stay as scoped. Gate: 352/352 hook tests before and after.
- 2026-07-31: Graduated the memory-store relevance-pass item (filed 2026-07-30) to a plan after its requested brainstorming pass ran: a design-council round (three lenses plus neutral facilitator, converged in two rounds) settled the recall architecture as a no-query complete digest the model scores inside a fixed announced budget, and Scott's follow-on call replaced earned permanence with frequency-extended decay thresholds plus a judgment-set pin. The full design, the ArkVale references ("ArkVale: Efficient Generative LLM Inference with Recallable Key-Value Eviction", NeurIPS 2024; https://github.com/pku-liang/ArkVale), and the council's converged synthesis live in `claude-kit_memory-recall-and-reinforcement_spec_v1.md` (in `plans/` while active).

- 2026-08-16: Closed the docs-write-guard containment item. Its fix-when-it-next-bites trigger fired: a QA verifier was denied a report write to a session-scratchpad path holding a `docs/` segment, captured as a kaizen note. The guard now judges targets by containment against the git root above the payload cwd, mirroring the readonly-agent-guard's shape: a path resolving outside the project tree is structurally out of scope, in-tree docs/ writes stay denied in absolute and relative form, and a payload with no cwd or a target unresolvable before the shell runs keeps the prior shape-only judgment. Regression tests watched red then green in `test/docs-write-guard.test.js`; `docs/security-model.md` updated to the as-built behavior. Source: 2026-08-16 kaizen pass.

- 2026-08-16: Retired the plan-doc `Model:` vocabulary-mismatch warning in `fleet-integration.md`. The kit stopped emitting the decorated `fable (inline)` form: locus rides its own `Locus:` line, `executing-work` corrects the decorated spelling on sight, and `curating-docs` documents the bare tokens as the machine contract. The doc now states the value rule in present tense (bare tokens only, silent sonnet downgrade on anything else) without the standing-mismatch framing. Decided by Scott in the 2026-08-16 kaizen/backlog pass.

- 2026-08-21: Delivered the mechanize-the-chapter-boundary-checkpoint item (filed 2026-08-17 from the kaizen pass on the NEO-CLAUDE run) as a detector plus a directive, not an auto-open. `chapter-boundary-nudge.js` is a PostToolUse hook on Edit/MultiEdit/Write: when the session bound to an armed kit goal appends a Chapter to the armed plan's own doc, it puts the executing-work boundary steps in front of the model through the nested `hookSpecificOutput.additionalContext` JSON form. The wrinkle the item named decided the shape rather than blocking it: because a hook watching the doc sees only the Chapter append and not the commit, an auto-open would admit a compaction between the Chapter write and its commit, so the hook reminds and the session opens the checkpoint after honoring the commit model. Seven guards, all failing toward a silent exit 0, so the worst failure is a missed reminder rather than a degraded edit loop. Three further surfaces gained the same fact so the loop does not rest on one hook: three of the Stop hook's four block reasons, the doctrine's chapter-close bullet in both copies, and the gate's operator-facing note. Section 1 was a kill-or-confirm probe against the installed harness, and it killed the documented channel: the top-level `additionalContext` key the hooks documentation shows is parsed and discarded, and only the nested form reaches the model. Source: `archive/claude-kit_boundary-ritual-reinforcement_spec_v1.md`.

- 2026-08-21: Delivered the refresh-the-gate's-boundary-note item (filed 2026-08-19 from the boundary cadence finishing review) in the same effort, which is what its own "carry it on the slate of the mechanize item" instruction asked for. `BOUNDARY_NOTE` now names both boundary kinds rather than the superseded single one, states the hold as bounded by the safety valve, and carries an operator diagnostic: the condition is the note still firing after a Chapter has closed, which is when the checkpoint was likely never opened. The condition is deliberately NOT a repetition count, because the gate's own header records 19 consecutive denials followed by a clean checkpointed landing as the healthy case, so a count-keyed diagnostic would accuse every long section. The remedy names `status` before the mutating `open`, qualifies the open with "at a true boundary", and composes the command path from the hook's own `__dirname` rather than a repo-relative literal that would resolve only where the kit is dogfooded in its own checkout. Source: same plan.

- 2026-08-22: Retired the memq-shim PATH-resolution env-failure item (filed 2026-08-06, evidence captured 2026-08-11) with receipts rather than a fix to the shim, because the shim was never at fault. The 2026-08-11 evidence pass had already narrowed it from a resolution failure to a path-spelling one: `own.Resolved` returned the temp directory in its long form where the test expected the 8.3 short (`~1`) spelling of the same directory, both strings otherwise identical down to the per-run temp suffix. Commit `e276aa3` ("memq-shim tests: long-form the temp dirs the PowerShell comparisons ride on") fixed the tests to compare on one spelling. Confirmed retired by a full-suite run on SCOTT-CLAUDE during the shared-tier authoring effort: 1196 pass / 0 fail / 0 skipped, exit 0, with both named tests among the passes, where this item's whole content was those two tests failing on every run on this machine. Source: shared-tier authoring effort's close-out sweep.
- 2026-08-25: Delivered the record-every-compaction-offer item (filed 2026-08-18 from the DeepSeek Harness context-model read) as Section 1 of `claude-kit_compaction-deferral-signal_spec_v1.md`: the PreCompact gate now writes a decision state to `.kit/compact-gate.json` and an append-only, size-capped journal to `.kit/compact-gate.jsonl`, and `kit-compact-checkpoint.js status` reports the last decision and any open deferral episode. The item was found by re-reading the backlog during that plan's own execution rather than at its intake, which is why it closed inside the effort that answered it.

- 2026-08-25: Closed the judge-the-boundary-cadence item (filed 2026-08-19, carried out of the boundary cadence effort as operator-only) on the observation it asked for, arriving in the failing direction and then answered. A leashed run compacted at the safety valve mid-section after three adjudications with no section closing; `claude-kit_compaction-deferral-signal_spec_v1.md` was that reopened round, and its own leashed run produced the receipt as a file rather than a scrollback reading: `.kit/compact-gate.jsonl` recorded two boundary denials and then an allow whose reason is `checkpoint`, at 3260 seconds against a ten-minute short bound. What closes is the mechanism question; the cadence-over-time question stays active on the turn-it-on item, which is still operator-only.

- 2026-08-25: Delivered the give-the-finishing-pass-a-boundary item (filed 2026-08-19 from the boundary cadence finishing review) as Section 4 of `claude-kit_compaction-deferral-signal_spec_v1.md`. The item said the trigger had to be named rather than borrowed, since finishing has no section closes to count; the answer is the gate's own recorded deferral, so the finishing pass takes an interim board entry on the same signal the section loop uses. Shipped in `finishing-work/SKILL.md` at `d6a4753`.

- 2026-08-25: Retired the AI-OS-consume-kit-events item (filed 2026-07-25) with receipts: the consumer shipped on the AI-OS side as `src/Spine.Kernel/KitEventConsumer.cs` (verified at that path on this machine, 27 KB, mtime 2026-08-17, `goal-complete`/`goal-blocked` constants at lines 52-53), documented in the AI-OS `docs/architecture.md` Goal-event section, exactly-once via a rows-plus-cursor single transaction, with an out-of-grammar run id landing uncorrelated rather than dropped. Surfaced by a cross-session consult with the AI-OS session, which also reported one fact the kit should keep beside its `KIT_EVENTS_PATH` documentation: the consumer deliberately ignores that override, because the engine sets it on spawns and honoring it at ingest would let a worker redirect what the kernel reads. The item's queue-cadence and dead-binding caveats remain documented in `architecture.md` under Goal release events and are the consumer's concern now. Source: the peer-messaging experiment's AI-OS consult.

- 2026-08-25: Promoted the kit-goal-queue-and-worktree-semantics item (parked earlier the same day from the peer-messaging kaizen pass, the operator's artifact-borne-authority direction added hours later) to `claude-kit_dispatch-authority_spec_v1.md`, which carries all five clustered lessons, the operator's principle as its organizing design, and the adjudications: cwd-scoping stays and worktree-pointer following is rejected on the arm-in-worktree evidence, and the guard-compound fold retires separately since the refusing mechanism is the harness's own isolation screen, making the kaizen batch's doctrine exception the kit's whole remedy. Source: the dispatch-authority spec's creation.

- 2026-08-25: Retired the implementer-charter Dispatch Brief drift item (parked 2026-08-18, from the standing-watch effort) on receipts. Section 1 of the kaizen batch took the pointer shape the item itself recommended over the alternative pinning test: the four implementer charters, plus the two reviewer charters' input restatements, now name the Dispatch Brief template in `skills/executing-work/SKILL.md` as the field list's sole owner and keep only their agent-specific deltas inline (verified at `agents/implementer-haiku.md:12` and `agents/implementer-fable.md:12`, both reading "that template, not this charter, owns the field list"). Six restatements collapse to one owner, so the compounding drift the item recorded has no surface left to recur on. Source: `archive/claude-kit_kaizen-batch_spec_v1.md`, section 1.

- 2026-08-26: Retired the coordination-needs-a-durable-spine item (parked 2026-08-25 from the peer-messaging kaizen pass) as consumed rather than aged out, which is exactly what its own stated signal called for: the first coordinator brainstorm has fired, and `claude-kit_coordinator-and-roles_spec_v1.md` is its answer. The item asked where a cross-session dispatch brief lives when the receiver's context window is the only thing holding it, and offered three candidates: a plan-doc Coordination section, a `.kit/` ledger in standing-watch's shape, or memory. The answer takes the second and changes what the chassis says about it. The coordinator seat's ledger is `docs/coordinator-board.md`, committed rather than gitignored, because a watch's state dies with the watch by design while a seat's state is how the seat survives the session holding it, and a successor taking the seat cold has nowhere else to read what its predecessor promised or brokered. The generalization the item was reaching for is a reading of the design rather than shipped text, and it is worth stating as such: any session holding cross-session commitments records them durably in its own repo, of which the coordinator's machine-wide board is one instance and the narrower rule that does ship is the other, `plugins/claude-kit/skills/peer-sessions/SKILL.md` having each side of a handoff record a held or unconverted plan in its own Chapter. What the board deliberately does not carry is authority, for the structural reason `docs/security-model.md` states under The coordinator board: a board the seat writes itself cannot satisfy a receiver's trace back to the operator however it is worded. Source: `claude-kit_coordinator-and-roles_spec_v1.md`, section 3.

- 2026-08-26: Retired the shared-plan-status-helper item (parked 2026-06-19) because half its premise moved and the surviving half is stated more accurately by a newer item. It asked for a `hooks/lib/` helper if the "Status: Complete in docs/plans/" scan, then duplicated across `session-start.js` and `stop-docs-hygiene.js`, ever gained nuance. Two things happened. The scan is no longer two spellings but three, which the 2026-08-26 item on the loose Status-header rule records with the third named and with the reason consolidating them needs its own plan and red-first tests per behaviour. And the READ beneath the predicate is already single-sourced: kaizen batch 2 routed the SessionStart inventory and then, in its finishing round, `stop-docs-hygiene.js` through the library’s `planHeadText` kind rule, which is the shared helper this item asked for, arrived at from the security side rather than the duplication side. What is left is the predicate alone, and it is carried by the newer item rather than by two items describing one seam. Source: `claude-kit_kaizen-batch-2_spec_v1.md`, finishing round, curator drift [D6].

- 2026-08-27: Retired the merged-PR push guard block-test flake (filed 2026-08-04). The entry root-caused the test as inheriting the hook's 8-second production `execSync` budget, which a loaded box exceeds, flipping the fail-open hook so the block assertion reads a missing block. The hook now carries a signal-only switch, `KIT_MERGED_PR_GUARD_NO_DEADLINE` honored alongside `KIT_MERGED_PR_GUARD_NO_DEADLINE_ALLOW=1`, which sets each query's `execSync` timeout to 0, Node's spelling of no deadline. The production budgets are unchanged, so a developer's push is guarded exactly as before. The suite sets the pair on every spawn of the hook, so no assertion in that file depends on a query finishing inside a budget, which removes the load-dependence rather than widening it: a numeric budget was built first and rejected on a consult's ruling, because the recorded excursion was roughly 400x and no ceiling is a guarantee on a box whose load has no ceiling. Two security defects of the same file rode with the fix: the push detector missed newline-, `&`- and `(`-separated segments, so a push written after any of them was not guarded at all, and `sh()` inherited an ambient `GIT_*` environment able to redirect every git read the decision rests on. Lane at retirement: `node --test test/merged-pr-push-guard.test.js`, 35 tests, 35 pass, 0 fail, exit 0. Source: `claude-kit_testing-discipline_spec_v1.md`, section 4.

- 2026-08-27: Retired the memq stale-lock race flake (filed 2026-08-01, roughly 1 run in 4 red under a full suite). The timing window the entry named is gone: each child writes a marker into a shared readiness directory and spins until both are present before either attempts the lock, so the two attempts land milliseconds apart by construction rather than by luck, and the barrier's own deadline sits well below the stale threshold so a child cannot wait long enough for the threshold it then relies on to move underneath it. Three assertions the test never had now stand behind it: each child reports whether it cleared the barrier or timed out and both must have cleared it, so a pass can no longer be bought by a race that never happened; each child's exit code is asserted; and a spawn error is caught and surfaced rather than hanging the wait. The uncaptured assertion text the entry asked for is moot, since the run that would have captured it no longer reds. Lane at retirement: `node --test test/memq.test.js`, 525 tests, 525 pass, 0 fail, exit 0. Source: `claude-kit_testing-discipline_spec_v1.md`, section 4.

- 2026-08-29: Retired the SessionStart 64 KB backlog-count item (parked 2026-08-26). `summarizeBacklog` now reads through `hooks/kit-read-lib.js` at a 1 MB ceiling and reports a bounded summary rather than a partial count presented as a total, with the bound covering the oldest date and the undated tally as well as the count, and a bounded read that reached no item at all replacing the figures with a sentence naming the limit. Pinned by fixtures deliberately larger than the old window and by the ceiling, each proven red-first. The same fix is sited at `countPendingKaizen`, which carried the identical fixed read latently.

- 2026-08-29: Retired the `docs/backlog.md` kind-rule item (parked 2026-08-26). The item asked for either a general head-read helper over an arbitrary project-relative file or a second kind-screened reader; the first is what shipped. `hooks/kit-read-lib.js` stats before it opens and refuses anything that is not a regular file, so a FIFO planted at `docs/backlog.md` or at a kaizen note can no longer block session start. A test counts `openSync` calls to prove the refusal is a stat verdict rather than a rescued open. The item also asked that the sweep take any other bare `openSync` over a repo-relative path in `hooks/`: the two `session-start.js` readers are routed, and the remaining single-`readSync` sites read fixed header or tail windows they treat as windows rather than as whole files, which is a different and milder shape. Standing Brief Amendment 3 of `claude-kit_plan-lifecycle-and-diagnostics_spec_v1.md` binds the next author at that boundary.

- 2026-08-29: Retired the three-spellings plan-doc Status-header item (parked 2026-08-26). The predicate is single-sourced: `classifyPlanStatus` in `kit-goal-lib.js` is the only rule that reads a plan doc's `Status:` value, and both consumers call it, `session-start.js`'s plan-recovery scan and `stop-docs-hygiene.js`'s terminal check. A whole-tree search finds no independent Status-header pattern left in the hooks. The one separate reader that remains, `hasPlanHeaderContract`, asks whether the header rows are present rather than what they say, by documented design, so that a curated doc carrying a value no classifier names yet is never read as scratch. Closed by Section 2 of the plan-lifecycle-and-diagnostics effort, which added the Ready leg and collapsed the two consumers onto the library rule.

- 2026-09-02: Delivered the read-only guard’s directory-driven agent-roster parity check (parked 2026-08-18). The memory-read-side plan’s section 3 review round moved the classifier into `plugins/claude-kit/hooks/kit-agent-identity-lib.js` as `reviewAgentClass`, read by the read-only guard and by the recognition nudge’s dispatch stand-down alike, and added the derivation test in `test/readonly-agent-guard.test.js`: it reads every definition under `plugins/claude-kit/agents/` and asserts that each one granting no file-writing tool in its `tools:` frontmatter resolves to a governed class, so a read-only seat the classifier does not know is a red rather than an agent running ungoverned. The adjacent candidate this item sat beside, the two payload-reading guards’ hand-copied agent-type spelling chains, stays open in `docs/backlog.md` as its own item.

- 2026-09-02: Retired the doctor-reads-its-own-launch-directory item (parked 2026-08-26). The item rested on the goal family resolving a worktree across to the main checkout, which the worktree-goals plan removed: `goalPath` now joins the caller's own directory with no pointer read, so there is no second file for a hook and the doctor to disagree about. What the item asserted about the doctor was also wrong in its own terms, and the correct statement now ships in `plugins/claude-kit/skills/kit-goal/SKILL.md`: the doctor derives the checkout it inspects from the location of its own script file rather than from a launch directory, its goal-state block runs only where its payload sits in a repo clone, and a run from an installed payload reports no goal state at all.

- 2026-09-02: Retired the goal-events-split-project-identity item (parked 2026-08-26). The item's whole rationale was that `emitGoalEvent` records `project: cwd` while the leash it describes lived in the main checkout, so one repository's leash wrote its releases under two project identities. Co-located goal state dissolves that: the leash now lives in the tree the stopping session runs in, so `project: cwd` names exactly the tree whose leash the event is about, and the two identities are one.

- 2026-09-02: Retired the cross-tree-agreement-spelled-twice item (parked 2026-08-26). The item asked for one shared predicate behind two spellings of the rule that a decision needs both trees to agree, in `kit-goal-stop.js`'s gone cross-check and `kit-goal-lib.js`'s `queueEntryState`. The worktree-goals plan dissolved the subject rather than answering it: `goalRoot` is retired, goal state and plan docs belong to one tree, and both call sites are single-tree reads with no second tree to consult, so there is no agreement rule left to unify.

- 2026-09-03: Retired the judgment-sidecar operator-only acceptance criteria item (parked 2026-08-31). Both criteria passed against the real store on this machine once the operator started the daemon, and the item's own reopener, either criterion failing when run, did not fire. Criterion one, schema-valid spool lines for real tool calls: the machine coordinator seat validated 72 of its own captured Bash calls against the contract's line schema with a withheld malformed control line caught, which is reported from that seat; the Expert seat's own read of the day spool at `~/.claude/kit-sidecar/spool/2026-09-03.jsonl` parsed 4413 lines with zero unparseable, which confirms parseability and not the field schema. Criterion two, end-to-end delivery after a deliberately trapped call: confirmed from the sidecar's own records, the verdict log for the coordinator session records call `be6cbd34950a96a6` captured 10:01:53Z and judged `diverged` at 10:04:20Z with a correct reason, and `logs/offsets.json` lists `alert:be6cbd34950a96a6` as delivered. The coordinator's caveat rides with the receipt: the trap was an obvious divergence, so this proves the valve delivers and says nothing about the judge's sensitivity on a subtle one. The distiller item's gate is reduced in the same edit to the operator's call alone.
- 2026-09-05: Retired the mutating-CLI record item (parked 2026-09-04 from the subtraction-bars plan's section 4). It asked `docs/security-model.md` to name the `init` verb of `plugins/claude-kit/scripts/kit-size.js`, what it writes and the refusals that bound it, since that document's read-only reviewer guard paragraph names the kit's own mutating CLIs as the exposure it deliberately leaves open and the payload had gained one with no entry. The subtraction-bars finishing pass's docs curation wrote that paragraph, naming the exclusive create, the containment that binds the write whatever `--budget` names, and the five refusals that keep an untrustworthy cap from being written; confirmed at source at `plugins/claude-kit/scripts/kit-size.js:1542-1551` and `:1586-1588`.
- 2026-09-06: Retired the docs/README.md-archive-narrative-behind item (parked 2026-08-29 from the seat-infrastructure finishing pass). It recorded that the two indexes' rolling archive chains disagreed by three efforts and prescribed re-deriving both against `docs/archive/` at the next close-out that touched either. The reviewer-fable-uncap close-out found the two chains equal to each other (endpoint-dialect-key, subtraction-bars, gating-definitions, memory-recognition-reach) and both missing `claude-kit_board-routing-and-homing_spec_v1.md`, archived 2026-09-04 by `git log --diff-filter=A -- docs/archive/`; it re-derived both chains to reviewer-fable-uncap, endpoint-dialect-key, subtraction-bars, board-routing-and-homing, so the two indexes now agree with each other and with the archive's four most recent plans.
