# The review loop ends on the class of what remains, and a finding that changes no behavior cannot hold a section open

Status: In Progress
Commit Model: Commit-and-Push
Created: 2026-09-07

Session model: any executor session in the kit repo; three sections in order, since section 1 mints the class definition sections 2 and 3 copy and pin. Authored by the KIT: Expert seat on the operator's keyboard rulings of 2026-09-07, taken in that session after a NEO-CLAUDE worker's diagnosis of a seventeen-round section: claim findings rate Minor under a tag, a behavior Major that reverses the prior round's fix mandates a consult at the second reversal, and the plan runs second in the worker's queue. Anchors are authoring-time and named by passage; re-locate every hit by content. Every file in scope sat exactly at its size-ratchet cap at authoring. Under the ledger rule section 4 writes (the operator's ruling of 2026-09-08, taken mid-run), a section raises the cap of a file it grows in the same change and records the net in its Chapter, and the sentences each section supersedes are named as the first place to shrink.

## Dispatch Authorization

Authorized 2026-09-07 by the operator at the keyboard of the KIT: Expert session, adopting the class-keyed exit, the severity cap with its two exceptions and the consult at the second reversal as one plan for the kit worker's armed queue at the position behind the plan in flight, the grant covering any session holding this plan. The Expert seat authored the spec and the operator's own instruction is the arming, so author and citer are different parties.

## Goal

The executing-work skill's findings step says what to do with a Critical, a Major and a Minor, and says when a fix delta owes another review round, and never says when the loop ends. Every run derives its own stop, and the derivation that comes naturally, a round returning only Minors closes the loop, cannot terminate on a section whose remaining findings are claims: a comment, a header sentence, a test's because-string, a test instrument's stated reach. A max-effort reviewer can always find a sentence that claims one word more than the code proves, every fix writes new sentences, and the charters rate a false sentence Major under maintainability damage, so the loop runs until someone stops it by hand. The kit has recorded this shape four times without acting on it: a fifteen-round section with two consults on this machine (2026-09-03), a thirteen-round seesaw and a finishing pass with no stopping rule (2026-09-07, this machine), and a seventeen-round section over thirty hours on NEO-CLAUDE (2026-09-07), plus the operator-tier memory that states the structural reason and the journal entry that says a count-keyed rule misfires. When this plan is done: executing-work's findings step owns one class definition, telling a behavior finding from a claim finding by whether it states a failure scenario, and one terminal condition keyed on it; the code-reviewer charters carry that definition as a pinned copy and rate a claim finding Minor under a `[claim]` tag, with two narrowly drawn exceptions that keep their bar; the recurrence rule sends a claims class to a mechanical check or a deletion rather than a Standing Brief Amendment; a behavior Major that reverses the prior round's fix on the same passage mandates a consult at the second reversal; responding-to-review states that a claim finding's fix is a deletion or a check and never a rewritten sentence; the finishing pass points at the same condition for the fix rounds it runs; and a scenario probe holds the moment so a later wording change that reopens the loop is a finding rather than a surprise.

## Evidence

- `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4 (`**Address findings.**`): `Critical: must be fixed before the section closes. Major: fix, or record the justification for not fixing in the Chapter. Minor: note in the Chapter`; then the recurrence rule, which on a second instance of a class amends the plan's `Standing Brief Amendments` block; then `**A fix delta can owe a review round of its own.**` with its three triggers and the class beyond them. No sentence in the step, or anywhere in the skill, states a condition under which the review loop ends. A grep of every skill and charter for `only minor`, `minors alone`, `clean round` and their variants on 2026-09-07 returned nothing.
- The same skill, step 1's tier-escalation bullet: `a review round is the full set of reviewers dispatched for the section, security included when it ran, and a round fails when any of them returns a Critical that survives adjudication`. That is the kit's only definition of a round, and it names failure, never success.
- The same skill's Chapter template: `Metrics: <review rounds; NEEDS_CONTEXT count; escalations; consults <n>>`, the data feed for the backlog's tier-band and consult-adoption experiments.
- `plugins/claude-kit/agents/adversarial-reviewer.md`, the severity list: `**Major** - likely bug, meaningful maintainability or performance damage, spec ambiguity resolved badly. Fix or justify.` and `**Minor** - style deviations, naming, small cleanups.` A false header sentence rates Major under the maintainability clause as written. The output line is `[CRITICAL|MAJOR|MINOR] [confidence: high|medium|low] file:line - ...`.
- `plugins/claude-kit/agents/blind-reviewer.md`: `**No style review.** Naming, formatting, house style, and comment quality belong to the adversarial-reviewer`, so comment findings are in the adversarial lens's charter by the blind lens's own text; its Major is `likely bug, or correctness that survives only by accident`, which a test instrument's overclaimed reach satisfies.
- `plugins/claude-kit/agents/security-reviewer.md`: no severity list of the same shape; its closing line asks for severity honesty in both directions. Untouched by this plan, per Decision 2's first exception.
- `plugins/claude-kit/skills/responding-to-review/SKILL.md`: five sections (the two sources, how to respond, corroboration across lenses, a clearance is a claim, no performative agreement); nothing on the shape a claim finding's fix takes.
- `plugins/claude-kit/skills/finishing-work/SKILL.md`, step 2: `Major findings: fix or present to me with the tradeoff`; step 3, the final adversarial review. Neither names when a fix round finishing runs ends.
- `plugins/claude-kit/skills/consult/SKILL.md`, the trigger floor, trigger (a): `A second failed attempt at the same problem, whatever its shape: an implementation round, a debugging hypothesis, a review round. Where executing-work's tier-escalation ladder already owns the moment, it governs`. The seesaw is an instance of (a) that no text names as one.
- `plugins/claude-kit/skills/writing-skills/SKILL.md`, `## Know it works before you trust it`: the RED, GREEN and REFACTOR steps, and the paragraph on the probe pair in the kit's own repository, which runs at the section's close over the section's whole change and stands in for the reps where the probe is `ruled`; for a `proposed` probe the after leg alone runs and the reps stand.
- `test/probes/README.md`: one file per governed moment, four to six options, a hand adjudication frozen at the ruling, a cap entry in `test/size-budget.json` per probe; `tools/probe-corpus/probe-file.mjs` owns the file format.
- `test/output-style-parity.test.js`: the sibling for a pinned copy across surfaces, marker-delimited region in the copy, located by lead in the sources, line endings normalized, byte-exact otherwise.
- `test/size-budget.json`, read 2026-09-07 against the worktree: executing-work 21,857 words at a cap of 21,857; finishing-work 17,701 at 17,701; responding-to-review 1,758 at 1,758; adversarial-reviewer 3,070 at 3,070; blind-reviewer 1,119 at 1,119; consult 781 at 781. The ratchet lowers and never rises.
- `kaizen/notes-SCOTT-CLAUDE.md`, 2026-09-03: the loop has no exit for a claims section; fifteen rounds and two consults; the run derived its own condition (a Major is the section's to fix only where it sits in the delta and contradicts an acceptance clause or principle) and asked that step 4 carry one. 2026-09-07 (Worker, scenario-probes sections 3 and 5): thirteen opus rounds on a sonnet prose section, each partly reversing the last on the same three passages. 2026-09-07 (Worker, output-channel eliding finishing): finishing-work names no stopping rule, so two sessions stop in different places.
- `kaizen/notes-NEO-CLAUDE.md`, 2026-09-07, reported by that worker and not re-derived here: seventeen rounds over thirty hours on a three-section plan's section 3; from round 12 every adversarial Major (6, 5, 4, 6, 4, 5 per round) was header prose, a because-string or a test instrument's control reach; a Standing Brief Amendment adopted at round 10 for that class did not stop it; the security reviewer was not dispatched in rounds 13 to 17, so the engine was the sighted adversarial lens plus an orchestrator adopting every Major as owed.
- Operator-tier memory `claims-sections-need-more-review-rounds-than-code-sections`: a section delivering claims about the system does not converge as code does, because a false sentence has no failing test to enumerate it; writing the class into a standing brief does not stop it, the class being invisible to its own author; the fix is a mechanical check or a deletion, never more brief text or more rounds.
- Journal `kit.review.rounds`, 2026-09-01: a Major-count stopping rule misfires; judge convergence on finding composition, not on the count.

## Approach

Key the exit on the class of a finding rather than on its count or its rating. The class definition is three sentences executing-work owns. A behavior finding states a failure scenario, an input or a state on which the code does the wrong thing on a reachable path or a test exercises the wrong thing, so its fix changes what runs. A claim finding states none, no input the sentence names failing today, and its fix changes a sentence and nothing that runs: a comment, a header, a docstring, a test's because-string or title, a test instrument's stated reach. Two exceptions hold a claim finding to a behavior finding's bar: a claim on a security boundary, meaning any of the surfaces step 3's security-reviewer trigger names (input handling, authentication or authorization, SQL construction, secrets or configuration, shell or process execution, a command permission grant composed or widened, a hook that emits an allow or deny decision, an external boundary), owed like a behavior finding whoever raised it; and a claim on a published contract surface, a README, a skill, a charter, a document under `docs/`, owed like one only where the sentence sits inside the section's own delta and contradicts an acceptance criterion or a principle the plan states, or is a pointer that delta left aimed at nothing wherever it sits, every other claim there rating as a claim finding does. That second exception is drawn narrowly on purpose: in the kit's own repository nearly every section edits a skill or a charter, so an exception that kept every sentence on those surfaces at full bar would keep the loop open on exactly the sections this plan exists to close, which is what the 2026-09-03 inbox note's derived condition (delta-internal, contradicts an acceptance clause or a principle) already answered. The class is read at adjudication, before any fix exists, from whether the finding states a failure scenario; the fix then follows the class, and where a finding could be closed either way the orchestrator's choice is recorded in the Chapter, a sentence-fix chosen over a behavior-fix on a finding that stated a failure scenario being a Major justified-not-fixed rather than a claim dispositioned. The terminal condition follows: a section's review loop ends at the first round whose adjudicated findings, each read at its class rather than at the rating it arrived with (a Critical so re-read being the adjudication downgrade the Chapter names), carry no Critical and leave no owed Major neither fixed, routed under the out-of-scope route, nor recorded in the Chapter as justified-not-fixed, and whose fix delta owes no round under the fix-delta bar, an owed Major being a behavior finding or a claim either exception holds to that bar. Claim findings from that round are dispositioned in the same fix round by deletion of the false sentence, by a mechanical check where one is cheap and the claim is worth keeping, or by a Chapter line naming the sentence left standing and why the finding does not hold; a fix delta of prose deletions alone, none dispositioning a finding an exception held to the behavior bar, owes no round of its own, and any other takes the fix-delta bar; a round the below-bar judgment adds by choice is one the delta did not owe, and its findings take the condition like any round's. Severity follows the class in the charters, so the reviewers stop rating a false sentence Major in the ordinary case, and the terminal condition still keys on the class rather than the rating, so a reviewer that ignores the tag cannot reopen the loop. The recurrence rule splits on the same class: a second instance of a behavior class still writes a Standing Brief Amendment; a second instance of a claims class writes a mechanical check or runs a deletion sweep and never an amendment, with the memory record's reason carried into the skill in one sentence, since a rule that gives no reason is the one a later reader relaxes. The one loop the terminal condition does not bound, an owed Major that reverses the prior round's fix on the same passage, is bounded by naming it an instance of the consult skill's trigger (a) at the second reversal on one passage (round N+2 asking to undo what round N+1 did to round N's fix, on the same lines), whose ruling closes the passage; no numeric cap is added, per Decision 3.

The definition lives in executing-work inside a marked region, because agents inherit no skills and so the charters cannot point at it: the adversarial and blind charters carry the region as a designed byte-identical copy, and a parity test pins the set, the same shape the output style's register core takes against the doctrine. responding-to-review and finishing-work point at executing-work rather than copying, since both are skills a session loads. The probe is authored `proposed`, with its answer awaiting the operator's ruling in the rulings batch, so its after leg is evidence and the RED and GREEN reps stand for section 1 as writing-skills states.

## Decisions

Decided 2026-09-07 by the operator at the keyboard of the KIT: Expert session, on the Expert's recommendation.

1. **The exit keys on the class of a finding, never on the reviewer's count or rating.** A count-keyed rule terminates only where the class shrinks with each fix, and a claims class produces new reviewable surface with every fix. The class is decided by what the fix changes, which the orchestrator can read off the fix itself, so the rule holds even against a reviewer that rates every sentence Major.
2. **A claim finding rates Minor under a `[claim]` tag, with two exceptions that keep their bar.** The alternative, leaving the charters unchanged and having only the terminal condition ignore claim Majors, was weighed and loses: it fills the Chapter with justified-not-fixed Majors and puts the whole classification burden on adjudication every round. The exceptions are a claim on a security boundary, keyed on the security-reviewer trigger surfaces so the charters have a predicate rather than a word, and a claim on a published contract surface that sits inside the section's delta and contradicts an acceptance criterion or a plan principle; the second is drawn that narrowly because a file-list exception (every skill, every charter) would keep the loop open on the kit's own prose sections, which are the local instances that surfaced this.
3. **A consult at the second reversal bounds the seesaw; no numeric round cap.** A numeric cap would close a code section with a real behavior Major standing at round N, and the consult skill already exists for a second failed attempt at the same problem. The seesaw is named as an instance of that trigger rather than as a new one, so the consult skill's own text changes by one clause at most.
4. **The definition is owned by executing-work and copied into the two code-reviewer charters under a parity pin.** An agent charter cannot resolve a pointer into a skill, so the copy is unavoidable; pinning it byte-identical is what keeps the copy from becoming a second definition.
5. **A claim finding's fix is a deletion or a mechanical check, never a rewritten sentence.** A rewritten sentence re-enters the class it was written to leave. Where the claim is worth keeping and a check is cheap, the check is the fix; where neither holds, the sentence goes.
6. **Standing Brief Amendments stay the remedy for a behavior class and stop being one for a claims class.** The memory record measured the amendment failing on a claims class twice, once in the module it was adopted for, and the NEO run reproduced it at round 10.
7. **The security reviewer's charter is untouched.** Its findings on a boundary are the first exception by construction, and its closing sentence already asks for severity honesty in both directions.

## Sections of Work

### 1. Executing-work owns the class definition, the terminal condition, the recurrence split and the seesaw exit, and a probe holds the moment. Model: fable

In `plugins/claude-kit/skills/executing-work/SKILL.md`, step 4, split the first paragraph after the sentence that dispositions Critical, Major and Minor (the one ending `rather than this line.`) and insert the class definition there as a continuation block of list item 4: a blank line, the marker `<!-- KIT-CLAIM-CLASS:BEGIN -->` on its own line at the item's three-space continuation indent, the region's three sentences at that same indent, the marker `<!-- KIT-CLAIM-CLASS:END -->` on its own line, a blank line, and then the rest of the original paragraph (`Weigh each finding per...` onward) resuming as the next continuation paragraph. The three sentences are the Approach's: what a behavior finding is (it states a failure scenario, so its fix changes what runs or what a test exercises), what a claim finding is (it states none; its fix changes a sentence and nothing that runs: a comment, a header, a docstring, a test's because-string or title, a test instrument's stated reach), and the two exceptions with the security boundary keyed on step 3's security-reviewer trigger surfaces and the published contract surface keyed on a delta-internal contradiction of an acceptance criterion or a plan principle. The region carries the list indent in this file and none in the charters; section 3's pin dedents before comparing, so the copies are byte-identical after that normalization and no other. Follow the region, in the resumed paragraph or as one bold-led paragraph after it, with the terminal condition: the loop ends at the first round over the latest delta with no Critical and no owed Major, an owed Major being a behavior finding or a claim either exception holds to that bar, other than one the Chapter records as justified-not-fixed; the class is read at adjudication from whether the finding states a failure scenario, before any fix exists, and whether the loop ends is read after the fix round; the fix follows the class, with the orchestrator's choice recorded where a finding could close either way and a sentence-fix on a finding that stated a scenario leaving a Major justified-not-fixed or a Critical unfixed; the round's claim findings are dispositioned in that same fix round by deletion, by a mechanical check where one is cheap and the claim earns keeping, or by a Chapter line naming the sentence left standing and why the finding does not hold; a fix delta of prose deletions alone, none dispositioning a finding an exception held to the behavior bar, owes no round, and any other takes the fix-delta bar; and every finding is read at its class, never at the rating the reviewer gave. Split the recurrence rule on the class: keep its amendment sentence for a behavior class, and add one sentence sending a second instance of a claims class to a mechanical check or a deletion sweep with the reason (the class is invisible to the author who just wrote the sentence, so an amendment reaches no one who can see it). Add one sentence naming the seesaw as an instance of the consult skill's trigger (a): an owed Major's second reversal on one passage (round N+2 undoing what N+1 did to N's fix) mandates a consult, whose adopted ruling closes the passage. In the Chapter template's `Metrics:` line, extend `review rounds` to `review rounds <n>, closed <clean | claim-exit | major-closed>`, read off the last round that carried findings, `major-closed` where that round carried an owed Major, `claim-exit` where it carried a claim finding and no such Major, and `clean` otherwise, a section with no round included, so the backlog's experiments can read how often the exit fires. In `plugins/claude-kit/skills/consult/SKILL.md`, trigger (a), add the seesaw as one named shape of a second failed review round, in a clause and not a sentence, and only if the skill's cap can be paid inside its own text; where it cannot, executing-work's sentence stands alone and the Chapter records that the consult skill was left as it is.

Author `test/probes/review-round-returning-only-claim-majors.md` in the format `tools/probe-corpus/probe-file.mjs` validates, with the ruling line in the `proposed YYYY-MM-DD` form that file requires: the moment is a section's fifth review round returning zero Criticals, five Majors every one of which is a header sentence, a because-string or a test instrument's reach with no failure scenario stated, and two Minors, on a section whose files are scripts and tests, not a published contract surface and not a hook that emits a decision; the options, closed at these six, are close the section after dispositioning the claims in one fix round, run another review round, write a Standing Brief Amendment for the class, escalate the implementer tier, convene a consult, declare BLOCKED; the proposed answer is the first; two shapes, `executing-work` naming that skill alone, and `executing-work-plus-charter` naming it with the adversarial charter and responding-to-review. Add the probe's cap entry to `test/size-budget.json` at the word count it lands at. Pay executing-work's added words from step 4 prose the terminal condition supersedes, keeping every sentence that states a reason: in the adoption-trigger paragraph the reason sentence stays and the worked example that follows it (`the implementer receives the amendment in its brief and looks correct, while a later reviewer...`) is the trim, and in the fix-delta paragraph the founding-shape narrative (`The founding shape is a fix landing a guard...`) is the trim, that being the journey layer the doctrine's state-not-journey rule sends to the journal; the trim is named in the Chapter with its word count, and where those two do not cover the addition the Chapter names what else was taken and why.

Acceptance: the probe's after leg over its first shape, `node tools/probe-corpus/run.mjs --only review-round-returning-only-claim-majors --shape executing-work`, run once at the section's close under the heavy-process claim the role skill's claim protocol states (`skills/role/SKILL.md` under the kit plugin root), returns the proposed answer, recorded as evidence for the rulings batch and not as a pass, the second shape being section 2's to run once it has changed the charter; the RED and GREEN reps writing-skills states, since the probe is proposed: a fresh general agent handed the pre-change step 4 text verbatim and the five-claim-Major round above, asked what the loop does next, runs another round or writes an amendment (RED, its rationalization recorded verbatim), and handed the post-change text closes the section (GREEN), three reps each, and where the pre-change reps close the section in all three the Chapter records that and the section proceeds on the four field incidents under Evidence as its RED, named as such; the size ratchet is green with executing-work's cap lowered to the count it ends at, never raised; `test/probe-set.test.js` is green over the new file; and the terminal condition, the class region, the recurrence split and the seesaw sentence are each locatable by a grep for a phrase the Chapter records.

Files in scope: `plugins/claude-kit/skills/executing-work/SKILL.md`, `plugins/claude-kit/skills/consult/SKILL.md`, `test/probes/review-round-returning-only-claim-majors.md` (new), `test/size-budget.json`.

Tests: the probe is the durable instrument; the ratchet and the probe-set shape test are the gates. The expensive failure is a later wording change that drops the class region's second sentence and returns the loop to its count-keyed reading, which the probe's after leg reports as a moved answer.

### 2. The code-reviewer charters rate a claim finding Minor under its tag, responding-to-review states the shape of its fix, and finishing-work points at the terminal condition. Model: opus

In `plugins/claude-kit/agents/adversarial-reviewer.md` and `plugins/claude-kit/agents/blind-reviewer.md`, add the class region as a copy of section 1's with the list indent removed, between the same two marker lines, placed immediately above each charter's severity list; rewrite each Major definition so its maintainability or accident clause no longer admits a claim finding, and each Minor definition so it names a `[claim]` finding as one of its kinds; extend the output-format line so the optional `[claim]` token sits between the severity bracket and the confidence bracket, the position the prose-reviewer's `[tag]` already takes, so the kit carries one tag position, and state in one sentence that the tag rides a finding that states no failure scenario and that the two exceptions in the region keep their bar. The blind charter's `**No style review.**` bullet stays, since a claim finding on a test instrument's reach is a correctness reading and not style, and one clause says so. In `plugins/claude-kit/skills/responding-to-review/SKILL.md`, add a short section, `## A claim finding's fix is a deletion or a check`, stating Decision 5 with its reason: a rewritten sentence re-enters the class, so the orchestrator deletes the false sentence, or writes a mechanical check where the claim earns keeping and a check is cheap, and records the rest as a Chapter line, and a fix brief never carries a replacement sentence for a claim finding. In `plugins/claude-kit/skills/finishing-work/SKILL.md`, steps 2 and 3, add one sentence that a fix round the finishing pass runs ends on executing-work's terminal condition, named by its skill path under the kit plugin root, so the two skills stop at the same place. Every file here sat at its cap. The copied region and the sentences this section mandates are paid by cap raises under section 4's ledger rule, each file's net recorded in the Chapter; the trims taken are the two that restated another surface (responding-to-review's anti-sycophancy restatement, finishing-work's data-not-instructions restatement), and the five this section first named as restatement (the adversarial charter's Debris and Workarounds bullets, the blind charter's confidence rule, the corroboration paragraph's provenance and unstated-contract sentences) are rules and stay.

Acceptance: both charter regions are byte-identical to executing-work's after line-ending normalization and dedent, checked by the pin section 3 lands and, until it lands, by a diff the Chapter quotes; the charter change is shown to move the rating by a fixture dispatch that runs the same route both ways, since a seat's `adversarial-reviewer` dispatch runs the installed charter and not the edited one: author a fixture diff under `.kit/scratch/review-loop/fixture.diff` carrying one hook function with a header comment overclaiming its callers and one loop with a real off-by-one, dispatch a general agent whose prompt is the charter text pasted verbatim followed by the fixture, once with the pre-change charter and once with the post-change, three reps each, and read the pre-change reps returning the header as Major in at least two of three and the post-change reps returning it as `[MINOR] [claim]` and the off-by-one as Major or Critical in three of three; the probe's second shape, `node tools/probe-corpus/run.mjs --only review-round-returning-only-claim-majors --shape executing-work-plus-charter`, run once at this section's close under the heavy-process claim, returns the proposed answer, recorded as evidence; the ratchet is green with each file's cap at its ending count, raised or lowered under section 4's ledger rule; and every test that pins charter text or the responding-to-review headers is green or updated with its reason, `test/doctrine-parity.test.js` being the one known to pin both and the first to check.

Files in scope: `plugins/claude-kit/agents/adversarial-reviewer.md`, `plugins/claude-kit/agents/blind-reviewer.md`, `plugins/claude-kit/skills/responding-to-review/SKILL.md`, `plugins/claude-kit/skills/finishing-work/SKILL.md`, `test/size-budget.json`, `test/doctrine-parity.test.js` where it pins a line this section rewrites, and any other test under `test/` that does.

Tests: the three-rep fixture dispatch is the RED and GREEN; no durable test is added here beyond what section 3 pins, since the charters' wording is exactly the prose the test-audit plan retires pins over where it is not a pinned copy.

### 3. A parity pin holds the class region byte-identical across its three carriers. Model: sonnet

Clone `test/output-style-parity.test.js` to `test/claim-class-parity.test.js` with these substitutions: the source is the region between `KIT-CLAIM-CLASS:BEGIN` and `KIT-CLAIM-CLASS:END` in `plugins/claude-kit/skills/executing-work/SKILL.md`; the copies are the same-marked regions in `plugins/claude-kit/agents/adversarial-reviewer.md` and `plugins/claude-kit/agents/blind-reviewer.md`; the frontmatter strip and the line-ending normalization are the sibling's, plus one normalization the sibling lacks, a dedent that strips each region's common leading whitespace line by line, because the owner's region sits inside a numbered list item at its continuation indent and the charters' copies sit at column one; there are no frontmatter pins. The test fails naming the file and the first differing line, and it fails when a marker is absent, naming which. Add a control case in the same file that alters one word of a copy in a temporary buffer and asserts the comparison reports it, so the pin's silence is earned. Add the file's cap entry to `test/size-budget.json`.

Acceptance: the test is green on the tree section 2 left; red with the named file and line when one word of either copy is changed in a scratch copy of the tree, shown once in the Chapter and restored from the pre-probe copy per the doctrine's probe rule; the ratchet is green.

Files in scope: `test/claim-class-parity.test.js` (new), `test/size-budget.json`.

Tests: this section is the test. The expensive failure is a charter edit that drifts the copy from the owner, which is the second definition Decision 4 exists to prevent.

### 4. The size budget is a ledger rather than a ceiling: growth is reviewed and weighed, never prohibited, and kit-size gains a sync verb that moves caps both ways. Model: sonnet

Ruled 2026-09-08 by the operator at the keyboard, mid-run, and recorded here because the Decisions block froze at approval: Subtraction Bars exists to encourage and reward cutting, never to forbid adding; growth is a signal to confirm and validate value, reviewed and weighed like any other prose and allowed where it earns its place; shrinking is the benefit to take whenever a passage is being reworded. The mechanism already behaves that way (`test/size-ratchet.test.js` reds when a curated file outgrows the cap `test/size-budget.json` holds, and editing the cap in the same change is what makes growth visible rather than forbidden); what hardened it into a prohibition was the norm around it: the archived subtraction-bars plan's framing of a raise as an exception, plan headers and briefs saying every added word is paid by a trim in the same file, and reviewers treating a raise as a finding. In `plugins/claude-kit/skills/writing-skills/SKILL.md`, immediately after the `**The kit stays lean.**` bullet, add one bullet that owns the moment a curated file grows, locatable by the phrase `ledger rather than a ceiling`: the budget file and the ratchet test make growth visible, never forbidden; a file that grows raises its cap in the same change and one that shrinks lowers it, by the sync verb below; the Chapter's Delta line is where the net is read; a raise is never a finding on its own, and what a reviewer weighs is whether the added words earn their place; and a session rewording a passage takes the shrink where one is available. In `plugins/claude-kit/scripts/kit-size.js`, add a `sync` verb beside `check`, `report` and `init`: it rewrites every measured entry the budget file already holds to its file's current size, in both directions, prints one line per cap it moved with the old value, the new value and the direction, prints the count of entries it left unchanged, takes the same `--repo` and `--budget` flags and the same no-corpus refusals as `check`, adds no entry for a path the budget lacks (that is `init`'s job, and `sync` names such a path in one line rather than silently skipping it), and preserves the file's line endings and key order. In `test/size-ratchet.test.js`, in the file's existing child-process harness, add cases: `sync` raises a cap sitting below its file's size and lowers one sitting above and prints both moves with their directions; leaves an entry already at size unchanged and counts it; leaves the budget file byte-identical when nothing moved; and names an unlisted measured path without adding it. In `plugins/claude-kit/skills/operating-instructions/references/ownership-map.md`, add a row for the moment (a curated file growing, and who moves its cap) owned by `writing-skills` (The kit stays lean), pointing at `test/size-ratchet.test.js` and `scripts/kit-size.js`. Words this section adds are paid by nothing: it is the first instance of its own rule, the orchestrator runs `sync` at the section's close, and the Chapter records the net. Two records are the main thread's at close: `docs/backlog.md`'s entry on the unowned ratchet moment (2026-09-05) retires to the quarter snapshot naming the row that closed it, and the plan's own pays-with-a-trim sentences are amended (done at this section's append).

Acceptance: the new test cases were watched red first against a `sync` that writes nothing (RED recorded in the Chapter) and are green after; `node plugins/claude-kit/scripts/kit-size.js sync --repo .` run once at the section's close moves every cap this plan's sections grew or shrank to its current size, prints each move, and `test/size-ratchet.test.js` is green after it; the writing-skills bullet is locatable by its phrase; the ownership-map row is present; the lane over `test/size-ratchet.test.js`, `test/doctrine-parity.test.js`, `test/output-style-parity.test.js` and every other test file that reads writing-skills, the ownership map or kit-size is green from the worktree root, exit code read from the run.

Files in scope: `plugins/claude-kit/scripts/kit-size.js`, `test/size-ratchet.test.js`, `plugins/claude-kit/skills/writing-skills/SKILL.md`, `plugins/claude-kit/skills/operating-instructions/references/ownership-map.md`, `test/size-budget.json` (the sync at close, the main thread's).

Tests: the sync verb's cases are the durable cover; the ratchet test itself is unchanged in what it gates, so the expensive failure this section guards against is a later reader of the archived plan reinstating the prohibition, which the writing-skills bullet now answers as the owner.

### 5. The curated documents that describe the size reader name its sync verb and the ledger rule. Model: sonnet
Locus: inline

Surfaced by section 4's review round 1 (the adversarial and security lenses) and appended under executing-work's out-of-scope route: four curated documents outside section 4's file list still describe `kit-size.js` as a three-verb reader whose only write is `init`, and two of them still state the ceiling reading of a cap that section 4 retired. In `docs/architecture.md`, the size-ratchet paragraph: name `sync` beside `check`, `report` and `init`, and reword the sentence opening `Caps carry no headroom by design` to the ledger rule, pointing at the writing-skills bullet as the owner. In `docs/README.md`, the `architecture.md` entry's clause `with a raise riding in the diff as a deliberate edit to the budget and a lowering belonging to the audits`: reword to the ledger rule. In `docs/security-model.md`, the paragraph opening `**Its third verb writes`: state that the reader has two write verbs, `init` and `sync`, the containment rule binding both whatever `--budget` names, sync's write landing on the containment-resolved path by rename from a sibling temporary file, and sync's refusal over any check failure other than an over-cap file or a missing cap. In `README.md`, the `kit-size.js` inventory entry: add `sync`. Each edit takes the paragraph as its unit per writing-skills, and shrinks where the rewording allows. No code changes.

Acceptance: a grep over `docs/`, `README.md` and `plugins/claude-kit/` for `belongs to the audits`, `belonging to the audits` and `Its third verb writes` returns nothing, with a control grep for `sync` over the four files returning a hit in each; every test file that reads any of the four documents is green from the worktree root, exit code read from the run.

Files in scope: `docs/architecture.md`, `docs/README.md`, `docs/security-model.md`, `README.md`.

Tests: none added; the section is trivial and self-contained, so its per-section reviewer pair is skipped under executing-work's trivial-section clause, recorded on its Chapter's review line, and the finishing pass's docs curation and adversarial review read the four documents whole.

## Out of Scope

- A numeric round cap of any kind (Decision 3).
- The security reviewer's charter (Decision 7).
- The prose-reviewer's bar, which applies to documents where the sentence is the product and stays as it is.
- The test-audit plan's retirement of wording pins; the pin section 3 adds is over a designed copy, the class that plan keeps.
- The corpus audit's sweep of the kit's prose for density, which may later trim the same skills further; this plan trims only what it supersedes.
- Repairing the review loop's behavior on the NEO-CLAUDE section that surfaced this; that section's disposition is its own worker's and its operator's.

## Assumptions

- assumed 2026-09-07 (default): Commit-and-Push, the kit's default and the worker queue's model; reversal: a header edit before arming.
- assumed 2026-09-07 (source: the operator's placement ruling in the authoring session): the plan runs behind the plan in flight in the worker's queue, inserted before this plan's run by the Expert seat's hand edit of the goal state with the condition recomposed through the goal library and a backup kept, since the CLI has no insert verb and a bare re-arm would rebind the leash; the executing session performs no insertion and finds the plan already queued; reversal: restore the backup or move the entry.
- assumed 2026-09-07 (source: `test/size-budget.json` read against the worktree): every file in scope is at its cap, so each section's trims are part of its work rather than a finding; reversal: none needed, a section whose file has headroom simply trims less and says so. Superseded 2026-09-08 by the operator's ledger ruling recorded at section 4: a file whose additions it cannot pay raises its cap in the same change, and the raise is weighed rather than forbidden.
- assumed 2026-09-07 (source: `plugins/claude-kit/skills/writing-skills/SKILL.md`, the probe-pair paragraph): a proposed probe's after leg is evidence for the rulings batch and the RED and GREEN reps stand for the skill change; reversal: none, a pointer.
- assumed 2026-09-07 (source: the operator-tier memory `a-seat-runs-the-installed-skill-not-the-committed-one`): the change reaches a session only through the installed plugin, so the running worker's own remaining sections keep today's loop until a plugin update and a session start; reversal: none, the Operator Verification names the update.

## Operator Verification

- Run `claude plugin update` after this plan closes, so sessions started after it execute the terminal condition; the worker session running the queue keeps the installed copy it started with. What reopens the work: a section closed after the update whose Chapter records four or more rounds with every Major in the last two rated `[claim]`.
- Rule on the proposed probe in the next rulings batch. What reopens the work: a ruling other than the proposed answer, which is a re-derivation of the terminal condition rather than a probe fix.

## Open Questions

None.

## Related

- Operator-tier memory `claims-sections-need-more-review-rounds-than-code-sections`, the structural argument this plan encodes.
- `kaizen/notes-SCOTT-CLAUDE.md`, 2026-09-03 and the two 2026-09-07 review-loop notes; `kaizen/notes-NEO-CLAUDE.md`, the 2026-09-07 review-loop note. This plan dispositions all four.
- `docs/plans/claude-kit_test-audit_spec_v1.md`: retires wording pins over prose that is not a pinned copy; section 3's pin is over a designed copy and stays.
- `docs/plans/claude-kit_corpus-audit_spec_v1.md`: the cold-reader sweep whose bloat lane may trim the same skills further, after this plan.
- `docs/archive/claude-kit_scenario-probes_spec_v1.md`: the probe instrument section 1 adds a moment to.

## Chapters

### Chapter 1 - 2026-09-08
Completed: 1. Executing-work owns the class definition, the terminal condition, the recurrence split and the seesaw exit, and a probe holds the moment.
Implemented By: implementer-fable (Fable-led session, no override), with fix rounds 1 through 7 applied by the main session.
Metrics: review rounds 7, closed major-closed; NEEDS_CONTEXT 0; escalations 0; consults 1
Decisions / Surprises: The header read Ready at the run's start and now reads In Progress. The run's locus changed on the operator's keyboard instruction of 2026-09-08: the KIT: Expert session executes this plan in the disjoint worktree `.kit/scratch/expert/wt-rle` on main, and the plan was removed from the worker's armed queue by a hand edit of that checkout's goal state with the condition recomposed through the goal library and a backup beside it (`.kit/goal-state.json.pre-remove-review-loop-2026-09-08T00-31-36-722Z`), so the spec's second assumption (the executing session finds the plan already queued) no longer holds; the run is unleashed, because the goal and checkpoint hooks resolve the session's own cwd, which is the main checkout holding the worker's leash, so no checkpoint step ran and this Chapter is the boundary record. Mid-section the worker's plan merged to origin/main (5cd2a68); the worktree was fast-forwarded under the unstaged work by a tagged stash, with two conflicts resolved (the size budget keeps the worker's values on lines this section never touched, the backlog keeps both new entries) and the stash dropped. The class region is three sentences and names the eight security surfaces step 3's trigger names, not the six the Approach listed; the spec's Approach and section text were amended to what shipped (eight surfaces, the tiebreak clause, the pointer case, the three `closed` values read off the last round that carried findings, the terminal condition's routed disposition and by-choice clause, the probe's script files). The terminal condition is one condition read after the fix round: no Critical, no owed Major neither fixed, routed nor justified-not-fixed, and a fix delta owing no round under the fix-delta bar; a behavior finding is owed whatever rating it arrived with, since every finding is read at its class. Seven review rounds, the count this plan exists to bound, on the section that writes the bound: rounds 1 to 4 each found a hole in the rule's logic (a justified Major never terminating, the security exception missing from the owed-Major definition, a mechanical-check fix contradicting "changes nothing that runs", a deletion carve-out swallowing the exception beside it, three doctrine-parity pins broken by trims), rounds 5 to 7 each found one or two seams the previous fix introduced plus claims; the consult convened after round 6 (later than trigger (a)'s second failed round, recorded as the miss it was) ruled to close after round 7 with the `closed` legend simplified by deletion and a discriminator for the residue: a finding is a defect only where it names a run input on which the text yields a different next action from the Approach, and the residue is fixed by deletion or recorded, never by an added sentence. Round 7's residue under that discriminator, recorded rather than fixed: a behavior finding rated Minor is read at its class and so takes the terminal condition rather than the Minor line (the Minor line governs claim-class Minors); a correction of a claim on a published surface is regulated by the writing-skills paragraph rule and is the disposition of an owed claim, while a non-owed claim's disposition stays deletion, check, Chapter line or route; the consult skill's seesaw clause carries no owed-Major qualifier because that file's cap has no room and executing-work's sentence carries it; the region's markers have no partner copy or pin until sections 2 and 3 land them; the region's security list is a designed copy of step 3's (an agent charter resolves no pointer), routed to `docs/backlog.md` as a shape-matched pin to add; a comment deletion inside a security-surface file is read as a claim on the boundary and keeps its bar. Lens reversals, each stopping at the first: the region's "where the security reviewer's judgment stands" clause was removed at the blind lens's request in round 2 and its return asked by the adversarial lens in round 3 (stands removed: the exception keeps the bar whoever raised the claim); the adoption trigger's reason sentence was dropped in round 1 at one lens's option and restored in one-clause form at the other's in round 4. Trims in executing-work beyond the two the spec named, each with its count and why, the reason clauses among them marked: the implementer's step 4 trims, "The triggers name the shapes worth" 19 (restated the preceding class closure), "folded into every later dispatch brief" 16 (step 1 owns the delivery), "and finding it was never the" 8 (flourish), the handoff-is-first-class clause 11 and the backlog-ranks-last sentence 26 (reason clauses; both restate the doctrine's found-work rule, which every session loads), "the full set of reviewers the" 18 (restates step 1's round definition), the destination preview 29 (each bullet states its own condition), the arrival-path enumeration 24 (later compressed further, 19), "Expect most raw findings to be" 16 (calibration the region now carries), "This is the default rather than" 18 (reason clause restating the bullet's lead), "That is a prohibition, not a" 7 twice (emphasis), "never carried past the round that" 8 (restates the preceding clause), "Two instances of a finding class means" 19 (the recurrence split now carries the ground; step 1's pointer repointed to the recurrence rule); the implementer's step 3 trims, the diff-describing enumeration 18, "Their fixes land in step 4" 26 (restored verbatim in round 4, pinned), the tier ladder example 15 (the one-tier-up rule and the effort table carry it), "because reading without the intent story" 10 (reason clause duplicating the code pair's three sentences earlier), "Record the document pair on the" 16 (restored in round 3 as a clause outside the ladder predicate after round 4 found the merge had widened that predicate), "and its absence from a blind" 14, the defect-class examples 13, the effort-table preview 22, the unavailability pointer 13 (restored verbatim in round 4, pinned), the amendments-line restatement 26; the main session's trims across the fix rounds, the fold-predicate antecedent 17 (restored verbatim in round 4, pinned), the dangling reason sentence 17 (restored in one-clause form), "a sibling speaking the same shape" 6, the fold-edit examples 6, "and a hallucinated or poisoned line dies" 11 (reason clause; the hypothesis rule's purpose is the doctrine's), the Delegating-section remark 15, the off-limits examples 11, "and diff-describing framing is exactly that shape" 8, the exhaustive-by-construction tail 20, the Workflow-route enumeration 67 and the route's contract restatement 45 (the table and the contracts above carry both; the captured-diff bar was put back on step 3's blind sentence in round 6), the contention examples 24, the predicate-is-a-property clause 15, "Correctly out of scope means correctly outside" 11 (in tension with the routed-closes reading), the below-fable override restatement 24, the property-test parenthetical 12, "named so the table is whole" 6, "whatever rating the claim arrived with" 6, "at any rating" 3, "the security reviewer's judgment stands" 6. Consult's trim: "A session that cannot state its decision plainly enough to brief has learned something already." 15, the observation after the reason sentence. Executing-work's cap moved 21857 to 21759 while the section added roughly 500 words; the operator's mid-run question on the ratchet is answered in the close-out: the archived subtraction-bars plan's Decision 1 permits a deliberate, visible raise, and none was taken here. Probe pair reading: the probe is proposed, so RED and GREEN reps ran per writing-skills: three sonnet general agents handed the pre-change step 4 text and the five-claim-Major round answered close, amend, close (rep 2's rationalization verbatim: "repeated findings of the same class signal the dispatch-brief content itself is generating the defect, so the fix is a Standing Brief Amendment"), so the reps do not reproduce the field failure cleanly and the RED rests on the four field incidents under Evidence plus that reading; three reps handed the post-change text answered close, close, close, each quoting the disposition sentence (GREEN). Section 2's RED was taken in this section's span: three opus general agents handed the pre-change adversarial charter and the fixture diff at `.kit/scratch/review-loop/fixture.diff` rated the header-comment overclaim Major in two of three (one Minor) and the off-by-one Critical or Major in three of three, at 00:50Z to 00:53Z. The probe's after leg over its first shape ran twice, once at 01:49Z over the round 5 text and once at 02:03Z over the closing text, both returning the proposed answer. The probe file's option slug and scenario avoid every term the change introduces. Memory correction: the project memory `the-probe-runner-is-a-paid-box-claimed-run` already carried the worker's correction (no box claim, `--touching`); its index line was brought in step with the body. Kaizen: one line appended to the inbox on the review loop over rule text.
Assumptions: assumed 2026-09-08 (default, section 1): the RED and GREEN reps run as sonnet general agents and the charter reps as opus general agents, the cheapest tiers that read the text faithfully; reversal: rerun at another tier. assumed 2026-09-08 (default, section 1): the probe's tier field is opus, the corpus rule for a moment only an orchestrator meets; reversal: a one-word swap. assumed 2026-09-08 (source: the operator-tier memories on the goal and checkpoint hooks resolving the session cwd, section 1): the run is unleashed in the worktree and the Chapter is the boundary record; reversal: arm from a session whose cwd is the worktree.
Review Findings: review: code pair at fable, Agent tool, seven rounds; security: not dispatched, the section touching none of its trigger surfaces. Round 1: 3 Majors adversarial, 5 blind, all addressed. Round 2: 4 and 4, addressed. Round 3: 2 and 4, addressed; 2 Minors recorded (reason drops; the security-reviewer clause). Round 4: 1 Critical (three doctrine-parity pins) fixed by restoring the pinned sentences verbatim; 5 and 4 Majors addressed; probe files and the pointer clause amended into the spec. Round 5: adversarial approved with concerns; 1 blind Major addressed. Round 6: adversarial approved with concerns, its Major addressed; 4 blind Majors addressed. Round 7: 1 adversarial Major (the `closed` legend) fixed by deletion per the consult; 3 blind Majors, one fixed by deletion ("at any rating"), two recorded under the consult's discriminator; the step 1 pointer repointed. Minors across the rounds recorded above.
Stamps: adjudicated 4, stamped 4 (the-probe-runner-is-a-paid-box-claimed-run, kit-goal-inert-when-session-cwd-is-not-project-root, kit-compact-checkpoint-lapses-from-a-worktree, forward-resource-arrangements-into-dispatch-briefs); the close sweep over 4h listed no live record awaiting a stamp.
Gate: targeted lane `node --test test/size-ratchet.test.js test/probe-set.test.js test/doctrine-parity.test.js test/output-style-parity.test.js test/chapter-boundary-nudge.test.js test/compact-deferral-nudge.test.js test/kit-goal-stop.test.js test/probe-runner.test.js` (every test file that reads the two skills, plus the ratchet and the probe-set shape test) 574/571/0/3 exit 0 read from the run's own marker (`.kit/scratch/review-loop/lane-s1m.exit`) at 02:01Z on 2026-09-08 in the worktree `.kit/scratch/expert/wt-rle` at 5cd2a68 with the section unstaged, under this session's heavy-process claim; the three skips are environmental (a symbolic-link case this box refuses); baseline on the same lane: 572/569/0/3 before the worker's merge landed two tests, so the delta is +2 tests from the merge and no red; earlier lanes on the way here, each under a claim: 172/171/0/1 on the two-file lane at the implementer's delivery, 572/566/3/3 exit 1 at round 4 (the three pins), 574/571/0/3 after every later fix round. Whole gate before the push to main (main being the install surface): `node --test test/*.test.js` 3483/3468/3/12 exit 1 read from the run's own marker (`.kit/scratch/review-loop/whole-s1.exit`) at 02:19Z on 2026-09-08 in the worktree at 5cd2a68 with the section unstaged, under this session's claim, 592 s; the three reds: `a pinned directory too long to name faithfully stands the session down` (test/memory-session.test.js), the permanent box-caused red the suite baseline memory names; `loadIndex answers a status, never a throw, for a cwd the store refuses to name` (test/kit-sidecar-memory-index.test.js) and `the cross-store hit line has one composer` (test/memq.test.js), both in files this section never touched, both reproduced on a second whole run from the worktree and both green on the control run of those two files from the main checkout at 686b947 (724/724 exit 0 under this session's claim), the three files being byte-identical between the two trees, so the two are artifacts of running the suite from a linked worktree (memq resolves the worktree to the main checkout's store) and not regressions; baseline on the whole gate: the worker's finishing run before its merge, read from its plan's archived Chapters, carried the one permanent red, so the delta against it is +2 worktree-run artifacts and no regression in any file this section changed. Contention lane: none defined for this repo. Probe set: `probe-corpus: 1 pairs, 0 mismatches (0 on proposed rulings, 0 designed), 0 errors, exit 0, tier opus, report .kit/probe-runs/2026-09-08T02-03-20-639Z/report.md`, the after leg over the shape `executing-work` at 02:03Z on the closing text, no warning lines; a before leg does not apply to a new probe.
Next: 2. The code-reviewer charters rate a claim finding Minor under its tag, responding-to-review states the shape of its fix, and finishing-work points at the terminal condition.
Commit Model: Commit-and-Push
Delta: measured at 2026-09-08T02:03:47Z in the worktree `.kit/scratch/expert/wt-rle` at 5cd2a68 with the section unstaged, no foreign process on the box

```
repository: wt-rle
plugins/claude-kit/skills/consult/SKILL.md: 776 words, cap 776, -5
plugins/claude-kit/skills/executing-work/SKILL.md: 21759 words, cap 21759, -98
test/probes/review-round-returning-only-claim-majors.md: 345 words, cap 345, untracked so HEAD holds no blob and the totals below exclude it
words: 218831 of cap 218832 across 58 curated files
test lines: 113748 of cap 113748 across 58 test files
tests: 3406
changed paths under no measured root: 2 (2 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
excluded from those totals, untracked under a measured root: test/probes/review-round-returning-only-claim-majors.md
```

### Interim board 1 - 2026-09-08
In flight: section 2 at review round 2, not yet dispatched at this writing. Round 1 (code pair at fable, Agent tool) returned adversarial 4 Majors 3 Minors and blind 2 Majors 5 Minors; adjudicated under the section's own class, eight items owed and fixed (the adversarial Major line's "later change" clause that re-admitted a claim finding, the blind posture and output line contradicting a `[claim]`, a lens clause on which exception legs the blind copy's reader can observe, the responding-to-review section's missing fourth disposition and its unbounded no-replacement rule, the blind confidence rule the spec's trim removed, both Minor lines' unqualified `[claim]`, the finishing pointer's plugin-root suffix and its changeset clause), one trim reversed on a false ground (Workarounds), two recorded (the parity pin is section 3's; the spec's "file-audit line" amended above). Section 4 appended this boundary on the operator's ledger ruling and not yet dispatched. Live dispatches: none at this writing (round 1's two reviewers and the three post-change charter reps completed; the reps rated the fixture's overclaiming header `[MINOR] [claim]` and its off-by-one Critical or Major, three of three, over the round 1 text).
Gate baseline: targeted lane `node --test test/size-ratchet.test.js test/doctrine-parity.test.js test/output-style-parity.test.js test/docs-write-guard.test.js test/kit-sidecar-capture.test.js test/memory-recognition-nudge.test.js test/readonly-agent-guard.test.js test/kit-goal-statusline.test.js test/memory-session.test.js` 655/652/1/2 exit 1 read from `.kit/scratch/review-loop/s2/lane-fix1.exit` at 03:32Z on 2026-09-08 in the worktree `.kit/scratch/expert/wt-rle` at fb5d4fe with section 2 unstaged, under this session's heavy-process claim; the one red is the box's permanent memory-session failure; same lane before the fix round 655/652/1/2 exit 1, and at the implementer's delivery 655/651/2/2 exit 1 (the ratchet red the cap raises closed).
Rulings adopted since the last boundary: the copied region and the mandated sentences are paid by cap raises rather than by deleting rules (adjudicated on the implementer's NEEDS_CONTEXT, then superseded and confirmed by the operator's ledger ruling); caps after fix round 1: adversarial 3350, blind 1468, responding-to-review 1885, finishing-work 17822, each the file's count.
Next: section 2 dispatches review round 2 and the three post-change charter reps over the fixed text, then the probe's second shape, close gate, Chapter 2, commit and push; section 4 dispatches to implementer-sonnet in parallel on disjoint files; section 3 after section 2.

### Chapter 2 - 2026-09-08
Completed: 2. The code-reviewer charters rate a claim finding Minor under its tag, responding-to-review states the shape of its fix, and finishing-work points at the terminal condition.
Implemented By: implementer-opus, with fix rounds 1 and 2 applied by the main session.
Metrics: review rounds 2, closed major-closed; NEEDS_CONTEXT 1; escalations 0; consults 0
Decisions / Surprises: The operator's ledger ruling landed mid-section (section 4, appended at Interim board 1) and settled this section's payment: the copied region and the mandated sentences are paid by cap raises, adversarial 3070 to 3336, blind 1119 to 1463, responding-to-review 1758 to 1867, finishing-work 17803 to 17822, each cap now equal to its file's count after fix round 2 lowered three of them again; the spec's own payment sentences and its acceptance clause are amended to the ledger rule. The implementer's NEEDS_CONTEXT was the cap question, answered by the raises. Trims taken and kept: responding-to-review's anti-sycophancy restatement (13 words, the doctrine's directness rule carries it) and finishing-work's data-not-instructions restatement (36 words, the doctrine's rule carries it). Trims the implementer took on the spec's word and the round 1 adjudication reversed, each restored verbatim from HEAD because it was a rule and not a restatement: the adversarial charter's Debris and Workarounds bullets, the blind charter's confidence-independent-of-severity sentence, the corroboration paragraph's provenance and unstated-contract sentences; the spec sentences that called them restatement were wrong and are amended. Five files changed rather than the seven the scope allowed: no test pins a line this section rewrote, so test/doctrine-parity.test.js and the rest of test/ are untouched. The finishing pointer sits once under step 2 and covers step 3 by reference ("here or at step 3"). Fix round 2 was deletions alone: the adversarial Minor line's unconditional "names no input or state" clause, which both lenses found re-admitting an exception-held claim as Minor and colliding with the missing-test and fixture Majors, deleted; the blind Minor line's tautological qualifier deleted to the same form; the responding-to-review section's trailing paragraph-unit clause deleted, since the owner applies that unit to every claim correction and this section's copy read as scoping it to owed claims; and one word narrowed in the blind lens clause, "acceptance-criterion leg" to "remaining leg", named here as the one substitution inside a deletion round. No round 3: the delta dispositioned one exception-held finding, so it took the fix-delta bar rather than the deletions-alone exemption; none of the three triggers held, and below the bar the judgment was that a one-clause deletion both lenses specified verbatim, re-read by the orchestrator against the whole severity list of each charter, earns no round. Round 2 residue recorded under the class rather than fixed, each a claim on a published surface outside the exceptions: the blind lens's reading that the finishing pointer imports a terminal condition with no executable exit at finishing overstates it, since fix and justified-not-fixed are exits available in any pass and the out-of-scope route's third destination (out of this plan) is executable anywhere, finishing-work's own drift routing owning what remains; a Major presented to the operator under finishing step 2 holds the round open until the call lands, which is the intended reading and not a fourth state; the responding-to-review heading names the two fixes because a Chapter line and the route change no file; the blind "No style review" clause carves out exactly what the spec named, and a false code comment stays the adversarial lens's, which reads whole files and the spec; the replacement a fix brief never carries is the false sentence's, and the neighbours' coherence under the owner's paragraph re-read is not one; the finishing trim also removed the guard clause on the session being the one principal the read-only guard does not govern, and no next action changes without it. The missing parity pin, raised by the blind lens in both rounds, is section 3's deliverable. Probe pair reading: the section's RED and GREEN were the charter reps rather than a probe leg. RED, taken in section 1's span (Chapter 1): three opus general agents handed the pre-change adversarial charter and the fixture diff rated the header-comment overclaim Major in two of three. GREEN: three opus general agents handed the fixed charter (the round 1 text) and the same fixture rated the header `[MINOR] [claim]` and the off-by-one Major in three of three, at 03:10Z to 03:14Z on 2026-09-08; the fix round 2 delta narrowed the Minor line and touched neither the tag paragraph nor the region the reps rated by, so the reading stands for the closing text. The probe's second shape ran once at the close over the closing text, its line on the Gate line below.
Assumptions: assumed 2026-09-08 (default, section 2): the GREEN reps over the round 1 text stand for the closing text because fix round 2 only removed clauses from the Minor lines; reversal: three more reps at opus over the closing text. assumed 2026-09-08 (source: the section's own class, section 2): a fix delta of deletions both lenses specified verbatim earns no round of its own below the fix-delta bar; reversal: dispatch round 3.
Review Findings: review: code pair at fable, Agent tool, two rounds; security: not dispatched, the section touching none of its trigger surfaces. Round 1: adversarial 4 Majors 3 Minors, blind 2 Majors 5 Minors; eight owed items fixed (recorded at Interim board 1), one trim reversed, two recorded. Round 2: adversarial 1 Major, owed and fixed by deletion, 4 Minors (two deletions, one one-word narrowing, one recorded); blind 3 Majors (the same owed Major, fixed; the finishing exit reading, recorded with its reason above; the parity pin, section 3's), 4 Minors (one closed by the responding-to-review deletion, three recorded above).
Stamps: adjudicated 4, stamped 4 (claims-sections-need-more-review-rounds-than-code-sections, which steered the deletion-only fix shape; workflow-parallel-caps-at-two and a-queued-agent-looks-identical-to-one-that-never-started, which set the wait on section 4's three-lens round; a-safeguard-and-the-act-it-guards-must-share-granularity, whose branch-scoped read ran before this section's push); the sweep over 3h listed the first and the last.
Gate: targeted lane `node --test test/size-ratchet.test.js test/doctrine-parity.test.js test/output-style-parity.test.js test/docs-write-guard.test.js test/kit-sidecar-capture.test.js test/memory-recognition-nudge.test.js test/readonly-agent-guard.test.js test/kit-goal-statusline.test.js test/memory-session.test.js` 658/654/2/2 exit 1 read from `.kit/scratch/review-loop/s2/lane-fix2.exit` at 2026-09-08T03:22:02.513Z in the worktree `.kit/scratch/expert/wt-rle` at 8be59b4 with sections 2 and 4 unstaged, under this session's heavy-process claim; the reds: `a pinned directory too long to name faithfully stands the session down`, `this repository is inside its size budget, with every tracked file under a measured root classified`, the first the box's permanent memory-session failure and the second the ratchet naming exactly section 4's three grown files (writing-skills, the ownership map, test/size-ratchet.test.js), whose caps section 4's close syncs; baseline on the same lane 655/652/1/2 exit 1 (Interim board 1), so the delta is +3 tests (section 4's sync cases) and +1 red (section 4's in-flight over-cap), none in a file this section changed. Whole gate before the push to main (main being the install surface): `node --test test/*.test.js` 3486/3470/4/12 exit 1 read from `.kit/scratch/review-loop/s2/whole-s2.exit`, started 2026-09-08T03:25:46.109Z and ended 2026-09-08T03:33:54.045Z in the worktree at 8be59b4 with sections 2 and 4 unstaged, under this session's claim; the reds: `loadIndex answers a status, never a throw, for a cwd the store refuses to name`, `a pinned directory too long to name faithfully stands the session down`, `the cross-store hit line has one composer: the provenance label is read inside it alone, and every surface printing the line composes through it`, `this repository is inside its size budget, with every tracked file under a measured root classified`; baseline on the whole gate 3483/3468/3/12 exit 1 (Chapter 1: the permanent box red plus the two linked-worktree artifacts in kit-sidecar-memory-index and memq), so the delta is the tests section 4 added and its in-flight over-cap red, and no regression in a file this section changed. Contention lane: none defined for this repo. Probe set: `probe-corpus: 1 pairs, 0 mismatches (0 on proposed rulings, 0 designed), 0 errors, exit 0, tier opus, report .kit/probe-runs/2026-09-08T03-24-53-196Z/report.md`, no warning lines, the after leg over the shape `executing-work-plus-charter` at the close on the closing text; a before leg does not apply to a new probe.
Next: 3. A parity pin holds the class region byte-identical across its three carriers.
Commit Model: Commit-and-Push
Delta: measured at 2026-09-08T03:34:39Z in the worktree `.kit/scratch/expert/wt-rle` at 8be59b4 with sections 2 and 4 unstaged, under this session's heavy-process claim

```
repository: wt-rle
plugins/claude-kit/agents/adversarial-reviewer.md: 3336 words, cap 3336, +266
plugins/claude-kit/agents/blind-reviewer.md: 1463 words, cap 1463, +344
plugins/claude-kit/skills/finishing-work/SKILL.md: 17822 words, cap 17822, +19
plugins/claude-kit/skills/operating-instructions/references/ownership-map.md: 2205 words, cap 2177, +28
plugins/claude-kit/skills/responding-to-review/SKILL.md: 1867 words, cap 1867, +109
plugins/claude-kit/skills/writing-skills/SKILL.md: 3373 words, cap 3278, +95
test/size-ratchet.test.js: 3059 lines, cap 2989, +70; tests 87, +3
words: 220037 of cap 219915 across 59 curated files
test lines: 113818 of cap 113748 across 58 test files
tests: 3409
changed paths under no measured root: 1 (1 differing from HEAD, 0 untracked), which this tool does not measure and which no row above names; named-exclusion paths in the changeset: test/size-budget.json, which a root holds and no shape measures, so no row above names them
```

### Interim board 2 - 2026-09-08
In flight: section 4 at review round 1, implemented by implementer-sonnet (DONE: the sync verb reusing check's refusal path, three sync tests watched RED against a no-op sync then GREEN, the writing-skills bullet, the ownership-map row; its lane 170/169/1 exit 1 from `.kit/scratch/review-loop/s4/lane-s4.exit`, the red the expected over-cap on its three grown files); the round is the three lenses at opus, effort high, through Workflow (adversarial with the implementer's containment question handed as a question, blind with the four-file list only, security over the flag-named write path). Section 3 not yet dispatched.
Gate baseline: the lane and whole gate on Chapter 2's Gate line, both with section 4 unstaged.
Rulings adopted since the last boundary: none.
Next: adjudicate section 4's round, fix, run `node plugins/claude-kit/scripts/kit-size.js sync --repo .` at its close, its lane, Chapter 4 with the backlog retirement, commit and push; then section 3 to implementer-sonnet; then finishing-work.

### Interim board 3 - 2026-09-08
In flight: section 4 at review round 2 over its fix round 1 delta. Round 1 (three lenses at opus, effort high, through Workflow run wf_b9d4213d-0e2) returned adversarial 1 Critical 5 Majors 4 Minors, blind 2 Criticals 5 Majors 8 Minors, security 4 Majors 3 Minors; adjudicated under the section's class and deduplicated to eleven owed behavior findings, all fixed by the main session (the write's containment now init's, the write on the resolved path by rename from a sibling temporary file, pending caps moved, every check failure other than over-cap and missing-entry refusing the write at exit 2, line endings, indent and key order preserved from the file, paths named after the verb scoping the move and adding a first cap for a named unlisted path, the bullet's command carrying --repo, the printed cap screened, a null-prototype accumulator, the loader guard opening sync), four claim findings dispositioned (a test comment's enumeration deleted, the ownership-map parenthetical corrected to the ledger bullet, a kit-size comment corrected, the blind Critical on the three grown files' over-cap downgraded to the close's sync), and the four out-of-scope curated documents appended as section 5 (docs-only, inline, reported over the relay channel at the append). The full record is at `.kit/scratch/review-loop/s4/round1-adjudication.md`. Round 2 is the same three lenses through Workflow task wgxxn5r6q (run wf_26bf16a7-a2e), owed because the delta changes a write's containment. Section 5 appended and not started. Section 3 not yet dispatched.
Gate baseline: section 4 lane `node --test test/size-ratchet.test.js test/doctrine-parity.test.js test/output-style-parity.test.js` 175/174/1 exit 1 read from `.kit/scratch/review-loop/s4/lane-fix1c.exit` at 2026-09-08T03:48:59.150Z in the worktree `.kit/scratch/expert/wt-rle` at 5620b2b with section 4 unstaged, under this session's heavy-process claim; the red is the ratchet self-check naming the three grown files (writing-skills, the ownership map, test/size-ratchet.test.js), which the close's sync moves; the eight sync cases alone 8/8 exit 0 from `.kit/scratch/review-loop/s4/sync-fix1.exit`; baseline on the same lane before the fix round: the implementer's 170/169/1 exit 1, so the delta is +5 tests (the new sync pins) and the same one red.
Rulings adopted since the last boundary: sync's write follows init's containment rule (the implementer's handed question, answered by all three lenses the same way); sync takes optional paths and adds a first cap for a named path the budget lacks, a deviation from the spec's adds-no-entry sentence, recorded for the Chapter and the spec amendment.
Next: adjudicate round 2, fix, run `node plugins/claude-kit/scripts/kit-size.js sync --repo . plugins/claude-kit/skills/writing-skills/SKILL.md plugins/claude-kit/skills/operating-instructions/references/ownership-map.md test/size-ratchet.test.js` at the close, the lane, Chapter 4 with the backlog retirement and the spec amendments, commit and push; then section 5 inline; then section 3 to implementer-sonnet; then finishing-work.
