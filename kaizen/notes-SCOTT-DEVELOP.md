2026-07-03 | SCOTT-DEVELOP | Neuro-Evolution-Operations | pr-docs-guard false-positives on a chained command: 'git commit -F msg && git push && gh pr create' was blocked because the PreToolUse check runs before the command executes, so the docs changes the command itself commits still read as uncommitted. Forces splitting commit and pr-create into separate Bash calls. Fix idea: when the command chain contains a git commit ahead of the gh pr create, let it pass (or gate only commands that START with gh pr create).
